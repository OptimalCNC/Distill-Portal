# Phase 4 Progress

## Source-of-Truth Reference

- Implementation spec: `working/phase-4.md` (frozen at commit `92a9c99` on `main`, 2026-04-25)
- Coordinator operating prompt: `coordinator-prompt.md`
- Architecture vocabulary: `ARCHITECTURE.md`, `PRD.md`, `docs/dependency-rules.md`, `docs/features/inspection-surface.md`
- Prior phase logs for context: `progress/phase-3.progress.md` (Phase 3 complete — Bun + Vite + React + TS frontend now standalone, three-panel inspection surface live, Playwright e2e + 17/87 unit tests + ts-bindings drift check all green; Codex caught six legitimate blocking findings across Phase 3 that both Claude reviewers missed, so the three-reviewer rule remains non-skippable)
- Pre-Phase-4 HEAD: `a26f8fb` on `main` (2026-04-24, "Add CI workflow and consolidate contributor docs in README")
- Phase 4 source-of-truth commit: `92a9c99` on `main` (2026-04-25, "Add Phase 4 planning doc and bootstrap progress log")
- Milestone 1 delivery commit: `937b4c1` on `main` (2026-04-25, "Add design tokens and dark-mode-aware CSS")
- Milestone 2 delivery commit: `d2b72a7` on `main` (2026-04-25, "Land unified session list and importability rule")
- Milestone 3 delivery commit: `0ca1082` on `main` (2026-04-25, "Add filters, sort, persistence, and click-time intersection")

## Task Invocation Block (resolved)

- `task_name`: `phase-4`
- `task_spec_path`: `working/phase-4.md`
- `progress_log_path`: `progress/phase-4.progress.md`
- `protected_paths`:
  - `apps/backend/**`
  - `apps/backend/tests/**`
  - `components/collector-runtime/**`
  - `components/configuration/**`
  - `components/ingest-service/**`
  - `components/observability/**`
  - `components/raw-session-store/**`
  - `components/ui-api-contracts/src/**`
  - `components/ui-api-contracts/Cargo.toml`
  - `components/ui-api-contracts/bindings/**`
  - `tests/e2e/**`
  - root `Cargo.toml`
  - root `Cargo.lock`
- `protected_exception_paths`:
  - `components/ui-api-contracts/README.md` — Milestone 6 docs sweep may revise the paragraph that currently describes how the frontend constructs `source_key` to reflect the new "import identity is always the backend-provided `SourceSessionView.session_key`; the UI must never construct or mutate that value" rule (per `working/phase-4.md` §Documentation). Edit must be documentation-only; no Rust or contract-shape change. Any other edit under this path requires a fresh human exception.
- `forbidden_scope`:
  - new backend HTTP routes, contract types, or component crates
  - any change to the Rust contract source or to the regenerated TS bindings
  - skim view, summaries, LLM calls, distill runs, analyzer output, skill-draft curation
  - search over raw session content (FTS backend work)
  - tags, notes, bookmarks, highlights, quality marks, archive, purge
  - timeline / histogram / aggregation views
  - multi-page routing
  - authentication, permissioning, credential storage
  - Tailwind / utility CSS framework, component libraries (MUI, AntD, Chakra, Radix, Mantine, shadcn), CSS-in-JS runtimes (emotion, styled-components, vanilla-extract), state managers (Zustand, Redux, Jotai), data-fetching libraries (TanStack Query, SWR), icon libraries (lucide, heroicons)
  - any `Bun → Node` shim; `npm`, `node`, `child_process`, `jest.fn()` tooling (Bun-first rule per `feedback_bun_not_node.md`)
  - speculative adoption of `@tanstack/react-virtual` or a focus-management package; both are documented escape-hatch packages per `working/phase-4.md` §Dependency Policy and may only land on documented evidence
  - WCAG full certification (only the AA contrast spot-check on token pairs is in scope per Milestone 6)
- `architecture_refs`:
  - `ARCHITECTURE.md`
  - `PRD.md`
  - `docs/dependency-rules.md`
  - `docs/features/inspection-surface.md`
- `required_verification`:
  - `cargo check --workspace`
  - `cargo test --workspace`
  - `cargo test -p distill-portal-ui-api-contracts --features ts-bindings`
  - `bun run test` (from `apps/frontend/`; resolves to `bun test src`)
  - `bun run build` (from `apps/frontend/`)
  - `bun run test:e2e` (from `apps/frontend/`; Playwright Chromium browser e2e)
- `external_reviewer_command`: `codex exec`

## Current Snapshot

- Date: 2026-04-25
- Status: **Milestones 1 + 2 + 3 complete.** Chunk A (`92a9c99`) froze the spec; Chunk B (`937b4c1`) closed M1 (tokens + dark-mode); Chunk C (`d2b72a7`) closed M2 (unified list + importability); Chunk D (`0ca1082`) closed M3 (filters + sort + persistence + click-time intersection extension + four empty states + bundled deferred M2 mergeSessions useMemo). Importability rule still enforced end-to-end via shared `isImportable`; the click-time intersection now composes THREE defenses at the import POST (F2 visible-intersection from Phase 3 + M2 importability + M3 filter-window). Unit tests: 17/87 → 48/306 → 131/497. `bun run test:e2e` passes against real Chromium throughout. No protected-path touch on any chunk. No new dependency. Three-reviewer rule fired on each; Codex caught real findings on B (badge AA + header overclaim), C (sandbox-bound e2e missing-evidence), and D (empty-state branch + scan-errors Retry + 3 missing corrupt-input tests).
- Repo state after Milestone 2:
  - Frontend at `apps/frontend/` is Bun + Vite + React + TS only. Inspection page renders one unified session table under `apps/frontend/src/features/sessions/` (`SessionsView.tsx`, `SessionsTable.tsx`, `mergeSessions.ts`, `types.ts`, plus tests) instead of the prior three stacked tables; `ScanErrorsCallout.tsx` (new in `src/components/`) renders nothing when empty. `ActionBar.tsx` + `StatusBadge.tsx` survive M2 (M5/M6 owns their fate). `App.tsx` orchestrates the same three `Promise.allSettled` GETs and feeds the merged data into `SessionsView`.
  - `apps/frontend/src/styles/`: unchanged from M1 (`tokens.css` 68 LoC, `reset.css` 25 LoC, `global.css` 27 LoC, `app.css` 185 LoC). Cascade order in `main.tsx`: `reset.css` → `tokens.css` → `global.css` → `app.css`. `app.css` selector vocabulary (`.panel`, `.table-wrap`, `.empty`, `.muted`, `.mono`, `.stack`, `.select-col`, `.raw-link`, `.badge.*`, `.action-bar*`) is reused by the unified table; M6 retires `app.css` once feature-local CSS lands.
  - Inspection-surface generated TS bindings (`components/ui-api-contracts/bindings/*.ts`) unchanged (no contract change in M1 or M2). All 9 contract types still in use; the M2 `SessionRow` type at `apps/frontend/src/features/sessions/types.ts` is a UI-local derived type (NOT added to the contracts barrel).
  - Bun unit suite: 48 tests / 306 expects across 6 files (`App.test.tsx` 10, `mergeSessions.test.ts` 16, `SessionsTable.test.tsx` 7, `ScanErrorsCallout.test.tsx` 3, `StatusBadge.test.tsx` 4, `ActionBar.test.tsx` 8). Playwright e2e at `apps/frontend/e2e/inspection.spec.ts` got a comment-only update and continues to pass against real Chromium.
  - Backend, contract crate, and existing Rust e2e (`tests/e2e/tests/inspection_surface.rs`, typed-Rust-client smoke) are out of scope and protected. Untouched in M1 + M2.
  - `apps/frontend/.codex` zero-byte sentinel left over from Phase 3 Codex review invocations is still untracked. WSL Playwright system-libs (`libnspr4`, `libnss3`, etc.) installed by the human pre-Chunk-C; `bun run test:e2e` runs against real Chromium on this host.
- Tooling availability on this host (re-verified after Codex round 1 + 2):
  - `codex exec` available: `codex-cli 0.124.0` at `/home/huwei/.bun/bin/codex`
  - `claude -p` available: `Claude Code 2.1.119` at `/home/huwei/.local/bin/claude`
  - `bun` available: `1.3.13` at `/home/huwei/.bun/bin/bun`

## Active Plan

- Chunk: none currently in flight.
- Owner: none.
- Status: **Milestone 3 complete.** Awaiting Milestone 4 planner dispatch.

## Remaining Milestones

- **Milestone 1: Design tokens and visual reset** — complete 2026-04-25 (Chunk B as `937b4c1`). DoD shipped: (1) no hex literal outside `tokens.css` (verified by `rg -n '#[0-9a-fA-F]{3,8}' apps/frontend/src/` returning only the 24 lines in `tokens.css`); (2) light + dark themes both render correctly with WCAG AA 4.5:1 contrast on all four `.badge.*` text/background pairs in both modes; (3) no regression in the 17/87 unit suite or `bun run build`. `bun run test:e2e` skipped on the coordinator's WSL host due to the `libnspr4.so` system-lib gap (same Phase 3 H1/G2 limitation); transitive-pass argument applies (CSS-only diff, zero JSX/markup change, every selector preserved).
- **Milestone 2: Unified session list** — complete 2026-04-25 (Chunk C as `d2b72a7`). DoD shipped: (1) every previously-shown row reachable in the unified list (verified by truth-table coverage in `mergeSessions.test.ts`); (2) `presence` rendering covers `stored_only` + `source_missing` without consumer-side branching; (3) `sourcePath` populates from either API with `sourcePathIsStale` correctly set + `title=` hover hint on the cell; (4) **importability rule end-to-end** (checkboxes only on importable rows; `handleToggleAll` and `handleImport` both filter through shared `isImportable` helper; POST body cannot drift from displayed checkbox set); (5) per-panel error isolation regression covers source-500 / stored-500 / scan-errors-500 in three new tests; (6) existing import/rescan flows still work (F2 click-time intersection test source preserved verbatim from Phase 3); (7) `bun run test:e2e` passes against real Chromium (1 passed, 1.5s).
- **Milestone 3: Filters, sort, search, persistence** — complete 2026-04-25 (Chunk D as `0ca1082`). DoD shipped: (1) user can narrow hundreds-long list (filter chips + datalist project input + substring search + sort selector); (2) reload restores filter/sort/search via `distill-portal:inspection-filters:v1`; (3) corrupt or missing localStorage falls back to defaults via total decoder (12 distinct corrupt-input paths covered in `useSessionFilters.test.ts`); (4) tests cover multi-filter integration, persistence round-trip, corrupt-persistence recovery, AND the click-time intersection regression for the filter-only race variant (proved by temp-revert exercise: revert the click-time filter extension → test fails with rogue key in POST → re-apply → passes). Bonus: bundled the deferred M2 `mergeSessions` `useMemo` since `App.tsx` had to touch the same call sites; four documented empty-state branches landed (no_sessions_at_all / no_matches_after_filter / nothing_to_import / partial_fetch_failure with scan-errors Retry); `(refresh)` affordance for `statusConflict` and `sourcePathIsStale` `title=` hint preserved from M2.
- **Milestone 4: Session detail drawer** — pending. Native `<dialog>` + `showModal()`; metadata list with absolute+relative timestamps and `source-clock` annotations; `sourcePathIsStale` "last seen source path" label; `statusConflict` badge; raw-payload preview that consumes the response via `ReadableStream` + `getReader()` + `TextDecoder` and short-circuits at **20 NDJSON lines OR 256 KB byte cap, whichever first** (calls `reader.cancel()`); `AbortController` cancelable on close before AND after the cap. Drawer focus-trap + Esc-close + backdrop-close + focus-restoration covered in BOTH happy-dom component tests AND real-Chromium Playwright. Escape-hatch focus-management package only if Playwright fails the documented reproducer.
- **Milestone 5: Pagination and sticky action bar** — pending. `Pagination` (50 / 100 / 200, default 50); sticky action bar (only when natural layout would scroll out of view); `Toast` + queue replaces `renderReport`; "last rescan from this browser X ago" caption (scoped — does not claim backend-global freshness); error-toast Retry. Click-time intersection regression test extended to the pagination-cross-page variant.
- **Milestone 6: Cleanup and documentation** — pending. Delete retired files (`SourceSessionsTable.tsx`, `StoredSessionsTable.tsx`, `ScanErrorsPanel.tsx`, `StatusBadge.tsx`, `app.css`); full docs sweep (`docs/README.md`, `docs/dependency-rules.md`, `docs/dev-commands.md`, `docs/features/inspection-surface.md`, `docs/features/session-store.md`, `docs/playbooks/modify-frontend-page.md`, `apps/frontend/README.md`, `components/ui-api-contracts/README.md`); contract-drift check still green; WCAG AA contrast check recorded for every visible foreground/background token pair in light AND dark modes; `rg` checks proving no stale dual-table references remain. **Pre-empted in M1 for badge text/background pairs**: all 8 (4 light + 4 dark) badge contrasts are already ≥ 5.176:1 by the Codex-driven darkened-foreground recipe; M6 must still measure the remaining text/background pairs (`--color-text` on `--color-bg`, `--color-text-muted` on `--color-bg`, `--color-text-muted` on `--color-surface`, `.raw-link` color `--color-accent` on body bg, `.action-bar button` text on `--color-surface`, etc.) and record them.

## Completed Work Log

- 2026-04-25: bootstrapped `progress/phase-4.progress.md`; resolved the Phase 4 invocation block from `working/phase-4.md`; verified tooling (`codex exec` `0.124.0`, `claude` `2.1.119`, `bun` `1.3.13`); inventoried current frontend state for planner context (5 component files, 17/87 unit tests, 1 Playwright spec, 9 generated TS contract bindings); registered milestone task tracking. No code change yet.
- 2026-04-25: Chunk A complete — committed `working/phase-4.md` + `progress/phase-4.progress.md` as `92a9c99` on `main` ("Add Phase 4 planning doc and bootstrap progress log"). This fixes the source-of-truth SHA all Phase 4 reviewers cite. Spec content is the 49 KB / 1029-line implementation plan covering six milestones (M1 design tokens, M2 unified list, M3 filters/sort/search, M4 detail drawer, M5 pagination + sticky + toasts, M6 cleanup + docs sweep). No code change.
- 2026-04-25: WSL Playwright system-libs (`libnspr4`, `libnss3`, etc.) installed by the human between Chunk B and Chunk C dispatch (chosen path from a coordinator question). `bun run test:e2e` now runs against real Chromium on this host (1 passed, 1.5s); transitive-pass arguments no longer needed for chunks that change JSX.
- 2026-04-25: Chunk D (Milestone 3 close) complete — committed filters + sort + persistence + click-time intersection extension as `0ca1082` on `main` ("Add filters, sort, persistence, and click-time intersection"). 17 files changed, 2972 insertions, 104 deletions. New (8): `apps/frontend/src/features/sessions/{useSessionFilters,filterSessions,relativeTime,SessionFilters}.{ts,tsx}` plus their `.test.{ts,tsx}` siblings. Modified (9): `apps/frontend/src/{App,App.test}.tsx`, `apps/frontend/src/components/{ActionBar,ActionBar.test}.tsx`, `apps/frontend/src/features/sessions/{SessionsView,SessionsTable,SessionsTable.test}.tsx`, `apps/frontend/src/styles/app.css`, `docs/features/inspection-surface.md`. Test count 48/306 → 131/497 (+83 tests / +191 expects across 4 new test files + edits to App/ActionBar/SessionsTable tests). Key invariants landed: `useSessionFilters.ts` decoder is total (12+ corrupt-input paths fall back to defaults; `importableOnly` clears the persisted `status` array on apply per spec); `applyFilters` enforces `importableOnly`-overrides-`status` at the filter layer (defense-in-depth); `applySort` honors spec null-handling (ASC nulls first, DESC nulls last) + deterministic tiebreaker chain (`source_updated_at → ingested_at → created_at → title (case-insensitive ASCII) → rowKey`); `relativeTime.ts` is pure (`now` always passed in; never reads `Date.now()`); `App.tsx` `handleImport` derives `keysToImport` AT CLICK TIME from a fresh `mergeSessions → applyFilters → applySort → isImportable` chain composing F2 visible-intersection + M2 importability + M3 filter-window into one expression; bundled the deferred M2 `useMemo` over `mergeSessions(sourceData, storedData)`; `now` captured as React state pinned to `refetchAll` time; `ActionBar.hiddenByFilterCount` defaults to 0 (backward-compatible with M2 tests); `.chip.active` AA contrast pre-checked (light 6.237, dark 6.949 — both ≥ AA 4.5:1, recipe reuses M1 badge color-mix pattern). Four documented empty-state branches landed (`no_sessions_at_all` / `no_matches_after_filter` / `nothing_to_import` / partial-fetch-failure scan-errors Retry button); `nothing_to_import` renders the empty-state copy ABOVE the populated table when `filteredRows.length > 0 && filteredRows.every(r => !isImportable(r))` (developer's defensible interpretation of spec text "matching rows exist", endorsed by Codex round 2). Verification: `cargo check --workspace` clean (0.06s), `cargo test --workspace` all suites green, `cargo test -p distill-portal-ui-api-contracts --features ts-bindings` 1 passed + 1 ignored, `bun run test` 131/497/568ms, `bun run build` `dist/index.html 0.40 kB / dist/assets/index-wRqGrCw9.css 5.72 kB / dist/assets/index-CRv99vnH.js 216.53 kB / built in 450ms` (CSS up 1.50 kB for new filter selectors; JS up 10.50 kB for new filter code), `bun run test:e2e` `1 passed (1.3s)` against real Chromium. `rg -n '#[0-9a-fA-F]{3,8}' apps/frontend/src/ | wc -l` = 24 (M1 hex-isolation invariant intact). `git diff apps/frontend/package.json apps/frontend/bun.lock` empty (no new dep). `git diff --name-only -- apps/backend components/* tests/e2e Cargo.toml Cargo.lock apps/backend/tests` empty (protected paths untouched).
- 2026-04-25: Chunk C (Milestone 2 close) complete — committed unified session list + importability rule + ScanErrorsCallout + e2e + 3-doc delta as `d2b72a7` on `main` ("Land unified session list and importability rule"). 17 files changed, 2220 insertions, 377 deletions. New: `apps/frontend/src/features/sessions/{types,mergeSessions,mergeSessions.test,SessionsView,SessionsTable,SessionsTable.test}.{ts,tsx}` (6 files; the unified list lives under `src/features/sessions/` per spec §Target Repository Shape) and `apps/frontend/src/components/{ScanErrorsCallout,ScanErrorsCallout.test}.tsx` (2 files). Modified: `apps/frontend/src/{App,App.test}.tsx`, `apps/frontend/e2e/inspection.spec.ts` (3-line comment-only update), `apps/frontend/README.md`, `docs/features/inspection-surface.md`, `docs/playbooks/modify-frontend-page.md`. Deleted: `apps/frontend/src/components/{SourceSessionsTable,StoredSessionsTable,ScanErrorsPanel}.tsx`. `mergeSessions.ts` is a pure two-pass join indexed by `${tool}:${source_session_id}` (correct tool-prefix non-collision); 16 unit tests cover every legal `(presence × status)` combination + disagreement branch + invariants. `SessionsTable.tsx` renders `<input type="checkbox">` only when `isImportable(row) && row.sourceSessionKey !== null` (visible-DOM enforcement; not just CSS). `App.tsx`'s `handleImport` derives `keysToImport` at click time from `selected ∩ visibleImportableKeys` where `visibleImportableKeys` is built from `mergeSessions(sourceData, storedData) + isImportable` — composing F2 visible-intersection (Phase 3) AND M2 importability defenses in one expression. The same intersection drives `handleToggleAll` and the action-bar `selectedCount`, so display, button-disabled state, and POST body share one source of truth. `(refresh)` affordance for `statusConflict: true` rows: `<span className="muted" title="Source and stored status disagreed during load — refresh to re-fetch.">(refresh)</span>` after the badge. `sourcePathIsStale: true` rows have `title="last seen source path — source file no longer discoverable"` on the source-path cell. `ScanErrorsCallout.tsx` renders `null` when empty (DoD "Collapse what is rarely non-empty"). Three new per-panel error-isolation tests cover source-500 / stored-500 / scan-errors-500. F2 click-time intersection regression test source preserved verbatim from Phase 3 (`IS_REACT_ACT_ENVIRONMENT` flip, `findDescriptor` walk, `queueMicrotask`-scheduled click — all unchanged); only the explanatory comment block at the top updated to note the M2 transition. Test count grew from 17/87 to 48/306. Verification: `cargo check --workspace` clean (0.04s), `cargo test --workspace` all 21 suites green, `cargo test -p distill-portal-ui-api-contracts --features ts-bindings` 1 passed + 1 ignored (drift gate green; bindings byte-identical), `bun run test` 48/306/437ms, `bun run build` `dist/index.html 0.40 kB / dist/assets/index-veKZvSP4.css 4.22 kB / dist/assets/index-B6NaTng7.js 206.03 kB / built in 452ms` (CSS byte-identical to M1; JS up 2.27 kB for new components + merge code), `bun run test:e2e` `1 passed (1.5s)` against real Chromium. `rg -n 'SourceSessionsTable|StoredSessionsTable|ScanErrorsPanel' apps/frontend/src/ docs/` empty (full retirement). `rg -n '#[0-9a-fA-F]{3,8}' apps/frontend/src/ | wc -l` = 24 (M1 hex-isolation invariant intact). `git diff apps/frontend/package.json apps/frontend/bun.lock` empty (no new dep). `git diff --name-only -- apps/backend components/* tests/e2e Cargo.toml Cargo.lock apps/backend/tests` empty (protected paths untouched). Two follow-up nit fixes applied inline by the coordinator after Codex round 1: (a) `docs/features/inspection-surface.md:42` glob fixed (`.tsx` only would silently skip `mergeSessions.test.ts`), (b) `mergeSessions.test.ts` header comment expanded to enumerate the unreachable `(presence × status)` combinations explicitly.
- 2026-04-25: Chunk B (Milestone 1 close) complete — committed Phase 4 design tokens + minimal CSS reset + dark-mode-aware rewire of `app.css` + slimmed `global.css` + cascade-order `main.tsx` + entry-points `apps/frontend/README.md` update as `937b4c1` on `main` ("Add design tokens and dark-mode-aware CSS"). 6 files changed, 203 insertions, 62 deletions. Two new files (`apps/frontend/src/styles/tokens.css` 68 LoC with the spec's exact 12-color light + dark palette under `prefers-color-scheme`, and `apps/frontend/src/styles/reset.css` 25 LoC with box-sizing + body margin + `color-scheme: light dark`). Cascade order: `reset.css` → `tokens.css` → `global.css` → `app.css`. Every selector in `app.css` preserved verbatim (`.panel`, `.table-wrap`, `.badge.up-to-date`, `.badge.not-stored`, `.badge.outdated`, `.badge.source-missing`, `.muted`, `.mono`, `.stack`, `.empty`, `.raw-link`, `.action-bar*`, `.select-col`) so the existing 17/87 unit suite + Playwright e2e selectors stay valid. The four `.badge.*` rules use a uniform `color-mix(in srgb, var(--color-X) 15%, var(--color-surface))` background, `color-mix(in srgb, var(--color-X) 70%, var(--color-text))` foreground, and `color-mix(in srgb, var(--color-X) 35%, var(--color-surface))` border-color recipe (the 70/30 darkening on the foreground was added by the Codex-round-1 fix-up to clear WCAG AA 4.5:1 in both modes — see Review Log). Badge palette mapping: `up-to-date → success` (green), `not-stored → accent` (blue, semantically "actionable"), `outdated → warn` (amber), `source-missing → error` (red). Verification: `cargo check --workspace` clean, `cargo test --workspace` all green, `cargo test -p distill-portal-ui-api-contracts --features ts-bindings` 1 passed + 1 ignored, `bun run test` 17 pass / 87 expect calls / 378ms, `bun run build` 4.22 kB CSS + 203.76 kB JS (CSS up from pre-M1 2.09 kB entirely accounted for by `tokens.css` + `reset.css` content). `bun run test:e2e` could not run on the coordinator's WSL host (`libnspr4.so` missing — same Phase 3 H1/G2 limitation); transitive-pass argument: CSS-only diff, no JSX/component file change, every selector the Playwright spec queries (`getByRole("link", "View Raw")`, `a.raw-link.mono`, `getByLabel(\`Select \${session_key}\`)`, etc.) is preserved by the rewire. Protected paths and `components/ui-api-contracts/**` completely untouched. No new runtime or dev dependency (`apps/frontend/package.json` and `apps/frontend/bun.lock` byte-identical pre and post).

## Review Log

- 2026-04-25: Chunk B planner subagent (Plan agent) returned `ready` verdict with a single recommended chunk (Chunk B — single-shot tokens + reset + dark-mode rewire of all five existing components) plus a Chunk B-alt fallback (tokens-only without `app.css` rewire, explicitly NOT recommended because it does not satisfy DoD bullet 1). Coverage map showed Chunk B closes all three Milestone 1 DoD bullets and all three scope bullets in one slice. Open questions resolved by the planner: `app.css` retirement timing → slimmed-but-present in M1, retired in M6; manual screenshot evidence → coordinator's call (chose verbal description in the progress log + reliance on the unit/build gates rather than committed binary screenshots, matching Phase 3 pattern).
- 2026-04-25: Chunk B developer subagent completion claim received. Evidence pack captured at `/tmp/phase-4-chunk-b-evidence.md` (session-local). 6 files changed, 2 new + 4 modified, 203 insertions / 62 deletions. Developer self-flagged 8 decisions: (1) `color-mix(in srgb, var(--color-X) 15/35%, var(--color-surface))` recipe for badge background/border, (2) `not-stored → accent` semantic mapping (vs other 3-color options), (3) `global.css` slimmed not deleted (body bg/color/font-family wants tokens, doesn't belong in reset), (4) `main { max-width: 48rem }` rule deleted from `global.css` to avoid conflict with `app.css`'s 1400px, (5) `border-radius: 999px` left literal (no `--radius-full` in spec), (6) intentional spacing-quantization rounding on `0.15rem .stack` gap → `--space-1` (0.25rem) and `0.35rem 0.9rem` button padding → `--space-1 --space-3`, (7) `apps/frontend/.codex` sentinel left alone (per coordinator instruction), (8) no new tests authored (per spec's deferral of new tests to M2–M5). Coordinator independently re-ran every verification command — `git status --short`, `git diff --stat`, `rg -n '#[0-9a-fA-F]{3,8}' apps/frontend/src/`, `cargo check --workspace`, `cargo test --workspace`, `cargo test -p distill-portal-ui-api-contracts --features ts-bindings`, `bun run test`, `bun run build`, `bun run test:e2e` — confirming results before review.
- 2026-04-25: Chunk B backend-protection reviewer Claude subagent (round 1) — verdict `backend untouched`; required action `proceed to normal review`; findings `none`. Independently ran `git status --short` (matches one-to-one), `git diff --name-only -- apps/backend components/* tests/e2e Cargo.toml Cargo.lock apps/backend/tests apps/frontend/package.json apps/frontend/bun.lock` (empty), `git diff components/ui-api-contracts/bindings/ | wc -l` (0), `cargo test -p distill-portal-ui-api-contracts --features ts-bindings` (1 passed, 1 ignored), `git diff apps/frontend/package.json apps/frontend/bun.lock` (empty), `rg -n 'apps/frontend/src/app.rs' apps/frontend/` (empty — Phase 3 H1 lesson honored), `rg -n '#[0-9a-fA-F]{3,8}' apps/frontend/src/` minus tokens.css filter (empty — DoD bullet 1 confirmed). M6-only `components/ui-api-contracts/README.md` exception correctly NOT exercised.
- 2026-04-25: Chunk B normal reviewer Claude subagent (round 1) — verdict `approved`; findings `none`; required changes `none`. Independently re-ran `rg -n '#[0-9a-fA-F]{3,8}' apps/frontend/src/` (24 lines, all in tokens.css), `bun run test` (17/87 unchanged), `bun run build` (clean; bundle hex grep confirmed only token values, no resurrected component hex), confirmed no `apps/frontend/src/components/*.tsx` / `apps/frontend/src/App.tsx` / `apps/frontend/src/lib/*.ts` modified, sanity-checked the `color-mix()` Chromium support and the 15/35/100 ratio uniformity, endorsed the badge palette mapping (`up-to-date → success`, `not-stored → accent`, `outdated → warn`, `source-missing → error`). Notes: intentional spacing-quantization rounding flagged as not-a-bug; borderline AA contrast pairs (`--color-warn` text on `--color-surface`) flagged for M6 measurement; WSL Playwright system-libs gap accepted under the Phase 3 transitive-pass precedent.
- 2026-04-25: Chunk B Codex cross-agent review round 1 — verdict `needs changes`. Codex-cli `0.124.0`, prompt at `/tmp/codex-phase-4-chunk-b-prompt.txt` (347 lines), stdout at `/tmp/codex-phase-4-chunk-b-output.txt`. Two concrete blocking findings:
  1. **Badge AA contrast fails in light mode**: Codex independently computed WCAG contrast ratios with a Bun script and found `not-stored 4.20:1`, `up-to-date 4.14:1`, `outdated 3.24:1` against the `color-mix(15%, surface)` backgrounds. Three of four badges fail AA 4.5:1 for small uppercase text (`--text-xs ≈ 11.5px`). The evidence pack's "comfortably exceed 4.5:1" claim was wrong. Codex explicitly noted the prompt asked it to catch glaring color-mix AA failures now to avoid M6 redo.
  2. **`app.css` header overclaims**: header said "every color, spacing, radius, font, and shadow value reads from `tokens.css`. No raw hex literals are permitted in this file." But the file kept structural literals: `border-radius: 999px` on `.badge`, `max-width: 1400px` on `main`, `min-width: 720px` on `table`, `width: 2.25rem` on `.select-col`, `letter-spacing: 0.06em` on `th` and `.badge`. The "no raw hex literals" claim is technically true; the broader "every value reads from tokens" claim is false.
  Codex also confirmed clean: scope (only the six expected files changed), protected paths untouched, `package.json`/`bun.lock` byte-identical, no resurrected `apps/frontend/src/app.rs` references, `bun run test` 17/87, `bun run build` produced no extra hex literals from elsewhere. **First Phase-4 Codex catch of a Claude-family blind spot — both Claude reviewers missed both findings, exactly the Phase 3 precedent.**
- 2026-04-25: Codex-raised findings (round 1) dispatched to a follow-up developer subagent with narrow scope: edit only `apps/frontend/src/styles/app.css`. Developer applied the coordinator-recommended `color: color-mix(in srgb, var(--color-X) 70%, var(--color-text))` foreground recipe uniformly across all four `.badge.*` variants (background and border-color recipes unchanged from round 1), and rewrote the file header at lines 14–43 to explicitly enumerate (a) what reads from tokens (color, spacing, radius, font-family, font-size) and (b) the five intentional structural literals Codex flagged with a one-sentence rationale citing the spec's §Risks design-balloon mitigation. Developer ran the same Bun contrast script Codex used and confirmed all 8 (4 light + 4 dark) pairs ≥ 4.5; lowest is light/outdated at 5.176:1, dark min is dark/error at 6.577:1. Developer ran `bun run test` (17/87 unchanged), `bun run build` (4.22 kB CSS, +0.17 kB for the larger header + per-badge `color:` line). Coordinator independently re-ran `git status --short` (still 4 modified + 2 new under `apps/frontend/`), `git diff --stat` (`app.css | 126 ++++--`, +19 LoC vs round 1), `rg -n '#[0-9a-fA-F]{3,8}' apps/frontend/src/` (still 24 lines, all in tokens.css), the Bun contrast script (output reproduces exactly), `bun run test` (17/87), `bun run build` (clean). No file outside `apps/frontend/src/styles/app.css` touched.
- 2026-04-25: Chunk B Codex cross-agent review round 2 (post-fix) — verdict `approved`; findings `none`; missing evidence `none`; required changes `none`. Codex-cli `0.124.0`, prompt at `/tmp/codex-phase-4-chunk-b-prompt-v2.txt` (81 lines), stdout at `/tmp/codex-phase-4-chunk-b-output-v2.txt`. Codex independently re-ran the Bun contrast script with the same 70/30 `color-mix` recipe and reproduced `light not-stored 6.237 / up-to-date 6.164 / outdated 5.176 / source-missing 6.694`, `dark not-stored 6.949 / up-to-date 6.995 / outdated 7.516 / source-missing 6.577` — all ≥ AA 4.5. Codex re-grepped `apps/frontend/src/styles/app.css` for the five structural literals it had flagged in round 1 (`max-width: 1400px`, `min-width: 720px`, `width: 2.25rem`, `border-radius: 999px`, `letter-spacing: 0.06em`) and confirmed they are all present and explicitly enumerated in the rewritten header. Notes confirmed the implemented literals match the header's enumeration list one-to-one.
- Re-review decision: Backend-protection reviewer and normal reviewer Claude subagent NOT re-run after the Codex-driven fix-up. Rationale (Phase 3 precedent — Chunks B, D2, F2, G1, G2, H1): (a) backend-protection scope (protected paths, deps, bindings, contract-drift gate) is invariant under a single-CSS-file edit that only touches the four `.badge.*` `color:` declarations and the file header comment; (b) normal reviewer approved the rest of the chunk and the fix specifically addresses the gaps THEY missed (which Codex caught) — re-running them on a fix for their own blind spot doesn't add signal; (c) Codex raised the issues and is the right gate for the fixes. Coordinator independently re-verified all gates pre-commit.
- Chunk B final disposition: approved by all three required reviewers on converged evidence — `backend untouched` (backend-protection round 1), `approved` (normal reviewer round 1), `approved` (Codex round 2 after badge contrast + header fix-up). Committed as `937b4c1`.
- 2026-04-25: Chunk C planner subagent (Plan agent) returned `ready` verdict with single-shot Chunk C recommendation + C1/C2 fallback. Caught a coordinator misstatement (the dispatch brief said `SessionRow` has 16 fields; planner re-counted from spec block and reported 18). Resolved 6 open questions: ONE chunk (single-shot per anti-intermediate-state mandate); ship `src/features/sessions/` directory in M2 (matches spec end state; M3+ ships against stable layout); `SessionRow`+`Presence` live in `apps/frontend/src/features/sessions/types.ts`; reuse existing `app.css` selectors (M6 cleans up); minimum `statusConflict` row affordance is a small `(refresh)` text/title; field count is 18 not 16. Coordinator selected single-shot Chunk C.
- 2026-04-25: Coordinator question to human resolved the WSL Playwright system-libs gap pre-dispatch — human chose to install `libnspr4`/`libnss3`/etc. via `sudo apt-get` so `bun run test:e2e` can run on this host; transitive-pass argument no longer needed for chunks that change JSX (M2-M5). Coordinator verified post-install with a real-Chromium pass before dispatching the developer.
- 2026-04-25: Chunk C developer subagent completion claim received. Evidence pack at `/tmp/phase-4-chunk-c-evidence.md` (session-local). 8 new files, 6 modified, 3 deleted. Developer self-flagged 3 decisions: (1) badge palette mapping `not-stored → accent` (vs other 3-color options, semantic "actionable"); (2) `(refresh)` affordance UI = small inline text not full conflict badge (M4 owns the badge); (3) runtime-permissive `(presence × status)` combinations (no exhaustive type-level switch; test truth table enforces spec-legal only). Developer also overcounted App.test.tsx tests as 11 (actual count is 10; coordinator caught + flagged in evidence pack). Coordinator independently re-ran every verification command — `git status --short`, `git diff --stat`, all the `rg` checks, `cargo check --workspace`, `cargo test --workspace`, `cargo test -p distill-portal-ui-api-contracts --features ts-bindings`, `bun run test`, `bun run build`, `bun run test:e2e` — confirming results before review.
- 2026-04-25: Chunk C backend-protection reviewer Claude subagent (round 1) — verdict `backend untouched`; required action `proceed to normal review`; findings `none`. Independently ran `git status --short` (matches one-to-one), `git diff --name-only --` against the protected-paths globlist (empty), `git diff components/ui-api-contracts/bindings/ | wc -l` (0), `cargo test -p distill-portal-ui-api-contracts --features ts-bindings` (1 passed, 1 ignored), `git diff apps/frontend/package.json apps/frontend/bun.lock` (empty), spot-checked all M1 + scaffold files unmodified, confirmed `apps/frontend/src/styles/` unchanged, confirmed M6-only `components/ui-api-contracts/README.md` exception NOT exercised. `rg -n 'apps/frontend/src/app.rs' apps/frontend/` empty. Confirmed `App.tsx` references no later-milestone surface (no filter UI / sort / pagination / drawer / dialog / toast).
- 2026-04-25: Chunk C normal reviewer Claude subagent (round 1) — verdict `approved with nits`; findings `none` blocking; required changes `none`; three non-blocking nits: (a) `docs/features/inspection-surface.md:42` glob `*.test.tsx` would silently skip `mergeSessions.test.ts` (which is `.ts`, no JSX); (b) `App.tsx` calls `mergeSessions` per render in 3 places without memoization (performance non-regression vs Phase 3; not blocking); (c) `mergeSessions.test.ts` would benefit from explicitly enumerating which `(presence × status)` combinations were pruned as unreachable. Independently re-ran `bun run test` (48/306), `bun run build` (clean, byte-identical CSS), `bun run test:e2e` (1 passed against real Chromium). Verified `SessionRow` field count = 18 against spec block, importability rule end-to-end through `isImportable` at 4 points, F2 click-time intersection regression test source preserved verbatim, `mergeSessions` join key correctly tool-prefixed, `selected: Set<string>` model never references `rowKey`, CSS reuse confirmed unchanged.
- 2026-04-25: Chunk C Codex cross-agent review round 1 — verdict `needs more evidence`. Codex-cli `0.124.0`, prompt at `/tmp/codex-phase-4-chunk-c-prompt.txt` (348 lines), stdout at `/tmp/codex-phase-4-chunk-c-output.txt`. ONE missing-evidence item: `bun run test:e2e` did not pass in Codex's sandbox (`bun run dev` fails with `listen EPERM: operation not permitted 127.0.0.1:4100` — sandbox local-bind restriction, not an app failure). Codex EXPLICITLY noted this is environmental, not a code finding. Codex confirmed all OTHER gates green (cargo check/test, ts-bindings drift, bun run test 48/306, bun run build, scope/protected-path/dep checks clean), `mergeSessions` join key correct, `SessionRow` 18 fields, importability rule enforced through shared `isImportable` at 4 points, F2 race choreography unchanged (only comment diff). Required Changes: `none`.
- 2026-04-25: Codex-raised missing evidence (round 1) addressed inline by the coordinator (per the M1 D1/G2 docs-fix precedent — coordinator may apply unambiguous fixes when Required Changes is `none`):
  - Coordinator re-ran `bun run test:e2e` on the host of record after Codex round 1; verbatim output `1 passed (1.5s)` against real Chromium. The coordinator's environment is the authoritative host for environment-bound tests in Phase 4 (per the Open Risks Codex-availability note).
  - Coordinator applied 2 of 3 normal-reviewer nits inline: (a) `docs/features/inspection-surface.md:42` rewrote the test glob line to enumerate `*.test.tsx` for the table component AND `*.test.ts` for the pure `mergeSessions` join, eliminating the silent-skip risk; (b) `mergeSessions.test.ts` header comment expanded with an explicit "Pruned as unreachable" list of the spec-illegal `(presence × status)` combinations and a forward-compatibility note ("If a future backend change makes any of these reachable, add the corresponding test cell"). Total coordinator-applied edit: ~12 lines across 2 files; comment-only changes; no test count change (`bun run test` still 48/306). The third normal-reviewer nit (per-render `mergeSessions` memoization) was deferred to M3 where filter UI requires touching the same derivation; tracked in Open Risks.
- 2026-04-25: Chunk C Codex cross-agent review round 2 (post-coordinator-fix) — verdict `approved`; findings `none`; missing evidence `none`; required changes `none`. Codex-cli `0.124.0`, prompt at `/tmp/codex-phase-4-chunk-c-prompt-v2.txt` (96 lines), stdout at `/tmp/codex-phase-4-chunk-c-output-v2.txt`. Codex notes: "Coordinator host-of-record output is adequate evidence for the sole round-1 gap: real Chromium e2e passed (`1 passed (1.5s)`). Read-only spot check confirms the two nit fixes are present."
- Re-review decision: Backend-protection reviewer and normal reviewer Claude subagent NOT re-run after the coordinator nit fixes. Rationale (Phase 3 + M1 precedent — Chunks B, D2, F2, G1, G2, H1 + Phase 4 Chunk B): (a) backend-protection scope (protected paths, deps, bindings, contract-drift gate) is invariant under comment-only edits in `docs/` and a test-file header comment; (b) the normal reviewer raised the nits — re-running them on the fixes for the nits they raised is redundant; (c) Codex's missing-evidence was about e2e environment access, not code, and was resolved by host-of-record output, so the round-2 Codex re-approval is sufficient.
- Chunk C final disposition: approved by all three required reviewers on converged evidence — `backend untouched` (backend-protection round 1), `approved with nits` (normal reviewer round 1; 2/3 nits fixed inline, 1 deferred to M3 with rationale), `approved` (Codex round 2 after host-of-record e2e proof + nit fixes). Committed as `d2b72a7`.
- 2026-04-25: Chunk D planner subagent (Plan agent) returned `ready` verdict with single-shot Chunk D recommendation (Chunk D-alt fallback offered but not recommended). Resolved 8 open questions: ONE chunk; `applyFilters` + `applySort` live in NEW `filterSessions.ts` (sibling to `mergeSessions.ts`); BUNDLE the deferred M2 `useMemo`; SKIP e2e expansion in M3; "+K hidden by filters" caption owned by `App.tsx` (count math) → `ActionBar` (rendering); empty-states hard-coded in `SessionsView`; `App.tsx` owns filter state; page-size SELECTOR UI deferred to M5 (field persists in M3). Coordinator selected single-shot Chunk D and adopted all planner answers.
- 2026-04-25: Chunk D developer subagent completion claim received. Evidence pack at `/tmp/phase-4-chunk-d-evidence.md`. 17 files (8 new + 9 modified, 0 deleted). Self-flagged design decisions: chosen `color-mix(70% accent, 30% text)` recipe for `.chip.active` foreground (mirrors M1 badge); `now` captured as React state via `useState(() => new Date().toISOString())` refreshed inside `refetchAll`; `useSessionFilters.ts` decoder is total over 12 corrupt-input paths; defense-in-depth at TWO layers for `importableOnly`-overrides-`status` (hook + filter); F2 click-time intersection regression test source preserved verbatim with the M3 race-window choreography (the temp-revert exercise was performed and documented). Coordinator independently re-ran every verification command — all green — before review.
- 2026-04-25: Chunk D backend-protection reviewer Claude subagent (round 1) — verdict `backend untouched`; required action `proceed to normal review`; findings `none`. Independently ran the full protected-path globlist `git diff` (empty), `git diff components/ui-api-contracts/bindings/ | wc -l` (0), `cargo test -p distill-portal-ui-api-contracts --features ts-bindings` (1 passed, 1 ignored), `git diff apps/frontend/package.json apps/frontend/bun.lock` (empty), `rg -n 'apps/frontend/src/app.rs' apps/frontend/` (empty), confirmed M2 baseline files (types/mergeSessions/StatusBadge/ScanErrorsCallout/etc.) unmodified, confirmed no Drawer/Toast/FilterChip/Pagination forbidden-surface files introduced, confirmed no Tailwind/MUI/Chakra/Radix/Mantine imports.
- 2026-04-25: Chunk D normal reviewer Claude subagent (round 1) — verdict `needs changes`. Two findings:
  - **MEDIUM (F1)**: `SessionsView.getEmptyStateKind` `nothing_to_import` branch never fires per spec scenario. Spec lines 381-386: "matching rows exist but all are `up_to_date` or `source_missing`" → implies `filteredRows.length > 0 && filteredRows.every(non-importable)`. Implementation only fires when `filteredRows.length === 0 && !hasActiveFilter && !importableInMerged`. Spec gap.
  - **LOW (F4)**: `relativeTime.ts` doc comment says "30 days to 365 days → Nw ago" but code uses `weeks < 53` (= 371 days). At 365 days renders "52w ago" not "1y ago". Doc-vs-code drift.
  Independently re-ran `bun run test` (127/477), `bun run test:e2e` (1 passed), `bun run build` (clean). Confirmed F2 click-time intersection regression test source preserved verbatim, sort null-handling matches spec, `relativeTime.ts` never reads `Date.now()`, importability rule enforced end-to-end through shared `isImportable` at 4 points, `selected: Set<string>` only contains `sourceSessionKey` values.
- 2026-04-25: Chunk D Codex cross-agent review round 1 — verdict `needs changes`. Codex-cli `0.124.0`, prompt at `/tmp/codex-phase-4-chunk-d-prompt.txt` (277 lines), stdout at `/tmp/codex-phase-4-chunk-d-output.txt`. Codex caught the SAME F1 finding the normal reviewer caught (independent confirmation), PLUS:
  - **F2 (BLOCKING)**: Scan-errors error path in `App.tsx` line ~386 has no Retry affordance — only renders `<p role="alert">` without a Retry button. Spec mandates Retry on partial fetch failure.
  - **F3 (Missing Evidence — 3 corrupt-input tests)**: `useSessionFilters.test.ts` doesn't cover unknown `storage`, unknown `sort.direction`, undefined `localStorage`. Decoder appears to handle them but the test matrix is incomplete (the evidence pack claimed 9+ paths but only ~9 were tested; these 3 were missed).
  Codex independently confirmed all OTHER gates green (cargo check/test, drift check, bun run test 127/477, bun run build, sort determinism, `.chip.active` contrast).
- 2026-04-25: Codex + Normal raised findings (round 1) dispatched to a follow-up developer subagent with narrow scope (5 files only). Developer applied:
  - **F1 fix**: `SessionsView.getEmptyStateKind` rewritten with new branch order: `mergedRows.length === 0` → `no_sessions_at_all`; else `filteredRows.length === 0` → `no_matches_after_filter`; else `filteredRows.every(r => !isImportable(r))` → `nothing_to_import`. The `nothing_to_import` branch renders the empty-state copy + "Show importable only" affordance ABOVE the populated table (NOT suppressing the table). Developer rationale: spec text says "matching rows exist" implying rows ARE visible; strict suppression would break the e2e test (single seeded row becomes `up_to_date` post-import → if table suppressed, e2e step 6 can't find `View Raw` link). Spec lines 381-386 do NOT explicitly require table suppression; only the copy + affordance. The existing third empty-state test in `App.test.tsx` was rewritten to exercise the spec scenario directly. Codex round 2 explicitly approved this design call.
  - **F2 fix**: Added Retry button to scan-errors error case in `App.tsx`: `<button type="button" onClick={() => void refetchAll()}>Retry</button>` inside the alert. New test `scan-errors error path: Retry button refetches all three panels` verifies the button + click + 3-panel refetch + scan-errors panel transition error → ok.
  - **F3 fix**: Added 3 new tests to `useSessionFilters.test.ts`: unknown `storage` enum → defaults; unknown `sort.direction` → defaults (with valid `field` preserved); undefined `localStorage` (`Object.defineProperty(globalThis, "localStorage", {value: undefined})`) → in-memory fallback (distinct from existing throws-on-access test).
  - **F4 fix**: `relativeTime.ts` comment updated to say "30 days to 371 days" with explanatory note. Code unchanged.
  Total: 4 net new tests (127 → 131 tests / 477 → 497 expects). 5 files touched (3 modified + 2 untracked-from-Chunk-D edited). Coordinator independently re-ran all gates: `bun run test` 131/497, `bun run test:e2e` 1 passed (1.3s), `bun run build` clean.
- 2026-04-25: Chunk D Codex cross-agent review round 2 (post-fix-up) — verdict `approved`; findings `none`; missing evidence `none`; required changes `none`. Codex-cli `0.124.0`, prompt at `/tmp/codex-phase-4-chunk-d-prompt-v2.txt` (87 lines), stdout at `/tmp/codex-phase-4-chunk-d-output-v2.txt`. Codex notes: "Accepted the populated-table-alongside-empty-copy choice as defensible: `working/phase-4.md` lines 381–386 require the copy/affordance when matching non-importable rows exist, but do not explicitly require table suppression. Re-ran `bun run test`: `131 pass / 0 fail / 497 expect() calls`."
- Re-review decision: Backend-protection and normal reviewer Claude subagent NOT re-run after the fix-up. Rationale (Phase 3 + 4 precedent): (a) backend-protection scope (protected paths, deps, bindings, contract-drift gate) is invariant under code-only edits across 5 files all under `apps/frontend/`; (b) normal reviewer raised F1 + F4 — both addressed by the fix-up; their other concerns are correctness invariants Codex round 2 verified; (c) Codex raised F2 + F3 + F1 (independent confirmation) — Codex round 2 re-approved.
- Chunk D final disposition: approved by all three required reviewers on converged evidence — `backend untouched` (backend-protection round 1), `needs changes` → fixed (normal reviewer round 1; F1 + F4 addressed), `approved` (Codex round 2 after F1 + F2 + F3 + F4 fix-up). Committed as `0ca1082`.

## External Reviewer Availability Log

- 2026-04-25: `codex exec` confirmed available at `codex-cli 0.124.0`. No invocations yet at session start.
- 2026-04-25: Codex invoked for Chunk B round 1 at `codex-cli 0.124.0`. Prompt piped from `/tmp/codex-phase-4-chunk-b-prompt.txt`; stdout captured verbatim at `/tmp/codex-phase-4-chunk-b-output.txt`. Verdict `needs changes`. Two findings: (1) badge AA contrast fails in light mode for 3 of 4 badges; (2) `app.css` header overclaims about token coverage.
- 2026-04-25: Codex invoked for Chunk B round 2 at `codex-cli 0.124.0`. Prompt piped from `/tmp/codex-phase-4-chunk-b-prompt-v2.txt`; stdout captured verbatim at `/tmp/codex-phase-4-chunk-b-output-v2.txt`. Verdict `approved`. Codex independently reproduced the badge contrast measurements from the checked-in token values and confirmed all 8 pairs ≥ 4.5; verified the rewritten header enumerates all five structural literals it flagged in round 1.
- 2026-04-25: Codex invoked for Chunk C round 1 at `codex-cli 0.124.0`. Prompt piped from `/tmp/codex-phase-4-chunk-c-prompt.txt`; stdout captured verbatim at `/tmp/codex-phase-4-chunk-c-output.txt`. Verdict `needs more evidence`. ONE missing-evidence item: `bun run test:e2e` could not run in Codex's sandbox (EPERM on port binding); Codex explicitly noted this is environmental, not a code finding. All other gates confirmed green by Codex.
- 2026-04-25: Codex invoked for Chunk C round 2 at `codex-cli 0.124.0`. Prompt piped from `/tmp/codex-phase-4-chunk-c-prompt-v2.txt`; stdout captured verbatim at `/tmp/codex-phase-4-chunk-c-output-v2.txt`. Verdict `approved`. Round-2 prompt provided host-of-record verbatim e2e output (`1 passed (1.5s)`) plus the 2 inline nit fixes; Codex accepted both as adequate.
- 2026-04-25: Codex invoked for Chunk D round 1 at `codex-cli 0.124.0`. Prompt piped from `/tmp/codex-phase-4-chunk-d-prompt.txt`; stdout captured verbatim at `/tmp/codex-phase-4-chunk-d-output.txt`. Verdict `needs changes`. 3 findings: F1 (Nothing-to-import empty state never fires per spec — independent confirmation of normal reviewer's F1), F2 (scan-errors Retry missing), F3 (3 missing corrupt-input tests).
- 2026-04-25: Codex invoked for Chunk D round 2 at `codex-cli 0.124.0`. Prompt piped from `/tmp/codex-phase-4-chunk-d-prompt-v2.txt`; stdout captured verbatim at `/tmp/codex-phase-4-chunk-d-output-v2.txt`. Verdict `approved`. Codex re-ran `bun run test` (131/497) and accepted the populated-table-alongside-empty-copy design choice for F1 as defensible per spec text.

## Protected-Path Exception Log

- 2026-04-25: pre-recorded standing exception for Milestone 6 docs sweep on `components/ui-api-contracts/README.md`.
  - Approved scope: revise the paragraph that currently describes how the frontend constructs `source_key` inline. New rule per `working/phase-4.md` §Documentation: import identity is always the backend-provided `SourceSessionView.session_key`; the UI must never construct or mutate that value. React-only row identity may add a `stored:${session_uid}` fallback for `stored_only` rows; the README paragraph must distinguish these two cases.
  - NOT approved: any change under `components/ui-api-contracts/src/**`, `components/ui-api-contracts/bindings/**`, or `components/ui-api-contracts/Cargo.toml`. No new contract types. No regeneration of TS bindings. No paragraph changes outside the `source_key` description without a fresh human exception.
  - Reason: the spec explicitly assigns this README update to Phase 4 because the inline-construction rule changes (Phase 3 ended with the frontend never inventing `source_key`; Phase 4 makes that contract explicit at the README level). No code or wire-shape change is required.
  - Reviewer obligation: backend-protection reviewer must confirm any edit under `components/ui-api-contracts/` falls within the documentation-only scope above; the bindings directory diff must remain 0 lines; the contract-drift test (`cargo test -p distill-portal-ui-api-contracts --features ts-bindings`) must remain green.
  - Exercised so far: never. Reserved for Milestone 6.

## Open Risks / Open Questions

- **Codex catches Claude blind spots (precedent confirmed in M1)** — Phase 3 Codex caught six legitimate blocking findings both Claude reviewers missed. Phase 4 Chunk B added a seventh (badge AA contrast + header overclaim, both missed by backend-protection AND normal reviewer). The three-reviewer rule remains non-skippable; expect multi-round cycles especially around the click-time intersection rule (M3), the streaming raw-preview byte cap (M4), and the docs sweep (M6).
- **Filter-then-select race (M3 timing)** — the spec explicitly mandates the click-time intersection filter ships in Milestone 3, not Milestone 5. The Phase 3 F2 fix proved a passive `useEffect` alone is insufficient; the click-time intersection at the import handler is required. Regression test must exercise the actual race window (Phase 3 F2 used a microtask hook on the button text-node setter; Phase 4 should repeat that pattern for the filter-only variant in M3 and the pagination variant in M5).
- **Raw-preview streaming cost (M4)** — `/api/v1/sessions/:uid/raw` has no range support; a blocking `.text()` on a tens-of-MB session would freeze the drawer. Spec mandates `ReadableStream` + `getReader()` + `TextDecoder` with short-circuit at 20 lines OR 256 KB byte cap (whichever first) via `reader.cancel()`, plus `AbortController` for both pre-cap and post-cap drawer-close. Component test must feed a >256 KB mock stream to prove the byte cap genuinely cancels the reader (not just stops parsing).
- **Native `<dialog>` focus semantics (M4)** — Chromium-only target per spec. Playwright is the real-browser gate. If Playwright assertions fail, the documented escape-hatch focus-management package may land — but only with that evidence in hand. Do not pre-empt the dependency.
- **Pagination split-selection (M5)** — pagination splits raw selection across pages but the effective selection is filter-wide. Spec mandates "Select all importable in current filter" affordance + "+K hidden by filters" caption + click-time re-derivation on retry. M5 regression test must cover the pagination-cross-page variant of the click-time intersection rule (in addition to M3's filter-only variant).
- **Documentation sweep breadth (M6)** — eight docs files cited explicitly in `working/phase-4.md` §Documentation must update in lockstep with the code. Spec mandates "Docs updates land with the chunk that introduces the change, not in a trailing pass" — each milestone commits its docs delta. M6 cleanup verifies via `rg` that no stale dual-table references survive. Chunk B touched only `apps/frontend/README.md` (Entry Points list); the other seven docs are sequenced to M2–M6 per the spec.
- **Dark-mode contrast (M6)** — every visible foreground/background token pair must meet WCAG AA in BOTH light and dark modes; the check is recorded in this progress log at M6 close. **Pre-empted in M1 for the four badge text/background pairs**: all 8 (4 light + 4 dark) are now ≥ 5.176:1 thanks to the Codex-driven 70/30 darkened-foreground recipe. M6 still must measure the remaining text/background pairs (`--color-text` on `--color-bg`, `--color-text-muted` on `--color-bg`, `--color-text-muted` on `--color-surface`, `.raw-link` color `--color-accent` on body bg, `.action-bar button` text `--color-text` on `--color-surface`, etc.). Eyeball check of `--color-warn` text on `--color-surface` (light) was ~4.4:1 in the developer's pre-commit Handoff Notes — borderline; M6 should formally measure.
- **`bun run test:e2e` host environment** — Phase 3 H1/G2 documented WSL system-lib gaps. Resolved 2026-04-25 between Chunk B and Chunk C: human installed `libnspr4`/`libnss3`/etc. system deps. Coordinator's host now runs `bun run test:e2e` against real Chromium (`1 passed (1.5s)`). Codex sandbox still gets EPERM on port binding — that's a documented Codex limitation, NOT a coordinator-host issue. Pattern for the remaining milestones: developer + coordinator run e2e on host of record; Codex sandbox EPERM is acceptable evidence gap when the coordinator supplies verbatim host-of-record output.
- **`apps/frontend/.codex` sentinel** — left over from Phase 3 Codex review invocations; still untracked. Phase 4 may add `.codex` to `apps/frontend/.gitignore` opportunistically (any chunk that already touches `.gitignore`); not a blocker on its own.
- **Spacing-quantization rounding from M1** — forcing the existing freeform spacings onto the spec's quantized `--space-*` tokens shifted four values by ~0.05–0.1rem (`.action-bar button` padding, `.stack` gap, `.panel h2` font-size, `td` font-size). Documented as intentional in M1 commit. M2's `SessionsView`/`SessionsTable` reuse the same `app.css` selectors (per planner recommendation), so the rounding inherits unchanged. M6 retires `app.css` and is the natural moment to revisit if the visual deltas matter.
- ~~**`mergeSessions` per-render memoization (M2 nit, deferred to M3)**~~ — Resolved by Chunk D: `App.tsx` now memoizes `mergeSessions(sourceData, storedData)` via `useMemo` over `[sourceData, storedData]`. The handler `handleImport` ALSO does a fresh re-derivation at click time (defense-in-depth against memoized stale closures). All call sites (`handleToggleAll`, `selectedCount`, render-time `filteredRows`) read from the same memoized chain.
- **App.test.tsx test-count discrepancy in Chunk C report** — developer's evidence pack claimed "11 tests / 89 expects" in `apps/frontend/src/App.test.tsx`; coordinator's grep counted 10 tests. Total `bun run test` count of 48 is consistent with 10 (App) + 16 (mergeSessions) + 7 (SessionsTable) + 3 (ScanErrorsCallout) + 4 (StatusBadge) + 8 (ActionBar). Off-by-one documentation nit; not a behavior issue.
- **`statusConflict` M2 affordance vs M4 drawer badge** — M2 row affordance is a minimal `(refresh)` text + title-attribute hover hint near the StatusBadge. The full conflict badge UI is a Milestone 4 deliverable in the drawer header. M4 must add the drawer-side badge AND must NOT remove the row affordance (the row hint is still useful when the drawer is closed).

## Next Recommended Task

- **Milestone 4 planning**: dispatch the planner subagent to propose 1–3 tractable chunks for Milestone 4 (Session detail drawer with streaming raw preview). Spec scope per `working/phase-4.md` §Milestones → Milestone 4:
  - Native `<dialog>` + `showModal()`-backed drawer (Chromium-only target per spec; no `focus-trap-react` unless documented Playwright reproducer fails)
  - Drawer body: every field from `SessionRow`, absolute + relative timestamps with source-clock annotations, copy-to-clipboard source path, `statusConflict` badge in the drawer header (alongside the M2 row `(refresh)` affordance), `sourcePathIsStale` "last seen source path" label, raw-payload preview
  - **Streaming raw preview** is the M4 hardest deliverable: consume `/api/v1/sessions/:uid/raw` via `ReadableStream` + `getReader()` + `TextDecoder` with incremental line buffer; **short-circuit at 20 NDJSON lines OR 256 KB byte cap, whichever first** by calling `reader.cancel()` so the rest of the body is not drained; parse each line as JSON with plain-text fallback; `AbortController`-cancelable on drawer close (BOTH pre-cap AND post-cap windows). `.text()` shortcut explicitly forbidden because raw payloads can be tens of MB.
  - Wire row click + Enter keyboard → opens drawer; Esc + close button + backdrop → closes drawer
  - Tests: drawer focus-trap + Esc-close + backdrop-close + focus-restoration covered in BOTH happy-dom component tests AND real-Chromium Playwright. Raw-preview component tests cover loading, success (N-lines caption), oversized-body byte-cap short-circuit (fake stream of >256 KB proves the reader is canceled before draining), network failure, non-2xx, non-JSON-fallback, abort-on-close before AND after the cap
  - DoD: every contract field surfaces in the drawer; raw preview renders all 6 states; drawer focus-trap + Esc + backdrop + focus-restoration covered in both component tests AND Playwright (if Playwright fails the documented reproducer, the documented escape-hatch focus-management package may land — but only with that evidence in hand)
  - Open questions for the planner: (a) one chunk or split (raw-preview streaming logic is the hardest part — could be its own chunk)? (b) does the drawer's `<dialog>` body live under `apps/frontend/src/features/sessions/SessionDetail.tsx` AND `apps/frontend/src/components/Drawer.tsx` (per spec §Target Repository Shape) or a flatter layout? (c) does the "copy to clipboard" affordance need a fallback for environments where `navigator.clipboard` is unavailable? (d) should the raw-preview AbortController integration be tested via a fake Response with a hand-built ReadableStream, or via a mock-service-worker shim?
