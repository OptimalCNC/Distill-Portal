# Historical Delivery Logs

Each `phase-N.progress.md` in this directory is the frozen durable coordination log for a completed phase — the full evidence and review trail captured at delivery time. These documents are preserved for provenance.

They are not current-state instructions.

For live status see `../docs/` and the `README.md` files under `../apps/` and `../components/`.
