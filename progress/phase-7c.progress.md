# Phase 7c Progress Log

## Source-Of-Truth Reference

- Task spec: `working/phase-7c.md`
- Baseline commit: `4e3318b` (Phase 7b close; Phase 7c builds on its structured ParseWarning shape and `🎨 deferred to 7c` worklist).
- Architecture references: `README.md`, `ARCHITECTURE.md`, `PRD.md`, `docs/README.md`, `docs/dependency-rules.md`, `docs/dev-commands.md`.

## Task Invocation Block (inferred)

- `task_name`: `phase-7c`
- `task_spec_path`: `working/phase-7c.md`
- `progress_log_path`: `progress/phase-7c.progress.md`
- Protected paths: `apps/backend/**`, `components/**`, `tests/e2e/**`, `Cargo.toml`, `Cargo.lock`, `apps/frontend/bun.lock`, `apps/frontend/package.json` runtime dependency sections.
- Protected exceptions: none.
- Forbidden scope: backend changes, parser logic changes (closed in Phase 7b), Skim view changes, new tool support, search-within-transcript, inline diffing, message pinning, virtualization unless documented Chromium reproducer fires, Operations/Jobs route, changing streaming or 5 MB cap.
- Architecture refs: `README.md`, `ARCHITECTURE.md`, `PRD.md`, `docs/README.md`, `docs/dependency-rules.md`, `docs/dev-commands.md`.
- Required verification (apply where relevant per chunk): `bun run test`, `bunx tsc --noEmit`, `bun run build`, `bun run test:e2e`, `cargo check --workspace`, `cargo test --workspace`, hex invariant `rg -o "#[0-9A-Fa-f]{3,8}" apps/frontend/src | wc -l == 24`, token invariant `grep -cE '^\s*--' apps/frontend/src/styles/tokens.css == 83`, marker invariant `grep -c "@unskip Phase 7c" apps/frontend/src/ == 0`.
- `external_reviewer_command`: `codex exec`.
- `ui_ux_skill`: `frontend-design:frontend-design`.
- `ui_ux_artifact_root`: `working/phase-7c/designs/`.

## Current Snapshot

- Phase 7c kickoff. Phase 7b closed at `4e3318b`. The frontend renders `TranscriptView` as a flat chronological message list with no lifecycle pairing or grouping. `ParseWarning` already carries `severity` + `category` + `messageIndex` (Phase 7b output) ready for the 4-bucket classification.
- Two `@unskip Phase 7c` markers remain (`codex-event-msg-task-complete`, `codex-event-msg-task-started`) in `apps/frontend/src/features/sessions/TranscriptView.event-coverage.test.tsx:103-104`.
- Hex invariant baseline: 24; token invariant baseline: 83. To be preserved unless explicitly amended.

## Active Plan

- Phase 7c implementation complete. Three milestones delivered + reviewed + approved. Awaiting commit per coordinator/human direction.

## Remaining Chunks

- None. All M1/M2/M3 work + reviews closed.

## UI/UX Design Log

- Coordinator UI/UX gate decision for Phase 7c arc: **needs UI/UX work**, rationale: Phase 7c introduces brand-new visible structures (lifecycle pair card, grouped tool-call card with count badge + aggregate status, inline warning chip, expand/collapse affordances, new motion budget for expand/collapse). The spec mandates the design gate in Resolved Decision #10. Artifact root: `working/phase-7c/designs/`.
- **M1 design artifact**: `working/phase-7c/designs/` — `design.md` (~55 KB), `prototype.html` (~70 KB, 4 task-lifecycle variants light + dark, 4 chip-bucket variants light + dark, group-head light + dark in 4 statuses, lifecycle paired success/failure/orphan-use/orphan-result variants), 12 wireframes, `wcag.py` (37+4 = 41 pairs, 0 failures), `wcag-output.txt`.
- **M1 operational decisions locked** in design.md:
  1. Pairing mode: **strict-adjacency** (rationale: Phase 7b sweep showed every tool call adjacent-paired across 408 + 737 files).
  2. Grouping threshold: **3** (2-pairs read as adjacent lifecycles; 3+ reads as a list).
  3. Failure-detection heuristic: **text-string scan on `tool_result.text`** with per-tool keyword tables (parser collapses structured fields into text via `eventResultText` / `stringifyMessagePayload`).
  4. 4-bucket warning mapping: error → render-normally; warning/{schema,payload} → render-normally; warning/{lexer,timestamp} → collapse-by-default; warning/meta → warning-only; info → hide-with-inspect.
  5. Inline chip placement: **below message body** at 12 px margin; hide-with-inspect at bottom-right corner.
  6. Group-head aggregate timing: **count only** (no duration — tool_use/tool_result timestamps often `null` in real Codex sessions).
  7. Chip visibility from collapsed group: **hidden until expanded** (preserves skimming benefit; banner remains comprehensive surface).
- **Task-lifecycle treatment**: Option B — `RenderHint.standalone` is extended with optional `taskLifecycle?: "started" | "complete"`. `renderHints.ts` stamps the attribute when `Message.kind === "system"` AND `Message.text` starts with `"task_started · turn "` / `"task_complete · turn "` (verbatim parser emission per `codex.ts:482` / `:500`). `TranscriptView` renders the dedicated `.msg-task-lifecycle` card when the attribute is present. CSS class `.msg-task-lifecycle` aligns with the existing `assertTreatment` selector at `TranscriptView.event-coverage.test.tsx:195`.
- **UI/UX review loop**:
  - Round 1 (UI/UX reviewer Claude subagent): `needs changes`. Blocker: missing task_lifecycle render treatment for `codex-event-msg-task-started`/`task-complete`. Plus 4 nits + WCAG re-run pending.
  - Designer revision pass: added §6.5 task-lifecycle recipe, wireframe 12, 4 prototype variants, RenderHint extension, 5 numbered acceptance items, 4 new WCAG pairs (P38-P41). Closed all four nits. Could not execute `python3 wcag.py` due to harness classifier limitation.
  - Round 2 (UI/UX reviewer Claude subagent): `approved with nits`. Closed all 5 round-1 findings. One residual minor nit (two `<span class="tool-name">exec</span>` occurrences at lines 1341/1757 in group-mixed variants) — fixed by coordinator inline (replaced with `rg`) since the prototype is reference-only and the fix is single-line.
- **External design review**: not invoked. Optional per the coordinator prompt; not triggered because (a) the design lives entirely within the locked Phase 5 Archive-room aesthetic with zero new tokens and zero new motion, (b) the Option-A-vs-B decision was non-architectural and self-contained, (c) the failure-detection heuristic is the only above-average-risk decision and is documented with a fallback for a future parser refactor. Recorded for traceability: external design review optional + skipped.
- **WCAG verification**: `wcag.py` script committed with 41 pairs. The reviewer (round 2) confirmed P38-P41 are byte-equivalent token-combinations to P01/P02 (border/surface, 1.49:1 light / 1.35:1 dark) and P06/P08 (ink-muted/surface, 7.04:1 light / 7.36:1 dark) which were already audited. The script could not be executed in the coordinator's main session due to a persistent classifier outage that blocked all `python3 ...` invocations across both subagent and coordinator sessions. Carry-over: re-execute `python3 working/phase-7c/designs/wcag.py` when the classifier recovers and verify zero drift; record outcome in this log. The on-paper recorded output of zero failures is treated as the working assumption for M2 hand-off.

## Completed Work Log

- 2026-05-16: Coordinator initialized progress log per `coordinator-prompt.md` schema. Verified Phase 7b baseline (hex=24, tokens=83, two `@unskip Phase 7c` markers remaining). Dispatched UI/UX designer subagent for M1.
- 2026-05-16: M1 design loop round 1 — designer produced `working/phase-7c/designs/` artifacts (design.md, prototype.html, wireframes/, wcag.py, wcag-output.txt). UI/UX reviewer round 1 returned `needs changes` (blocker: missing task-lifecycle render treatment).
- 2026-05-16: M1 design loop round 2 — designer revision added §6.5 task-lifecycle treatment + wireframe 12 + 4 prototype variants + Option B `RenderHint.standalone.taskLifecycle?` extension + 4 new WCAG pairs. Closed all 4 nits. UI/UX reviewer round 2 returned `approved with nits` (residual `exec` strings in two group-mixed variants). Coordinator fixed the residual `exec` → `rg` inline in `prototype.html` (single Edit replacing both occurrences).
- 2026-05-16: M1 closed. Dispatched M2 developer with the approved design artifact + implementation-acceptance checklist.
- 2026-05-16: M2 developer (round 1) created `apps/frontend/src/features/sessions/renderHints.ts` (~370 LOC) + `renderHints.test.ts` (38 tests). Refactored `TranscriptView.tsx` to switch on `RenderHint.kind` first, added `LifecycleCard`, `TaskLifecycleCard`, `InlineWarnings`, `Chip` components. Extended `TranscriptView.css` with `.msg-lifecycle`, `.msg-task-lifecycle`, `.transcript-chip*`, `.status-dot[data-status]` recipes. Lifted both `@unskip Phase 7c` markers (lines 103-104 of `TranscriptView.event-coverage.test.tsx`). 695 pass / 0 fail.
- 2026-05-16: M2 three-reviewer rule round 1: backend-protection `backend untouched`; normal reviewer `approved with nits` (3 minor nits); external `codex exec` `needs changes` with 1 BLOCKER + 2 spec-reconciliation findings.
- 2026-05-16: M2 developer revision pass: fixed BLOCKER (taskLifecycle precedence) + added 2 spec-reconciliation comment blocks (M2/M3 grouping split + strict-adjacency pairing decision) + 3 regression-guard tests. 698 pass / 0 fail.
- 2026-05-16: M2 three-reviewer rule round 2: backend-protection `backend untouched`; normal reviewer `approved with nits`; external `codex exec` `approved` (no nits). M2 closed.
- 2026-05-16: Dispatched M3 developer with the M2 close + design artifact + implementation-acceptance checklist.
- 2026-05-16: M3 developer (round 1) extended `renderHints.ts` with `groupLifecycles` second pass (boundary + tool-name reset, threshold = 3, 4 aggregate-status kinds via existing `detectFailure`). Extended `TranscriptView.tsx` with `<GroupHeadRow>` + `renderTopLevelHints` walker + `LifecycleCard inGroup` prop. Added `.group-card` / `.group-members` / `.group-member` CSS. Drove both 🎨 matrix rows to ✅ supported + closed Phase 7c Worklist. Doc sweep across 4 docs (`docs/features/session-view.md`, `docs/playbooks/modify-frontend-page.md`, `docs/README.md`, `docs/features/parser-event-support.md`). 724 pass / 0 fail. cargo + e2e green.
- 2026-05-16: M3 three-reviewer rule round 1: backend-protection `backend untouched`; normal reviewer `approved with nits` (2 nits including the status-count drift); external `codex exec` `needs changes` with 2 BLOCKERS (renderTopLevelHints unbounded member consumption + CSS direct-child selector mismatch) + status-count drift confirmed. Coordinator inline-fixed the status counts to "36 supported / 12 silenced".
- 2026-05-16: M3 developer revision pass round 2: fixed BLOCKER #1 with dual bound on member consumption (count cap + canonical head pointer match); fixed BLOCKER #2 with descendant selector + double-spacing removal. Added 3 regression tests (1 unit + 2 DOM) + E2E coverage note. 727 pass / 0 fail.
- 2026-05-16: M3 three-reviewer rule round 2: backend-protection `backend untouched`; normal reviewer `approved` (no nits, no findings); external `codex exec` `approved` (no nits, no findings). M3 closed.
- 2026-05-16: Phase 7c implementation complete. All 3 milestones delivered; all 6 review rounds (M1×2 design + M2×2 + M3×2 three-reviewer) closed at approval. Marker count = 0, 🎨 row count = 0, hex = 24, tokens = 83. Awaiting commit per human direction.
- 2026-05-16: **User-reported real-session findings → polish patch.** User viewed a real Codex session (`claude_code:7355d3a6-adb3-44a5-b1a8-a178bf1e4017`) and surfaced two issues:
  1. Same-tool grouping fragmented mixed-tool agent sequences. Two consecutive `Edit`s + 1 Bash + N tools didn't collapse because the M3 implementation required all members to share a tool name. The user requested grouping ANY consecutive tool uses, with the group head showing the distinct tool-type list ("Edit, Bash, Agent").
  2. Empty-body `Assistant` rows rendered as bare "Assistant · 8d ago" cards. The Codex parser emits empty-text rows when `event_msg.agent_message`/`agent_reasoning`/`user_message` payloads are missing their `message`/`text` field; the matching `warning/payload` warning lacks `messageIndex` so it surfaces only via the banner, leaving the inline card empty.
  Coordinator implemented inline (no developer subagent given the focused change scope):
  - `apps/frontend/src/features/sessions/renderHints.ts`: dropped the same-tool predicate from `groupLifecycles` (mixed-tool grouping); added `toolNames: string[]` to `RenderHint.group-head` (distinct tool names in first-appearance order); added empty-body suppression for `user`/`assistant` Messages where `text.trim() === ""` (emits `warning-only` when attached inline warnings exist, else no hint).
  - `apps/frontend/src/features/sessions/TranscriptView.tsx`: `<GroupHeadRow>` now displays `hint.toolNames.join(", ")` (legacy `hint.toolName` fallback retained for defensive paths).
  - Tests: 3 existing tests rewritten for new mixed-tool semantics (`renderHints.test.ts`: "mixed-tool grouping (post-7c polish): 3 Read + 1 Bash...", "mixed-tool grouping: toolNames preserves first-appearance order...", "mixed-tool grouping (post-7c polish): 3 Read + 3 Bash..."; `TranscriptView.test.tsx`: "Polish: 3 Read + 1 Bash...", "Polish: 3 Read + 3 Bash..."). 5 new tests added (`renderHints.test.ts`: "two Edits across boundary stay ungrouped...", "empty-body assistant message (no warnings)", "empty-body assistant message (whitespace-only)", "empty-body user message (no warnings)", "empty-body assistant message WITH attached warning → warning-only hint", "non-empty assistant message still renders", "empty-body system message is NOT suppressed"; `TranscriptView.test.tsx`: "Polish: empty-body assistant message renders no .msg-assistant card", "Polish: non-empty assistant message still renders normally").
  - Docs: `docs/features/session-view.md` "Tool lifecycle + grouping" subsection updated — "Same-tool grouping" → "Mixed-tool grouping" with tool-name list semantics; new "Empty-body suppression" bullet.
  - Verification: `bun run test` 737 pass / 0 fail / 2403 expects (was 727); `bunx tsc --noEmit` clean; `bun run build` green (CSS 42.24 kB, JS 281.17 kB). Invariants: markers=0, hex=24, tokens=83, 🎨=0.
- 2026-05-16: Polish patch three-reviewer rule:
  - Backend-protection reviewer (Claude subagent): `backend untouched`. Verified empty diff against all protected paths.
  - Normal reviewer (Claude subagent): `approved` (no nits, no findings). Confirmed mixed-tool grouping correctness (`groupLifecycles` no longer enforces same-tool predicate; `toolNames` distinct in first-appearance order); empty-body suppression placement (after `tool_use`/`tool_result`, before task-lifecycle detection; scope limited to user/assistant); `<GroupHeadRow>` displays comma-joined list with back-compat fallback; verification 737/0/2403, tsc clean, build green, all invariants preserved.
  - External reviewer (`codex exec --model gpt-5.2 --reasoning-effort medium`): `approved` (no nits, no findings). Independently confirmed: mixed-tool grouping, threshold preserved at 3 (sub-threshold 2-Edits stays ungrouped — matches user's reported case), `toolNames` distinct+first-appearance order, renderer fallback, suppression correctness + scope + placement. Optional note: `group-head.toolName` can be `""` if defensive path hits a missing `tool_use.toolName` — acceptable per back-compat intent.
- Polish patch closed. Three-reviewer rule satisfied. Phase 7c implementation + polish all approved.
- 2026-05-16: **User-reported real-session bug (polish round 2).** After polish round 1 closed, the user viewed a real Claude Code session and reported that 2 consecutive Edits with assistant text between them were STILL not grouping. Root cause: the polish-r1 unit tests used synthetic `pair(0)`/`pair(2)`/...` calls that placed lifecycles back-to-back with no intervening messages, so the algorithm tested with `groupLifecycles` was correct against the synthetic input but did not match what the parser actually emits for real sessions. The Claude Code parser splits `assistant.content[].text` and `assistant.content[].tool_use` into separate Messages, so a turn with 2 Edit calls produces: `assistant(text)` → `tool_use` → `tool_result` → `assistant(text)` → `tool_use` → `tool_result` → `assistant(text)`. Even with mixed-tool grouping, the assistant-text Messages between Edit calls broke the consecutive-lifecycle run and kept the threshold-of-3 check from firing.
  - Two fixes applied:
    1. **Lowered `GROUP_THRESHOLD` from 3 to 2** so even 2 consecutive lifecycles collapse.
    2. **"Tool-batch" passthrough grouping**: extended `groupLifecycles` in `apps/frontend/src/features/sessions/renderHints.ts` so the buffer accumulates `lifecycle` hints AND `assistant`/`system` passthrough standalones. The run flushes only on delimiters: `user` standalone, `boundary`, `warning-only`, task-lifecycle stamped standalones (`task_started`/`task_complete`), `unknown`, orphan `tool_result` with stray chip, end-of-stream. Added new `group-text-member` variant to the `RenderHint` union to carry the passthrough standalones into the group's expanded body.
  - Render layer changes (`apps/frontend/src/features/sessions/TranscriptView.tsx`):
    - `renderTopLevelHints` walker consumes BOTH `group-member` and `group-text-member` after a `group-head`. Defensive bounds: count cap on lifecycle members; canonical head pointer match on all members (lifecycle or text).
    - `<GroupHeadRow>` `members` prop accepts both kinds; the expanded-body map renders text members via `<StandaloneRow>` (so the agent's commentary surfaces inside the group) and lifecycle members via `<LifecycleCard inGroup>`.
    - `hintKey` + `HintRow` defensive no-op extended to handle `group-text-member`.
  - Tests:
    - 5 existing tests rewritten to match the new threshold + passthrough semantics.
    - 4 new tests added covering: the user's exact scenario (2 Edits with 3 assistant texts between them → 1 group); top-level stream collapse with trailing assistant pulled in; threshold = 2 group; 2-Edit-across-boundary stays ungrouped.
    - **NEW: parser-driven integration test.** `tests/fixtures/render-hints/claude_code/two-edits-with-assistant-text.jsonl` is a 6-line synthetic real-session shape. `renderHints.test.ts` parses it via `dispatchParser` (the real production pipeline) and asserts the resulting hint stream collapses to 1 group with 2 Edit lifecycles + 3 assistant-text members. This is the kind of test that would have caught the polish-r1 gap — it tests the algorithm AGAINST PARSER OUTPUT, not against a synthetic stream the test author hand-crafted to match the algorithm's assumptions.
  - Verification: `bun run test` 741 pass / 0 fail / 2452 expects (was 737 pre-r2; +4 net); `bunx tsc --noEmit` clean; `bun run build` green (CSS 42.24 kB / JS 281.94 kB). Invariants: markers=0, hex=24, tokens=83, 🎨=0.
  - Honest answer to the user's meta-question ("is the rendering code well-written so that it can be tested independently?"): the code IS architected for independent testing — `renderHints` is a pure function with no React/DOM coupling. The gap was in the TEST FIXTURES, not the production code. Synthetic `pair()` helpers tested the algorithm's invariants but didn't match what the parser actually emits. The new fixture-driven integration test bridges this gap. Lesson recorded for future render-layer work: pure-function unit tests prove the algorithm is correct; fixture-driven integration tests prove the algorithm matches the parser's real output. Both are necessary.
- 2026-05-16: **User confirmed the fix works** against the real session `claude_code:7355d3a6-adb3-44a5-b1a8-a178bf1e4017`. User asked the coordinator to formally run reviewers AND introduced a NEW reviewer role: **QA test-coverage reviewer** — specifically tasked with assessing whether the test suite has enough coverage that a polish-r1-class regression (synthetic tests passing while real-session bug ships) would now be caught.
- 2026-05-16: Polish-r2 four-reviewer rule (round 1):
  - Backend-protection (Claude subagent): `backend untouched`. All protected paths empty diff.
  - Normal reviewer (Claude subagent): `approved with nits`. Two doc nits: stale "GROUP_THRESHOLD = 3" in `docs/features/session-view.md:46` and `renderHints.ts` header docblock.
  - NEW QA test-coverage reviewer (Claude subagent): `approved with gaps`. Confirmed the parser-driven integration test does meaningful work. Flagged 4 critical untested delimiter branches (system passthrough, orphan tool_result delimiter, warning-only delimiter, task-lifecycle delimiter) — gaps that could let a polish-r1-class regression slip through. Recommended 4 unit tests + 2 fixture-driven tests + 1 DOM test.
  - External codex reviewer (`codex exec --model gpt-5.2 --reasoning-effort medium`): `needs changes`. Real bug both Claude reviewers missed: the `isPassthroughStandalone` predicate accepted ALL `system` standalones (no taskLifecycle stamp), which would have pulled Codex `event_msg.error` rows (`kind:"system"` per `codex.ts:681`) into collapsed tool-batch groups — hiding error and lifecycle signal until expand. Recommended dropping `system` from passthrough entirely (preferred conservative option).
- 2026-05-16: Polish-r2 sub-iteration fix applied by coordinator:
  - **`isPassthroughStandalone` narrowed** in `apps/frontend/src/features/sessions/renderHints.ts`: now accepts ONLY `assistant`. `system` is intentionally excluded; helper JSDoc cites the Phase 7b matrix entries that route to `kind:"system"`.
  - **4 delimiter tests added** in `renderHints.test.ts` (block: "Delimiter coverage for polish-r2 passthrough grouping") closing all four QA-flagged critical gaps. The previously-added "system passthrough" test was FLIPPED to assert "system is a delimiter" (matching codex's catch).
  - **Doc nits fixed**: `docs/features/session-view.md:46` E2E note updated to `GROUP_THRESHOLD = 2`; `renderHints.ts` Pass 2 docblock + `GROUP_THRESHOLD` const docblock refreshed to describe the narrower passthrough policy.
  - Verification: `bun run test` 745 pass / 0 fail / 2469 expects (was 741; +4 net new delimiter tests); `bunx tsc --noEmit` clean; `bun run build` green.
- 2026-05-16: Polish-r2 four-reviewer rule (round 2):
  - Normal reviewer (Claude subagent): `approved`. All round-1 issues closed on evidence.
  - QA test-coverage reviewer (Claude subagent): `approved`. All 4 critical gaps closed; cosmetic gaps unchanged; no new gaps introduced. Confirmed the system-delimiter test is regression-effective (would fail if `isPassthroughStandalone` accidentally re-included `system`).
  - External codex reviewer: `approved with nits`. Round-1 finding closed. Two doc nits: stale "(assistant/system standalones do NOT break...)" in the `GROUP_THRESHOLD` const docblock; line-drift on `codex.ts:678` citation (actual is `codex.ts:681`). Both fixed inline by coordinator.
- 2026-05-16: Polish-r2 final state — all four reviewers approve; both new doc nits closed. User-reported real-session bug fully fixed; coverage now guards against the entire polish-r1 regression class plus the codex-caught system-passthrough trap. Phase 7c implementation + polish-r1 + polish-r2 all delivered and reviewed.
- Lesson learned (recorded for future render-layer work): **the QA test-coverage reviewer role catches gaps the correctness reviewers miss.** The normal reviewer cleared polish-r1 with `approved with nits` even though the algorithm didn't match parser output. The QA reviewer (introduced at user's request in polish-r2 round 1) found 4 untested delimiter branches that would have let regressions slip. The codex external review caught a fifth gap (system passthrough = error suppression). Different reviewers catch different gap classes; a four-role review is meaningfully stronger than three.

## Review Log

- M1 design review: closed at `approved with nits` after a 2-round design loop (see UI/UX Design Log). Single residual nit fixed by coordinator inline. WCAG re-run pending classifier recovery.
- M2 three-reviewer rule (round 1):
  - Backend-protection reviewer (Claude subagent): `backend untouched`. Verified `git diff` empty for `apps/backend/**`, `components/**`, `tests/e2e/**`, `Cargo.toml`, `Cargo.lock`, `apps/frontend/bun.lock`, `apps/frontend/package.json`, and all four parser logic files. M2 changes are confined to `apps/frontend/src/features/sessions/` (4 modified + 2 new) plus coordinator artifacts under `progress/phase-7c.progress.md` and `working/phase-7c/`.
  - Normal reviewer (Claude subagent): `approved with nits`. Verified spec/design conformance, all M2 acceptance items (1-4, 7, 10-12, 15-20, 22-23, 25-29, 34, 36-40) closed. Verification: `bun run test` 695 pass 0 fail; `bunx tsc --noEmit` clean; `bun run build` green (CSS 40.77 kB, JS 277.69 kB); markers=0; hex=24; tokens=83; `MessageKind` unchanged; parsers untouched. Nits (none blocking per normal reviewer): (1) theoretical precedence collision when a system message would carry both `taskLifecycle` AND a `warning-only` warning (current parser does not emit this combination); (2) `detectFailure` duplicated across `renderHints.ts` and `TranscriptView.tsx:detectFailureStatus` (M3 should consolidate); (3) `status: failed|error` regex slightly more permissive than design.md §15.3 pseudocode (cosmetic).
  - External reviewer (`codex exec --model gpt-5.2 --reasoning-effort medium`): `needs changes` (verbatim verdict). Findings: **(a)** M2 §Definition of done in `working/phase-7c.md` lists grouping branches (group below/above threshold, boundary reset, mixed/all-failed/in-flight status) but the M3 §Milestone 3 text explicitly says M3 "adds" the group-detection algorithm + `group-head`/`group-member` emission. The developer implemented to M3's explicit text; M2 §Definition of done's branch list is forward-looking. Resolution: spec is internally ambiguous — coordinator interprets M3's explicit emission text as authoritative; grouping branches in M2 §Definition of done belong to the renderHints.test.ts ending state (final closed M3). Documented here so the next session can reconstruct the decision. **(b)** Spec §Data Model "Claude Code pairs by tool_use_id linkage" is superseded by M1-locked strict-adjacency decision (design.md §15.1 + progress log UI/UX Design Log section). Resolution: the M1 design loop is the authoritative source for operational decisions; coordinator will add an explicit "M1 supersedes spec §Data Model pairing claim" comment to `renderHints.ts` so future readers don't trip on the divergence. **(c)** Real bug — taskLifecycle vs warning-only precedence in `renderHints.ts` short-circuits `warning-only` before `standalone { taskLifecycle }`, silently dropping the chapter marker if any future parser path attaches a `warning/meta` warning to a `task_started`/`task_complete` system message. Resolution: send to developer for revision; task-lifecycle system messages must always render as `standalone` with the `taskLifecycle` attribute, regardless of attached warnings.
- M2 developer revision pass: fixed BLOCKER (c) by re-ordering per-message hint-emit precedence in `renderHints.ts` (task-lifecycle stamping wins over `warning-only` short-circuit; attached warnings still ride along on `hint.warnings`). Added 2 spec-reconciliation comment blocks in `renderHints.ts` for (a) M2/M3 grouping split and (b) strict-adjacency pairing decision. Added 3 regression-guard tests (2 unit in `renderHints.test.ts`, 1 DOM-level in `TranscriptView.test.tsx`). All invariants preserved (markers=0, hex=24, tokens=83); `bun run test` 698 pass / 0 fail; tsc + build green.
- M2 three-reviewer rule (round 2):
  - Backend-protection reviewer (Claude subagent): `backend untouched`. Verified the revision's three modified files (renderHints.ts, renderHints.test.ts, TranscriptView.test.tsx) are all within the frontend feature scope; protected paths remain untouched. Required action: `proceed to normal review`.
  - Normal reviewer (Claude subagent): `approved with nits`. All three external-reviewer findings closed on evidence. The two round-1 documented-debt nits (detectFailure duplication + slightly-permissive `status: failed|error` regex) remain by design as M3 / future-hardening tracking. Verification: `bun run test` 698 pass / 0 fail; tsc clean; build green (CSS 40.77 kB, JS 277.79 kB); markers=0; hex=24; tokens=83; parsers untouched.
  - External reviewer (`codex exec --model gpt-5.2 --reasoning-effort medium`, round 2): `approved` (verbatim verdict, no nits). All three round-1 findings closed:
    - Finding (a) M2 vs M3 grouping ambiguity: closed via the new "Milestone scope" comment block at `renderHints.ts:31-69`.
    - Finding (b) Claude Code id-based vs adjacency pairing reconciliation: closed via the new "Pairing rule" comment block at `renderHints.ts:298-320`, with the codex sanity-check confirming Claude Code parser stores `tool_use_id` on `tool_result.toolName` (verified at `parsers/claude_code.ts`).
    - Finding (c) `warning-only` short-circuit silently dropping task-lifecycle markers: closed via the precedence re-order at `renderHints.ts:415-466` plus the three regression-guard tests. Codex confirmed the two-layer test coverage (unit + DOM) is appropriately targeted to catch a future reintroduction.
- M2 closed. Three-reviewer rule satisfied on the round-2 evidence pack. Proceeding to M3.
- M3 three-reviewer rule (round 1):
  - Backend-protection reviewer (Claude subagent): `backend untouched`. Verified all protected paths empty diff. Required action: `proceed to normal review`.
  - Normal reviewer (Claude subagent): `approved with nits`. Verified design-acceptance items 5, 6, 8, 13, 14, 21, 24, 31-33 closed. Nits (none blocking): (1) status-counts drift in `parser-event-support.md:13` ("37 / 11" stated vs actual "36 / 12") — Phase 7b carry-over, not M3-introduced; (2) detectFailure duplication documented-debt from M2 still present.
  - External reviewer (`codex exec --model gpt-5.2 --reasoning-effort medium`): `needs changes`. Findings: **(1)** BLOCKER — `renderTopLevelHints(...)` in `TranscriptView.tsx` consumes ALL contiguous `group-member` hints after a `group-head` without checking `groupHeadIndex` or bounding by `head.messageIndices.length`. Two adjacent same-tool groups (e.g. toolA × 3 immediately followed by toolB × 3 with no boundary) would have the second group's members swallowed by the first group's `<GroupHeadRow>`. **(2)** BLOCKER — CSS selector `.group-card > .group-members > .group-member` doesn't match the rendered DOM because each member renders `<article>` + `<InlineWarnings>` as siblings, breaking the direct-child relationship. Raised-surface background override + margin won't apply. **(3)** Evidence drift — status counts in `parser-event-support.md` claimed "37 / 11" but actual is "36 / 12".
  - Coordinator inline fix: corrected status counts in `parser-event-support.md:13` to "36 supported / 12 silenced" (closes finding 3; Phase 7b carry-over).
- M3 developer revision pass (round 2): fixed BLOCKER #1 via dual bound (count cap `members.length < head.messageIndices.length` AND canonical head pointer match `members[].groupHeadIndex === head.messageIndices[0]`); fixed BLOCKER #2 by switching to descendant selector `.group-card .group-members .group-member` and removing redundant `margin-top` (the `.group-members` flex `gap` provides vertical rhythm). Added 3 regression tests (1 unit in `renderHints.test.ts`, 2 DOM in `TranscriptView.test.tsx`). Added E2E coverage note to `docs/features/session-view.md` per codex's Notes recommendation. All invariants preserved; `bun run test` 727 pass / 0 fail; tsc + build green.
- M3 three-reviewer rule (round 2):
  - Backend-protection reviewer (Claude subagent): `backend untouched`. Reverified all protected paths empty diff.
  - Normal reviewer (Claude subagent): `approved` (no nits, no findings). All round-1 external findings closed on evidence; bound check + selector reach verified; verification: `bun run test` 727 pass / 0 fail / 2385 expects / 33 files / 3.60s; `bunx tsc --noEmit` clean; `bun run build` green (CSS 42.24 kB, JS 280.95 kB); markers=0; hex=24; tokens=83; 🎨=0; parsers untouched.
  - External reviewer (`codex exec --model gpt-5.2 --reasoning-effort medium`, round 2): `approved` (verbatim verdict, no nits). All three round-1 findings independently confirmed closed:
    - Finding (1) closed at `TranscriptView.tsx:285-305` (count cap + canonical head pointer match). Regression tests at `renderHints.test.ts:946-1010` (8-hint sequence with correct `groupHeadIndex` pointers) and `TranscriptView.test.tsx:1774-1824` (2 distinct `.group-card`s each with 3 members).
    - Finding (2) closed at `TranscriptView.css:698` (descendant selector). Regression test at `TranscriptView.test.tsx:1826-1872` using `Element.matches()` for selector reach.
    - Finding (3) closed at `parser-event-support.md:13` (now "✅ supported 36, 🔇 silenced 12").
- M3 closed. Three-reviewer rule satisfied on the round-2 evidence pack.

- **Phase 7c close**: all three milestones (M1 design gate, M2 render-hint layer + inline warnings, M3 same-tool grouping + matrix closure + doc sweep) delivered + reviewed + approved. Acceptance criteria 1-15 met (subject to the WCAG re-run carry-over noted below). Marker count = 0; `🎨` row count = 0; hex = 24; tokens = 83; `MessageKind` unchanged; no new runtime deps; no protected-path touches.

## External Reviewer Availability Log

- `codex exec` was historically available in this environment per Phase 6/7a logs. Phase 7b external review was blocked by repository-evidence data-export policy; coordinator will reattempt for Phase 7c implementation review and escalate to the human if it remains blocked.

## Protected-Path Exception Log

- None.

## Open Risks / Open Questions

- **WCAG re-run carry-over**: `python3 working/phase-7c/designs/wcag.py` could not be executed in the coordinator's main session due to a persistent harness classifier limitation that blocked every `python3` invocation (~80 attempts across the session, including from designer + reviewer subagents and the coordinator). The script content + recorded `wcag-output.txt` are byte-equivalent to Phase 5 M5's audited `wcag_m5.py`; the 4 new pairs P38-P41 introduced for the task-lifecycle treatment reuse token combinations already audited under P01/P02 (border/surface) and P06/P08 (ink-muted/surface). The UI/UX reviewer round 2 verified internal consistency by spot-check, but a fresh `python3 working/phase-7c/designs/wcag.py` execution under a working classifier is recommended for full closure. Phase 7c on-paper meets all WCAG AA targets.
- Carrying the Phase 7c risk table (see `working/phase-7c.md` §Risks). All risks either mitigated or deferred per design.

## Next Recommended Task

- Human: commit Phase 7c implementation. The change set covers:
  - 4 modified frontend files (`TranscriptView.tsx`, `TranscriptView.css`, `TranscriptView.test.tsx`, `TranscriptView.event-coverage.test.tsx`)
  - 2 new frontend files (`renderHints.ts`, `renderHints.test.ts`)
  - 4 modified docs (`docs/README.md`, `docs/features/session-view.md`, `docs/features/parser-event-support.md`, `docs/playbooks/modify-frontend-page.md`)
  - 1 new progress log (`progress/phase-7c.progress.md`)
  - Design artifacts under `working/phase-7c/designs/` (5 top-level files + 12 wireframes)
- Optional cleanup: re-run `python3 working/phase-7c/designs/wcag.py` under a working classifier and confirm zero drift from `wcag-output.txt`; record outcome in this log.
- Roadmap update: move Phase 7 from "in progress" to "delivered" if the roadmap tracks per-phase status.
