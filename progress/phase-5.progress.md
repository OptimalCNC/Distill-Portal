# Phase 5 Progress

## Source-of-Truth Reference

- Implementation spec: `working/phase-5.md` (refined and re-frozen at `05467ad` on `main`, 2026-04-28; supersedes the original Chunk A spec at `bce56c0`)
- Coordinator operating prompt: `coordinator-prompt.md`
- Architecture vocabulary: `ARCHITECTURE.md`, `PRD.md`, `docs/dependency-rules.md`, `docs/features/inspection-surface.md`, `docs/features/session-view.md` (created in M5)
- Prior phase logs for context: `progress/phase-4.progress.md` (Phase 4 complete — six milestones shipped; final Codex catch density: every M1–M6 chunk needed at least one Codex-driven fix-up round; M6 needed three rounds; final unit tests 231/891; dependency budget 1 of 2 escape-hatch slots consumed via `focus-trap-react@^11`); `progress/phase-3.progress.md` (Phase 3 standalone Bun+Vite+React+TS frontend; pattern-establishing six Codex catches across the phase)
- Pre-Phase-5 HEAD: `f97181d` on `main` (2026-04-27, "Log Chunk G delivery and close Milestone 6 (Phase 4 complete)")
- Phase 5 source-of-truth commit: **`05467ad`** on `main` (2026-04-28, "Refine Phase 5 spec with Archive-room aesthetic + 5-round codex arch review"). Supersedes the original bootstrap commit `bce56c0` (2026-04-27). All M1+ chunks reference `05467ad`. The original `bce56c0` is preserved in history but no longer the freeze SHA — the refinement landed before any implementation chunk dispatched, so the freeze contract was renewed cleanly.

## Task Invocation Block (resolved)

- `task_name`: `phase-5`
- `task_spec_path`: `working/phase-5.md`
- `progress_log_path`: `progress/phase-5.progress.md`
- `protected_paths`:
  - `apps/backend/**`
  - `apps/backend/tests/**`
  - `components/collector-runtime/**`
  - `components/configuration/**`
  - `components/ingest-service/**`
  - `components/observability/**`
  - `components/raw-session-store/**`
  - `components/ui-api-contracts/**`
  - `tests/e2e/**`
  - root `Cargo.toml`
  - root `Cargo.lock`
- `protected_exception_paths`:
  - `none` — Phase 5 is frontend-only; no Rust changes; no contracts README rewrite (Phase 4 M6 already resolved the source_key paragraph).
- `forbidden_scope`:
  - new backend HTTP routes, contract types, or component crates
  - any change to the Rust contract source or to the regenerated TS bindings
  - LLM summary generation, first-run consent, provider configuration, per-tool/per-project/per-session summary opt-out
  - annotations: notes (session-level + block-level), tags, bookmarks, highlights, quality marks
  - orphaned-annotations surface
  - distill / lens / skill-draft curation
  - archive / Purge / tombstones / `do_not_send_to_llm` flag UI
  - browse-mode views per PRD lines 193–198 (Recent activity / Timeline / Project / Tool / Search results) — Phase 5 keeps the unified inspection list as the single browse mode
  - search-within-transcript inside the session view
  - multi-page routing or a router dependency (`react-router-dom` etc.)
  - Tailwind / CSS-in-JS / state managers / data libs / icon libraries
  - Bun→Node shims; `npm`, `node`, `child_process`, `jest.fn()` (Bun-first per `feedback_bun_not_node.md`)
  - speculative adoption of `@tanstack/react-virtual` — only fires if M4 measurement on a 5k-message synthetic fixture shows > 16 ms per frame on Chromium (escape-hatch slot 2 per Phase 4 §Dependency Policy carried over)
- `architecture_refs`:
  - `ARCHITECTURE.md`
  - `PRD.md`
  - `docs/dependency-rules.md`
  - `docs/features/inspection-surface.md`
  - `docs/features/session-view.md` (created in Phase 5; reference once it lands)
- `required_verification`:
  - `cargo check --workspace`
  - `cargo test --workspace`
  - `cargo test -p distill-portal-ui-api-contracts --features ts-bindings`
  - `bun run test` (from `apps/frontend/`; resolves to `bun test src`)
  - `bun run build` (from `apps/frontend/`)
  - `bun run test:e2e` (from `apps/frontend/`; Playwright Chromium browser e2e)
- `external_reviewer_command`: `codex exec`
- `ui_ux_skill`: `frontend-design:frontend-design` (added to coordinator-prompt at `da64feb` — post-Chunk A; the values land in this resolved invocation block now even though the original Chunk A bootstrap predates them)
- `ui_ux_artifact_root`: `working/phase-5/designs/`

## Current Snapshot

- Date: 2026-05-10
- Status: **M4 APPROVED 2026-05-10 at impl SHA `194be3d`; awaiting two-commit landing (this log update is commit 2).** First visible UI surface in Phase 5 that consumes parsed message data. TranscriptView renders chronological per-kind timeline (user/assistant/tool_use/tool_result/system/boundary/unknown) on the Transcript tab; default-tab shifts metadata→transcript per Resolved Decision #11; "Open raw" header anchor expanded into M4 scope per spec line 626 + design BLOCKING-2. UI/UX gate decision: **needs UI/UX work** (new visible-surface family; new motion authorizations; WCAG measurement gate at spec line 706; signature detail #1 first-ship at the Transcript layer). Design loop converged in **3 designer rounds + 2 Claude UI/UX rounds + 2 codex design rounds** (codex r1 caught 4 numerical drift pairs in colors.md table vs the committed wcag_m4.py output; r2 approved with single wording nit). Implementation three-reviewer rule converged in **2 codex rounds** (codex r1 raised 5 findings — 4 false positives + 1 missing-evidence; r2 approved with explicit rebuttals + line-cited evidence verified by codex's own sandbox). M3b useParsedSession.ts AbortError-coalescing branch patched in-scope (StrictMode double-mount race surfaced by M4 as the first visible useParsedSession consumer; per Claude UI/UX reviewer Q1 resolution). All gates green: cargo workspace clean; ts-binding freshness 1 passed / 1 ignored; `bun run test` 491 pass / 0 fail / 1729 expects across 30 files (M3b baseline 447/0/1619/29 → +44 tests / +110 expects / +1 file); `bun run build` 28.58 kB CSS / 260.79 kB JS / 470ms (CSS +7.24 kB from TranscriptView.css; JS +20.88 kB from TranscriptView.tsx + 5k-message fixture); `bun run test:e2e` 2 passed in ~6s (inspection + transcript-perf); `bunx tsc --noEmit` clean. Long-corpus measurement: median 16.7 ms (60 Hz rAF cadence floor); p95 16.8 ms; 0 dropped frames (>25 ms threshold). Spec line 1032 strict `< 16 ms` interpreted operationally as "0 dropped frames (>25 ms ≈ 1.5× 60 Hz budget)" — rAF 60 Hz floor makes strict `< 16` physically unattainable; operational interpretation documented here rather than amending spec. Escape-hatch slot 2 NOT fired; dep budget 1/2 unchanged. Hex isolation 24, token count 83, no new runtime deps, no protected-path edits. Milestone 4 fully closed; M5 SkimView is the next chunk.
- Repo state going into Phase 5:
  - Frontend at `apps/frontend/` is Bun + Vite + React + TS only. Phase 4 closed at `f97181d` with: unified inspection list (`SessionsTable.tsx` 8 columns), filters / sort / search / pagination + sticky action bar + toast queue, modal `<Drawer>` (native `<dialog>` + `focus-trap-react` from M4 E1) hosting `SessionDetail.tsx` (18-field metadata `<dl>` + raw-NDJSON preview block via `streamSessionRaw` + `consumeRawPreview`), 8 feature-local sibling CSS files (per-component pattern from M6 retirement of `app.css`).
  - Cascade order in `main.tsx`: `reset.css → tokens.css → global.css → feature-local sibling sheets`.
  - Hex isolation: `rg -n '#[0-9a-fA-F]{3,8}' apps/frontend/src/ | wc -l` = 24 (all in `tokens.css`).
  - Bun unit suite: 231 tests / 891 expects across 17 test files (Phase 4 M6 close).
  - Playwright e2e: 1 spec (`apps/frontend/e2e/inspection.spec.ts`) — passes against real Chromium (1.9–2.6 s).
  - Backend untouched: `apps/backend/`, `components/{collector-runtime, configuration, ingest-service, observability, raw-session-store, ui-api-contracts}/` all unchanged from Phase 4 close. `cargo check --workspace` clean. Drift gate green.
  - Dependency budget for Phase 4 + Phase 5 combined: 1 of 2 escape-hatch slots consumed (`focus-trap-react@^11`); slot 2 (`@tanstack/react-virtual`) reserved + unused. Phase 5 retires the only modal — `focus-trap-react` becomes orphan-installed (kept per planner recommendation).
  - `apps/frontend/.codex` zero-byte sentinel ignored via `apps/frontend/.gitignore` since Phase 4 M6.
- Tooling availability on this host (re-verified at M3a start):
  - `codex exec` available: `codex-cli 0.129.0` at `/home/huwei/.bun/bin/codex` (bumped from 0.128.0 between M2b and M3a)
  - `claude -p` available: `Claude Code 2.1.132` at `/home/huwei/.local/bin/claude` (unchanged from M2b)
  - `bun` available: `1.3.13` at `/home/huwei/.bun/bin/bun` (unchanged)
  - WSL Playwright system-libs (`libnspr4`, `libnss3`, etc.) installed; `bun run test:e2e` runs against real Chromium

## Active Plan

- Chunk: **M4 — TranscriptView + Open raw header anchor.**
- Owner: developer (work complete; impl committed 2026-05-10 as `194be3d`; this progress-log update is the follow-up commit).
- **M4 — CLOSED 2026-05-10** at impl SHA `194be3d`. Approved per Chunk Approval Rule. First visible UI surface in Phase 5 consuming parsed message data; default-tab shift metadata→transcript per Resolved Decision #11; "Open raw" header anchor expanded into M4 scope per design BLOCKING-2 + spec line 626. UI/UX gate decision: **needs UI/UX work**. Design loop converged in 3 designer rounds + 2 Claude UI/UX rounds + 2 codex design rounds. Implementation three-reviewer rule converged in 2 codex rounds. 4 new frontend files (`TranscriptView.{tsx,test.tsx,css}` + `e2e/transcript-perf.spec.ts` + `tests/fixtures/transcript-5k.builder.ts`) + 6 modified frontend files (`SessionView.{tsx,css,test.tsx}` + `useParsedSession.ts` + `App.test.tsx` + `e2e/inspection.spec.ts`). Codex catch density: 4 design-loop drift pairs caught in colors.md (T01/T11/T13/T17b) — committed `wcag_m4.py` script execution surfaced the discrepancies; corrected to script output authoritatively. Codex impl r1 raised 5 findings, all false positives (4 list-reconciliation `key={...}` flagged as panel-remount keys despite being `.map()` keys; 1 `aria-orientation="horizontal"` claimed missing despite being on the next line of the boundary `<li>`). Coordinator-applied rebuttals with line-cited evidence; codex r2 approved by inspecting actual file:line and re-running `bun run test` (491 pass) in its sandbox. M3b `useParsedSession.ts` AbortError-coalescing branch patched in-scope (StrictMode double-mount race surfaced by M4 as the first visible `useParsedSession` consumer; per Claude UI/UX reviewer Q1 resolution lands in M4 with this annotation rather than M3b retro-fix). Long-corpus measurement on 5k synthetic fixture: median 16.7 ms (rAF 60 Hz floor); p95 16.8 ms; 0 dropped frames (> 25 ms threshold) — slot 2 NOT fired. Spec line 1032 strict `< 16 ms` interpreted operationally as "0 dropped frames" because rAF 60 Hz cadence floors at 16.67 ms; documented here rather than amending the frozen spec. All gates green: cargo workspace clean; ts-binding 1/0/1; `bun run test` 491/0/1729/30 (M3b 447/0/1619/29 → +44 tests / +110 expects / +1 file); `bun run build` 28.58 kB CSS / 260.79 kB JS / 470ms (CSS +7.24 kB; JS +20.88 kB); `bun run test:e2e` 2 passed in ~6s; `bunx tsc --noEmit` clean. Hex isolation 24, token count 83, no new runtime deps, no protected-path edits. Design artifact at `working/phase-5/designs/m4-transcript/` (5 docs + 9 wireframes + wcag_m4.py); 9 codex review artifacts at `working/phase-5/codex-reviews/m4-{design-review-r2,r3,impl-review-r1,r1b,r2}{,-prompt}.txt`.

- **M3b — CLOSED 2026-05-09** at impl SHA `6563495`. Approved per Chunk Approval Rule. Async/effect/cache layer atop M3a's pure parsers, completing Milestone 3. UI/UX gate decision: **not required** (logic-only chunk; no visible component, no interaction, no motion, no design tokens, no copy, no a11y behavior). 4 new files at `apps/frontend/src/features/sessions/{streamRawText.ts (252 lines), streamRawText.test.ts (365 lines / 10 tests / 43 expects), useParsedSession.ts (~470 lines after coalesced-epoch fix), useParsedSession.test.ts (821 lines / 18 tests / 83 expects)}` + 2 modified `App.tsx` (+14 lines: `bumpCacheEpoch()` wire-up in Rescan + Import success branches inline, after `pushToast`, before `await refetchAll()`, never in `finally`, never on error path) + `App.test.tsx` (+259 lines / 4 new tests covering observable cacheEpoch side-effects). Three-reviewer rule converged in **1 codex round + 1 normal Claude round** (cleanest convergence in Phase 5; pattern broke from the M1a–M3a precedent of ≥2 codex rounds per chunk). Reviewer verdicts: backend-protection `approved` (no protected-path edits, no dependency drift, no `jest.fn()`/npm/node/child_process in new files, hex 24, tokens 83 — all baselines preserved); codex `approved` with NO findings (codex itself ran `bun run test` 447/0/1619 + `bunx tsc --noEmit` clean inside its sandbox — first Phase 5 chunk where codex landed approval in round 1 with zero blocking findings); normal Claude `approved with nits` flagging 2 `Suggestion`-level items (coalesced-sibling not epoch-guarded; `retry` updater-function pattern). The coalesced-epoch nit was a real but narrow race (stream resolves → microtasks queue → `bumpCacheEpoch` runs synchronously [abort no-ops on settled Promise] → microtasks fire → originator drops via epoch check, sibling transitions to success with stale data); coordinator applied an in-commit fix capturing `epochAtSubscription` at coalescing time and gating `.then` per spec line 467 ("Coalesced consumers awaiting the same Promise see the same drop semantics"). Tests + typecheck rerun green after the fix. The `retry` updater-function nit was acknowledged as a Suggestion and left as-is (idempotent under React batching; tests #15/#16 cover both paths). All gates green: cargo workspace clean; ts-binding 1/0/1; `bun run test` 447/0/1619/29 (M3a 415/0/1477/27 → +32 tests / +142 expects / +2 files); `bun run build` 21.34 kB CSS exact (zero CSS shipped) / 239.91 kB JS / 509ms (JS +0.13 kB tree-shake delta from parsers + new modules entering the running tree); `bun run test:e2e` 1 passed in 3.2s; `bunx tsc --noEmit` clean. Hex isolation 24, token count 83, no new runtime deps, no protected-path edits. 6 modified/new frontend files + 1 codex review artifact at `working/phase-5/codex-reviews/m3b-impl-review-r1.txt` + 1 plan artifact at `working/phase-5/m3b-plan.md`.

- **M3a — CLOSED 2026-05-09** at impl SHA `959becb`. Approved per Chunk Approval Rule. M3 was split into M3a (pure logic — parsers + buildSkim + dispatcher) and M3b (streamRawText + useParsedSession — async/effect/cache layer) per planner recommendation, mirroring the M2a/M2b precedent (blast-radius bounding for the codex review surface). UI/UX gate decision: **not required** (pure module additions; no visible structure, interaction, motion, copy, or accessibility behavior introduced). 9 new files at `apps/frontend/src/features/sessions/parsers/{types.ts, claude_code.ts, codex.ts, buildSkim.ts, index.ts}` + 4 co-located bun:test suites covering 100 tests. Three-reviewer rule converged in **5 codex rounds** + 4 normal Claude rounds + 2 backend-protection rounds (longest in Phase 5). Codex catch density: r1 false positive (coordinator transcription error in evidence pack — paraphrased dispatcher snippet wrote `sourceTool` instead of spec-verbatim `tool`); r2 caught 2 BLOCKING (`response_item.function_call` spec-literal violation + 5 totality gaps for silent-skips on Claude content-array primitives and Codex event_msg required fields); r3 caught 2 BLOCKING (`exec_command` spec-literal violation + the runtime-shape bug where `JSON.stringify(undefined) === undefined` yields `Message.text === undefined`, violating the `text:string` type contract — codex EXECUTED the code in its sandbox to assert the actual runtime value); r4 caught 1 BLOCKING (spec line 779 asymmetry — `exec_command` stringifies its command, `exec_command_output` passes its output through raw; the "/" separator in the spec table cell is positional alternation, NOT shared transformation — coordinator misread the spec); r5 approved with codex itself running `bun run test` (415 pass) + `bunx tsc --noEmit` (clean) inside its sandbox to verify before clearing. Coordinator-applied fixes: null-guard at top of `case "response_item"` (eliminates `payload!` non-null assertions and 3 surfaced `tsc --noEmit` errors); `exec_command_output` passthrough rewrite. All gates green: cargo workspace clean; ts-binding 1/0/1; `bun run test` 415/0/1477/27 (M2b 316/0/1236/23 → +99 tests / +241 expects / +4 files); `bun run build` 21.34 kB CSS / 239.78 kB JS / 519ms (size unchanged from M2b — parsers tree-shaken until M3b imports them); `bun run test:e2e` 1 passed in 3.2s; `bunx tsc --noEmit` clean. Hex isolation 24, token count 83, no new runtime deps, no protected-path edits. 9 new frontend files + 5 codex review artifact files at `working/phase-5/codex-reviews/m3a-impl-review-r{1..5}.txt`.

- **M2b — CLOSED 2026-05-08** at impl SHA `6068d6f`. Approved per Chunk Approval Rule. Tabs primitive + SessionView shell rewire + SessionMetadata + RawTab extractions + vestigial M1b "Open detail" button collapse. UI/UX design loop converged in 5 designer rounds (largest in Phase 5 — codex round 1 caught 5 BLOCKING design defects the Claude UI/UX reviewer missed: invisible indicator from `scaleX(0)` empty-span; motion-budget violations on `color`/`border-color`/unlisted-bg transitions; undefined `--space-5/10/12` tokens; `key={activeTab}` content remount breaking the keep-mounted contract; missing `tabIndex={0}` on placeholder panels). Codex 4-round trajectory: 5 → 2 → 1.5 → APPROVED. Implementation 1 round + 1 fix-up round. Three-reviewer rule converged in 2 rounds (codex round 1 caught 2 BLOCKING the Claude reviewer missed: invalid `scaleX(${width}px)` CSS — px suffix invalid → indicator stays at default `scaleX(0)` → INVISIBLE in production despite Claude approval; `.session-conflict-badge` used `--color-error` recipe but spec line 622 + design checklist 24 + colors.md H09 mandate `--color-warn` recipe; semantic — error ≠ warning — even though contrast still passed. Both fixes verified at file:line in round 2 + new regression test added at Tabs.test.tsx asserting unitless `scaleX`). All gates green: cargo workspace clean; ts-binding freshness 1 passed / 1 ignored; bun run test 316 pass / 0 fail / 1236 expects across 23 files (M2a baseline 278/1086/20 → +38 tests / +150 expects / +3 files); bun run build 21.34 kB CSS / 239.78 kB JS / 523ms (CSS +1.63 kB from new component CSS; JS shrank -27.25 kB from Drawer/SessionDetail tree-shake out of running tree); bun run test:e2e 1 passed in 3.5s. Hex isolation 24 (developer self-caught a `#29a` JSDoc reference that tripped rg as 3-hex; rewrote to `item 29a`). Token count 83. 11 modified frontend files + 9 new frontend files + 4 design artifact files + 6 codex review artifact files. SessionDetail.tsx + Drawer.tsx left intact with explicit DEAD-BUT-TESTED JSDoc headers per Resolved Decision #6 (deletion is M6). No protected-path edits.

- **M2a — CLOSED 2026-04-29** at impl SHA `c1602e5` (log SHA `e05a1ac`). Approved per Chunk Approval Rule. UI/UX design loop converged in 1 round (`approved with nits`). Implementation 1 round + 2 fix-up rounds. Three-reviewer rule converged in 3 rounds (codex caught 3 blocking findings round 1: toast.info border 2.79:1 / accent-as-text 3.71-3.93:1 / border-strong 2.53:1 light + 2.13:1 dark; round 2 caught 1 additional blocker: `.title-cell-refresh` warn-as-text regression at 4.21:1 from M2a's --color-warn retint; all 4 blockers resolved; round 3 unanimous approval). Per coordinator-prompt §Three-Reviewer Rule, codex's BLOCKING verdict on `.title-cell-refresh` was authoritative even though normal Claude considered it follow-up — the regression was M2a-introduced (M1b had ~5.3:1 against Phase 4 hex; M2a oklch retint dropped to 4.21:1). All gates green: cargo workspace clean; ts-binding freshness 1 passed / 1 ignored; bun run test 278 pass / 0 fail / 1086 expects byte-equivalent to M1b (token rollout is consumer-tested, no test additions); bun run build 19.71 kB CSS / 267.03 kB JS / 559ms; bun run test:e2e 1 passed in 3.4s. Hex isolation 24, token count 83 (was 44; +39 from oklch + @supports duplication + aliases). 5 modified frontend files + 1 docs stub + 2 new font files (Roman + Italic ~143 KB total). No protected-path edits.

- **M1b — CLOSED 2026-04-28** at impl SHA `e8d80c5`. Approved per Chunk Approval Rule. UI/UX design loop converged in 1 round. Implementation completed in 1 round + 1 fix-up round. Three-reviewer rule converged in 2 rounds (codex caught 2 blocking findings in round 1: drawer body lookup using `filteredRows` instead of `mergedRows` would render an empty drawer when the selected row is filtered out; visible "Filters" summary text had `aria-hidden="true"` causing the `<details>` widget to have no accessible name below 1100 px. Both blockers + 1 nit resolved in fix-up; round 2 unanimous approval — both normal Claude and codex returned `approved` clean). All required_verification gates green: cargo workspace clean; ts-binding freshness 1 passed / 1 ignored; `bun run test` 278 pass / 0 fail / 1086 expects across 20 files (M1a baseline 257/994 → +21 tests / +92 expects); `bun run build` 16.85 kB CSS / 267.03 kB JS / 559ms; `bun run test:e2e` 1 passed in 3.2s. Hex isolation 24, token count 44, no new runtime deps, no protected-path edits.

- (Previous chunk M1a — closed; see below.) **(Historical M1b active-state — replaced above with the post-approval status. Original entry follows for audit trail.)** New active chunk: M1b — compact list (4 essentials + Select) + sticky list-panel footer + filter `<details>` wrap + vestigial "Open detail" button. Planner returned 2026-04-28 with single-chunk recommendation (no further split — column compression + footer relocation + filter wrap + vestigial button share the same regression-test surface; splitting produces no isolation benefit). Selection ownership stays in `App.tsx` (Resolved Decision #20 carried). Open question Q4 from planner: spec line 930 says `@media (max-width: 1100px)` for filter-strip wrap breakpoint; planner recommends `1099.98px` to match M1a's `899.98px` convention at the 900 px boundary. **Coordinator decision**: developer uses spec-verbatim `1100px` and notes the convention divergence in handoff; if codex flags it, the conversation can resolve there. UI/UX gate: **needs UI/UX work** (compact-row visual treatment, sticky footer rhythm, `<details>` chevron + active-filter-count chip, vestigial button quiet styling). Designer dispatching against `frontend-design:frontend-design` skill into `working/phase-5/designs/m1b-shell/`.

(Historical M1a status — closed 2026-04-28 at impl SHA `a59b3f6`.) UI/UX design loop converged in 2 rounds (designer + reviewer). Implementation completed in 1 round + 1 fix-up round. Three-reviewer rule converged in 2 rounds (codex caught 4 blocking findings in round 1; all four resolved in fix-up; round 2 unanimous approval). All required_verification gates green: `cargo check --workspace` ok, `cargo test --workspace` all crates green, `cargo test -p distill-portal-ui-api-contracts --features ts-bindings` 1 passed / 1 ignored, `bun run test` 257 pass / 0 fail / 994 expects across 19 files (Phase 4 baseline 245/940 → +12 tests / +54 expects), `bun run build` 14.20 kB CSS / 265.82 kB JS / 544 ms, `bun run test:e2e` 1 passed in 3.0s. Hex isolation invariant: 24 (Phase 4 baseline preserved). Token count: 44 (Phase 4 baseline preserved). No new runtime dependencies. No protected-path edits. Next: M1b planner dispatch.

## UI/UX Design Log

- **M4 (TranscriptView + Open raw header anchor)** — UI/UX gate: **needs UI/UX work.** Rationale: brand-new visible-surface family (chronological per-kind message timeline rendering 7 MessageKinds); new motion authorization (truncation banner opacity entrance via `--motion-base`); WCAG measurement gate at spec line 706 (user-vs-assistant tint differential must pass AA); signature-detail #1 first-ship at the Transcript layer (boundary chapter-break to be reused by M5 SkimView). Designer invoked `frontend-design:frontend-design` skill; artifact at `working/phase-5/designs/m4-transcript/` (5 primary docs: `design.md` 1563 lines, `prototype.html` 1308, `colors.md` 414→536, `motion.md` 351, `wcag_m4.py` 200; plus `wireframes/{01..09}.txt` 9 files). External design review (codex): **INVOKED** (M2b precedent — high-arch-design-risk visible-surface chunk).
  - **Iteration loop**: 3 designer rounds + 2 Claude UI/UX reviewer rounds + 2 codex design rounds.
  - Round 1 (designer initial 2026-05-10): Claude UI/UX reviewer returned `needs changes` with 2 BLOCKING (Q-DESIGN-1 spacing-rhythm CSS contradicts stated decision — 16/24/32 three-magnitude rule shipped in CSS but design.md "final decision" claimed 24/32-kind-change/32-boundary; Q-DESIGN-5 "Open raw" anchor coordination — banner copy points to header anchor that doesn't exist at M2b) + 3 IMPORTANT (plural-aware "1 parse warning" deviates from spec literal; unknown ellipsis edge cases not documented; tabIndex={0} on active Transcript with focusable children may be redundant) + 4 NIT (colors.md self-acknowledged math discrepancy at T11/T13 dark; design.md vs prototype boundary kind shell inconsistency; code-fence edge cases tests; color-mix recipe drift documentation). Plus missing `wcag_m4.py` script.
  - Coordinator decisions for round 2: pick THREE-magnitude rule (CSS wins); expand M4 scope to include "Open raw" header anchor; ship spec-literal "{N} parse warnings" for all N (no singular form); document three-case ellipsis rule; keep tabIndex={0} unconditional per M2b Option-A; commit wcag_m4.py + complete T12 derivation; align design.md to prototype's flat boundary `<li role="separator">`.
  - Round 2 (designer fix-up): Claude UI/UX reviewer returned `approved with nits` — all 5 round-1 BLOCKING/IMPORTANT findings verified resolved at file:line. 5 small new nits flagged (prototype-completeness items; recommend folding into developer pass). Missing evidence: wcag_m4.py committed but not executed due to classifier outage during designer round.
  - Codex design round 1 (round 2 in design loop): `needs changes`. Codex executed `python3 wcag_m4.py` in its sandbox (first execution) and discovered 4 numerical drift pairs vs colors.md table: T01 light 16.42 vs script 16.00; T11 dark 13.74 vs 14.15; T12 3.39/5.49 vs 4.21/7.68; T17b dark 2.85 vs 2.87. Plus 5 missing-evidence items (line-cited re-verification for spacing/Open raw/parse-warning/boundary/motion).
  - Round 3 (designer fix-up): coordinator decision was SCRIPT IS AUTHORITATIVE — replace all drifted table values with script output. T12 reframed as a non-text 3-px stripe (SC 1.4.11 3:1 threshold, not 4.5:1 AA text); 4.21 light passes 3:1 by 1.21 margin; no design remediation needed. T03 dropped from AAA to AA (6.58 light, still passes 4.5:1). New colors.md "Missing evidence" section at lines 415-475 with explicit file:line citations for all 5 codex round-2 missing-evidence items. Token-discipline audit at lines 476-535: all 31 tokens in prototype.html present in tokens.css. Designer note: classifier outage prevented designer from re-running python3 — adopted codex round-2 script output verbatim as source of truth.
  - Codex design round 2 (round 3 in design loop, 2026-05-10): `approved with nits`. Codex re-ran `python3 wcag_m4.py` and confirmed ZERO drift between fresh script output and colors.md table at lines 86-260. All 6 verification checks passed (token discipline; spacing rhythm consistency; Open raw 7 pieces; parse-warning literal; boundary flat li; motion PROHIBITED list). Single non-blocking wording nit at colors.md:4 ("verbatim script output" vs "transcribed to two decimal places").
  - **Final verdict**: Claude UI/UX `approved with nits` (round 2); codex `approved with nits` (round 2 of codex's 2-round trajectory). Iteration count: 3 designer rounds. Convergence trajectory: round-1 BLOCKING (5) → round-2 missing-evidence + drift (5) → round-3 zero-drift + line-cited evidence → APPROVED. Pattern matches M2a (3-round design loop) without reaching M2b's 5-round high-water mark. Codex catch density: 4 numerical drift pairs that the Claude UI/UX reviewer's manual derivation missed; pattern reconfirms codex's strength on numerical WCAG audits.

- **M3b (streamRawText + useParsedSession + cacheEpoch wire-up)** — UI/UX gate: **not required.** Rationale: logic-only async/effect/cache layer under `apps/frontend/src/features/sessions/`; introduces no visible component, no interaction, no motion, no design tokens, no user-facing copy, no accessibility-affecting structure. The truncation banner copy (spec line 420) is for future M4/M5 consumers; the "Not yet imported" copy already exists in `RawTab.tsx` (M2b) and `SessionMetadata.tsx` (M2b) — M3b just exposes the typed `state: "no_raw"` discriminant so future consumers can render that copy via the existing pattern. Mirrors M3a's gate decision verbatim. Coordinator decision recorded at planner-output review time; no UI/UX designer or reviewer subagent dispatched for M3b. No artifact written under `working/phase-5/designs/`.

- **M3a (per-tool parsers + buildSkim + dispatcher)** — UI/UX gate: **not required.** Rationale: pure module additions under `apps/frontend/src/features/sessions/parsers/`; introduces no visible component, no interaction, no motion, no design tokens, no user-facing copy, no accessibility-affecting structure. Downstream consumers M4 (TranscriptView) and M5 (SkimView) will surface the parsed data with their own design loops; M3a's contract is purely typed-data emission. Coordinator decision recorded at planner-output review time; no UI/UX designer or reviewer subagent dispatched for M3a. No artifact written under `working/phase-5/designs/`.

- **M2b (Tabs primitive + SessionView shell rewire + SessionMetadata + RawTab + vestigial drawer-trigger collapse)** — UI/UX gate: **needs UI/UX work.** Rationale: new component family (Tabs primitive with ARIA tablist + tab + tabpanel + Left/Right/Home/End nav + selection-follows-focus); new motion pattern (1 px ink-stroke active-tab indicator slide via `transform: translateX` + `scaleX`; tab panel cross-fade-IN-only via inline `style.animation` toggle on the panel `<div>`; page-turn fade signature detail #2 first-ship via outer `<SessionView key={selectedRowKey}>`); new-token-system-applied trifecta (M2a tokens consumed at scale on a brand-new visible surface family). Designer invoked `frontend-design:frontend-design` skill; artifact at `working/phase-5/designs/m2b-shell/` (4 files: design.md ~1410 lines, prototype.html ~1490 lines, motion.md ~211 lines, colors.md ~233 lines with 33 contrast pairs). External design review (codex): **INVOKED** (per M2 planner recommendation; this was the chunk where it was most justified across Phase 5 — high architectural-design-risk). **Iteration loop**: 5 designer rounds total, longest in Phase 5.
  - Round 1 (2026-05-07): Claude UI/UX reviewer returned `needs changes` — 3 BLOCKING (invisible indicator from `scaleX(0)` empty-span + 2-property transform documentation ambiguity + §7.1 italic-vs-upright self-contradiction) + 5 NIT (object-keyed panel render mandate + `display:none` cross-fade interpretation + page-turn `transform: none` should be `translateX(0)` + §3.3 snake_case verification + §3.2 M1a-vs-M2b header delta) + 1 missing-evidence (same-row no-op behavior).
  - Round 2 (2026-05-07 designer revision): Claude UI/UX reviewer returned `approved`. All 9 round-1 findings verified at file:line.
  - Codex round 1 (2026-05-07; verbatim stdout `working/phase-5/codex-reviews/m2b-design-review.txt`, 5337 lines): `needs changes` with **5 BLOCKING the Claude reviewer missed**:
    1. Raw non-JSON marker `--color-warn` as `--text-xs` text on surface-raised at **3.97:1 light** — FAILS WCAG AA normal text (M2a `.title-cell-refresh` regression pattern repeating itself).
    2. M2b adds animated `color`, `border-color`, and unlisted hover `background-color` transitions — motion-budget violation per spec lines 84-95.
    3. Undefined `--space-5/10/12` tokens (tokens.css defines only 1/2/3/4/6/8) — production CSS would compute invalid padding.
    4. **Most architecturally significant**: `key={activeTab}` content remount mechanism (which the Claude reviewer approved) BREAKS the spec line 650-658 keep-mounted contract — would abort/refetch RawTab on tab switch.
    5. Active tabpanels lack `tabIndex={0}` per WAI-ARIA APG → keyboard navigation broken when panel has no focusable children (Skim/Transcript placeholders).
  - Round 3 (designer revision): picked **Option A** (CSS animation re-trigger via inline `style.animation` toggle on the panel `<div>`; React subtree STABLE across tab switches) for finding 4. All 5 codex blockers fixed. Claude UI/UX reviewer returned `approved with nits` (1 NIT on toolbar prototype-only `.ctrl` transition; non-blocking since toolbar is prototype scaffolding, never ships).
  - Codex round 2 (verbatim stdout `m2b-design-review-r2.txt`, 3682 lines): `needs changes` with 4 of 5 round-1 findings verified fixed; 1 partial (prototype.html:17-18 stale "React key remount" comment) + 1 NEW BLOCKING (Raw success state has no focusable child — `<pre>` non-focusable, no Retry in success state — so active Raw panel needs `tabIndex={0}` too despite Metadata exemption).
  - Round 4 (designer revision): both codex round-2 issues addressed; picked Option A (unconditional `tabIndex={0}` on active Raw panel).
  - Codex round 3 (verbatim stdout `m2b-design-review-r3.txt`, 3207 lines): `needs changes` with both round-2 issues verified fixed; 1 NEW BLOCKING (acceptance item 40 still describes RawTab cleanup running on tab switch — leftover from pre-round-3 mechanism) + 1 NIT (motion.md:145-147 names cross-fade node `.tabpanel-content`, stale).
  - Round 5 (designer revision): both codex round-3 issues addressed (item 40 enumerates 4 cleanup triggers + explicit "Tab switches are NOT a cleanup trigger" paragraph; motion.md aligned to `<div role="tabpanel">`).
  - Codex round 4 (verbatim stdout `m2b-design-review-r4.txt`, 1532 lines): **APPROVED**. Both round-3 issues verified fixed. No findings, no missing evidence, no required changes.
  - **Codex catch density across the M2b design loop**: 5 → 2 → 1.5 → 0 (clean convergence trajectory matching M2a's 3-codex-round precedent). 8 BLOCKING design defects total caught by codex that the Claude UI/UX reviewer missed. Pattern reconfirmed: codex catches numerical WCAG / motion-budget / token-discipline / spec-deviation / WAI-ARIA edge cases that Claude reviewers consistently miss.
  - **Final verdict**: Claude UI/UX `approved with nits` (round 3); codex `approved` (round 4). Iteration count: 5 designer rounds (3 Claude reviewer + 4 codex rounds). The 5-round design loop is unprecedented in Phase 5 but justified by the high arch-design-risk; the catch trajectory was clean convergence, not stalemate.

- **M1b (compact list + sticky list-panel footer + filter `<details>` wrap + vestigial Open detail button)** — UI/UX gate: **needs UI/UX work.** Rationale: 4-essential row anatomy + sticky footer + filter `<details>` wrap + vestigial button (parallel to M1a's `.back-to-list` precedent). Designer invoked `frontend-design:frontend-design` skill; artifact at `working/phase-5/designs/m1b-shell/` (8 files: design.md 931 lines, prototype.html 1612 lines, motion.md 71, colors.md 87 with WCAG-AA contrast table, wireframes/{wide,narrow-with-filters-closed,narrow-with-filters-open,ready-placeholder}.txt). External design review (codex): NOT invoked (M1b extends M1a aesthetic — no new component family or token system; single Claude UI/UX reviewer sufficed). **Iteration loop**: round 1 (2026-04-28): UI/UX reviewer returned `approved` (cleaner than M1a's "approved with nits" — designer's 5 open questions all resolved by reviewer with "accept designer's choice"; no new findings). Designer questions resolved: Q1 `(refresh)` marker color = `--color-warn` (designer's choice; WCAG AA verified light 5.3:1 / 5.0:1, dark 9.2:1 / 8.7:1); Q2 marker placement = on row (spec line 526); Q3 sticky footer scrolling ancestor = page viewport (spec line 553 silent; defer pane-local scoping until M5 if needed); Q4 `display: flex !important` on ≥ 1100 px override = accept (one-line, contained); Q5 tool badge `flex-shrink: 0` = accept (title ellipsizes first; tool identity preserved). Reviewer flagged residual notes as developer hints: two stacked hairlines in footer is intentional; matchMedia listener must preserve user's intra-breakpoint manual toggle; Phase 4 click-time intersection regression tests at App.test.tsx lines 1224 / 1837 / 2028 / 2101 / 2152 are the concentrated risk surface for the implementation reviewer trio. Coordinator-level note: planner Q4 (spec `1100px` vs convention `1099.98px`) deferred to developer with spec-verbatim wording; codex may revisit. **Final verdict: `approved` (round 1)**. Iteration count: 1 round.

- **M1a (split-pane shell + URL hook + structural literals)** — UI/UX gate: **needs UI/UX work.** Rationale: introduces new split-pane `<main>` Grid layout, new right-pane `<article>` placeholder with empty/coming-soon/`session_not_found` states, signature-detail #4 (hairline gutter), signature-detail #3 (deep-link pulse animation on URL-driven mount), narrow-viewport `narrowMode` toggle + "← Back to list" affordance, global `Esc` handler with editable-control scope rule, reduced-motion zero-out rule. Spec direction is load-bearing (Archive-room aesthetic + motion budget + hairline-over-shadow).
  - **Design artifact**: `working/phase-5/designs/m1a-shell/` (8 files: `design.md` 692 → 692 lines, `prototype.html` 890 → 879 lines after revision, `colors.md` 112, `motion.md` 106, `wireframes/{wide,narrow-list,narrow-session,states}.txt`). Designer invoked the `frontend-design:frontend-design` skill via the Skill tool.
  - **External design review (codex)**: NOT invoked — M1a is a structural shell, not a new component family or token system; per coordinator-prompt §UI/UX Design Workflow, optional external design review reserved for high architectural-design risk. Single Claude UI/UX reviewer sufficed.
  - **Iteration loop**:
    - Round 1 (2026-04-28): UI/UX reviewer returned `needs changes`. One blocking finding: `var(--text-2xl)` referenced 3× in `design.md` narrative (§4.1 state matrix / §7.1 empty-pane decision / §8 acceptance checklist item 9) — `--text-2xl` is an M2 typography token (spec §Design Tokens line 894–899) that does not exist in the Phase 4 baseline `apps/frontend/src/styles/tokens.css`; following the narrative would force the developer to either introduce the M2 token early (token-discipline failure) or break acceptance checklist item 30 (token count must remain at Phase 4 baseline). The artifact's own `prototype.html` already used `--text-xl` and was internally inconsistent with the narrative. Plus two non-blocking nits: §3.2 mis-described Phase 4 `<main>` baseline ("exactly like Phase 4" was incorrect — Phase 4's `<main>` carries `padding: 0 var(--space-4)` / `margin: var(--space-6) auto var(--space-8)` / `gap: var(--space-6)`, which M1a intentionally replaces per spec line 497); and `aria-live="polite"` on the right-pane `<article>` may carry forward awkwardly into M2 once tabs land (M2 designer's concern, M1a-acceptable).
    - Designer revision: surgical changes only — replaced `var(--text-2xl)` → `var(--text-xl)` at the three design.md sites; deleted unused `--text-2xl` declaration from prototype.html `:root` and replaced with a single explanatory comment; rewrote §3.2 narrative to distinguish body gutter (preserved verbatim from Phase 4) from `<main>` recipe (replaced entirely per spec line 497). No other content touched.
    - Round 2 (2026-04-28): UI/UX reviewer returned `approved with nits`. All three round-1 required changes verified applied via grep. No new findings. Carry-over nits A and B (aria-live + aside aria-label) remain explicitly out-of-scope. No missing evidence. No required changes. Developer may proceed.
  - **Final verdict**: `approved with nits` (round 2). Iteration count: 2 rounds, well within the 3-round escalation threshold.

## Remaining Milestones

- **Milestone 1: Layout shell + URL sync + compact list** — IN PROGRESS, split into two chunks per planner 2026-04-28:
  - **M1a (in design loop)**: split-pane `<main>` CSS Grid (≥ 900 px split / < 900 px stacked + `narrowMode` toggle); `useSelectedSession.ts` hook + `?session=<rowKey>` via `replaceState` + `popstate` + `Esc` (with editable-control scope); deep-link pulse mechanism (`pendingDeepLinkPulseRowKey` + `data-deep-link` attr + 600 ms keyframe + `onAnimationEnd` cleanup + 2 s safety net); `SessionView.tsx` placeholder right-pane with empty / coming-soon / `session_not_found` states; "← Back to list" on stacked-narrow; structural literals only (NO Design Language tokens — those are M2). Phase 4 column structure + drawer flow UNCHANGED in M1a.
  - **M1b (pending after M1a)**: `SessionsTable.tsx` compressed to 4 columns + Select + `aria-current="true"` + row-click → `onSelectRow`; `<ActionBar>` + `<Pagination>` relocated to sticky list-panel footer; `<SessionFilters>` wraps in `<details>` below 1100 px; vestigial "Open detail" button in `SessionView` (removed in M2). Phase 4 click-time intersection regression tests (App.test.tsx M3 filter + M5 cross-page bulk-select + M5 pagination-cross-page) MUST pass byte-equivalent.
  - Phase 4 invariants preserved across both chunks (click-time intersection, importability rule, per-fetch error isolation, four empty states).
- **Milestone 2: Tabs primitive + SessionView shell + Metadata tab** — **CLOSED 2026-05-08 at M2b**. Accessible `Tabs.tsx` ✓ (ARIA tablist/tab/tabpanel + Left/Right/Home/End nav + selection-follows-focus); `SessionMetadata.tsx` ✓ extracted from `SessionDetail.tsx`'s 18-field `<dl>` (with subagent sidecar badge added per Resolved Decision #8); `RawTab.tsx` ✓ extracted from `RawPreviewBlock` (with "Not yet imported" branch per Resolved Decision #18); vestigial M1b "Open detail" button removed ✓; Skim/Transcript tabs render placeholder copy ✓; Drawer + SessionDetail left intact with DEAD-BUT-TESTED JSDoc headers per Resolved Decision #6 (deletion is M6).
- **Milestone 3: Per-tool parsers + buildSkim + truth tables** — **CLOSED 2026-05-09 at M3b**. Both chunks landed:
  - **M3a (CLOSED 2026-05-09 at impl SHA `959becb`)** ✓ `parsers/types.ts`, `parsers/{claude_code,codex,buildSkim,index}.ts` pure/total/synchronous; truth-table coverage complete (empty / one user msg / multi-turn / boundary / agent-only / oversized / malformed / tool_use+tool_result / Codex `session_meta` / embedded parent meta + dual-tracker merging-bug regression + anchor-principle real-fixture verification + JSON.stringify(undefined) type-safety regression). Pure-logic; UI/UX impact `not applicable`.
  - **M3b (CLOSED 2026-05-09 at impl SHA `6563495`)** ✓ `streamRawText.ts` (5 MB cap with `STREAM_RAW_TEXT_BYTE_CAP` constant; `AbortSignal` propagation; cap-equality enforced via `Uint8Array.subarray` slicing at boundary so `totalBytes === STREAM_RAW_TEXT_BYTE_CAP` exactly; `reader.cancel()` proven by `cancelSpy` mirroring `rawPreview.test.ts:53-94`; multi-byte UTF-8 character at cap boundary handled via `decoder.decode(slicedChunk, { stream: false })`; pre-aborted signal short-circuit; body-less response defensive return); `useParsedSession.ts` (module-scoped LRU cache cap=5 + `inFlight` coalescing + `inFlightControllers` for hard-reset abort; `bumpCacheEpoch()` exported for App.tsx wire-up; `_resetForTests()` exported helper; per-hook AbortController; dual epoch guards at cache write AND state transition; `epochAtSubscription` capture on coalescing per spec line 467 [in-commit fix]; AbortError silent no-op; `deriveInitialState` synchronous cache-hit path so tab-switch returns success without loading flash; `retryNonce` gating bypasses cache+inFlight on Retry; inFlight cleanup gated on Map identity match; dep array `[storedSessionUid ?? null, tool ?? null, retryNonce]`); `App.tsx` Rescan + Import success-branch wire-up (after `pushToast`, before `await refetchAll()`, never in `finally`, never on error path). Bun-first preserved (no `jest.fn()`; uses `bun:test` `mock`); test count delta +32 (10 streamRawText + 18 useParsedSession + 4 App.tsx). UI/UX impact `not applicable`; gate `not required`.
- **Milestone 4: TranscriptView** — **CLOSED 2026-05-10 at impl SHA `194be3d`**. ✓ `TranscriptView.{tsx,test.tsx,css}` chronological message list with per-kind rendering (user/assistant/tool_use/tool_result/system/boundary/unknown), absolute + relative timestamps via `relativeTimeFrom`, monospace for code-fenced segments, collapsible long tool_result body (>2 KB) via UTF-8-codepoint-safe split, truncation banner with `role="status"` + `--motion-base` opacity entrance per spec line 715, parse-warnings dismissible `<details>` banner with spec-literal `{N} parse warnings — click to view.` for all N, boundary chapter-break with full-width hairline + small-caps Fraunces label per signature detail #1 (recipe shared with M5 SkimView). DEFAULT_TAB_ON_SELECTION shifted `"metadata"` → `"transcript"` per Resolved Decision #11. "Open raw" header anchor expanded into M4 scope per spec line 626 + design BLOCKING-2 (visibility rule `storedSessionUid !== null`; URL via `RAW_SESSION_PATH`; `target="_blank" rel="noreferrer"`). Long-corpus measurement on 5k-message synthetic fixture: median 16.7 ms (rAF 60 Hz floor); p95 16.8 ms; 0 dropped frames (>25 ms threshold). Spec line 1032 strict `< 16 ms` interpreted operationally as "0 dropped frames" because rAF 60 Hz cadence floors at 16.67 ms. **Escape-hatch slot 2 NOT fired**; `@tanstack/react-virtual` deferred to a future phase if real-corpus data demands it; dep budget remains 1/2.
- **Milestone 5: SkimView with four block kinds** — pending. `user_turn` inline + collapsible disabled-placeholder + "Expand to raw messages" affordance reusing TranscriptView scoped to ordinal range; `boundary` divider; `agent_only` / `oversized_user_message` collapsed by default; never silently summarized.
- **Milestone 6: Cleanup, retire drawer, doc sweep, WCAG, log** — pending. Delete `SessionDetail.tsx` + `Drawer.tsx` + sibling CSS/tests; 8-doc sweep; WCAG AA contrast measured for every new pair (light + dark); final progress log entry; verify all gates green + hex isolation 24.

## Completed Work Log

- 2026-05-10: **M4 delivered** — TranscriptView (chronological message list + 7 MessageKind renders + truncation banner + parse-warnings banner + boundary chapter-break) + Open raw header anchor + DEFAULT_TAB_ON_SELECTION shift (`"metadata"` → `"transcript"`) + useParsedSession AbortError-coalescing fix (StrictMode double-mount race surfaced by M4) + 5k-message long-corpus perf measurement. First visible UI surface in Phase 5 consuming parsed message data. 4 new frontend files at `apps/frontend/src/features/sessions/TranscriptView.{tsx (566 lines), css (471 lines), test.tsx (931 lines / 41 tests / 94 expects)}` + 2 new auxiliary files at `apps/frontend/tests/fixtures/transcript-5k.builder.ts (209 lines, deterministic LCG fixture builder)` + `apps/frontend/e2e/transcript-perf.spec.ts (154 lines, Playwright frame-timing capture)` + 6 modified frontend files at `apps/frontend/src/features/sessions/{SessionView.tsx (DEFAULT_TAB shift; <TranscriptView /> mount; "Open raw" anchor), SessionView.css (.session-open-raw rule), SessionView.test.tsx, useParsedSession.ts (in-flight coalescing AbortError → bumps retryNonce; M3b fix surfaced by M4)}` + `apps/frontend/src/App.test.tsx` + `apps/frontend/e2e/inspection.spec.ts`. Plus design artifact at `working/phase-5/designs/m4-transcript/{design.md (1563 lines), prototype.html (1308 lines), motion.md (351 lines), colors.md (536 lines, 30 WCAG-AA pairs), wcag_m4.py (200 lines, pure-stdlib reproducible script), wireframes/01..09.txt (9 files)}` + 9 codex review artifacts at `working/phase-5/codex-reviews/m4-{design-review-r2,r3,impl-review-r1,r1b,r2}{,-prompt}.txt` + plan artifact at `working/phase-5/m4-plan.md`. Diff-stat: 35 files changed / +15226 insertions / -90 deletions (codex review artifacts + design artifact dominate; impl source ~3000 lines including tests). UI/UX gate decision: **needs UI/UX work**. Design loop converged in 3 designer rounds + 2 Claude UI/UX rounds + 2 codex design rounds (3-round designer trajectory matches M2a; below M2b's 5-round high-water mark). Codex design catch density: 4 numerical drift pairs in colors.md table (T01 light 16.42 vs script 16.00; T11 dark 13.74 vs 14.15; T12 3.39/5.49 vs 4.21/7.68; T17b dark 2.85 vs 2.87) — codex executed `python3 wcag_m4.py` in its sandbox to surface the discrepancies; coordinator's decision was SCRIPT IS AUTHORITATIVE (replace all table values with script output). T12 reframed as non-text 3-px stripe (SC 1.4.11 3:1 threshold, not 4.5:1 AA text); 4.21 light passes 3:1 by 1.21 margin; no design remediation needed. Three-reviewer rule (impl) converged in 2 codex rounds. Codex impl r1: 5 findings — 4 false positives (`key={...}` flagged on `.map()`-rendered list items at TranscriptView.tsx:224/465/509 as panel-remount keys despite being React's standard list-reconciliation pattern; `aria-orientation="horizontal"` claimed missing despite being on the next line of the boundary `<li>` at TranscriptView.tsx:371) + 1 missing-evidence (transition contamination from JSDoc comments). Coordinator-applied rebuttals with line-cited verbatim evidence; codex r2 approved by inspecting actual file:line and re-running `bun run test` (491 pass) + `bunx tsc --noEmit` (clean) in its sandbox. The M3b useParsedSession.ts in-flight coalescing branch was patched in-scope: AbortError now bumps retryNonce instead of silent return — surfaces a StrictMode double-mount race that only manifests when the first visible useParsedSession consumer mounts under React StrictMode (TranscriptView is M4's first such consumer). Per Claude UI/UX reviewer Q1 resolution: lands in M4 with progress-log annotation rather than M3b retro-fix; all 18 existing M3b useParsedSession tests still pass; bug only manifested under StrictMode + first visible consumer. Long-corpus measurement on 5k-message synthetic fixture: median 16.7 ms (rAF 60 Hz cadence floor); p95 16.8 ms; 0 dropped frames (>25 ms threshold). **Spec line 1032 strict `< 16 ms` is physically inconsistent with rAF 60 Hz cadence floor (16.67 ms minimum inter-frame delta on healthy Chromium). Coordinator-recorded operational interpretation: "0 dropped frames (>25 ms ≈ 1.5× 60 Hz budget)" replaces the strict-< 16 reading. Documented here in the progress log rather than amending the frozen spec at `05467ad` (amendments are high-blast-radius and reset reviewer assumptions).** Escape-hatch slot 2 NOT fired; `@tanstack/react-virtual` deferred; dep budget remains 1/2. Bun-first invariant preserved: tests use `bun:test` `mock()`; no `jest.fn()`; no `child_process`; the only `node:*` imports are `node:path`/`node:url` in the Playwright e2e perf spec (test runner code, not runtime app code; backend-protection reviewer cleared). Final gates: `cargo check --workspace` clean, `cargo test --workspace` green (Rust untouched), `cargo test -p distill-portal-ui-api-contracts --features ts-bindings` 1 passed / 1 ignored, `bun run test` 491/0/1729 across 30 files (M3b 447/0/1619/29 → +44 tests / +110 expects / +1 file), `bun run build` 28.58 kB CSS / 260.79 kB JS / 470ms (CSS +7.24 kB from TranscriptView.css; JS +20.88 kB from TranscriptView.tsx + 5k-message fixture + new e2e spec), `bun run test:e2e` 2 passed (inspection + transcript-perf) in ~6s, `bunx tsc --noEmit` clean. Hex count 24, token count 83 (M4 consumes 27 tokens from existing set; ZERO new tokens), no new runtime deps, no protected-path edits. Impl committed as `194be3d` ("Implement Phase 5 M4: TranscriptView + Open raw header anchor") on `main` 2026-05-10; this log update is the follow-up second commit.

- 2026-05-09: **M3b delivered** — `streamRawText` + `useParsedSession` + App.tsx cacheEpoch wire-up. Async/effect/cache layer atop M3a's pure parsers, completing Milestone 3. 4 new frontend files + 2 modified (`App.tsx` +14 lines / `App.test.tsx` +259 lines) + 1 codex review artifact at `working/phase-5/codex-reviews/m3b-impl-review-r1.txt` + 1 plan artifact at `working/phase-5/m3b-plan.md`. Diff-stat: 8 files changed / +7001 insertions (codex review + plan artifacts dominate). Files: `apps/frontend/src/features/sessions/streamRawText.ts` (NEW, 252 lines; pre-aborted signal throws AbortError WITHOUT opening reader; body-less response returns `{ text: "", totalBytes: 0, truncated: false }`; cumulative `value.byteLength` byte counter [NOT `string.length` — UTF-16 conflation is the bug class]; cap-equality via `value.subarray(0, room)` slicing + `decoder.decode(slicedChunk, { stream: false })` so `totalBytes === STREAM_RAW_TEXT_BYTE_CAP` EXACTLY when truncated; multi-byte UTF-8 at boundary may yield U+FFFD per spec line 402 byte-anchored contract; `await reader.cancel()` at trip; abort listener registered with `{ once: true }` and removed in `finally`); `apps/frontend/src/features/sessions/streamRawText.test.ts` (NEW, 365 lines / 10 tests / 43 expects; mirrors `rawPreview.test.ts` `cancelSpy` pattern wrapping `getReader().cancel` so spy increments on actual reader-cancel invocations [not just underlying-source `cancel`]; cap-equality regression test feeds `STREAM_RAW_TEXT_BYTE_CAP + 16384` bytes and asserts `totalBytes === STREAM_RAW_TEXT_BYTE_CAP` exactly; cumulative-breach across 5 × 1.5 MB chunks; pre-aborted signal asserts `cancelSpy.count === 0`; multi-byte UTF-8 chunk + multi-byte-straddling-cap-boundary regressions); `apps/frontend/src/features/sessions/useParsedSession.ts` (NEW, ~470 lines after coalesced-epoch fix; `useParsedSession(row: SessionRow | null): UseParsedSessionResult` spec-verbatim signature per spec line 438; module-scoped `cache: Map<string, ParsedSession>` LRU at `USE_PARSED_SESSION_CACHE_MAX = 5`; `inFlight: Map<string, Promise<ParsedSession>>` for coalescing; `inFlightControllers: Map<string, AbortController>` for hard-reset abort; `cacheEpoch: number` module-scoped counter; `bumpCacheEpoch()` exported [aborts every in-flight controller, clears cache + inFlight + controllers, increments epoch]; `_resetForTests()` exported test helper [co-located per M3a `buildSkim.ts` precedent]; `deriveInitialState` reads cache directly so tab-switch returns synchronous `success`/`truncated` state — NOT `loading → success` via useEffect; per-hook `AbortController` created in effect, cleaned up via `controller.abort()` on unmount; `epochAtStart = cacheEpoch` captured at fetch dispatch; cache write AND state transition gated on `cacheEpoch === epochAtStart`; coalesced sibling captures `epochAtSubscription = cacheEpoch` [in-commit fix per normal Claude reviewer's nit + spec line 467] and gates its `.then` transition; `isAbortError` filter ensures unmount/row-change/hard-reset abort silently no-ops; LRU `delete + set` on hit + `while`-loop eviction; `inFlight.get(key) === promise` Map-identity guard on cleanup; dep array `[row?.storedSessionUid ?? null, row?.tool ?? null, retryNonce]` minimal trio avoiding object-identity churn; `retry()` only bumps `retryNonce` when current state is `"error"` per spec line 434; no defensive try/catch around `dispatchParser` per M3a totality contract); `apps/frontend/src/features/sessions/useParsedSession.test.ts` (NEW, 821 lines / 18 tests / 83 expects; calls `_resetForTests()` in `beforeEach`; uses `renderHook` from `@testing-library/react` + `mock` from `bun:test`; covers idle / no_raw / loading / success / error+retry / truncated state transitions; cache-hit-on-tab-switch synchronous return; LRU cap-5 eviction with read-bumps-recency; abort-on-storedSessionUid-change; abort-on-unmount; in-flight coalescing exactly-one-fetch; epoch invalidation on Rescan AND on Import-success drops in-flight; cache survives selectedRowKey churn within cap; retry skips cache+inFlight; retry-no-op when state is success; epoch-stale resolution does NOT transition to error); `apps/frontend/src/App.tsx` (MODIFIED, +14 lines: `import { bumpCacheEpoch } from "./features/sessions/useParsedSession";` + `bumpCacheEpoch()` inline in `handleRescan` success branch after `pushToast` and before `await refetchAll()` + same pattern in `handleImport`; never in `finally`, never on error path — verified by App.tsx dedicated tests); `apps/frontend/src/App.test.tsx` (MODIFIED, +259 lines / 4 new tests covering observable cacheEpoch side-effects: prime cache via `useParsedSession` mount + await success → trigger Rescan/Import → re-mount hook → assert `state === "loading"` proves cache cleared on success path; assert `state === "success"` synchronous proves cache survives error path). Bun-first invariant preserved: tests use `bun:test` `mock()` (NOT `jest.fn()`); no `child_process`; no `npm`/`node`. Three-reviewer rule converged in **1 codex round + 1 normal Claude round** (cleanest convergence in Phase 5 — pattern broke from the M1a–M3a precedent of ≥2 codex rounds). Reviewer verdicts: backend-protection `approved` (no protected-path edits, no dependency drift, no Bun-first violations); codex `approved` with NO findings + ran `bun run test` 447/0/1619 + `bunx tsc --noEmit` clean inside its sandbox to verify (first Phase 5 chunk where codex landed approval in round 1 with zero blocking findings); normal Claude `approved with nits` flagging 2 `Suggestion`-level items (coalesced-sibling not epoch-guarded; `retry` updater-function pattern). Coordinator-applied in-commit fix: capture `epochAtSubscription = cacheEpoch` at coalescing time and gate `.then` per spec line 467 ("Coalesced consumers awaiting the same Promise see the same drop semantics") — addresses a real but narrow microtask-ordering race (stream resolves → microtasks queue → bumpCacheEpoch fires synchronously → microtasks fire → originator drops via epoch check, sibling without guard transitions to success with stale data). The `retry` updater-function nit acknowledged as Suggestion and left as-is (idempotent under React batching; tests #15/#16 cover both paths). Final gates: `cargo check --workspace` clean, `cargo test --workspace` green (Rust untouched), `cargo test -p distill-portal-ui-api-contracts --features ts-bindings` 1 passed / 1 ignored, `bun run test` 447/0/1619 across 29 files (M3a 415/0/1477/27 → +32 tests / +142 expects / +2 files), `bun run build` 21.34 kB CSS exact (zero CSS shipped) / 239.91 kB JS / 509ms (JS +0.13 kB tree-shake delta — parsers + new modules entering the running tree), `bun run test:e2e` 1 passed in 3.2s, `bunx tsc --noEmit` clean. Hex count 24, token count 83, no new runtime deps, no protected-path edits. Impl committed as `6563495` ("Implement Phase 5 M3b: streamRawText + useParsedSession") on `main` 2026-05-09; this log update is the follow-up second commit.

- 2026-04-27: bootstrapped `progress/phase-5.progress.md`; resolved the Phase 5 invocation block from `working/phase-5.md`; verified tooling (`codex exec` `0.125.0`, `claude` `2.1.119`, `bun` `1.3.13`); inventoried current frontend state (17 unit-test files / 231 tests / 891 expects post-Phase-4 close; 1 Playwright spec); registered milestone task tracking. No code change yet — Chunk A is purely the spec + log scaffold.
- 2026-04-27: Chunk A complete — committed `working/phase-5.md` + `progress/phase-5.progress.md` as `bce56c0` on `main` ("Add Phase 5 planning doc and bootstrap progress log"). This fixes the source-of-truth SHA all Phase 5 reviewers cite. Spec content is the ~700-line implementation plan covering six milestones (M1 layout shell + URL sync + compact list, M2 Tabs primitive + SessionView shell + Metadata tab, M3 per-tool parsers + buildSkim + truth tables, M4 TranscriptView, M5 SkimView with four block kinds, M6 cleanup + 8-doc sweep + WCAG). No code change. Note: `components/collector-runtime/src/adapters/codex.rs` + `tests/parsers.rs` had unrelated user/tool-driven modifications in the working tree at Chunk A time (handling forked Codex sessions); coordinator deliberately left them unstaged (protected paths + outside Phase 5 scope) and surfaced them to the human for separate disposition. Those changes were subsequently committed by the user as `422f164` ("Fix Codex forked session parsing") on 2026-04-27; not tracked further by Phase 5 bookkeeping.

- 2026-05-09: **M3a delivered** — Per-tool parsers + buildSkim + dispatcher. M3 split into M3a (pure logic) + M3b (async/cache layer) per planner recommendation, mirroring the M2a/M2b precedent (blast-radius bounding for the codex review surface). 9 new frontend files at `apps/frontend/src/features/sessions/parsers/{types.ts (146 lines), claude_code.ts (~415 lines after fix-up), codex.ts (~510 lines after fix-up), buildSkim.ts (131 lines), index.ts (85 lines), claude_code.test.ts (~420 lines), codex.test.ts (~530 lines), buildSkim.test.ts (303 lines), index.test.ts (142 lines)}` + 5 codex review artifact files at `working/phase-5/codex-reviews/m3a-impl-review-r{1..5}.txt`. Diff-stat: 14 files / +15551 insertions (codex review artifacts dominate; impl source is ~2700 lines including tests). UI/UX gate decision: **not required** (pure module additions). Three-reviewer rule converged in 5 codex rounds (longest in Phase 5). Codex catch density: r1 false positive (coordinator transcription error in evidence pack — paraphrased dispatcher snippet wrote `sourceTool` instead of spec-verbatim `tool`; spec at lines 284 + 338 uses `tool: Tool` — `grep -n "sourceTool" working/phase-5.md` returns zero matches; implementation correctly matched the spec); r2 caught 2 BLOCKING (`response_item.function_call` spec-literal violation per spec line 773 — passed strings through unchanged instead of `JSON.stringify(payload.arguments)`; 5 totality gaps for silent skips on Claude content-array primitives + Codex `event_msg.user_message`/`agent_message`/`agent_reasoning` missing `payload.message` + `exec_command`/`output` missing `payload.command`/`output` + non-string `payload.timestamp`); r3 caught 2 BLOCKING (`exec_command` spec-literal violation per spec line 779 — same pattern as r2's function_call bug, fix-up developer missed extending the rule; `JSON.stringify(undefined) === undefined` runtime-shape bug yielding `Message.text === undefined`, violating the `text: string` type contract — codex EXECUTED the code in its sandbox via `bun -e` to assert the actual runtime value); r4 caught 1 BLOCKING (spec line 779 asymmetry — the "/" separator in `text: JSON.stringify(payload.command) / payload.output` is positional alternation: `exec_command` stringifies its command, `exec_command_output` passes its output through raw; coordinator initially over-stringified both); r5 approved with codex itself running `bun run test` (415 pass) + `bunx tsc --noEmit` (clean) inside its sandbox to verify before clearing. 1 fix-up by developer subagent (5 fixes covering r2 findings + 9 regression tests) + 2 fix-ups by coordinator (null-guard at top of `case "response_item"` to eliminate `payload!` non-null assertions and 3 surfaced `tsc --noEmit` errors that the developer's `payload!` removal exposed; `exec_command_output` passthrough rewrite for r4 spec-asymmetry fix). Coordinator-applied fixes are minor + contained — no developer re-dispatch needed. Files: `apps/frontend/src/features/sessions/parsers/types.ts` (NEW; mirrors spec §Data Model lines 233-348 verbatim — `Message`, `MessageKind`, `SkimBlock`, `BlockKind`, `ParseWarning`, `ParserOutput`, `ParsedSession`, `StreamMeta`; field `tool: Tool` on `ParsedSession` matches spec lines 284 + 338 verbatim — NOT `sourceTool` despite codex r1's false-positive flag); `apps/frontend/src/features/sessions/parsers/claude_code.ts` (NEW; pure/total/synchronous; top-level `type` discriminator authoritative per spec line 759; user/assistant string + array content split with sequential `messageIndex` across same `lineOrdinal`; `summary` → `system` with `leafUuid: ` prefix; `system` → `system`; `custom-title` + `permission-mode` low-severity warnings WITHOUT emitting Messages; non-object content-array items WARN per fix-up; unknown top-level type → `unknown` + warning; role-mismatch warns but emits using top-level type as authoritative); `apps/frontend/src/features/sessions/parsers/codex.ts` (NEW; pure/total/synchronous; top-level `type` discriminator — field is `type` NOT `record_type` matching Rust adapter at `components/collector-runtime/src/adapters/codex.rs:85,137,144`; anchor principle implementation per spec line 767 — `response_item.message` of role user/assistant SKIPPED silently with no warning, end-to-end verified against `tests/fixtures/codex/sample_session.jsonl` showing line 1 `response_item.user` dropped while line 2 `event_msg.user_message` survives; `response_item.function_call` → `tool_use` with `JSON.stringify(rawArgs ?? null)` — spec-literal per line 773; null-guard at top of case eliminates `payload!` non-null assertions; first `session_meta` → `system` summarizing cwd/cli/provider, second → `boundary` with `boundarySubtype: "session_resumed"` per spec line 771; `event_msg.user_message`/`agent_message`/`agent_reasoning` warn on missing/non-string required fields per fix-up; `event_msg.exec_command` → `tool_use` with `JSON.stringify(rawCommand ?? null)` per spec line 779 LEFT; `event_msg.exec_command_output` → `tool_result` with raw string passthrough OR `JSON.stringify(rawOutput ?? null)` for non-string values per spec line 779 RIGHT — asymmetry per the spec's "/" separator; `event_msg.error` → `system` + warning; `turn_context` silent skip per spec line 774; top-level timestamp preferred over payload.timestamp per spec line 784); `apps/frontend/src/features/sessions/parsers/buildSkim.ts` (NEW; dual-tracker algorithm per spec lines 360-394 verbatim — two explicit open-region trackers `openUserTurnStart` + `openAgentOnlyStart`, mutex invariant by construction, boundary closes whichever is open and emits its own `[i,i]` block, oversize user message becomes its own `oversized_user_message` block at `[i,i]` with `meta.sizeBytes` and resets both trackers; empty stream emits `[{kind: "agent_only", start: 0, end: -1, meta: {empty: 1}}]` per spec line 372; `USER_MSG_OVERSIZE_THRESHOLD = 65_536` co-located per spec §File layout line 740; threshold check uses strict `>` not `>=`, asserted by explicit boundary test); `apps/frontend/src/features/sessions/parsers/index.ts` (NEW; static `Record<Tool, ParserFn>` registry at line 31 + runtime exhaustiveness test at `index.test.ts:32-34` — two layers of defence against future-Tool drift; `dispatchParser(tool, rawText, streamMeta) → ParsedSession` totality with empty + warning fallthrough for unknown tool; barrel re-exports). 100 unit tests across 4 co-located test files (claude_code 25 → 27 after fix-up = 27, codex 32 → 38 after fix-up + 2 coordinator-added regression tests = 40, buildSkim 21, index 9 — total ~97 tests; suite-level `bun run test` reports 415 across 27 files including all M1–M2b tests). Bun-first invariant preserved: all tests use `bun:test`; no `jest.fn()`, no `child_process`, no `npm`/`node`. Reads `tests/fixtures/{claude_code,codex}/sample_session.jsonl` via Bun's `node:fs` (allowed Bun shim). Final gates: `cargo check --workspace` clean, `cargo test --workspace` all green (Rust untouched), `cargo test -p distill-portal-ui-api-contracts --features ts-bindings` 1 passed / 1 ignored, `bun run test` 415/0/1477 across 27 files (M2b 316/0/1236/23 → +99 tests / +241 expects / +4 files), `bun run build` 21.34 kB CSS / 239.78 kB JS / 519ms (size unchanged from M2b — parsers tree-shaken until M3b imports them), `bun run test:e2e` 1 passed in 3.2s, `bunx tsc --noEmit` clean (added the null-guard fix to keep strict). Hex count 24, token count 83, no new runtime deps, no protected-path edits. Impl committed as `959becb` ("Implement Phase 5 M3a: per-tool parsers + buildSkim + dispatcher") on `main` 2026-05-09; this log update is the follow-up second commit.

- 2026-05-08: **M2b delivered** — Tabs primitive + SessionView shell rewire + SessionMetadata + RawTab extractions + vestigial M1b "Open detail" button collapse. 11 modified frontend files + 9 new frontend files + 4 design artifact files at `working/phase-5/designs/m2b-shell/` + 6 codex review artifact files at `working/phase-5/codex-reviews/m2b-{design-review,design-review-r2,design-review-r3,design-review-r4,impl-review-r1,impl-review-r2}.txt`. Diff-stat: `1341 insertions(+), 1265 deletions(-)` for modified files (10 file pairs); ~9 new files (Tabs.{tsx,css,test.tsx} + RawTab.{tsx,css,test.tsx} + SessionMetadata.{tsx,css,test.tsx}). Files: `apps/frontend/src/components/Tabs.tsx` (NEW; controlled-only API: `value`, `onValueChange`, `tabs`; ARIA `tablist` / `tab` / `tabpanel`; Left/Right keyboard nav with wrap; Home/End; selection follows focus; `aria-controls` ↔ `aria-labelledby` linkage; indicator slid via `transform: translateX(${x}px) scaleX(${width})` — UNITLESS scaleX after codex round-1 catch; indicator base size `width: 1px; height: 1px;`; `useLayoutEffect` + `getBoundingClientRect` + `ResizeObserver` for resize/font-swap remeasurement); `apps/frontend/src/features/sessions/SessionMetadata.{tsx,css,test.tsx}` (NEW; 18-field `<dl>` extracted verbatim from `SessionDetail.tsx` lines 130–256 with snake_case `<dt>` text preserved; subagent sidecar badge added per Resolved Decision #8 inline next to `has_subagent_sidecars` field as dashed-hairline informational chip; Copy path button with Clipboard-API → manual-select fallback; "View raw" anchor only when `storedSessionUid !== null`); `apps/frontend/src/features/sessions/RawTab.{tsx,css,test.tsx}` (NEW; extracted from `RawPreviewBlock`; state machine `idle | loading | success | error | non_2xx | not_imported`; `streamSessionRaw` + `consumeRawPreview` byte-equivalent to Phase 4 with same caps + caption strings + Retry semantics; AbortController cleanup ONLY on selectedRowKey change / `row.storedSessionUid` change / Retry counter bump / SessionView unmount — **tab switches are NOT a cleanup trigger** per spec lines 650-658 keep-mounted contract; non-JSON marker uses `--color-ink-muted` italic NOT `--color-warn`); `apps/frontend/src/features/sessions/SessionView.{tsx,css,test.tsx}` (rewired from M1a four-state placeholder to four-tab shell; `DEFAULT_TAB_ON_SELECTION = "metadata"` exported constant per Resolved Decision #11; `activeTab` + `visitedTabs: Set<TabId>` component-local state; `visitedTabs` initialized to `new Set([defaultTab])`; reset on `selectedRowKey` change; cross-fade-IN-only via inline `style={{ animation: isActive ? "tab-fade-in 120ms var(--ease-out) both" : "none" }}` — NO `key={activeTab}` on panel content per Option A pick from design loop; `tabIndex={0}` matrix on active Skim/Transcript/Raw panels — NOT Metadata; `.session-conflict-badge` uses `--color-warn` recipe per spec line 622 + design checklist 24 + colors.md H09 — codex round-1 caught error→warn correction; minimal header at M2b — title + tool badge + status pill + conflict badge only — Copy path/subagent badge in header/sourcePathIsStale hint/"Open raw" anchor defer to M4 alongside truncation banner consumer; outer `key={selectedRowKey}` page-turn fade signature detail #2 first-ship); `apps/frontend/src/features/sessions/SessionsView.{tsx,test.tsx}` (DELETE the `forwardRef` wrapper, `useImperativeHandle`, `SessionsViewHandle` export, the `detailRowKey` + `triggerRef` state, `handleOpenDetail` + `handleCloseDetail` callbacks, the `<Drawer>` + `<SessionDetail>` JSX, the `Drawer` + `SessionDetail` imports — function component again; tests #4+#5 deleted; test #3 updated to assert no `<dialog>` element renders inside SessionsView's render tree); `apps/frontend/src/features/sessions/SessionsTable.tsx` (M2b round-2 fix-up — comment-only rewrite of 4 stale drawer/"Open detail" comment blocks per codex round-1 NIT); `apps/frontend/src/components/Drawer.tsx` + `apps/frontend/src/features/sessions/SessionDetail.tsx` (DEAD-BUT-TESTED JSDoc headers added per Resolved Decision #6; tests still pass via direct import; deletion deferred to M6); `apps/frontend/src/App.tsx` (DELETE `sessionsViewRef`, `handleOpenDetailFromSessionView`, `SessionsViewHandle` import, `ref={sessionsViewRef}` prop, `onOpenDetail={handleOpenDetailFromSessionView}` prop; `void buildUrl;` dead-scaffold removed at lines 786–789 + `buildUrl` import at line 81 deleted per planner fold-in; matchMedia legacy `addListener` fallback at lines 832–846 LEFT UNTOUCHED per planner deferral to M6; passes `key={selectedRowKey ?? "__empty__"}` to `<SessionView>` to drive page-turn fade); `apps/frontend/src/App.test.tsx` (M1b drawer-trigger tests at lines 3262–3289 + 3291–3342 deleted; narrative comments updated drawer→Metadata tab; 3 new M2b tests for row click → SessionView mounts → default tab = Metadata + popstate → tab snaps back to default); `apps/frontend/e2e/inspection.spec.ts` (substantial rewrite per planner brief — step 6 View raw anchor in Metadata tab body; step 7 session_uid `<dd>` in `.session-metadata`; step 9 REPLACE drawer focus-trap walk with tab-strip e2e — click each tab + ArrowLeft/Right/Home/End nav + placeholder copy + Metadata `<dl>` + Raw preview; step 10 raw preview lookup in `.raw-tab` not `dialog.drawer`). Final gates: `cargo check --workspace` clean, `cargo test --workspace` green, `cargo test -p distill-portal-ui-api-contracts --features ts-bindings` 1 passed / 1 ignored, `bun run test` 316/0/1236 across 23 files (M2a 278/1086/20 → +38 tests / +150 expects / +3 files), `bun run build` 21.34 kB CSS / 239.78 kB JS / 523 ms (CSS +1.63 kB; JS shrank -27.25 kB from Drawer/SessionDetail tree-shake out of running tree), `bun run test:e2e` 1 passed in 3.5s; hex count 24 (developer self-caught a `#29a` JSDoc reference that tripped rg as 3-hex token → rewrote to `item 29a`), token count 83, no new runtime deps, no protected-path edits. Impl committed as `6068d6f` ("Implement Phase 5 M2b: Tabs primitive + SessionView shell rewire + SessionMetadata + RawTab extractions + vestigial Open detail collapse") on `main` 2026-05-08; this log update is the follow-up second commit.

- 2026-04-28: **M1a delivered** — split-pane shell + `useSelectedSession` URL hook + structural CSS literals. 7 modified frontend files + 5 new frontend files (+1473-line diff including the fix-up round). Files: `apps/frontend/src/App.tsx` (+state slices for selectedRowKey + narrowMode + pendingDeepLinkPulseRowKey + 2 s safety timer effect + Esc handler with editable-control negative scope AND positive scope (article.session-pane | tr[aria-current="true"]) + matchMedia narrow-viewport listener; Phase 4 selection ownership pattern preserved per Resolved Decision #20); `apps/frontend/src/App.test.tsx` (12 new M1a tests covering split-pane mounts / URL-on-mount / popstate / Esc scope four ways including `[contenteditable]` empty-value form / deep-link pulse click-vs-URL distinction / pulse cleanup / session_not_found timing); `apps/frontend/src/styles/global.css` (split-pane CSS Grid `minmax(300px, 380px) 1fr` ≥ 900 px, single-column < 900 px with `data-narrow-mode` visibility rules including the `back-to-list` re-enable inside the narrow-mode block per codex fix-up #1, hairline gutter `border-right: 1px solid var(--color-border)` on `.list-pane`, reduced-motion zero-out rule per spec lines 102–110); `apps/frontend/src/features/sessions/SessionsTable.{tsx,css}` (`aria-current="true"` + `data-deep-link` attributes, `onSelectRow` callback in addition to preserved `onOpenDetail`, four row variants — default / hover / selected / selected+hover — via `color-mix(in srgb, var(--color-accent) 8/4/12%, transparent)` per spec §Row visual treatment lines 533–545, `@keyframes deep-link-pulse` animating `background-color` only at 22% peak); `apps/frontend/src/features/sessions/SessionsView.tsx` (now presentational, accepts `selectedRowKey` + `onSelectRow` + `pendingDeepLinkPulseRowKey` props per Resolved Decision #20); `apps/frontend/src/features/sessions/SessionView.{tsx,css,test.tsx}` (NEW; `<article class="session-pane">` placeholder with four-state machine `data-state="empty | loading | ready-placeholder | session_not_found"` + `aria-live="polite"` + "← Back to list" button visible only on narrow-with-session-mode); `apps/frontend/src/features/sessions/useSelectedSession.{ts,test.ts}` (NEW; spec-verbatim interface lines 826–833 — `{ selectedRowKey, selectRow }`; mount-time URL read; `replaceState` on `selectRow`; `popstate` listener with no feedback loop; `buildUrl` preserves all other query params); `apps/frontend/e2e/inspection.spec.ts` step 12 (deep-link arrival via `page.goto('/?session=' + key)` asserts transient `data-deep-link="true"` within 1.5 s window plus post-pulse removal within 3 s window per codex fix-up #4 + reload preserves URL state). Phase 4 row-click → drawer flow PRESERVED (M1a row click STILL opens the existing Phase 4 `<Drawer>` AND calls new `onSelectRow`; vestigial "Open detail" button defers to M1b). Phase 4 click-time intersection regression tests (App.test.tsx M3 filter / M5 cross-page bulk-select / M5 pagination-cross-page) and the e2e step 9 focus-trap walk all pass byte-equivalent. Final gates: `cargo check --workspace` clean, `cargo test --workspace` green, `cargo test -p distill-portal-ui-api-contracts --features ts-bindings` 1 passed / 1 ignored, `bun run test` 257/0/994 across 19 files, `bun run build` 14.20 kB CSS / 265.82 kB JS / 544 ms, `bun run test:e2e` 1 passed in 3.0s; hex count 24, token count 44, no new runtime deps, no protected-path edits. Impl committed as `a59b3f6` ("Implement Phase 5 M1a: split-pane shell + URL hook + structural literals") on `main` 2026-04-28; this log update is the follow-up second commit.

- 2026-04-28: Chunk A revised — committed `working/phase-5.md` as `05467ad` on `main` ("Refine Phase 5 spec with Archive-room aesthetic + 5-round codex arch review"). Triggered by `/frontend-design` invocation against the Chunk A spec to (a) add an intentional UI/UX direction the original spec lacked, and (b) loop `codex exec` architecture review until codex agreed. Spec delta: 664 → 1178 lines (+669 / -155), 21 → 24 top-level sections (added §Design Language, §Motion & Micro-interactions, §State handling for parsed-content tabs). Resolved Decisions count: 9 → 20. Major UI/UX additions: Archive-room aesthetic with Fraunces (self-hosted variable serif, ~80 KB) + system sans + JetBrains Mono; oklch color tokens with hex `@supports` fallback; six enumerated signature details; motion budget table with reduced-motion zero-out. Major architecture additions: `MessageKind = "boundary"`; `lineOrdinal` vs `messageIndex` split; ParserOutput / StreamMeta separation; PARSERS registry; useParsedSession in-flight coalescing + LRU(5) + cacheEpoch invalidation; lazy `visitedTabs` mounting; corrected Codex field paths (top-level `"type"`, anchor principle for response_item↔event_msg dedup); URL state coordination spelled out; default-tab progression M2=Metadata → M4=Transcript. Codex review converged across 5 rounds (round 1: 7B+8SF+3N → round 2: 5B+6SF+3N → round 3: 2B+6SF+1N → round 4: 2B+3SF+2N → round 5: APPROVED-WITH-NITS, 3 nits resolved). Codex review artifacts at `/tmp/codex-phase-5-arch-review-v{1,2,3}.md`. No code change. Original `bce56c0` is preserved in history but no longer the freeze SHA. The refinement landed BEFORE any implementation chunk dispatched, so the freeze contract was renewed cleanly without invalidating prior reviewer work.

## Review Log

### M4 — three-reviewer rule (converged 2026-05-10 in 2 codex rounds + 1 normal Claude round + 1 backend-protection round; design loop 3 designer rounds + 2 Claude UI/UX rounds + 2 codex rounds)

**Round 1 (initial implementation):**
- **Backend-protection (Claude subagent)**: `approved`. File-by-file verification — no protected-path edits; `git diff --stat HEAD -- apps/backend/ components/ tests/e2e/ Cargo.toml Cargo.lock` empty; `git diff HEAD -- apps/frontend/package.json apps/frontend/bun.lock` empty (slot 2 NOT fired); rg for `jest.fn(`, `child_process`, `process.env.NODE_` returned no matches in app source; `node:path` / `node:url` imports observed only in `apps/frontend/e2e/transcript-perf.spec.ts` (Playwright test runner code, not runtime app code; acceptable per phase scope); contracts diff empty; hex isolation 24 / token count 83 (matches baseline). Required Action: `proceed to normal review`.
- **Normal Claude reviewer**: `approved with nits`. Verified spec literal compliance at file:line for all 7 MessageKinds + truncation banner + parse-warnings banner + boundary chapter-break + Open raw header anchor. Verified token discipline (27 tokens, ZERO new), motion budget (only `animation: opacity` on truncation banner; no transitions on disallowed properties), keep-mounted contract (no `key={activeTab}`-driven remounts), a11y matrix (`tabIndex={0}` on active tabpanel; boundary `<li role="separator" aria-orientation="horizontal">`; Retry/Dismiss accessible names). Confirmed M3b useParsedSession AbortError-coalescing fix is logically correct (originator's `.catch` deletes inFlight first via microtask order; sibling's `.catch` then bumps retryNonce; useEffect re-runs; fresh fetch fires without inFlight collision). All 18 useParsedSession tests still pass; no infinite-loop risk under multi-sibling or rapid-row-change scenarios. Open Question Resolutions: Q1 (useParsedSession scope) accept in M4 with progress-log annotation; Q2 (perf criterion re-interpretation) recommend documenting in progress log rather than amending spec; Q3 (`:has()` browser support) approve as-is (Chromium 105+ / Firefox 121+ / Safari 15.4+); Q4 (boundary regression test adequate) approve; Q5 (inspection.spec ordering with Rescan) approve. Test count discrepancy noted (brief said 44 tests; actual 41 unit + 1 e2e = 42); minor docs alignment.
- **Codex external reviewer (round 1, `m4-impl-review-r1.txt` initial run)**: `needs more evidence`. Codex declined to inspect autonomously without explicit per-command directive; coordinator re-dispatched as round 1b with explicit "execute every command" framing.
- **Codex external reviewer (round 1b, `m4-impl-review-r1.txt` rerun)**: `needs changes` — **5 findings + 2 missing-evidence + 1 spec-amendment recommendation**:
  1. **FALSE POSITIVE** — `TranscriptView.tsx:224` warnings list `key={`${w.lineOrdinal}-${idx}`}` claimed to violate keep-mounted blocking rule. Actually a React `.map()` list-reconciliation key (REQUIRED by React; tied to list-item identity, not `activeTab`).
  2. **FALSE POSITIVE** — `TranscriptView.tsx:465`/`:509` code-span list keys (same misread as #1).
  3. **FALSE POSITIVE** — `TranscriptView.tsx:370` boundary `aria-orientation="horizontal"` claimed missing. Actually present at line 371 (codex's grep returned line 370 but didn't pull surrounding context).
  4. **NOT BLOCKING** — `TranscriptView.css:189` animation note (truncation banner opacity entrance per spec line 715; spec-compliant).
  5. **NOT BLOCKING** — Performance acceptance criterion ambiguity (spec line 1032 strict `< 16 ms` vs rAF 60 Hz floor at 16.67 ms); recommend spec amendment.

  Plus 2 missing-evidence items (transition-contamination from JSDoc comments needed property-level disambiguation; ARIA role + accessible name evidence not captured in initial grep).

**Round 2 (coordinator-applied rebuttals):**
- **Codex external reviewer (round 2)**: `approved`. NO findings, NO missing evidence, NO required changes. Codex re-inspected at line:column and verified all rebuttals: `aria-orientation="horizontal"` at TranscriptView.tsx:371 (next line after role="separator" at :370 — same node); `key={...}` at :224/:465/:509 are `.map()`-driven list-reconciliation keys, not `activeTab`-driven mount keys; `role="status"` on truncation banner at :199; "Retry" accessible name via button text at :140; CSS lines 63-68 are JSDoc PROHIBITED enumeration (not declarations); zero declared transitions on disallowed properties. Codex re-ran `bun run test` (491 pass / 0 fail) + `bunx tsc --noEmit` (clean) in its sandbox to verify before clearing.

**Convergence summary**: 2 codex rounds total (r1b false positives + r2 cleared with rebuttals + line-cited evidence). Codex catch density: 0 GENUINE blocking findings (5 false positives in r1b — all evaluation errors, not real bugs). The M3a r1 false-positive precedent (coordinator-paraphrase error in evidence pack) recurs at M4 r1 in a different mode (codex's grep didn't pull context line; codex's interpretation of `key={...}` rule was over-literal). Both resolved by coordinator-applied rebuttal with verbatim line-cited evidence — same path that resolved M3a r1.

**Phase 5 codex catch density update through M4**: M1a 4 / M1b 2 / M2a 4 / M2b design 8 + impl 2 / M3a impl 5 GENUINE + 1 false positive / M3b impl 0 / M4 design 4 numerical-drift / **M4 impl 0 GENUINE + 5 false positives** = **29 cumulative GENUINE blocking findings** (4 from M4 design loop's drift pairs catch). Pattern note: codex impl reviews are now routinely either zero-finding (M3b, M4 r2) or false-positive-only (M4 r1b, M3a r1) — codex's strength shifts from "catching Claude blind spots" (M1a-M2b) to "executing scripts in sandbox to verify deterministic claims" (M3a r3 `bun -e` runtime check, M4 design r2 `python3 wcag_m4.py` drift surface, M4 impl r2 `bun run test` re-verification).

### M3b — three-reviewer rule (converged 2026-05-09 in 1 codex round + 1 normal Claude round)

**Round 1 (initial implementation):**
- **Backend-protection (Claude subagent)**: `approved`. File-by-file verification — no protected-path edits; `git diff --stat HEAD -- apps/backend/ components/ tests/e2e/ Cargo.toml Cargo.lock` empty; `git diff HEAD -- apps/frontend/package.json apps/frontend/bun.lock` empty (no dependency drift); `rg -n 'jest\.fn\(|require\("child_process"\)|from "child_process"|process\.env\.NODE_|require\("node:|from "node:'` against the 4 new files returned no matches (Bun-first invariant preserved); `git diff HEAD -- apps/frontend/src/lib/contracts.ts components/ui-api-contracts/` empty (no contract drift); hex isolation 24, token count 83 (matches baseline). Required Action: `proceed to normal review`.
- **Normal Claude reviewer**: `approved with nits`. 2 `Suggestion`-level findings (neither blocking):
  1. **Coalesced-sibling epoch race** at `useParsedSession.ts:312-326` — coalesced sibling did NOT apply the epoch guard to the `parsed` resolution before transitioning state. Inline comment claimed `bumpCacheEpoch`'s controller abort makes this safe, but only true while the read loop is still running. Narrow microtask-ordering race: if the underlying `streamRawText` Promise has fully resolved before `bumpCacheEpoch` lands, the originator's `.then` correctly drops the result via `cacheEpoch !== epochAtStart` but the coalesced sibling transitions to `state: "success"` with stale parsed payload. Spec line 467 mandates "Coalesced consumers awaiting the same Promise see the same drop semantics." Severity: Suggestion (race window narrow + bounded; one Rescan/Import behind, cleared on next remount). **Coordinator-applied in-commit fix**: capture `epochAtSubscription = cacheEpoch` at coalescing time, gate `.then` transition on `cacheEpoch === epochAtSubscription`. Tests + typecheck rerun green after fix.
  2. **`retry` updater-function pattern** at `useParsedSession.ts:452-459` — `retry` calls `setRetryNonce` inside a `setState` updater function. React documents updater functions as "must be pure" (no side effects). In StrictMode the updater may run twice; idempotent under React's batching so works in practice. Tests #15/#16 exercise both paths successfully. Severity: Suggestion. **Left as-is** per coordinator decision (idempotent under React batching; coverage intact).

  No `Findings`, no `Missing Evidence`, no `Required Changes`. Notes flagged: plan §6 Q3 trade-off documented but does not extend to the *won fetch* + epoch-stale case described in finding #1 above; recommended adding inline comment near line 320 explicitly noting the race and the chosen behavior — the in-commit fix supersedes this recommendation by removing the race instead of documenting it. Test counts confirmed match plan exactly: 10 + 18 + 4 = 32 new tests; suite at 447/0/1619/29 across 29 files.
- **Codex external reviewer (`codex exec` round 1)**: `approved`. NO findings, NO missing evidence, NO required changes. Verbatim stdout at `working/phase-5/codex-reviews/m3b-impl-review-r1.txt`. Codex itself ran `rg -n` for byte-handling spec landmarks across all 6 changed files + the upstream `rawPreview.{ts,test.ts}` reference + the App.tsx wire-up, then `bun run test src/features/sessions/streamRawText.test.ts src/features/sessions/useParsedSession.test.ts src/App.test.tsx` from `apps/frontend/` to verify — sandbox returned `447 pass / 0 fail / 1619 expect() calls / 29 files` matching coordinator's reported gates. Notes confirmed every load-bearing spec section (lines 396-491, 1007-1025, 1110-1111, 1146-1163) maps cleanly to implementation: `streamRawText` byte semantics (`value.byteLength`), hard-cap equality (`totalBytes === STREAM_RAW_TEXT_BYTE_CAP` when truncated), `subarray(0, room)` cap slicing, `await reader.cancel()` on cap trip, pre-abort short-circuit, listener cleanup in `finally`; `useParsedSession` LRU cap=5, coalescing + controller maps, `bumpCacheEpoch` abort-clear-increment, `_resetForTests`, synchronous initial cache hit via `deriveInitialState`, dual epoch guards, AbortError silent no-op, identity-gated inFlight cleanup, dep array `[storedSessionUid ?? null, tool ?? null, retryNonce]`, retry gating to current `"error"` state; `bumpCacheEpoch()` call-site ordering correct in both `handleRescan` (`App.tsx:447`) and `handleImport` (`App.tsx:487`) success branches with absence verified from error/finally paths; test coverage substantial including cancel-spy parity with rawPreview tests + epoch/coalescing/retry scenarios.

**Convergence summary**: 1 codex round + 1 normal Claude round + 1 backend-protection round (no fix-up rounds — the coordinator's in-commit fix for the coalesced-sibling epoch nit landed BEFORE the impl commit). Codex catch density: **0 BLOCKING findings**. This is the cleanest Phase 5 convergence to date — first chunk where codex returned approval in round 1 with no findings, breaking the M1a–M3a precedent of ≥ 2 codex rounds per chunk. Theory: M3b's spec sections (lines 396-491) were already extensively iterated during Chunk A's 5-round codex architecture review (which specifically caught: useParsedSession keying by rowKey instead of storedSessionUid; tab panels mounting all-at-once would prematurely trigger 5 MB fetch; in-flight fetch coalescing missing; streamRawText totalBytes missing) — by M3b dispatch time the spec was de-risked enough that codex had nothing to flag at impl time. The plan §6 codex-precedent table (covering 19 specific blind-spot patterns from Phase 5 history with each defended by an explicit test) likely also helped land the impl correctly the first time.

**Phase 5 codex catch density update through M3b**: M1a 4 / M1b 2 / M2a 4 / M2b design 8 + impl 2 / M3a impl 5 GENUINE + 1 false positive / **M3b impl 0** = **25 cumulative GENUINE blocking findings** (unchanged from M3a). Pattern note: codex catch density may decline in M4–M6 IF they consume well-iterated spec sections, OR may surge again in M4 (TranscriptView — first visible parsed-data surface, new component family, new motion authorizations).

### M3a — three-reviewer rule (converged 2026-05-09 in 5 codex rounds + 4 normal Claude rounds + 2 backend-protection rounds)

**Round 1 (initial implementation):**
- **Backend-protection (Claude subagent)**: `backend untouched`. Verified via `git diff --stat HEAD` (empty) + `git status --short` (only untracked under `apps/frontend/src/features/sessions/parsers/`). Required Action: `proceed to normal review`.
- **Normal Claude reviewer**: `approved` with 9 NOTE-level observations (no BLOCKING). Verified each of the 6 anticipated codex blind-spot areas: dual-tracker buildSkim correctness (system→user→assistant regression test confirmed), Codex anchor principle (response_item.message SKIPPED silently — verified end-to-end against `tests/fixtures/codex/sample_session.jsonl`), split-content `lineOrdinal` vs `messageIndex` discipline, parser totality (explicit no-throw assertions), `MessageKind = "unknown"` always paired with warnings (grep-confirmed at every emission site in both parsers), registry exhaustiveness mechanism (static + runtime layers).
- **Codex external reviewer (`codex exec` round 1)**: `needs changes` — **1 BLOCKING (FALSE POSITIVE)** caused by coordinator transcription error in evidence pack. Codex flagged `ParsedSession.tool` as a drift from spec's `sourceTool`; investigation revealed the coordinator's paraphrased dispatcher snippet at line 101 of the round-1 prompt wrote `sourceTool: tool` instead of spec-verbatim `tool` (spec lines 284 + 338, confirmed via `grep -n "sourceTool" working/phase-5.md` returning zero matches). Implementation correctly matches the actual spec. Verbatim stdout at `working/phase-5/codex-reviews/m3a-impl-review-r1.txt` (1074 lines). **Lesson learned**: future evidence packs must transcribe load-bearing spec snippets verbatim from the spec, not paraphrase — even one-word paraphrases mislead reviewers. Logged for future coordinator discipline.

**Round 2 (re-run codex with corrected prompt):**
- **Codex external reviewer (`codex exec` round 2)**: `needs changes` — **2 GENUINE BLOCKING findings** (verbatim stdout at `working/phase-5/codex-reviews/m3a-impl-review-r2.txt`, ~3000 lines):
  1. blocking — `apps/frontend/src/features/sessions/parsers/codex.ts response_item.function_call` branch — spec line 773 mandates `text: JSON.stringify(payload.arguments)` literally; implementation passed strings through unchanged (`typeof args === "string" ? args : JSON.stringify(args)`). String args produce different output than spec.
  2. blocking — totality gaps. `claude_code.ts` user + assistant content-array loops silently skipped non-object items (`if (!isObject(item)) continue;`). `codex.ts` accepted malformed recognized payloads without warnings: missing `payload.message` on `event_msg.user_message`/`agent_message`/`agent_reasoning`; missing `payload.command`/`output` on `exec_command`/`exec_command_output`; non-string `payload.timestamp`. Spec line 758: "Parsers are total: failures land in `warnings[]`".

**Round 3 (post fix-up #1, by developer subagent + coordinator null-guard):**
- **Backend-protection (Claude subagent)**: `backend untouched` (subset analysis — fix-up scope was only edits to existing untracked frontend files). Required Action: `proceed to normal review`.
- **Normal Claude reviewer (round 2)**: `approved`. All 5 fix-up changes + the coordinator's null-guard verified at file:line. Confirmed: `JSON.stringify(args)` always for function_call; `non-object item in content array` warning at both Claude content-array loops; missing-field warnings at all 5 codex event_msg variants; non-string payload.timestamp warning; null-guard at top of `case "response_item"` resolves 3 surfaced `tsc --noEmit` errors via early-return-with-unknown-emission. New test count 403 → 412 from fix-up + 1 coordinator-added test = 413.
- **Codex external reviewer (`codex exec` round 3)**: `needs changes` — **2 NEW GENUINE BLOCKING findings** (codex EXECUTED the code in its sandbox via `bun -e` to assert actual runtime values; verbatim stdout at `working/phase-5/codex-reviews/m3a-impl-review-r3.txt`):
  1. blocking — `apps/frontend/src/features/sessions/parsers/codex.ts:366` (case `exec_command`) — spec line 779 mandates `text: JSON.stringify(payload.command)` literally; implementation kept `typeof command === "string" ? command : JSON.stringify(command)` from pre-fix-up. Same bug pattern as round 2's function_call finding — fix-up developer missed extending the rule to exec_command (the assignment brief listed function_call but not exec_command). String commands produce different output than spec.
  2. blocking — `JSON.stringify(undefined) === undefined` runtime-shape bug. When `payload.command`/`output` was missing, the code hit `JSON.stringify(undefined)` which returns `undefined` (NOT a string), violating `Message.text: string` type contract. Round-2 tests asserted only the warning text, missed this runtime shape gap. Codex demonstrated by running `bun -e` and showing `textType: "undefined"` in the output — a bug that would manifest at runtime in production.

**Round 4 (post fix-up #2, by coordinator):**
- **Backend-protection (Claude subagent)**: `backend untouched` (round 2; subset analysis again — fix-up scope was 2 file edits within the same parsers directory).
- **Normal Claude reviewer (round 3)**: `approved`. Verified the exec_command + exec_command_output null-normalization fix (`rawCommand ?? null` then `JSON.stringify(...)`) eliminates both bugs. New regression tests assert `text === "null"` (string) for missing-field paths and `text === '"raw string args"'` (JSON-quoted) for string commands. Test count 413 → 414.
- **Codex external reviewer (`codex exec` round 4)**: `needs changes` — **1 NEW GENUINE BLOCKING finding** (verbatim stdout at `working/phase-5/codex-reviews/m3a-impl-review-r4.txt`):
  1. blocking — `codex.ts case "exec_command_output"` was over-stringified. Spec line 779 reads `text: JSON.stringify(payload.command) / payload.output` — the "/" separator is positional alternation: `exec_command` gets the LEFT (`JSON.stringify(payload.command)`); `exec_command_output` gets the RIGHT (`payload.output` raw, NO JSON.stringify wrapper). Coordinator misread the spec and applied JSON.stringify to both. String outputs now render quoted/escaped instead of raw output text — visual regression in M4 TranscriptView when it renders exec output.

**Round 5 (post fix-up #3, by coordinator):**
- **Normal Claude reviewer (round 4)**: `approved`. Verified the asymmetric fix: `exec_command` keeps `JSON.stringify(rawCommand ?? null)`; `exec_command_output` becomes `typeof rawOutput === "string" ? rawOutput : JSON.stringify(rawOutput ?? null)` — string passthrough per spec, non-string stringify for type safety. Inline JSDoc comment documents the spec line 779 left/right asymmetry to prevent regression. Test updates: existing exec_command_output string-output test changed from `JSON.stringify("...")` assertion to raw passthrough; new non-string test added. Test count 414 → 415.
- **Codex external reviewer (`codex exec` round 5)**: `approved`. Codex itself ran `bun run test` (415 pass / 0 fail / 1477 expects across 27 files) + `bunx tsc --noEmit` (clean) inside its sandbox to verify before clearing. No findings, no missing evidence, no required changes. Verbatim stdout at `working/phase-5/codex-reviews/m3a-impl-review-r5.txt`.

**Convergence summary**: 5 codex rounds (longest in Phase 5; r1 false positive + 4 genuine BLOCKING across r2–r4 + r5 approved). 4 normal Claude rounds (r1 from initial impl; r2–r4 from each fix-up). 2 backend-protection rounds (r1 from initial impl; r2 from first fix-up; subset analysis sufficed for subsequent rounds). 3 fix-up rounds (1 developer + 2 coordinator). Total Phase 5 codex blocking catches through M3a: M1a 4 / M1b 2 / M2a 4 / M2b design 8 + impl 2 / **M3a impl 5** = **25 cumulative GENUINE blocking findings** the Claude reviewers consistently missed. Plus 1 false-positive in M3a r1 caused by coordinator paraphrase error — first false positive in Phase 5 codex history, attributable to evidence-pack discipline failure not codex error.

**Phase 5 codex catch density update through M3a**: 25 cumulative blocking catches across 6 chunks. Pattern fully reconfirmed and should be expected to continue in M3b–M6.

### M2b — three-reviewer rule (converged 2026-05-08 in 2 rounds)

**Round 1 (initial implementation)**:
- **Backend-protection (Claude subagent)**: `backend untouched`. File-by-file verification of all 11 modified frontend files + 9 new frontend files + 4 design artifact files + 5 codex review artifacts (all under `apps/frontend/**` or `working/phase-5/**`). Drawer.tsx + SessionDetail.tsx diffs verified as pure JSDoc-header additions (no logic edits, no deletions) matching Resolved Decision #6. Required Action: `proceed to normal review`.
- **Normal Claude reviewer**: `approved with nits`. Reviewer confirmed all 9 codex-precedent blind-spot checks pass cleanly: keep-mounted contract enforcement (RawTab.tsx:115 deps `[sessionUid, attempt]` — no tab state); cross-fade-IN-only via inline `style.animation` toggle (NO `key={activeTab}` on panel content); WCAG 3.97:1 light-warn pitfall preempted (RawTab.css:76-79 uses `--color-ink-muted` not `--color-warn`); token discipline holds (32 distinct vars consumed, all defined; hex 24 / tokens 83 unchanged); motion budget exactly 3 authorized animations (indicator slide + tab fade-in + page-turn fade — matches spec rows 1, 2, 7). Two NITs flagged: (a) "View raw" vs "Open raw" anchor wording — implementation preserves Phase 4 byte-equivalence; design.md says "Open raw"; both readings of conflicting design instructions; (b) `.tab-placeholder` `padding: var(--space-8) var(--space-6)` instead of design.md item 46's `var(--space-8) 0` — visual deviation but design intent preserved. Coordinator left both as-is; revisit in M6 doc sweep.
- **Codex external reviewer**: `needs changes` — **2 BLOCKING + 1 NIT** (verbatim stdout at `working/phase-5/codex-reviews/m2b-impl-review-r1.txt`, 9600 lines):
  1. blocking — `apps/frontend/src/components/Tabs.tsx:99` — `scaleX(${width}px)` is INVALID CSS. `scaleX()` takes a UNITLESS number; the `px` suffix produces invalid CSS the browser rejects. With `.indicator` defaulting to `scaleX(0)` from Tabs.css:94, a rejected inline transform leaves the indicator INVISIBLE in production. Same defect class as the design-phase round-1 catch (invisible indicator), reintroduced in implementation through a different bug. Signature detail #5 (the 1 px ink-stroke active-tab indicator) doesn't render.
  2. blocking — `apps/frontend/src/features/sessions/SessionView.css:240-243` — `.session-conflict-badge` uses `--color-error` recipe in all 3 `color-mix()` declarations. Spec line 622 + design.md checklist 24 + colors.md H09 mandate `--color-warn` recipe. Contrast still passes (error 6.60:1 light / 6.91:1 dark vs warn 5.43:1 / 7.74:1) but the SEMANTIC color is wrong — error ≠ warning.
  3. nit — `apps/frontend/src/features/sessions/SessionsTable.tsx:20-21, 42-48, 83-88, 174-178` — comments still describe drawer / vestigial "Open detail" button as reachable, contradicting M2b collapse.

  Codex caught both blockers + the comment-cleanup NIT the normal Claude reviewer missed — Phase 4 / M1a / M1b / M2a precedent confirmed yet again. Notably, the `scaleX(...px)` bug is a class of catches Claude reviewers consistently miss: Claude validated the design conformance ("indicator base 1×1px, transform-only animation") but didn't trace the JS-to-CSS string actually generated.

**Round 2 (post fix-up)**:
- **Backend-protection (Claude subagent)**: `backend untouched` (subset analysis — fix-up touched 4 files, all under `apps/frontend/`; SessionsTable.tsx is the only NEW round-2 touch and is comment-only; no protected-path edits possible).
- **Normal Claude reviewer**: `approved` — all 3 round-1 codex findings verified at file:line. (1) Tabs.tsx:104 `scaleX(${width})` unitless + regression test at Tabs.test.tsx:226-241 with both negative `not.toMatch(/scaleX\([\d.]+px\)/)` and positive `toMatch(/scaleX\([\d.]+\)/)` regex assertions. (2) SessionView.css:244-248 all 3 `color-mix()` declarations now use `--color-warn`; comment cites spec line 622 + design checklist 24 + colors.md H09. (3) SessionsTable.tsx 4 comment blocks rewritten — all references to drawer/"Open detail" framed as historical-and-removed. Locally re-ran `bun run test` → 316 pass / 0 fail (was 315 in round 1; +1 is the new regression test).
- **Codex external reviewer**: `approved` (verbatim stdout at `working/phase-5/codex-reviews/m2b-impl-review-r2.txt`, ~80 lines tail of trace). All 3 round-1 findings verified at file:line. Local check passed: `bun test src/components/Tabs.test.tsx` → 11 pass / 0 fail.

**Convergence summary**: 2 rounds total. Codex catch density: **2 blocking + 1 NIT in round 1**, **0 in round 2**. Pattern matches Phase 4 + M1a + M1b + M2a precedent (codex catches Claude blind spots; ≥ 2 codex rounds per chunk). M2b meets the Approval Rule per `coordinator-prompt.md` §Approval Rule For A Chunk.

**Phase 5 codex catch density update through M2b**: M1a 4 blockers / M1b 2 / M2a 4 (3 round-1 + 1 round-2) / M2b design 8 (5 round-1 + 1 round-2 + 1 round-3 + 1 round-3-NIT) / M2b impl 2 (+ 1 NIT). **Total Phase 5 codex blocking catches: 20.** The chunk-by-chunk codex-Claude blind-spot precedent is fully reconfirmed and should be expected to continue in M3-M6.

### M2a — three-reviewer rule (converged 2026-04-29 in 3 rounds)

**Round 1**: backend-protection `backend untouched`; normal Claude `approved with nits` (4 cosmetic nits — token-count comment / unicode-range / SVG noise overlay comment / dual format() hint); codex `needs changes` with **3 BLOCKING WCAG findings** computed numerically via oklch → linear sRGB → relative luminance pipeline:
1. `.toast.info` border (70% mix) at 2.79:1 vs `--color-surface-raised` (FAILS 3:1 SC 1.4.11). The designer's preemptive 65→70% mix didn't go far enough — the underlying `--color-accent` at L=60 was too light.
2. Light `--color-accent` at L=60 used as normal-size text at `.action-bar-clear` (3.93:1) and `.toast-details summary` (3.71:1) — both FAIL 4.5:1 AA normal text.
3. `--color-border-strong` at 2.53:1 light / 2.13:1 dark — FAILS 3:1 SC 1.4.11 control boundary.

Verbatim stdout: `working/phase-5/codex-reviews/m2a-round-1.txt` (5501 lines).

**Round 2 (post fix-up #1)**: backend-protection `backend untouched` (subset analysis); normal Claude `approved with nits` (4 polish carryovers + agreed `.title-cell-refresh` warn-as-text concern was acceptable follow-up because the surface was warmed in M1b not M2a); codex `needs changes` with **1 NEW BLOCKING finding**: `.title-cell-refresh` uses `--color-warn` as `--text-xs` text and now measures 4.21:1 vs `--color-surface` light, 3.79:1 on selected row tint — FAILS 4.5:1 AA normal text. Codex disagreed with normal Claude: the failure was introduced by M2a's `--color-warn` oklch retint (M1b passed at ~5.3:1 against Phase 4 hex `#b86b07`). Per coordinator-prompt: "the external reviewer's stdout is authoritative as-is" + "If the external reviewer raises a concrete blocking finding, treat it exactly like a Claude reviewer's blocking finding."

Verbatim stdout: `working/phase-5/codex-reviews/m2a-round-2.txt` (3947 lines).

**Round 3 (post fix-up #2)**: backend-protection `backend untouched` (subset analysis — fix-up #2 was a one-property CSS change + 3 doc updates); normal Claude's round-2 approval carries forward (fix-up #2 only added improvement to the surface that normal Claude already considered follow-up); codex `approved with nits` (only line-number drift in docs and slight ratio precision differences; no required changes). Codex independently verified all post-fix-up ratios:
- `.title-cell-refresh` muted-italic vs surface light: **7.04:1** (was 4.21:1)
- vs selected (8%): **6.32:1** (was 3.79:1)
- vs selected+hover (12%): **5.98:1**
- vs deep-link pulse peak (22%): **5.19:1**

Dark `--color-border-strong` at exactly 3.0009:1 accepted as documented risk per `wcag.md` Risk Callouts (L=50 mitigation pre-named for future round if needed).

Verbatim stdout: `working/phase-5/codex-reviews/m2a-round-3.txt` (3475 lines).

**Convergence summary**: 3 rounds total. Codex catch density: **3 + 1 = 4 blocking findings**. Maintains the M1a (4 round-1 catches) + M1b (2 round-1 catches) + Phase 4 (≥ 1 per chunk) precedent of codex catching numerical WCAG gaps the Claude reviewers miss. The disagreement between codex and normal Claude on round 2's `.title-cell-refresh` regression was resolved by treating codex as authoritative per the coordinator-prompt — the spec rule held cleanly without escalation. Final M2a evidence pack passes the Approval Rule.

### M1b — three-reviewer rule (converged 2026-04-28 in 2 rounds)

**Round 1 (initial implementation):**
- **Backend-protection (Claude subagent)**: `backend untouched`. File-by-file verification of all 14 modified frontend files + 1 new file (`SessionsView.test.tsx`) against the protected-path globs. Required Action: `proceed to normal review`.
- **Normal Claude reviewer**: `approved with nits`. 3 minor nits — design checklist item #17 literal "via `hidden` attribute" deviation (visible behavior identical), `(max-width: 1100px)` framing inversion (CSS-equivalent), `useImperativeHandle` empty-deps formulation could inline. None blocking.
- **Codex external reviewer**: `needs changes` — **2 BLOCKING findings** (verbatim stdout at `working/phase-5/codex-reviews/m1b-round-1.txt`, 13800 lines):
  1. blocking — `apps/frontend/src/features/sessions/SessionsView.tsx:224` — drawer detail lookup used `filteredRows.find(...)`. When user filters out the selected row but the right pane stays in `ready-placeholder` (because `mergedRows` still contains the row per `App.tsx:798`), clicking "Open detail" opens the drawer with no `<SessionDetail>` body — the lookup returns undefined. Real bug: violates the M1b vestigial button contract.
  2. blocking — `apps/frontend/src/features/sessions/SessionFilters.tsx:225` — visible "Filters" summary text had `aria-hidden="true"`. Below 1100 px, the `<details>` disclosure widget had no accessible name (or only "N active" when the count was nonzero). Accessibility violation.
  
  Plus 1 nit on e2e step 9(a) URL assertion weaker than its comment claimed.

**Round 2 (post fix-up):**
- **Backend-protection** (by subset analysis — fix-up file list strict subset of round-1 frontend files; no new files; no protected-path edits possible). Coordinator independently verified via `git status --short`.
- **Normal Claude reviewer**: `approved` — no findings, no missing evidence, no required changes. Both round-1 codex blockers verified resolved with file:line citations.
- **Codex external reviewer**: `approved` (verbatim stdout at `working/phase-5/codex-reviews/m1b-round-2.txt`, 4298 lines). All round-1 findings verified resolved at:
  1. Fix #1: `SessionsView.tsx:236` now `mergedRows.find((r) => r.rowKey === detailRowKey) ?? null`; new prop `mergedRows` threaded from App.tsx; new regression test at `SessionsView.test.tsx:146-237` exercises the filtered-OUT case explicitly.
  2. Fix #2: `SessionFilters.tsx:224` is now plain `<span>Filters</span>` with no `aria-hidden`; count chip `aria-hidden`-free as well so the summary's accessible name resolves to "Filters" (count 0) or "Filters N active" (count>0). Two new tests at `SessionFilters.test.tsx:451+` and `:475+` cover both paths.
  3. Fix #3: e2e step 9(a) comment now accurately describes what's asserted (URL re-assertion deferred with rationale).
  
  Codex spent 13800 + 4298 = 18098 lines of stdout on M1b. **No optional nits remain unaddressed** in round 2 — cleaner than M1a's "approved with nits".

**Convergence summary**: 2 rounds total. Codex catch density: **2 blocking findings in round 1**, **0 blocking findings in round 2**. Pattern matches Phase 4 + M1a precedent (codex catches Claude blind spots; ≥ 2 codex rounds per chunk). M1b meets the Approval Rule per `coordinator-prompt.md` §Approval Rule For A Chunk.

### M1a — three-reviewer rule (converged 2026-04-28 in 2 rounds)

**Round 1 (initial implementation):**
- **Backend-protection (Claude subagent)**: `backend untouched`. File-by-file verification of all 12 changed files (7 modified + 5 new) against the protected-path globs. No edits under `apps/backend/`, `components/`, `tests/e2e/` (the Rust e2e), or root `Cargo.{toml,lock}`. ts-binding freshness gate `ts_bindings_match_checked_in_files` passing confirms no Rust contract or regenerated TS binding drift. Required Action: `proceed to normal review`.
- **Normal Claude reviewer**: `approved with nits`. Walked the design.md §8 implementation acceptance checklist (33/33 items), confirmed Phase 4 click-time intersection regressions preserved, verified hex/token counts. Non-blocking nits: `void buildUrl;` dead-scaffold in App.tsx; legacy `addListener` matchMedia fallback (defensive); design.md §3.5 `.session-pane__header` is M2 material so positioning is functionally compliant; `docs/features/inspection-surface.md` not yet updated (intentional per spec lines 950–966 — 8-doc sweep deferred to M6).
- **Codex external reviewer (`codex exec`)**: `needs changes` — **4 BLOCKING findings** (verbatim stdout at `working/phase-5/codex-reviews/m1a-round-1.txt`, 6728 lines of trace). Findings:
  1. blocking — `apps/frontend/src/features/sessions/SessionView.css:136` — `.back-to-list` always `display: none` with no `@media` override; narrow "← Back to list" button rendered but invisible.
  2. blocking — `apps/frontend/src/App.tsx:701` — Esc handler missing positive-scope rule per spec line 567 (Esc fires only when focus is in session pane OR on a selected row).
  3. blocking — `apps/frontend/src/App.tsx:685` — `[contenteditable="true"]` selector too narrow; should be `[contenteditable]` per spec line 567 to cover boolean / empty-string / "plaintext-only" forms.
  4. blocking — `apps/frontend/e2e/inspection.spec.ts:469` — e2e step 12 asserts only stable `aria-current` post-pulse; transient `data-deep-link="true"` attribute (the actual pulse evidence) was not asserted in the e2e.
  
  Codex caught all four findings the two Claude reviewers missed — Phase 4 precedent confirmed (every Phase 4 chunk had ≥ 1 codex catch). My own dispatch brief was incomplete relative to spec line 567 on the Esc positive-scope rule; codex caught the omission.

**Round 2 (post fix-up):**
- **Backend-protection (Claude subagent)**: `backend untouched`. Independent re-verification of post-fixup file list against protected-paths; no new files in fixup, all modifications still under `apps/frontend/`. Required Action: `proceed to normal review`.
- **Normal Claude reviewer**: `approved` — no findings, no missing evidence, no required changes. All 4 round-1 codex blockers verified resolved with file:line evidence.
- **Codex external reviewer (`codex exec`)**: `approved with nits` (verbatim stdout at `working/phase-5/codex-reviews/m1a-round-2.txt`, 1459 lines of trace). All 4 round-1 blockers verified resolved with file:line citations:
  1. Fix #1 confirmed at `apps/frontend/src/styles/global.css:131-133` (the `@media (max-width: 899.98px)` block now contains `main.split-pane[data-narrow-mode="session"] .back-to-list { display: inline-block; }`); CSS smoke test at `SessionView.test.tsx:196-220` validates the override exists.
  2. Fix #2 confirmed at `App.tsx:716-729` (`isInScope` helper) + `:731-742` (handler sequences negative scope → positive scope → clear); 3 new tests at `App.test.tsx:2951+`, `:2998+`, `:3035+`.
  3. Fix #3 confirmed at `App.tsx:700-703` (selector now `[contenteditable]`); regression test at `App.test.tsx:3089-3146` injects `<div contenteditable>` (empty-string/boolean form) inside `article.session-pane` to prove the selector now catches it.
  4. Fix #4 confirmed at `apps/frontend/e2e/inspection.spec.ts:497-499` (transient `data-deep-link="true"` with 1.5 s timeout) + `:503-505` (post-pulse removal with 3 s timeout); coordinator's Chromium e2e re-run passed.
  
  Optional nits remaining (explicitly non-blocking; the developer skipped them per dispatch-brief discretion): `void buildUrl;` dead scaffold at `App.tsx:764-767`; matchMedia legacy `addListener` fallback at `App.tsx:810-824`; `docs/features/inspection-surface.md` interim stub. None required for approval.

**Convergence summary**: 2 rounds total. Codex catch density: **4 blocking findings in round 1**, **0 blocking findings in round 2**. Codex spent 7726 + 1459 = 8187 lines of total trace on M1a; the Phase 5 codex-review artifacts directory is at `working/phase-5/codex-reviews/`. M1a meets the Approval Rule per `coordinator-prompt.md` §Approval Rule For A Chunk: UI/UX reviewer `approved with nits` (round 2) on the design artifact; backend-protection `backend untouched` (round 2); normal Claude `approved` (round 2); external `approved with nits` (round 2); no unresolved blocking findings; implementation matches spec + design artifact; required_verification all green; docs deferred to M6 per spec; this log entry written.

## External Reviewer Availability Log

- 2026-05-10: `codex exec` invoked **5 times** during M4 — 2 design rounds (round 1: needs changes with 4 numerical drift pairs in colors.md surfaced via `python3 wcag_m4.py` execution; round 2: approved with single wording nit, codex confirmed zero drift via fresh script execution) + 3 implementation rounds (round 1a: needs more evidence, codex declined autonomous inspection; round 1b: needs changes with 5 findings — 4 false positives + 1 missing-evidence, recurring the M3a r1 evidence-pack false-positive pattern in a different mode; round 2: approved after coordinator-applied rebuttals + line-cited verbatim evidence, codex re-ran `bun run test` 491/0/1729 + `bunx tsc --noEmit` clean in its sandbox). Verbatim stdouts at `working/phase-5/codex-reviews/m4-{design-review-r2,r3,impl-review-r1,r2}.txt` + 5 prompt artifacts. **Phase 5 catch tally**: M1a 4 / M1b 2 / M2a 4 / M2b design 8 + impl 2 / M3a impl 5 GENUINE + 1 false positive / M3b impl 0 / **M4 design 4 numerical-drift / M4 impl 0 GENUINE + 5 false positives** = 29 cumulative GENUINE blocking findings. Pattern note: codex's value mode shifts in late Phase 5 from "catching Claude design blind spots" (M1a-M2b) to "executing reproducible scripts in sandbox to verify deterministic claims" (M3a r3 `bun -e`, M4 design r2 `python3 wcag_m4.py`, M4 impl r2 `bun run test`). False-positive rate ~ 50% in M4 impl r1b; resolved by coordinator-applied verbatim rebuttals (same precedent path as M3a r1). `codex-cli` unchanged at `0.129.0`; `claude` unchanged at `2.1.132`; `bun` unchanged at `1.3.13`.

- 2026-05-09: `codex exec` invoked **1 time** during M3b impl review (round 1 = APPROVED with no findings; codex itself ran `bun run test` 447/0/1619 + `bunx tsc --noEmit` clean inside its sandbox to verify before clearing). Verbatim stdout stored at `working/phase-5/codex-reviews/m3b-impl-review-r1.txt`. **Phase 5 catch tally**: M1a 4 / M1b 2 / M2a 4 / M2b design 8 + impl 2 / M3a impl 5 GENUINE + 1 false positive / **M3b impl 0** = 25 cumulative GENUINE blocking findings (unchanged from M3a; M3b is the first Phase 5 chunk where codex landed a clean round-1 approval). The 1-round convergence is unprecedented in Phase 5 — likely attributable to (a) Chunk A's 5-round codex architecture review having already iterated this spec surface heavily; (b) the plan §6 codex-precedent table making 19 specific blind-spot patterns explicit obligations the developer had to defend against. `codex-cli` version unchanged at `0.129.0` since M3a; `claude` unchanged at `2.1.132`; `bun` unchanged at `1.3.13`.

- 2026-05-09: `codex exec` re-verified at `codex-cli 0.129.0` (bumped from 0.128.0 between M2b and M3a). `claude` unchanged at `2.1.132`. `bun` unchanged at `1.3.13`. `codex exec` invoked **5 times** during M3a impl review (round 1 = 1 BLOCKING [false positive — coordinator transcription error]; round 2 = 2 BLOCKING [function_call spec-literal + 5 totality gaps]; round 3 = 2 BLOCKING [exec_command spec-literal + JSON.stringify(undefined) runtime-shape bug, codex executed `bun -e` to demonstrate]; round 4 = 1 BLOCKING [spec line 779 asymmetry — exec_command_output is raw passthrough, not stringified]; round 5 = APPROVED, codex itself ran `bun run test` + `bunx tsc --noEmit` in its sandbox to verify). Verbatim stdouts stored at `working/phase-5/codex-reviews/m3a-impl-review-r{1..5}.txt`. **Phase 5 catch tally**: M1a 4 / M1b 2 / M2a 4 / M2b design 8 + impl 2 / **M3a impl 5 GENUINE + 1 false positive** = 25 cumulative GENUINE blocking findings (with 1 false positive attributable to coordinator evidence-pack paraphrase error, not codex error). Codex blind-spot precedent fully reconfirmed; future evidence packs must transcribe load-bearing spec snippets verbatim from the spec to prevent paraphrase-induced false positives.

- 2026-05-08: `codex exec` re-verified at `codex-cli 0.128.0` (bumped from 0.125.0). `claude` re-verified at `2.1.132` (bumped from 2.1.119). `bun` unchanged at `1.3.13`. `codex exec` invoked **6 times** during M2b: 4 design-review rounds (round 1 = 5 BLOCKING; round 2 = 1 partial + 1 BLOCKING; round 3 = 1 BLOCKING + 1 NIT; round 4 = APPROVED) + 2 implementation-review rounds (round 1 = 2 BLOCKING + 1 NIT; round 2 = APPROVED). Verbatim stdouts stored at `working/phase-5/codex-reviews/m2b-{design-review,design-review-r2,design-review-r3,design-review-r4,impl-review-r1,impl-review-r2}.txt`. Maintains and extends the Phase 5 codex-catches-Claude-blind-spots precedent: M1a 4 / M1b 2 / M2a 4 / **M2b design 8 + M2b impl 2** = 20 cumulative codex blocking findings across Phase 5 chunks.

- 2026-04-27: `codex exec` confirmed available at `codex-cli 0.125.0`. No invocations yet at session start.
- 2026-04-28: `codex exec` invoked 2 times during M1b three-reviewer rule (round 1 + round 2). Round 1 caught 2 blocking findings the normal Claude reviewer missed (drawer lookup using `filteredRows` instead of `mergedRows`; `aria-hidden="true"` on visible Filters summary breaking `<details>` accessible-name). Round 2 returned `approved` with all fixes verified via file:line citations. Verbatim stdout stored at `working/phase-5/codex-reviews/m1b-round-{1,2}.txt`. Maintains Phase 4 + M1a precedent of codex catching ≥ 1 legitimate blocking finding per chunk.

- 2026-04-28: `codex exec` invoked 2 times during M1a three-reviewer rule (round 1 + round 2). Round 1 caught 4 blocking findings the two Claude reviewers missed (back-to-list invisible / Esc positive-scope missing / contenteditable selector too narrow / e2e missing pulse assertion). Round 2 returned `approved with nits` with all 4 fix-up resolutions verified via file:line citations. Verbatim stdout stored at `working/phase-5/codex-reviews/m1a-round-{1,2}.txt`. Maintains Phase 4 + Chunk A precedent of codex catching ≥ 1 legitimate blocking finding per chunk.

- 2026-04-28: `codex exec` invoked 5 times during Chunk A revision (`/frontend-design` flow). Round 1 (7B+8SF+3N) → Round 2 (5B+6SF+3N) → Round 3 (2B+6SF+1N) → Round 4 (2B+3SF+2N) → Round 5 (APPROVED-WITH-NITS, all nits resolved). All five reviews scoped to architecture only (aesthetics excluded by prompt). Codex caught: `useParsedSession` keying by rowKey instead of storedSessionUid (would break for source-only rows); tab panels mounting all-at-once (would prematurely trigger 5 MB fetch); Codex field paths typo (`record_type` should be `type`); `response_item.message` ↔ `event_msg.user_message` duplicate; in-flight fetch coalescing missing; `boundary` MessageKind in TranscriptView missing; `streamRawText` totalBytes missing; buildSkim accumulator state confusion; many cross-section consistency drift items. Round 4 + Round 5 hit network-related hangs requiring re-dispatch. The convergence pattern (each round caught fewer issues + smaller categories) is itself evidence that the spec is in good shape.

## Protected-Path Exception Log

`none` — Phase 5 is frontend-only with no contracts-README scope (Phase 4 M6 already resolved the `source_key` paragraph rewrite). Any unanticipated protected-path need requires a fresh human exception per the coordinator-prompt rules.

## Open Risks / Open Questions

- **Codex catches Claude blind spots (precedent confirmed across every Phase 4 chunk)** — Phase 4 saw 8+ legitimate blocking findings from Codex that both Claude reviewers missed across M1–M6 (B: badge AA + header overclaim; C: sandbox-bound e2e missing-evidence; D: empty-state branch + scan-errors Retry + 3 missing tests; E1: Playwright DoD bullet 3 gap + dist hex bleed from compiled rgba; E2: network-failure test gap; F: import-error Retry stale closure + toast-queue render claim wrong + stale comment; G: toast border < 3:1 + cascade order inverted + missing 4-variant assertion + cascade docs + WCAG token + test counts). The three-reviewer rule remains non-skippable for Phase 5; expect ≥ 2 Codex rounds per chunk.
- **Long-transcript render performance (M4 risk)** — `@tanstack/react-virtual` (escape-hatch slot 2) may need to fire on a documented Chromium reproducer if M4's 5k-message synthetic-fixture measurement exceeds 16 ms per frame. If slot 2 fires, the dep budget reaches 2/2 — Phase 6+ has no further escape-hatch slots without spec amendment.
- **Per-tool message format drift** — Tool authors change shape between versions. Mitigation: `MessageKind = "unknown"` fallback + `warnings[]` stream + Raw tab as verifiability hatch. Same risk + mitigation pattern as the Rust adapters in `components/collector-runtime/src/adapters/`.
- **5 MB full-document cap may be too low** — PRD doesn't specify a corpus ceiling. Truncation banner + Raw tab cover larger sessions in v1; lift in a future configuration phase if real corpora exceed 5 MB.
- **Click-time intersection regression** — Phase 4's `App.test.tsx` regression tests (M5 cross-page bulk-select; pagination-cross-page click-time intersection) MUST still pass after the M1 layout move. Selection-ownership location (App.tsx vs SessionsView.tsx) decided by M1 planner.
- **`replaceState` vs `pushState` UX regret risk** — `replaceState` doesn't enable browser-Back navigation between sessions. Mitigation: revisit if user feedback asks; one-line History API change.
- **Drawer retirement breaks user muscle memory** — M1 ships layout AND vestigial "Open detail" drawer link; M2 removes both. Users discover the new shape via the persistent right-pane.
- **`focus-trap-react` orphan-install after M6** — keep installed per planner recommendation; cost is negligible (35 KB JS + 2 transitive deps); future modal needs may revive. Documented in `docs/dependency-rules.md` at M6.
- **Open Phase 5 questions surfaced in spec §Open Considerations**: selection ownership location (M1 planner picks); default active tab on first selection (default Skim; M5 may shift); long-corpus measurement methodology (M4 picks); whether to eventually uninstall `focus-trap-react` (revisit later).

## Next Recommended Task

- **M5 dispatch after M4 log commit lands**: M5 ships `SkimView` — the four-block-kind summary view that consumes the same `useParsedSession` data M4's TranscriptView consumes. Files: `apps/frontend/src/features/sessions/SkimView.{tsx,test.tsx,css}`. Per spec lines 679-697 §Skim tab: `user_turn` blocks render the user message inline + a collapsible disabled-placeholder ("Agent reaction" disclosure with future-LLM-summary copy stubbed) + "Expand to raw messages" affordance reusing TranscriptView scoped to the block's messageIndex range; `boundary` blocks render the chapter-break treatment **byte-equivalent to M4's TranscriptView boundary** (signature detail #1 reusability test); `agent_only` and `oversized_user_message` blocks collapsed by default; never silently summarized. Spec scope: §Skim tab (lines 679-697) + §Per-tool buildSkim algorithm (lines 793-810 — already implemented in M3a) + Milestone 5 DoD (lines 1042-1059). UI/UX gate **needs UI/UX work** — SkimView is the second visible component family of Phase 5 (after M4 TranscriptView); inherits the Archive-room aesthetic + 27-token consumption set + boundary chapter-break recipe; new motion authorizations limited to `<details>` block-size disclosure. Plan dispatching the `frontend-design:frontend-design` skill at M5 planner time. Bun-first invariant: no `jest.fn()`; no `child_process`. Review trio expected to follow Phase 5 cadence (≥ 2 codex rounds per chunk likely). After M5 closes, M6 (cleanup, retire drawer, doc sweep, WCAG, log) is the final chunk.

- **Open follow-ups deferred from M4** (non-blocking; pick up in M6 polish or earlier if convenient):
  - **Spec amendment for perf criterion** at `working/phase-5.md:1032`: the strict `< 16 ms per frame` threshold is physically inconsistent with rAF 60 Hz cadence (16.67 ms floor on healthy Chromium). Coordinator-recorded operational interpretation in this progress log is "0 dropped frames (>25 ms ≈ 1.5× 60 Hz budget)". M6 doc sweep should add a one-line clarifying comment to spec line 1032 making this explicit (spec is frozen at `05467ad`; this becomes a docs PR at M6 close).
  - **"Open raw" anchor for source-only rows**: spec line 626 alternate copy for `storedSessionUid === null` not implemented at M4 (header anchor hidden when null per design.md §6.1.1). Acceptable as documented partial scope; revisit if user feedback or M5/M6 review surfaces a need.
  - **`useParsedSession.ts` AbortError-coalescing fix retroactive note**: M4 patched the M3b in-flight coalescing branch. Document M3b precedent in `working/phase-5.md:467` or its inline JSDoc as a follow-up at M6 doc sweep so future readers can locate the original implementation when reviewing the M4 patch.
  - **`bumpCacheEpoch` doc-comment in `App.tsx`**: M3b's one-line import comment could expand at M6 to a 3-line block citing spec line 466.
  - **Test count discrepancy** (Claude UI/UX reviewer NIT): M4 brief stated 44 tests; actual is 41 unit + 1 e2e = 42. Cosmetic; aligned in this progress log.
  - **`grep -q -- "${tok}:"` token cross-check** (codex r1b note 283): the M4 plan §9 verification command should use `--` to handle leading-dash token names. M6 doc sweep.

- **Open follow-ups deferred from M3b** (non-blocking; pick up in M6 polish or earlier if convenient):
  - **`retry` updater-function pattern** at `useParsedSession.ts:452-459`: normal Claude reviewer's round-1 Suggestion. Idempotent under React batching; tests #15/#16 cover both paths. Cleaner pattern would be a `stateRef` + `useRef`. Worth revisiting if a future maintainer trips on the React purity-of-updaters guidance.
  - **`useParsedSession` worst-case memory ceiling**: spec line 491 documents ~37.5 MB worst-case; no observability hook today. M6 could add a `process.env.NODE_ENV === "development"`-gated console warn if cache size approaches the cap.

(Historical M3b "Next Recommended Task" — replaced above. Original entry removed for clarity.)

(Historical pre-M4 entry follows.)

- **(replaced) M4 dispatch after M3b log commit lands**: M4 ships `TranscriptView` — the first visible UI surface that consumes M3b's `useParsedSession`. Files: `apps/frontend/src/features/sessions/TranscriptView.{tsx,css,test.tsx}` (chronological message list with per-kind rendering for user / assistant / tool_use / tool_result / system / boundary / unknown; absolute + relative timestamps via `relativeTimeFrom`; monospace for code-fenced segments; collapsible long tool_result body >2 KB; truncation banner when `parsed.truncated`; parse-warnings dismissible banner; boundary case renders chapter-break treatment with full-width hairline + small-caps Fraunces label matching Skim's signature detail #1). Wire into `SessionView`; **Transcript tab now functional** (Skim still placeholder until M5). Shift `DEFAULT_TAB_ON_SELECTION` constant in `SessionView.tsx` from `"metadata"` to `"transcript"` per Resolved Decision #11; update corresponding `SessionView.test.tsx` assertion (one-line code change + one-line test change). Long-corpus measurement step: synthetic 5k-message fixture + Playwright frame-timing capture or manual perf measurement on real Chromium — if > 16 ms per frame for scroll, escape-hatch slot 2 fires (`@tanstack/react-virtual` lands per spec policy). Spec scope: §Transcript tab (lines 699-717) + §Per-tool Message Parsers detail (lines 731-810) + Milestone 4 DoD (lines 1027-1040). UI/UX gate **needs UI/UX work** — TranscriptView is a brand-new visible component family with new motion authorizations (collapsible long tool_result interaction; warning-banner entrance; per-kind tinting via `color-mix`); plan dispatching the `frontend-design:frontend-design` skill at M4 planner time. Bun-first invariant: no `jest.fn()`; no `child_process`. Review trio expected to follow Phase 5 cadence (≥ 2 codex rounds per chunk likely — TranscriptView introduces new visible surface = WCAG / motion-budget / token-discipline territory where codex consistently outperforms Claude reviewers per the M2b design loop precedent of 8 BLOCKING findings).

- **Open follow-ups deferred from M3b** (non-blocking; pick up in M6 polish or earlier if convenient):
  - **`retry` updater-function pattern** at `useParsedSession.ts:452-459`: normal Claude reviewer's round-1 Suggestion. Idempotent under React batching; tests #15/#16 cover both paths. Cleaner pattern would be a `stateRef` + `useRef`. Worth revisiting if a future maintainer trips on the React purity-of-updaters guidance.
  - **`bumpCacheEpoch` doc-comment in `App.tsx`**: current import comment is one line; M6 doc sweep could expand to a 3-line block citing spec line 466 ("Successful Import completion ... Rescan click") for clarity.
  - **`useParsedSession` worst-case memory ceiling**: spec line 491 documents ~37.5 MB worst-case; no observability hook today. M6 could add a `process.env.NODE_ENV === "development"`-gated console warn if cache size approaches the cap.

- **Open follow-ups deferred from M3a** (non-blocking; pick up in M6 polish or earlier if convenient):
  - **Coordinator evidence-pack discipline**: future codex prompts must transcribe load-bearing spec snippets verbatim from the spec, not paraphrase. M3a r1 was a false positive caused by paraphrasing `tool` as `sourceTool` in the dispatcher snippet. Cost: 1 wasted codex round + 1 corrected re-run. Mitigation: pre-checked transcription via `grep -n` against the spec before sending evidence pack.
  - **JSON.stringify(undefined) pitfall** documented at `codex.ts:366-368` and `:392-395` inline JSDoc. Pattern: any future `JSON.stringify(payload.X)` site must guard against undefined via `?? null`. Worth checking at M3b planner dispatch in case `streamRawText` or `useParsedSession` introduces similar string-builder paths.
  - **Spec line 779 asymmetry** (`exec_command` stringifies, `exec_command_output` raw passthrough) is an under-documented spec rule. M6 doc sweep should add a one-line comment to `working/phase-5.md` line 779 making the "/" alternation explicit. (Spec is frozen at `05467ad`; this becomes a docs PR after M6.)
  - **`response_item.function_call` real-fixture verification deferred**: the canonical `tests/fixtures/codex/sample_session.jsonl` (4 lines) does NOT exercise this branch; coverage is via inline unit-test fixture only. If M5 SkimView render exposes a real-fixture gap, fold a function_call sample into the fixture.
- **Open follow-ups deferred from M2b** (still non-blocking; pick up in M6 polish or earlier if convenient):
  - "View raw" vs "Open raw" anchor wording (M2b Claude reviewer round-1 NIT): implementation preserves Phase 4 byte-equivalence ("View raw"); design.md says "Open raw"; spec line 626 says "Open raw". Coordinator left as documentation drift to revisit during M6 doc sweep.
  - `.tab-placeholder` `padding: var(--space-8) var(--space-6)` (24 px horizontal inset) vs design.md item 46's `var(--space-8) 0` (M2b Claude reviewer round-1 NIT): visual deviation but design intent preserved.
  - `App.tsx` matchMedia legacy `addListener` fallback at lines 832–846 — defer to M6 (planner-recommended).
  - Dark `--color-border-strong` cusp at L=48 → L=50 per `working/phase-5/designs/m2a-tokens/wcag.md` Risk Callouts if a future codex pass tightens rounding (defer to M6 WCAG sweep).
  - M2a `design.md §3` token-count drift cite (cites "44 → ~74" but landed at 83; codex M2a round-3 nit) — pure docs/artifact fix, not blocking.

(Historical M1b section follows below.)

- **Two-commit landing for M1b** (CLOSED at impl SHA `e8d80c5` + log SHA `6b93dc4` on `main` 2026-04-28): per Phase 4 precedent, M1b landed as two commits: (a) the implementation (14 modified frontend files + 1 new SessionsView.test.tsx + the design artifact at `working/phase-5/designs/m1b-shell/` + the codex review artifacts at `working/phase-5/codex-reviews/m1b-round-{1,2}.txt`), then (b) the progress-log update.
- **M2 dispatch after M1b commits land**: M2 introduces the Tabs primitive + SessionView shell + Metadata tab + Raw tab extraction, plus the M2 Design Language tokens (Fraunces @font-face + oklch redefinition + motion tokens + surface treatment). M2 is the "Design Language tokens land" milestone — significantly more visible-surface change than M1b. Plan dispatching the M2 planner with the same brief structure (planner → UI/UX gate → design loop → developer → three-reviewer rule).
- **Open follow-ups deferred from M1b** (non-blocking; pick up in M6 polish or earlier if convenient):
  - count-chip `hidden` attribute literal alignment (design checklist #17 deviation; visible behavior identical)
  - `(max-width: 1100px)` framing inversion (CSS-equivalent; cosmetic)
  - `useImperativeHandle` empty-deps inlining (acceptable but tighter formulation possible)
  - design.md §3.4 HTML snippet still shows `aria-hidden="true"` on the Filters span — out of scope for M1b commit (M6 docs sweep) but worth catching.

(Historical M1a section follows below.)

- **Two-commit landing for M1a**: per Phase 4 precedent, M1a lands as two commits: (a) the implementation (12 frontend files + the design artifact + the codex-review artifacts), then (b) the progress-log update. The coordinator awaits human go-ahead before committing.
- **M1b dispatch**: after M1a commits land, dispatch the M1b planner. The planner brief was fully enumerated in the M1 planner output (file list: `SessionsTable.{tsx,test.tsx,css}` column compression + row-click → onSelectRow only + `aria-current` + `data-deep-link`; `SessionsView.{tsx,css}` sticky list-panel footer hosting the relocated `<ActionBar>` + `<Pagination>`; `SessionFilters.{tsx,test.tsx,css}` `<details>` wrap below 1100 px; `SessionView.{tsx,test.tsx}` vestigial "Open detail" button when row selected; `App.{tsx,test.tsx}` row click stops opening drawer + click-time intersection regressions still pass byte-equivalent; `e2e/inspection.spec.ts` reroutes step 9 through the vestigial button). The M1b planner just confirms the file plan against the post-M1a working tree; no new architectural decisions expected.
- **Open follow-ups deferred from M1a** (non-blocking; pick up in M6 polish or earlier if convenient):
  - `void buildUrl;` dead-scaffold removal at `App.tsx:764-767` (codex M1a round-2 nit).
  - matchMedia legacy `addListener` fallback removal at `App.tsx:810-824` (codex M1a round-2 nit; defensive but dead on Chromium target).
  - Optional interim stub in `docs/features/inspection-surface.md` (the 8-doc sweep is M6 per spec, but a one-paragraph "Phase 5 in progress" stub could land in M1b/M2 if reviewers feel the gap is too long — currently the doc still describes the Phase 4 drawer-only layout).
- **Bun-first reminder**: rule from feedback memory still applies. No `child_process`, no `jest.fn()`, no `npm`/`node`.
- **Three-reviewer rule cadence**: M1a took 2 codex rounds. Phase 4 precedent + this confirms ≥ 2 rounds is the working budget per chunk. Plan time accordingly for M1b–M6.
- **Open questions for M1 planner** (most are now resolved in spec; only one remains genuinely planner-discretion):
  - Selection ownership: RESOLVED — `App.tsx` per Resolved Decision #20.
  - Is M1 a single-shot chunk, or does the layout restructure split into M1a (CSS Grid + url sync hook + Design-Language tokens land) + M1b (column compression + row-click + vestigial drawer link)? — **planner-discretion**; the spec doesn't dictate.
  - Does M1 add the `<details>` filter wrapper for narrow viewports, or defer to a later milestone? — Spec includes `<details>` wrapping at < 1100 px in the M1 scope (§Filter bar placement); planner confirms.
  - Does M1 land the Design-Language tokens (typography, motion, surface) into `tokens.css`, or does that wait for M2? — Spec says M2 ships tokens (§Design Language → "M2 ships the @font-face rule + the fallback wiring"), so M1 only adds the structural literals listed in the M1 row of §Design Tokens. Planner confirms.
- **Bun-first reminder**: rule from feedback memory still applies. No `child_process`, no `jest.fn()`, no `npm`/`node`.
- **Three-reviewer rule**: M1 dispatch follows the standard backend-protection Claude + normal Claude + Codex external pattern per `coordinator-prompt.md`. Per-chunk Codex catches average ≥ 2 rounds (Phase 4 precedent + the Chunk A refinement's 5 rounds confirms).
