# Historical Planning Specs

Each `phase-N.md` in this directory is the frozen planning spec for a completed phase of work. These documents describe what was planned at that time and are preserved for context and provenance.

They are not current-state instructions.

For live guidance see `../docs/` and the `README.md` files under `../apps/` and `../components/`.
