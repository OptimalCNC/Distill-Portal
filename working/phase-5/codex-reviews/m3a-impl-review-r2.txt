Reading prompt from stdin...
OpenAI Codex v0.129.0 (research preview)
--------
workdir: /home/huwei/ai_codings/distill-portal
model: gpt-5.5
provider: OpenAI
approval: never
sandbox: workspace-write [workdir, /tmp, /home/huwei/.codex/memories]
reasoning effort: xhigh
reasoning summaries: none
session id: 019e0a93-e7e5-7a32-ae6e-b6a0e438d4f5
--------
user
You are an external non-Claude reviewer for a concrete implementation chunk in the task named `phase-5`. The coordinator, planner, backend-protection reviewer, normal reviewer, and developer are all Claude subagents; your job is to provide the independent non-Claude review.

## Context

- `working/phase-5.md` defines the implementation scope, frozen at commit `05467ad`.
- `progress/phase-5.progress.md` is the persistent project log (HEAD `2bda30d`).
- Architecture vocabulary references: `ARCHITECTURE.md`, `PRD.md`, `docs/dependency-rules.md`, `docs/features/inspection-surface.md`.
- Protected paths (frozen unless an exact human-approved exception is cited): `apps/backend/**`, `apps/backend/tests/**`, `components/collector-runtime/**`, `components/configuration/**`, `components/ingest-service/**`, `components/observability/**`, `components/raw-session-store/**`, `components/ui-api-contracts/**`, `tests/e2e/**`, root `Cargo.toml`, root `Cargo.lock`.
- Protected exception paths: `none` for M3a (Phase 5 is frontend-only).

## Review goals

- verify the chunk matches `working/phase-5.md` §Milestone 3 (lines 1007-1025) for the M3a sub-chunk (parsers + buildSkim + dispatcher); M3b (streamRawText + useParsedSession) is intentionally deferred to the next chunk
- verify the claimed scope coverage is accurate
- verify the evidence is sufficient for approval
- verify tests and verification are appropriate for the changed behavior or structure
- verify dependency and ownership claims are supported by the supplied evidence
- verify the protected paths stayed untouched
- identify bugs, regressions, missing tests, documentation drift, or unsupported claims

## Rules

- review only from the text and evidence provided
- do not assume missing behavior exists elsewhere
- if approval-relevant evidence is missing, the verdict must be `needs more evidence`
- `approved` and `approved with nits` are not allowed while `Missing Evidence` is not `none`
- prefer concrete findings over style opinions

## Output exactly this structure

- **Verdict**: one of `approved`, `approved with nits`, `needs changes`, `needs more evidence`
- **Findings**: concise finding bullets, or `none`
- **Missing Evidence**: exact missing evidence required, or `none`
- **Required Changes**: exact prompt or code changes required before approval, or `none`
- **Notes**: optional improvements, or `none`

## Phase 5 codex catch-density precedent

M1a 4 BLOCKING / M1b 2 / M2a 4 / M2b design 8 + impl 2 — codex has caught 20 blocking findings across Phase 5 chunks that BOTH Claude reviewers missed. Specifically anticipate blind spots in:
- (a) dual-tracker buildSkim corner cases (e.g., the system→user→assistant merging-bug regression)
- (b) Codex anchor principle (`response_item.message` SKIPPED for user/assistant; emitted for function_call)
- (c) split-content `lineOrdinal` vs `messageIndex` discipline (share lineOrdinal, sequential messageIndex)
- (d) parser totality (no `throw`; every malformed input is a warning)
- (e) `MessageKind = "unknown"` fallthroughs always carry a `warnings[]` entry
- (f) registry exhaustiveness mechanism (static type + runtime test, both layers)

# Evidence Pack

## Chunk scope

`working/phase-5.md` §Milestone 3 lines 1007-1025 (M3a = parsers + buildSkim + dispatcher; M3b deferred). UI/UX gate decision: `not required` (pure module additions; no visible structure, interaction, motion, copy, or accessibility behavior introduced).

## Spec excerpts

### §Goal & Scope (lines 132-150)

```
Phase 5 restructures the inspection page into a persistent split-pane master-detail surface...
- Per-tool client-side message parsers for Claude Code and Codex in `apps/frontend/src/features/sessions/parsers/{claude_code,codex}.ts`. Pure, total (failures land in `warnings[]`, never throw), synchronous; consume the existing `/api/v1/sessions/:uid/raw` stream with a 5 MB safety cap.
- Typed `Message`, `MessageKind`, `SkimBlock`, `BlockKind`, `ParsedSession` data model in `parsers/types.ts`.
- `buildSkim.ts` pure function emitting `SkimBlock[]` from `Message[]`.
```

### §Data Model in the Browser (lines 233-348) — VERBATIM from `working/phase-5.md`

Per-tool parsers are pure, total, synchronous functions. They consume only `rawText` and emit `ParserOutput` (`{messages, warnings}`) — they have no knowledge of stream caps, byte counts, or truncation. The dispatcher wraps their output with stream metadata + `buildSkim` to produce the consumable `ParsedSession`.

The spec's `ParsedSession` shape (spec lines 282-290 verbatim):

```typescript
/** What the dispatcher assembles by wrapping ParserOutput with stream metadata + buildSkim. Consumed by Skim/Transcript views. */
export type ParsedSession = {
  tool: Tool;
  messages: Message[];
  skim: SkimBlock[];
  totalBytes: number;
  truncated: boolean;     // true when the 5 MB cap fired during streamRawText
  warnings: ParseWarning[];
};
```

The spec's `dispatchParser` body (spec lines 318-345 verbatim):

```typescript
// parsers/index.ts
import type { Tool } from "../../../lib/contracts";
import { parseClaudeCode } from "./claude_code";
import { parseCodex } from "./codex";
import { buildSkim, USER_MSG_OVERSIZE_THRESHOLD } from "./buildSkim";
import type { ParserOutput, ParsedSession, StreamMeta } from "./types";

export type ParserFn = (rawText: string) => ParserOutput;

export const PARSERS: Record<Tool, ParserFn> = {
  claude_code: parseClaudeCode,
  codex: parseCodex,
};

export function dispatchParser(
  tool: Tool,
  rawText: string,
  streamMeta: StreamMeta,
): ParsedSession {
  const parser = PARSERS[tool];
  const output: ParserOutput = parser
    ? parser(rawText)
    : {
        messages: [],
        warnings: [
          { lineOrdinal: 0, reason: `No parser registered for tool "${tool}"` },
        ],
      };
  return {
    tool,
    messages: output.messages,
    skim: buildSkim(output.messages, USER_MSG_OVERSIZE_THRESHOLD),
    totalBytes: streamMeta.totalBytes,
    truncated: streamMeta.truncated,
    warnings: output.warnings,
  };
}
```

**Note on round 1**: a previous codex round-1 review flagged `ParsedSession.tool` as a drift from the spec because round-1 evidence pack paraphrased this snippet using a non-spec field name (`sourceTool`). The actual spec text — verified at `working/phase-5.md` lines 284 and 338 via `grep -n "sourceTool\|tool: Tool" working/phase-5.md` (no `sourceTool` matches found anywhere in the spec) — uses `tool: Tool`. The implementation correctly matches the spec. The round-1 finding was a false positive caused by a coordinator transcription error in the round-1 evidence pack.

The four `BlockKind` values (`user_turn`, `boundary`, `agent_only`, `oversized_user_message`) are assigned exclusively in `buildSkim.ts` from the `Message[]` produced by the parsers. Parsers only assign `MessageKind`. This separation prevents a parser bug from corrupting block grouping.

### §User-turn boundary algorithm (lines 360-394)

`buildSkim(messages, threshold)` walks the message stream once with **two explicit open-region trackers** (no shared `currentTurnStart` variable):

- `userTurnStart: number | null`
- `agentOnlyStart: number | null`

Spec line 387: "system → user → assistant" yields `[agent_only [0,0], user_turn [1,2]]` (NOT `[user_turn [0,2]]` which would be the merging bug if a single shared tracker were used).

Empty stream emits `[{kind: "agent_only", start: 0, end: -1, meta: {empty: 1}}]`.

### §Per-tool Message Parsers — Claude Code field paths (lines 746-757)

Top-level `type` is the authoritative discriminator:
- `user` (string content) → 1 user msg
- `user` (array content) → split: text → user; tool_result → tool_result with toolName=tool_use_id
- `assistant` (string content) → 1 assistant msg
- `assistant` (array content) → split: text → assistant; tool_use → tool_use with toolName=name
- `summary` → system with leafUuid: prefix
- `system` → system
- `custom-title`, `permission-mode` → DO NOT EMIT a Message; warn at low severity
- unknown → unknown + warning

Top-level `/timestamp` → `Message.timestamp` (RFC3339; null if missing/unparseable). Top-level `/message/role` is consulted as a sanity check; mismatch warns but the parser still emits using top-level `type` as authoritative.

### §Per-tool Message Parsers — Codex field paths (lines 763-791)

Top-level `type` is the discriminator (NOT `record_type`).

Anchor principle (resolves `response_item` ↔ `event_msg` duplication): `event_msg` is canonical for user/assistant turns; `response_item.message` of role user/assistant is SKIPPED. `response_item.function_call` is emitted (event_msg might not carry this — fixture-verify required).

| top-level type | payload type | emission |
|---|---|---|
| `session_meta` | (none) | First → `system` summarizing cwd/cli_version/model_provider; subsequent → `boundary` with `boundarySubtype: "session_resumed"` |
| `response_item` | `message` (role user/assistant) | SKIP (anchor principle) |
| `response_item` | `function_call` | `tool_use` with `toolName: payload.name` (or `"function_call"` fallback), `text: JSON.stringify(payload.arguments)` |
| `turn_context` | — | Adapter metadata; silent skip |
| `event_msg` | `user_message` | `user` |
| `event_msg` | `agent_message` | `assistant` |
| `event_msg` | `agent_reasoning` | `assistant` (text from payload.text or fallback payload.message) |
| `event_msg` | `task_started` / `task_complete` | `system` |
| `event_msg` | `exec_command` | `tool_use` toolName="exec" |
| `event_msg` | `exec_command_output` | `tool_result` toolName="exec" |
| `event_msg` | `error` | `system` + warning |
| (anything unknown) | — | `unknown` + warning (totality) |

Top-level `/timestamp` preferred over `/payload/timestamp` (both checked).

### §Truth tables (lines 798-810)

Test fixtures must cover: empty / one user msg / multi-turn / boundary / agent-only / oversized (>64 KB) / malformed / tool_use+tool_result / Codex `session_meta` / embedded second `session_meta`. Plus the "system → user → assistant" merging-bug regression.

### §Milestone 3 DoD (lines 1015-1025)

For M3a (the parsers + buildSkim + dispatcher subset):

1. Both parsers produce a correct `ParserOutput` for every truth-table fixture. Parsers are pure of stream metadata.
2. `dispatchParser(tool, rawText, streamMeta)` correctly assembles `ParsedSession` from parser output + stream metadata + `buildSkim`. Unknown tool falls through to empty session + warning (total).
3. `buildSkim` 100% branch coverage on the algorithm above.
4. No new dependencies; no backend or contract changes.
5. All gates green.

(M3b DoD bullets — `useParsedSession` abort, no_raw, tab-switch no-refetch, `streamRawText` byte-cap — are intentionally deferred.)

## Changed file list

```
apps/frontend/src/features/sessions/parsers/types.ts                 (new, 146 lines)
apps/frontend/src/features/sessions/parsers/claude_code.ts           (new, 406 lines)
apps/frontend/src/features/sessions/parsers/codex.ts                 (new, 476 lines)
apps/frontend/src/features/sessions/parsers/buildSkim.ts             (new, 131 lines)
apps/frontend/src/features/sessions/parsers/index.ts                 (new, 85 lines)
apps/frontend/src/features/sessions/parsers/claude_code.test.ts      (new, 381 lines, 25 tests)
apps/frontend/src/features/sessions/parsers/codex.test.ts            (new, 466 lines, 32 tests)
apps/frontend/src/features/sessions/parsers/buildSkim.test.ts        (new, 303 lines, 21 tests)
apps/frontend/src/features/sessions/parsers/index.test.ts            (new, 142 lines, 9 tests)
```

`git diff --stat HEAD`: empty (only untracked new files). `git status --short`: `?? apps/frontend/src/features/sessions/parsers/`.

No moves / renames. No edits to existing tracked files.

## File contents (full)

### apps/frontend/src/features/sessions/parsers/types.ts

```typescript
// Type definitions for the per-tool message parsers, the dispatcher, and
// the buildSkim block model. Mirrors `working/phase-5.md` §Data Model in
// the Browser (lines 233-348) verbatim.

import type { Tool } from "../../../lib/contracts";

export type MessageKind =
  | "user"
  | "assistant"
  | "tool_use"
  | "tool_result"
  | "system"
  | "unknown"
  | "boundary";

export type Message = {
  /** 0-indexed JSONL line number. Multiple messages may share this when one line splits into N rows. */
  lineOrdinal: number;
  /** 0-indexed sequential position in `messages[]`. Stable identity used by skim block ranges. */
  messageIndex: number;
  /** RFC3339-parsed timestamp from the line, or `null` if missing/unparseable. */
  timestamp: string | null;
  kind: MessageKind;
  /** Human-readable text body. Tool calls JSON.stringify their input here. */
  text: string;
  /** Populated when `kind === "tool_use"` or `kind === "tool_result"`. */
  toolName?: string;
  /** Populated when `kind === "boundary"`. Consumed by `buildSkim` for the chapter-break label. */
  boundarySubtype?: "session_resumed" | "compacted";
  /** Verbatim NDJSON line for "Expand to raw" affordances. */
  raw: string;
  /** Approximate UTF-8 byte size for oversize detection in `buildSkim`. */
  bytes: number;
};

export type BlockKind =
  | "user_turn"
  | "boundary"
  | "agent_only"
  | "oversized_user_message";

export type SkimBlock = {
  kind: BlockKind;
  /** Inclusive `messageIndex` of the first message in the block. */
  start: number;
  /** Inclusive `messageIndex` of the last message in the block. -1 is reserved for the empty-stream sentinel. */
  end: number;
  meta?: Record<string, string | number>;
};

export type ParseWarning = {
  lineOrdinal: number;
  reason: string;
};

export type ParserOutput = {
  messages: Message[];
  warnings: ParseWarning[];
};

export type StreamMeta = {
  /** Bytes accepted into `text` (UTF-8). */
  totalBytes: number;
  /** True when the 5 MB cap fired during streamRawText. */
  truncated: boolean;
};

export type ParsedSession = {
  tool: Tool;
  messages: Message[];
  skim: SkimBlock[];
  totalBytes: number;
  truncated: boolean;
  warnings: ParseWarning[];
};
```

### apps/frontend/src/features/sessions/parsers/claude_code.ts

```typescript
// Pure / total / synchronous Claude Code NDJSON parser.
// Walks the raw text line-by-line and emits `Message` rows + `ParseWarning`
// entries per the truth table in `working/phase-5.md` lines 750-757.
//
// Hard rules:
// 1. NEVER throws. Every malformed input is captured as a warning + the line is skipped from `messages`.
// 2. `messageIndex` is sequential across the entire stream and increments once per emitted Message.
//    `lineOrdinal` is the 0-indexed JSONL line number — multiple messages emitted from the same line
//    share their `lineOrdinal` but get distinct `messageIndex` values.
// 3. Top-level `type` is the authoritative discriminator. `/message/role` is consulted only as a
//    sanity check; mismatch warns but the parser still emits using `type`.
// 4. `custom-title` and `permission-mode` records emit NO Message but DO add a low-severity warning.
// 5. Unknown top-level `type` → `Message{kind: "unknown"}` + warning.

import type { Message, ParseWarning, ParserOutput } from "./types";

type JsonValue = string | number | boolean | null | JsonValue[] | { [key: string]: JsonValue };
type JsonObject = { [key: string]: JsonValue };

const isObject = (value: unknown): value is JsonObject =>
  typeof value === "object" && value !== null && !Array.isArray(value);

export function parseClaudeCode(rawText: string): ParserOutput {
  const messages: Message[] = [];
  const warnings: ParseWarning[] = [];
  let nextMessageIndex = 0;
  const lines = rawText.split("\n");

  for (let lineOrdinal = 0; lineOrdinal < lines.length; lineOrdinal++) {
    const raw = lines[lineOrdinal];
    if (raw === "" && lineOrdinal === lines.length - 1) continue; // trailing newline
    if (raw === "") {
      warnings.push({ lineOrdinal, reason: "empty line" });
      continue;
    }

    let record: JsonValue;
    try { record = JSON.parse(raw) as JsonValue; }
    catch { warnings.push({ lineOrdinal, reason: "malformed JSON" }); continue; }

    if (!isObject(record)) {
      warnings.push({ lineOrdinal, reason: "top-level JSON value is not an object" });
      continue;
    }

    const topType = typeof record["type"] === "string" ? (record["type"] as string) : null;
    const timestamp = parseTimestamp(record, lineOrdinal, warnings);

    if (topType === null) {
      warnings.push({ lineOrdinal, reason: "missing top-level 'type' field" });
      messages.push({
        lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
        kind: "unknown", text: stringifyTruncated(record), raw, bytes: byteLength(raw),
      });
      continue;
    }

    switch (topType) {
      case "user": {
        const message = isObject(record["message"]) ? record["message"] : null;
        const content = message ? message["content"] : undefined;
        if (message && typeof message["role"] === "string" && message["role"] !== "user") {
          warnings.push({ lineOrdinal, reason: `top-level type 'user' but /message/role is '${message["role"]}'` });
        }
        if (typeof content === "string") {
          messages.push({
            lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
            kind: "user", text: content, raw, bytes: byteLength(content),
          });
        } else if (Array.isArray(content)) {
          for (const item of content) {
            if (!isObject(item)) continue;
            const itemType = typeof item["type"] === "string" ? item["type"] : null;
            if (itemType === "text" && typeof item["text"] === "string") {
              messages.push({
                lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
                kind: "user", text: item["text"] as string, raw, bytes: byteLength(item["text"] as string),
              });
            } else if (itemType === "tool_result") {
              const inner = item["content"];
              const text = typeof inner === "string" ? inner : JSON.stringify(inner ?? null);
              const toolUseId = typeof item["tool_use_id"] === "string" ? (item["tool_use_id"] as string) : undefined;
              messages.push({
                lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
                kind: "tool_result", text, toolName: toolUseId, raw, bytes: byteLength(text),
              });
            } else {
              warnings.push({ lineOrdinal, reason: `unknown user content item type '${itemType ?? "(missing)"}'` });
              messages.push({
                lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
                kind: "unknown", text: stringifyTruncated(item), raw, bytes: byteLength(raw),
              });
            }
          }
        } else {
          warnings.push({ lineOrdinal, reason: "user record /message/content is neither string nor array" });
          messages.push({
            lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
            kind: "unknown", text: stringifyTruncated(record), raw, bytes: byteLength(raw),
          });
        }
        break;
      }

      case "assistant": {
        const message = isObject(record["message"]) ? record["message"] : null;
        const content = message ? message["content"] : undefined;
        if (message && typeof message["role"] === "string" && message["role"] !== "assistant") {
          warnings.push({ lineOrdinal, reason: `top-level type 'assistant' but /message/role is '${message["role"]}'` });
        }
        if (typeof content === "string") {
          messages.push({
            lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
            kind: "assistant", text: content, raw, bytes: byteLength(content),
          });
        } else if (Array.isArray(content)) {
          for (const item of content) {
            if (!isObject(item)) continue;
            const itemType = typeof item["type"] === "string" ? item["type"] : null;
            if (itemType === "text" && typeof item["text"] === "string") {
              messages.push({
                lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
                kind: "assistant", text: item["text"] as string, raw, bytes: byteLength(item["text"] as string),
              });
            } else if (itemType === "tool_use") {
              const name = typeof item["name"] === "string" ? (item["name"] as string) : undefined;
              const input = item["input"] ?? null;
              const text = JSON.stringify(input, null, 2);
              messages.push({
                lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
                kind: "tool_use", text, toolName: name, raw, bytes: byteLength(text),
              });
            } else {
              warnings.push({ lineOrdinal, reason: `unknown assistant content item type '${itemType ?? "(missing)"}'` });
              messages.push({
                lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
                kind: "unknown", text: stringifyTruncated(item), raw, bytes: byteLength(raw),
              });
            }
          }
        } else {
          warnings.push({ lineOrdinal, reason: "assistant record /message/content is neither string nor array" });
          messages.push({
            lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
            kind: "unknown", text: stringifyTruncated(record), raw, bytes: byteLength(raw),
          });
        }
        break;
      }

      case "summary": {
        const leafUuid = typeof record["leafUuid"] === "string" ? (record["leafUuid"] as string) : "";
        const summary = typeof record["summary"] === "string" ? (record["summary"] as string) : "";
        const text = leafUuid ? `${leafUuid}: ${summary}` : summary;
        messages.push({
          lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
          kind: "system", text, raw, bytes: byteLength(text),
        });
        break;
      }

      case "system": {
        let text = "";
        if (typeof record["content"] === "string") text = record["content"] as string;
        else if (isObject(record["message"]) && typeof (record["message"] as JsonObject)["content"] === "string")
          text = (record["message"] as JsonObject)["content"] as string;
        else text = stringifyTruncated(record);
        messages.push({
          lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
          kind: "system", text, raw, bytes: byteLength(text),
        });
        break;
      }

      case "custom-title":
      case "permission-mode": {
        warnings.push({ lineOrdinal, reason: `Skipping Claude-meta type '${topType}' (session-level metadata, not a timeline message)` });
        break;
      }

      default: {
        warnings.push({ lineOrdinal, reason: `unknown top-level type '${topType}'` });
        messages.push({
          lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
          kind: "unknown", text: stringifyTruncated(record), raw, bytes: byteLength(raw),
        });
      }
    }
  }

  return { messages, warnings };
}

function parseTimestamp(record: JsonObject, lineOrdinal: number, warnings: ParseWarning[]): string | null {
  const value = record["timestamp"];
  if (value === undefined || value === null) return null;
  if (typeof value !== "string") {
    warnings.push({ lineOrdinal, reason: "timestamp field is not a string" });
    return null;
  }
  const millis = Date.parse(value);
  if (Number.isNaN(millis)) {
    warnings.push({ lineOrdinal, reason: `unparseable RFC3339 timestamp '${value}'` });
    return null;
  }
  return value;
}

function byteLength(text: string): number { return new TextEncoder().encode(text).length; }
function stringifyTruncated(value: unknown): string {
  try { return JSON.stringify(value).slice(0, 240); }
  catch { return "[unserialisable record]"; }
}
```

### apps/frontend/src/features/sessions/parsers/codex.ts

```typescript
// Pure / total / synchronous Codex NDJSON parser.
// Walks the raw text line-by-line and emits `Message` rows + `ParseWarning`
// entries per the truth table in `working/phase-5.md` lines 769-783.
//
// Anchor principle (spec line 767): the Codex stream emits BOTH an API-layer `response_item`
// AND a UI-layer `event_msg` for the same logical user/assistant turn. To prevent duplicate
// timeline rows, this parser SKIPS `response_item` whose `payload.type === "message"` AND
// `payload.role` is "user" or "assistant". `event_msg` is canonical.
//
// `response_item.function_call` is NOT skipped — explicit function-call payloads on response_item
// (which event_msg might not duplicate) emit as tool_use rows.
//
// Hard rules:
// 1. NEVER throws.
// 2. `messageIndex` is sequential across the entire stream.
// 3. Top-level `type` is the authoritative discriminator (the field is `type`, NOT `record_type`).
// 4. Top-level `/timestamp` preferred; falls back to `/payload/timestamp`.

import type { Message, ParseWarning, ParserOutput } from "./types";

type JsonValue = string | number | boolean | null | JsonValue[] | { [key: string]: JsonValue };
type JsonObject = { [key: string]: JsonValue };

const isObject = (value: unknown): value is JsonObject =>
  typeof value === "object" && value !== null && !Array.isArray(value);

export function parseCodex(rawText: string): ParserOutput {
  const messages: Message[] = [];
  const warnings: ParseWarning[] = [];
  let nextMessageIndex = 0;
  let sessionMetaSeen = false;
  const lines = rawText.split("\n");

  for (let lineOrdinal = 0; lineOrdinal < lines.length; lineOrdinal++) {
    const raw = lines[lineOrdinal];
    if (raw === "" && lineOrdinal === lines.length - 1) continue;
    if (raw === "") { warnings.push({ lineOrdinal, reason: "empty line" }); continue; }

    let record: JsonValue;
    try { record = JSON.parse(raw) as JsonValue; }
    catch { warnings.push({ lineOrdinal, reason: "malformed JSON" }); continue; }

    if (!isObject(record)) {
      warnings.push({ lineOrdinal, reason: "top-level JSON value is not an object" });
      continue;
    }

    const topType = typeof record["type"] === "string" ? (record["type"] as string) : null;
    const payload = isObject(record["payload"]) ? record["payload"] : null;
    const timestamp = parseTimestamp(record, payload, lineOrdinal, warnings);

    if (topType === null) {
      warnings.push({ lineOrdinal, reason: "missing top-level 'type' field" });
      messages.push({
        lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
        kind: "unknown", text: stringifyTruncated(record), raw, bytes: byteLength(raw),
      });
      continue;
    }

    switch (topType) {
      case "session_meta": {
        if (!sessionMetaSeen) {
          sessionMetaSeen = true;
          const cwd = payload && typeof payload["cwd"] === "string" ? (payload["cwd"] as string) : "(unknown cwd)";
          const cliVersion = payload && typeof payload["cli_version"] === "string" ? (payload["cli_version"] as string) : "(unknown cli)";
          const modelProvider = payload && typeof payload["model_provider"] === "string" ? (payload["model_provider"] as string) : "(unknown provider)";
          const text = `Codex session — cwd=${cwd}, cli_version=${cliVersion}, model_provider=${modelProvider}`;
          messages.push({
            lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
            kind: "system", text, raw, bytes: byteLength(text),
          });
        } else {
          messages.push({
            lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
            kind: "boundary", boundarySubtype: "session_resumed",
            text: "session resumed", raw, bytes: byteLength(raw),
          });
        }
        break;
      }

      case "response_item": {
        const payloadType = payload && typeof payload["type"] === "string" ? (payload["type"] as string) : null;
        const role = payload && typeof payload["role"] === "string" ? (payload["role"] as string) : null;

        if (payloadType === "message" && (role === "user" || role === "assistant")) {
          // ANCHOR PRINCIPLE: event_msg is canonical. SKIP entirely. No warning.
          break;
        }

        if (payloadType === "function_call") {
          const name = typeof payload!["name"] === "string" ? (payload!["name"] as string) : "function_call";
          const args = payload!["arguments"] ?? null;
          const text = typeof args === "string" ? args : JSON.stringify(args);
          messages.push({
            lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
            kind: "tool_use", toolName: name, text, raw, bytes: byteLength(text),
          });
          break;
        }

        warnings.push({ lineOrdinal, reason: `unknown response_item payload.type '${payloadType ?? "(missing)"}'` });
        messages.push({
          lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
          kind: "unknown", text: stringifyTruncated(record), raw, bytes: byteLength(raw),
        });
        break;
      }

      case "turn_context": {
        // Adapter metadata; not part of timeline. Silent skip.
        break;
      }

      case "event_msg": {
        const payloadType = payload && typeof payload["type"] === "string" ? (payload["type"] as string) : null;

        if (payloadType === null) {
          warnings.push({ lineOrdinal, reason: "event_msg missing payload.type" });
          messages.push({
            lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
            kind: "unknown", text: stringifyTruncated(record), raw, bytes: byteLength(raw),
          });
          break;
        }

        switch (payloadType) {
          case "user_message": {
            const text = payload && typeof payload["message"] === "string" ? (payload["message"] as string) : "";
            messages.push({
              lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
              kind: "user", text, raw, bytes: byteLength(text),
            });
            break;
          }
          case "agent_message": {
            const text = payload && typeof payload["message"] === "string" ? (payload["message"] as string) : "";
            messages.push({
              lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
              kind: "assistant", text, raw, bytes: byteLength(text),
            });
            break;
          }
          case "agent_reasoning": {
            const text = payload && typeof payload["text"] === "string"
              ? (payload["text"] as string)
              : payload && typeof payload["message"] === "string"
                ? (payload["message"] as string)
                : "";
            messages.push({
              lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
              kind: "assistant", text, raw, bytes: byteLength(text),
            });
            break;
          }
          case "task_started": {
            const turn = payload && typeof payload["turn_id"] === "string" ? (payload["turn_id"] as string) : "(unknown turn)";
            const text = `task_started · turn ${turn}`;
            messages.push({
              lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
              kind: "system", text, raw, bytes: byteLength(text),
            });
            break;
          }
          case "task_complete": {
            const turn = payload && typeof payload["turn_id"] === "string" ? (payload["turn_id"] as string) : "(unknown turn)";
            const text = `task_complete · turn ${turn}`;
            messages.push({
              lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
              kind: "system", text, raw, bytes: byteLength(text),
            });
            break;
          }
          case "exec_command": {
            const command = payload ? payload["command"] : null;
            const text = typeof command === "string" ? command : JSON.stringify(command);
            messages.push({
              lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
              kind: "tool_use", toolName: "exec", text, raw, bytes: byteLength(text),
            });
            break;
          }
          case "exec_command_output": {
            const output = payload ? payload["output"] : null;
            const text = typeof output === "string" ? output : JSON.stringify(output);
            messages.push({
              lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
              kind: "tool_result", toolName: "exec", text, raw, bytes: byteLength(text),
            });
            break;
          }
          case "error": {
            const errMsg = payload && typeof payload["message"] === "string" ? (payload["message"] as string) : "(no error message)";
            warnings.push({ lineOrdinal, reason: `event_msg.error: ${errMsg}` });
            messages.push({
              lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
              kind: "system", text: errMsg, raw, bytes: byteLength(errMsg),
            });
            break;
          }
          default: {
            warnings.push({ lineOrdinal, reason: `unknown event_msg payload.type '${payloadType}'` });
            messages.push({
              lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
              kind: "unknown", text: stringifyTruncated(payload), raw, bytes: byteLength(raw),
            });
          }
        }
        break;
      }

      default: {
        warnings.push({ lineOrdinal, reason: `unknown top-level type '${topType}'` });
        messages.push({
          lineOrdinal, messageIndex: nextMessageIndex++, timestamp,
          kind: "unknown", text: stringifyTruncated(record), raw, bytes: byteLength(raw),
        });
      }
    }
  }

  return { messages, warnings };
}

function parseTimestamp(record: JsonObject, payload: JsonObject | null, lineOrdinal: number, warnings: ParseWarning[]): string | null {
  const top = record["timestamp"];
  if (typeof top === "string") {
    const millis = Date.parse(top);
    if (!Number.isNaN(millis)) return top;
    warnings.push({ lineOrdinal, reason: `unparseable RFC3339 top-level timestamp '${top}'` });
  } else if (top !== undefined && top !== null) {
    warnings.push({ lineOrdinal, reason: "top-level timestamp is not a string" });
  }
  if (payload) {
    const inner = payload["timestamp"];
    if (typeof inner === "string") {
      const millis = Date.parse(inner);
      if (!Number.isNaN(millis)) return inner;
      warnings.push({ lineOrdinal, reason: `unparseable RFC3339 payload.timestamp '${inner}'` });
    }
  }
  return null;
}

function byteLength(text: string): number { return new TextEncoder().encode(text).length; }
function stringifyTruncated(value: unknown): string {
  try { return JSON.stringify(value).slice(0, 240); }
  catch { return "[unserialisable record]"; }
}
```

### apps/frontend/src/features/sessions/parsers/buildSkim.ts

```typescript
// Pure synchronous turn/agent/boundary block builder.
// Walks a parser-produced `Message[]` exactly once with TWO explicit open-region trackers —
// `openUserTurnStart` and `openAgentOnlyStart` — per `working/phase-5.md` §User-turn boundary
// algorithm (lines 360-394).

import type { Message, SkimBlock } from "./types";

export const USER_MSG_OVERSIZE_THRESHOLD = 65_536;

export function buildSkim(messages: Message[], threshold: number): SkimBlock[] {
  if (messages.length === 0) {
    return [{ kind: "agent_only", start: 0, end: -1, meta: { empty: 1 } }];
  }

  const blocks: SkimBlock[] = [];
  let openUserTurnStart: number | null = null;
  let openAgentOnlyStart: number | null = null;

  const closeOpenRegions = (endIndex: number): void => {
    if (openUserTurnStart !== null) {
      blocks.push({ kind: "user_turn", start: openUserTurnStart, end: endIndex });
      openUserTurnStart = null;
    } else if (openAgentOnlyStart !== null) {
      blocks.push({ kind: "agent_only", start: openAgentOnlyStart, end: endIndex });
      openAgentOnlyStart = null;
    }
  };

  for (let i = 0; i < messages.length; i++) {
    const m = messages[i];

    if (m.kind === "boundary") {
      closeOpenRegions(i - 1);
      const meta: Record<string, string | number> = {};
      if (m.boundarySubtype) meta["subtype"] = m.boundarySubtype;
      blocks.push({ kind: "boundary", start: i, end: i, meta });
      continue;
    }

    if (m.kind === "user") {
      if (m.bytes > threshold) {
        closeOpenRegions(i - 1);
        blocks.push({ kind: "oversized_user_message", start: i, end: i, meta: { sizeBytes: m.bytes } });
        continue;
      }
      closeOpenRegions(i - 1);
      openUserTurnStart = i;
      continue;
    }

    // assistant / tool_use / tool_result / system / unknown
    if (openUserTurnStart !== null) continue; // extend user_turn
    if (openAgentOnlyStart === null) openAgentOnlyStart = i;
    // else extend agent_only
  }

  closeOpenRegions(messages.length - 1);
  return blocks;
}
```

### apps/frontend/src/features/sessions/parsers/index.ts

```typescript
import type { Tool } from "../../../lib/contracts";
import { buildSkim, USER_MSG_OVERSIZE_THRESHOLD } from "./buildSkim";
import { parseClaudeCode } from "./claude_code";
import { parseCodex } from "./codex";
import type { ParsedSession, ParserOutput, StreamMeta } from "./types";

export type ParserFn = (rawText: string) => ParserOutput;

export const PARSERS: Record<Tool, ParserFn> = {
  claude_code: parseClaudeCode,
  codex: parseCodex,
};

export function dispatchParser(tool: Tool, rawText: string, streamMeta: StreamMeta): ParsedSession {
  const parser = PARSERS[tool] as ParserFn | undefined;
  const output: ParserOutput = parser
    ? parser(rawText)
    : {
        messages: [],
        warnings: [{ lineOrdinal: 0, reason: `No parser registered for tool "${tool}"` }],
      };
  return {
    tool,
    messages: output.messages,
    skim: buildSkim(output.messages, USER_MSG_OVERSIZE_THRESHOLD),
    totalBytes: streamMeta.totalBytes,
    truncated: streamMeta.truncated,
    warnings: output.warnings,
  };
}

export { buildSkim, USER_MSG_OVERSIZE_THRESHOLD } from "./buildSkim";
export { parseClaudeCode } from "./claude_code";
export { parseCodex } from "./codex";
export type {
  BlockKind,
  Message,
  MessageKind,
  ParsedSession,
  ParserOutput,
  ParseWarning,
  SkimBlock,
  StreamMeta,
} from "./types";
```

### apps/frontend/src/features/sessions/parsers/buildSkim.test.ts

(21 tests; all bun:test; covers: empty stream sentinel; single user; multi-turn; tool_use+tool_result extend turn; boundary mid-stream resets BOTH trackers; boundary mid agent_only also closes it; agent-only run; single oversize → oversized_user_message; oversize between two turns; **system → user → assistant emits [agent_only [0,0], user_turn [1,2]] (THE dual-tracker merging-bug regression)**; boundary at start opens agent_only after; boundary at end closes preceding tracker; boundary without subtype emits empty meta; trailing user message closes at last index; unknown extends open user_turn; unknown opens agent_only; oversized at index 0 null-close branch; user message exactly at threshold NOT oversized; agent_only / boundary / agent_only; USER_MSG_OVERSIZE_THRESHOLD === 65_536.)

Key assertion (regression for the system→user→assistant merging bug):

```typescript
test("system → user → assistant emits [agent_only [0,0], user_turn [1,2]] (dual-tracker regression)", () => {
  const result = buildSkim(
    [msg(0, "system"), msg(1, "user"), msg(2, "assistant")],
    USER_MSG_OVERSIZE_THRESHOLD,
  );
  expect(result).toEqual([
    { kind: "agent_only", start: 0, end: 0 },
    { kind: "user_turn", start: 1, end: 2 },
  ]);
});
```

Boundary regression:

```typescript
test("boundary mid-stream resets BOTH trackers and emits boundary block", () => {
  const result = buildSkim(
    [msg(0, "user"), msg(1, "assistant"),
     msg(2, "boundary", { boundarySubtype: "session_resumed" }),
     msg(3, "user"), msg(4, "assistant")],
    USER_MSG_OVERSIZE_THRESHOLD,
  );
  expect(result).toEqual([
    { kind: "user_turn", start: 0, end: 1 },
    { kind: "boundary", start: 2, end: 2, meta: { subtype: "session_resumed" } },
    { kind: "user_turn", start: 3, end: 4 },
  ]);
});
```

### apps/frontend/src/features/sessions/parsers/claude_code.test.ts

(25 tests covering every truth-table row + role-mismatch + timestamp variants + totality + real-fixture; 1 test reads `tests/fixtures/claude_code/sample_session.jsonl`; `messageIndex` monotonicity asserted across split content + skipped malformed lines.)

Excerpt — split-content discipline test:

```typescript
test("user with array content splits into multiple messages sharing lineOrdinal", () => {
  const raw = JSON.stringify({
    type: "user",
    message: { role: "user", content: [
      { type: "text", text: "tool gave me bad data" },
      { type: "tool_result", tool_use_id: "tu_123", content: "stderr: oops" },
    ] },
  });
  const out = parseClaudeCode(raw);
  expect(out.messages[0]).toMatchObject({ lineOrdinal: 0, messageIndex: 0, kind: "user", text: "tool gave me bad data" });
  expect(out.messages[1]).toMatchObject({ lineOrdinal: 0, messageIndex: 1, kind: "tool_result", text: "stderr: oops", toolName: "tu_123" });
});
```

Excerpt — totality:

```typescript
test("totality: parser does not throw on adversarial input", () => {
  const adversarial = ["", "null", "[]", "{}", "this is not json at all",
    JSON.stringify({ type: "user" }),
    JSON.stringify({ type: "user", message: null }),
    JSON.stringify({ type: 123 }),
    JSON.stringify({ type: "assistant", message: { content: { weird: true } } }),
    JSON.stringify({ type: "summary" }),
    "{\"type\":\"user\",\"message\":{\"role\":\"user\",\"content\":[",
  ].join("\n");
  expect(() => parseClaudeCode(adversarial)).not.toThrow();
});
```

### apps/frontend/src/features/sessions/parsers/codex.test.ts

(32 tests covering session_meta first/second; anchor principle SKIP for response_item.message user/assistant; response_item.function_call → tool_use; turn_context silent skip; all event_msg payload variants; malformed line; unknown top-level/payload types; timestamp top-level vs payload precedence; totality; real-fixture verifying the anchor principle.)

Excerpt — anchor principle real-fixture check:

```typescript
test("end-to-end: real fixture verifies anchor principle (response_item.user is SKIPPED)", () => {
  const raw = readFileSync("../../tests/fixtures/codex/sample_session.jsonl", "utf8");
  const out = parseCodex(raw);
  // 4 lines: session_meta / response_item.user / event_msg.user_message / turn_context.
  // Expect: 2 messages (system + user), 0 warnings.
  expect(out.messages).toHaveLength(2);
  expect(out.messages[0]).toMatchObject({ kind: "system", lineOrdinal: 0, messageIndex: 0 });
  expect(out.messages[1]).toMatchObject({ kind: "user", lineOrdinal: 2, messageIndex: 1, text: "Introduce omx and its subcommands." });
  expect(out.warnings).toEqual([]);
});
```

Excerpt — embedded second session_meta (CODEX_FORKED_FIXTURE-style):

```typescript
test("CODEX_FORKED_FIXTURE-style stream emits a session_resumed boundary on the second session_meta", () => {
  const raw = [
    JSON.stringify({ timestamp: "...", type: "session_meta", payload: { id: "child", cwd: "/child", cli_version: "0.130.0", model_provider: "OpenAI" } }),
    JSON.stringify({ timestamp: "...", type: "session_meta", payload: { id: "parent", cwd: "/parent" } }),
    JSON.stringify({ timestamp: "...", type: "event_msg", payload: { type: "user_message", message: "child prompt" } }),
  ].join("\n");
  const out = parseCodex(raw);
  expect(out.messages).toHaveLength(3);
  expect(out.messages[0].kind).toBe("system");
  expect(out.messages[1].kind).toBe("boundary");
  expect(out.messages[1].boundarySubtype).toBe("session_resumed");
  expect(out.messages[2]).toMatchObject({ kind: "user", text: "child prompt" });
});
```

### apps/frontend/src/features/sessions/parsers/index.test.ts

(9 tests covering registry exhaustiveness, dispatch routing, skim assembly, streamMeta propagation, unknown-tool fallthrough, totality, warning preservation.)

Key — registry exhaustiveness, two layers of defence:

```typescript
const EXPECTED_TOOLS: ReadonlyArray<string> = ["claude_code", "codex"];

test("PARSERS registry exhaustiveness matches Tool union", () => {
  expect(Object.keys(PARSERS).sort()).toEqual([...EXPECTED_TOOLS].sort());
});

test("PARSERS entries are the expected per-tool functions", () => {
  expect(PARSERS.claude_code).toBe(parseClaudeCode);
  expect(PARSERS.codex).toBe(parseCodex);
});
```

Plus dispatchParser totality + unknown-tool fallthrough:

```typescript
test("dispatchParser unknown tool returns empty session + warning, never throws", () => {
  const result = dispatchParser(
    "future_tool" as unknown as Parameters<typeof dispatchParser>[0],
    "anything",
    { totalBytes: 0, truncated: false },
  );
  expect(result.messages).toEqual([]);
  expect(result.warnings).toHaveLength(1);
  expect(result.warnings[0].reason).toContain("future_tool");
  expect(result.skim).toEqual([{ kind: "agent_only", start: 0, end: -1, meta: { empty: 1 } }]);
});
```

## Verification commands run

| Command | Result |
|---|---|
| `cargo check --workspace` | OK; `Finished dev profile [unoptimized + debuginfo] target(s) in 0.04s` (incremental — backend untouched) |
| `cargo test --workspace` | OK; all `test result:` lines `0 failed`. Matches M2b baseline exactly. |
| `cargo test -p distill-portal-ui-api-contracts --features ts-bindings` | `1 passed; 0 failed; 1 ignored` (the ignored `regenerate_ts_bindings` writes to `components/ui-api-contracts/bindings/` and is invoked explicitly when refreshing TS output) |
| `bun run test` (in `apps/frontend/`) | `403 pass / 0 fail / 1448 expect() / 27 files` — was M2b 316/0/1236/23 → +87 tests / +212 expects / +4 new test files |
| `bun run build` (in `apps/frontend/`) | OK; `dist/assets/index-Bt-npo7s.js 239.78 kB / dist/assets/index-BqRZCKcp.css 21.34 kB / built in 555ms` — near-zero delta from M2b (parsers are dead code in M3a, Vite tree-shakes) |
| `bun run test:e2e` (in `apps/frontend/`) | `1 passed (3.2s)` — `inspection.spec.ts` byte-equivalent to M2b |

## Tests added

87 new unit tests across 4 new test files, all under `apps/frontend/src/features/sessions/parsers/`, all bun-native (`bun:test`); no `jest.fn()`, no `child_process`, no `npm`/`node`.

## Tests not run

`none`.

## Docs touched

`none` (per spec; M6 doc sweep).

## Cited human-approved protected-path exceptions

`none`.

## Read-only consultation of protected paths

(Allowed for the developer per the task brief.) `components/collector-runtime/src/adapters/{claude_code,codex}.rs`, `components/collector-runtime/tests/parsers.rs` (CODEX_FORKED_FIXTURE inline byte literals), `tests/fixtures/{claude_code,codex}/sample_session.jsonl`, `components/ui-api-contracts/bindings/Tool.ts`. No writes to any protected path. `git status` shows zero protected-path entries.

## Backend-protection reviewer outcome

`backend untouched`. Required Action: `proceed to normal review`. Findings: none.

## Residual risks (developer-flagged for reviewer scrutiny)

- **Anchor-principle implementation** in `codex.ts:160-163`: silently SKIPS `response_item.message` with role `user`/`assistant` (no warning). Verified end-to-end against the canonical fixture (2 messages, 0 warnings).
- **Split-content discipline**: split-emitted Messages share `lineOrdinal` but get sequential `messageIndex` (e.g. assistant array carrying both `text` and `tool_use` items emit two Messages with `lineOrdinal: 0, 0` and `messageIndex: 0, 1`).
- **`messageIndex` monotonicity across malformed-line skips**: skipped malformed lines do NOT shift `messageIndex` (next message gets sequential index, not lineOrdinal index).
- **`response_item.function_call` branch** (`codex.ts:165-193`) is NOT exercised by `tests/fixtures/codex/sample_session.jsonl` (4-line fixture); unit-tested via inline literal in `codex.test.ts` "response_item.function_call emits a tool_use with toolName from payload.name".
- **Parser totality**: explicit "totality: parser does not throw on adversarial input" tests in both parsers cover null / [] / {} / garbage strings / truncated JSON / type=number / missing message / payload=null.
- **Registry exhaustiveness**: layered — static `Record<Tool, ParserFn>` at `index.ts:31` (compile-time) + runtime test at `index.test.ts:32-34` comparing `Object.keys(PARSERS).sort()` to a hard-coded `EXPECTED_TOOLS` array.

## Spec deviations or open questions

`none`.

# Begin review now.

codex
I’m going to keep this as an evidence-only review as requested, and focus on approval-relevant gaps rather than re-running commands.
codex
- **Verdict**: needs changes

- **Findings**:
  - `codex.ts` does not follow the specified `response_item.function_call` text rule exactly. The spec requires `text: JSON.stringify(payload.arguments)`, but the implementation emits raw strings unchanged, so string arguments produce different output.
  - Parser malformed-input warning coverage is incomplete. `claude_code.ts` silently skips non-object items inside user/assistant content arrays, and `codex.ts` accepts several malformed recognized payloads without warnings, such as missing `message` on `user_message`/`agent_message`, missing `command` on `exec_command`, or non-string `payload.timestamp`. The brief requires malformed input to land in `warnings[]`.

- **Missing Evidence**: none

- **Required Changes**:
  - Change Codex `response_item.function_call` to serialize `payload.arguments` exactly as specified, and add a string-arguments regression test.
  - Add warnings for silently skipped or malformed recognized payload shapes, especially Claude primitive content-array items and Codex missing/non-string required payload fields.

- **Notes**: The protected-path and frontend-only scope evidence is sufficient from the supplied status/file list.
tokens used
21,326
- **Verdict**: needs changes

- **Findings**:
  - `codex.ts` does not follow the specified `response_item.function_call` text rule exactly. The spec requires `text: JSON.stringify(payload.arguments)`, but the implementation emits raw strings unchanged, so string arguments produce different output.
  - Parser malformed-input warning coverage is incomplete. `claude_code.ts` silently skips non-object items inside user/assistant content arrays, and `codex.ts` accepts several malformed recognized payloads without warnings, such as missing `message` on `user_message`/`agent_message`, missing `command` on `exec_command`, or non-string `payload.timestamp`. The brief requires malformed input to land in `warnings[]`.

- **Missing Evidence**: none

- **Required Changes**:
  - Change Codex `response_item.function_call` to serialize `payload.arguments` exactly as specified, and add a string-arguments regression test.
  - Add warnings for silently skipped or malformed recognized payload shapes, especially Claude primitive content-array items and Codex missing/non-string required payload fields.

- **Notes**: The protected-path and frontend-only scope evidence is sufficient from the supplied status/file list.
