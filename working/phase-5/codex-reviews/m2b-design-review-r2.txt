Reading prompt from stdin...
OpenAI Codex v0.128.0 (research preview)
--------
workdir: /home/huwei/ai_codings/distill-portal
model: gpt-5.5
provider: OpenAI
approval: never
sandbox: workspace-write [workdir, /tmp, /home/huwei/.codex/memories]
reasoning effort: xhigh
reasoning summaries: none
session id: 019e0277-e556-7c41-888c-c43bcd441fed
--------
user
You are continuing as the external non-Claude reviewer providing the round-2 OPTIONAL design opinion on M2b for task `phase-5` of distill-portal. Your round-1 verdict was `needs changes` with 5 BLOCKING findings:

1. Raw non-JSON marker uses `--color-warn` as text → 3.97:1 light fails WCAG AA
2. M2b adds animated `color`, `border-color`, and unlisted hover `background-color` transitions → motion-budget violation
3. Artifact uses undefined `--space-5/10/12` (tokens.css defines only 1/2/3/4/6/8)
4. `key={activeTab}` content remount breaks the spec's keep-mounted contract (lines 650-658)
5. Active tabpanels lack `tabIndex={0}` for placeholders without focusable children

The designer applied a revision pass and reports each finding addressed. Verify each fix against the artifact at `working/phase-5/designs/m2b-shell/`. Re-compute the WCAG numerics where applicable. Report your round-2 verdict.

Designer's round-3 revision summary (verbatim):

- Finding 1: prototype.html `.line.text .marker` no longer sets `color: var(--color-warn)`; cascade from `.line.text` `var(--color-ink-muted)` + italic now drives the marker styling. design.md §3.4 RawTab anatomy updated. colors.md R03 replaced with muted-on-surface-raised pair.
- Finding 2: Removed every `transition: color`, `transition: border-color`, and unlisted hover `transition: background-color` from prototype.html (lines 484, 629, 673, 784). motion.md split into "M2b's three new motion surfaces" + "INSTANT (non-animated) surfaces". design.md §5.1 rewritten.
- Finding 3: Verified actual scale via `grep -E '^\s*--space-' apps/frontend/src/styles/tokens.css` (1, 2, 3, 4, 6, 8 only). Removed bogus token definitions from prototype.html:50-60. Replaced `var(--space-5)` → `var(--space-4)` and `var(--space-12)` → `var(--space-8)` across prototype.html and design.md.
- Finding 4: Picked Option A (CSS animation re-trigger via inline `style.animation` toggle). Panel `<div role="tabpanel">` carries `style={{ animation: isActive ? "tab-fade-in 120ms var(--ease-out) both" : "none" }}`. React subtree STABLE across tab switches. design.md §3, §3.2, §4.5, §5.1, §5.2, §7.2, §7.4 + checklist items 27/28/29 rewritten. prototype.html `activate()` uses `dataset.populated` sentinel + `repopulatePanelContent()` helper instead of remount.
- Finding 5: design.md §6.1 explicit rule: Skim + Transcript panels carry `tabIndex={0}`; Metadata + Raw don't. Checklist item 29a captures rule. Test obligation 54 expanded. prototype.html `activate()` toggles `tabindex` for skim + transcript only.

## Source files

- `working/phase-5.md` — implementation source of truth (frozen at commit `05467ad` on `main`, 2026-04-28)
- `progress/phase-5.progress.md` — current snapshot
- `working/phase-5/designs/m2b-shell/` — the design artifact under review:
  - `design.md` (revised; ~1401 lines)
  - `prototype.html` (~1475 lines)
  - `colors.md` (~233 lines)
  - `motion.md` (~211 lines)
- `working/phase-5/codex-reviews/m2b-design-review.txt` — your round-1 verbatim stdout (5337 lines)
- `working/phase-5/designs/m2a-tokens/` — frozen Design Language baseline
- `apps/frontend/src/styles/tokens.css` — actual token file
- `apps/frontend/src/features/sessions/SessionDetail.tsx` lines 160–256 — Phase 4 baseline
- `apps/frontend/src/features/sessions/SessionDetail.css:143-156` — Phase 4 non-JSON marker recipe

## Protected paths (frozen — do not propose edits)

`apps/backend/**`, `apps/backend/tests/**`, `components/collector-runtime/**`, `components/configuration/**`, `components/ingest-service/**`, `components/observability/**`, `components/raw-session-store/**`, `components/ui-api-contracts/**`, `tests/e2e/**`, root `Cargo.toml`, root `Cargo.lock`.

## Rules

- review only from the artifact + the cited spec sections + the cited code files
- compute oklch → linear sRGB → relative luminance numerically for any color pair you doubt
- verify EACH round-1 finding against the cited file:line/section before approving
- new findings are allowed (the revision may have introduced different defects)
- if approval-relevant evidence is missing, the verdict must be `needs more evidence`
- `approved` and `approved with nits` are not allowed while `Missing Evidence` is not `none`

## Output exactly this structure

- **Verdict**: one of `approved`, `approved with nits`, `needs changes`, `needs more evidence`
- **Round-1 Verification Trace**: a numbered list 1–5 of the round-1 findings with one of: `verified fixed at <file>:<line>`, `partially fixed (still needs <X>)`, `not fixed (cite where designer says fixed but isn't)`. This is the audit trail.
- **Findings**: NEW round-2 findings (not from round 1) with severity (BLOCKING / NIT) + file:line + spec section, or `none`
- **WCAG Numerical Audit**: re-compute the round-1 failing pair (R03 warn-as-text marker) at its new color (`--color-ink-muted`); + any new pair the revision introduced. Light + dark, with PASS/FAIL gate.
- **Missing Evidence**: exact missing artifact content required, or `none`
- **Required Changes**: exact changes required before the design is implementation-ready, or `none`
- **Notes**: optional improvements or residual risks the coordinator should be aware of, or `none`

codex
I’ll verify the revision from the artifact and cited source files only, then recompute the contrast pairs numerically from the actual token values before giving a round-2 verdict.
exec
/bin/bash -lc "sed -n '1,220p' README.md" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
# Distill Portal

Distill Portal is a local-first portal for collecting, storing, and analyzing coding sessions produced by AI coding tools.

Read [PRD.md](PRD.md) for the product summary and functional requirements.

For developers, please read [docs/README.md](docs/README.md) for documentation on the repository structure, component ownership, features, and development commands.

## Contributing

- Read [PRD.md](PRD.md) for the product summary and functional requirements.
- Read [ARCHITECTURE.md](ARCHITECTURE.md) for the architecture.
- Read [docs/README.md](docs/README.md) for the repository structure, component ownership, features, and development commands.

### Hard Rules

- Backend owns machine-consumable HTTP routes; do not reintroduce backend-owned end-user HTML.
- Frontend talks to the backend only over HTTP and only consumes generated contract types from `components/ui-api-contracts/bindings/*.ts`.
- `apps/frontend` must not import from `components/collector-runtime`, `components/ingest-service`, or `components/raw-session-store`.
- HTTP payload changes must update `components/ui-api-contracts`, the Rust and TS sides of both apps, and relevant tests together.
- Keep docs aligned with any layout, command, dependency, or payload-shape change.

### Quick Checks

Fast checks to run before committing:

```bash
cargo check --workspace
cargo test --workspace
```

From `apps/frontend/`:

```bash
bun run test
```

See [docs/dev-commands.md](docs/dev-commands.md) for the full reference, including targeted Rust suites, the TypeScript contract-drift check, and the browser e2e suite.

## Quick Start

Run the backend:

```bash
cargo run -p distill-portal-backend
```

Run the frontend (from `apps/frontend/`):

```bash
bun install
bun run dev
```

Then open the frontend address. By default:

- backend: `http://127.0.0.1:4000`
- frontend (Vite dev server): `http://127.0.0.1:4100`

See `docs/dev-commands.md` for the full list of frontend and test commands.

exec
/bin/bash -lc "sed -n '1,220p' docs/dev-commands.md" in /home/huwei/ai_codings/distill-portal
exec
/bin/bash -lc "sed -n '1,220p' docs/dependency-rules.md" in /home/huwei/ai_codings/distill-portal
exec
/bin/bash -lc "sed -n '1,220p' docs/README.md" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
# Dependency Rules

## Allowed Directions

- `apps/backend` may depend on:
  - `components/collector-runtime`
  - `components/configuration`
  - `components/ingest-service`
  - `components/observability`
  - `components/raw-session-store`
  - `components/ui-api-contracts`
- `apps/frontend` (Bun + Vite + React + TypeScript) may depend on:
  - `components/ui-api-contracts/bindings/*.ts` (generated TypeScript types; one-way import only)
  - external npm packages managed via Bun for UI, HTTP fetch, and test tooling. Phase 4 adopted a strict 2-package escape-hatch budget for browser-only concerns native APIs cannot meet; one slot is consumed (`focus-trap-react@^11`, fixing a native `<dialog>` Tab-cycling gap), one slot remains reserved (`@tanstack/react-virtual`, gated on documented evidence that pagination plus `useMemo` cannot keep render time bounded). Adding a third escape-hatch package requires a fresh planning round.
  - all backend communication is over HTTP to `apps/backend`
- `components/ingest-service` may depend on:
  - `components/collector-runtime`
  - `components/raw-session-store`
- `components/raw-session-store` may depend on:
  - `components/ui-api-contracts`
- `components/collector-runtime` may depend on:
  - `components/ui-api-contracts`
- `components/configuration`, `components/observability`, and `components/ui-api-contracts` should not depend on app crates

## Forbidden Directions

- `apps/frontend` must not depend on:
  - `components/collector-runtime`
  - `components/ingest-service`
  - `components/raw-session-store`
- No component crate may depend on `apps/backend` or `apps/frontend`
- `apps/backend` must not render end-user HTML
- `apps/frontend` must not reach into storage or ingest internals directly

## Contract Handling

- Shared page and JSON payloads live in `components/ui-api-contracts`
- `components/ui-api-contracts/src/lib.rs` is the single source of truth for the contract. The TypeScript declarations under `components/ui-api-contracts/bindings/` are checked-in generated artifacts derived from `src/lib.rs` by the `ts-bindings` cargo feature — they are the canonical downstream output and must stay in sync with the Rust source: regenerate and commit them together with any Rust contract change, never hand-edited
- The Bun frontend consumes contract types via one-way TypeScript imports from `components/ui-api-contracts/bindings/*.ts`, using the `@contracts/*` path alias wired in `apps/frontend/tsconfig.json` (or a direct relative path). The frontend MUST NOT re-declare contract types by hand; the thin barrel at `apps/frontend/src/lib/contracts.ts` re-exports the generated types for internal consumption
- If a backend JSON shape changes, update:
  - `components/ui-api-contracts/src/lib.rs`
  - `components/ui-api-contracts/bindings/*.ts` (regenerate via `cargo test -p distill-portal-ui-api-contracts --features ts-bindings -- --ignored regenerate_ts_bindings`; see `docs/dev-commands.md`)
  - `apps/backend/src/http_api.rs`
  - the typed API layer in `apps/frontend/src/lib/` (`api.ts`, `contracts.ts`)
  - any impacted frontend rendering under `apps/frontend/src/App.tsx`, the unified session feature at `apps/frontend/src/features/sessions/`, or the shared React primitives under `apps/frontend/src/components/`
  - the relevant tests in `apps/backend/tests/http_api.rs`, `apps/frontend/src/App.test.tsx`, `apps/frontend/e2e/inspection.spec.ts`, and `tests/e2e/tests/inspection_surface.rs`

## Layer Ownership

- Storage writes and reads belong to `components/raw-session-store`
- Replace-on-sync ingest rules belong to `components/ingest-service`
- Source discovery and parsing belong to `components/collector-runtime`
- Environment variable mapping belongs to `components/configuration`
- HTTP routes belong to the app crates only

## Frontend Dev-Time Topology

- `apps/frontend/` is a Bun-managed package (`package.json`, `bun.lock`, `vite.config.ts`, `index.html`, `src/*.tsx`).
- Frontend dev-time proxying of `/api/v1/**` and `/health` to the backend belongs in `apps/frontend/vite.config.ts`, not in any application code path.
- The Bun app must continue to honor the frontend-boundary rules above: no direct dependency on `components/collector-runtime`, `components/ingest-service`, or `components/raw-session-store`; all backend communication goes over HTTP to the Rust backend.

### Phase 5 M2a notes — Fraunces self-hosted as static asset

Phase 5 introduces a single self-hosted variable serif (Fraunces) for the Archive-room display layer (empty-pane preface, future M5 chapter-break labels, future app title). The font ships as two `.woff2` files at `apps/frontend/public/fonts/Fraunces-subset.woff2` (Roman) and `apps/frontend/public/fonts/Fraunces-Italic-subset.woff2` (Italic), referenced by two `@font-face` blocks in `apps/frontend/src/styles/tokens.css`. Per Phase 5 §Resolved Decision #15, **a self-hosted font file is a static asset, not a runtime dependency** — the 2-package escape-hatch budget (currently 1/2 consumed by `focus-trap-react@^11`) is **not affected**. No bundler plugin, font-loading library, or JS dep is involved; Vite serves the woff2 files directly from `public/`. To regenerate (only when upstream Fraunces revs, ~yearly per Google Fonts cadence): download both variable fonts from `https://github.com/undercasetype/Fraunces` (`fonts/variable/Fraunces[SOFT,WONK,opsz,wght].ttf` + the Italic counterpart), run `fonttools varLib.instancer <input.ttf> SOFT=50 WONK=0 -o <pinned.ttf>` to drop the design-time-only SOFT and WONK axes (saves ~70 KB per file), then `pyftsubset <pinned.ttf> --output-file=apps/frontend/public/fonts/Fraunces-subset.woff2 --unicodes='U+0020-007E,U+00A0-00FF,U+2010-2027,U+2030-2052' --layout-features='kern,liga,smcp,onum' --flavor=woff2 --desubroutinize`. Target file size: ~80 KB per file. Reversibility: deleting both `@font-face` blocks AND both woff2 files yields a still-cohesive system-serif aesthetic (Charter / Iowan Old Style / Georgia at ~99% of Fraunces's visual size via the `size-adjust` override, no broken layout).

 succeeded in 0ms:
# Dev Commands

## Run The Backend

```bash
cargo run -p distill-portal-backend
```

Useful backend env vars:

- `DISTILL_PORTAL_BACKEND_BIND` or `DISTILL_PORTAL_BIND`
- `DISTILL_PORTAL_DATA_DIR`
- `DISTILL_PORTAL_POLL_INTERVAL_SECS`
- `DISTILL_PORTAL_CLAUDE_ROOTS`
- `DISTILL_PORTAL_CODEX_ROOTS`

## Run The Frontend

The frontend lives under `apps/frontend/` as a Bun-managed package (`package.json`, `bun.lock`, `vite.config.ts`, `index.html`, `src/*.tsx`).

Commands (run from `apps/frontend/`):

```bash
bun install
bun run dev
bun run build
bun run test
```

- `bun run dev` starts the Vite dev server on `http://127.0.0.1:4100` with `strictPort: true` so a port collision fails fast.
- `bun run build` writes static assets to `apps/frontend/dist/`.
- `bun run test` runs `bun test src` (the unit suite; browser e2e is `bun run test:e2e`, documented below). After Phase 4 the unit suite is 231 tests / 891 expect calls across 17 files covering the unified session list, the session detail drawer, pagination, the toast queue, and the shared React primitives:
  - **Mounted-`App` integration** — `apps/frontend/src/App.test.tsx`: the read-only unified-list render against three `Promise.allSettled` GETs, the rescan and import mutation flows (including an explicit race-window reproducer for the stale-selection bug), per-panel independent-error behavior, the four documented empty-state branches, the click-time intersection rule under filter and pagination races, the toast queue replacing the old inline `lastReport`, and the cross-page bulk-select / retry-after-intervening-rescan invariants.
  - **Unified session feature** — `apps/frontend/src/features/sessions/`: `SessionsTable.test.tsx`, `SessionFilters.test.tsx`, `SessionDetail.test.tsx` (component-level integration), and pure helpers `mergeSessions.test.ts`, `filterSessions.test.ts`, `applyPagination.test.ts`, `relativeTime.test.ts`, `useSessionFilters.test.ts` (incl. 12+ corrupt-input fallbacks), `useToastQueue.test.ts`, `lastRescan.test.ts`, and the streaming `rawPreview.test.ts` (line-cap + byte-cap + abort + non-JSON fallback).
  - **Shared React primitives** — `apps/frontend/src/components/`: `ActionBar.test.tsx` (idle zero/non-zero selection, pending rescan/import disabled states, sticky modifier, last-rescan caption, hidden-by-filter caption + clear affordances), `Drawer.test.tsx` (native `<dialog>` shell, focus restoration, Esc-close, backdrop close), `Pagination.test.tsx` (page-size selector + Prev/Next + caption + clamp), `Toast.test.tsx` (kind variants + Retry + Dismiss roles), `ScanErrorsCallout.test.tsx` (renders nothing when empty).
  - The variant-matrix `StatusBadge.test.tsx` from M2 was retired in M6 (Chunk G); the four-variant DOM-shape contract is preserved by a dedicated test in `SessionsTable.test.tsx` (`SessionsTable: inlined status badge renders all 4 variants ...`) that iterates over `up_to_date / not_stored / outdated / source_missing` and asserts the inlined `<span class="badge {variant}">{label}</span>` markup byte-for-byte for each variant. `App.test.tsx` continues to assert the `up-to-date` variant at the integration level.

## Dev Topology

```
browser
   |
   v
http://127.0.0.1:4100  <-- Vite dev server (bun run dev)
   |
   |  /api/v1/**  and  /health   proxied by Vite
   v
http://127.0.0.1:4000  <-- Rust backend (cargo run -p distill-portal-backend)
```

- Backend: `127.0.0.1:4000` (Rust, `cargo run -p distill-portal-backend`).
- Frontend dev server: `127.0.0.1:4100` (Bun + Vite, `bun run dev` under `apps/frontend/`).
- Vite proxies `/api/v1/**` and `/health` to the backend so browser code can use same-origin paths without ad hoc CORS setup. Dev-time proxy config lives in `apps/frontend/vite.config.ts`, not in application code.

## Run Both Together

Terminal 1:

```bash
cargo run -p distill-portal-backend
```

Terminal 2 (from `apps/frontend/`):

```bash
bun run dev
```

Then open `http://127.0.0.1:4100/` in the browser. The Rust backend must be running for `/health` and `/api/v1/**` to resolve through the Vite proxy.

## Browser E2E (Playwright)

The inspection-surface browser e2e lives under `apps/frontend/e2e/` and
drives Chromium against the Bun + Vite dev server, which proxies to a
real Rust backend spawned inside the harness (`e2e/harness/backend.ts`).
The harness uses `Bun.spawn` per the Bun-first rule, so the suite must
be run with Bun as the runtime (`bun --bun x playwright test`, wired
into the `test:e2e` script below).

Commands (run from `apps/frontend/`):

```bash
bun run test:e2e:install   # one-time: install Chromium via Playwright
bun run test:e2e           # spawns the backend, starts Vite, runs Chromium
```

The harness binds the backend to `127.0.0.1:4000` because
`vite.config.ts` proxies `/api/v1/**` and `/health` to that address;
tests run serially (`workers: 1`) so the port is never double-bound.
See `playwright.config.ts` for the full configuration.

## Targeted Tests

```bash
cargo test -p distill-portal-collector-runtime --test parsers
cargo test -p distill-portal-backend --test http_api
cargo test -p distill-portal-e2e --test inspection_surface
```

## TypeScript Contract Bindings

The Rust contract types in `components/ui-api-contracts/src/lib.rs` are the source of truth. TypeScript declarations under `components/ui-api-contracts/bindings/` are generated from them via the `ts-bindings` cargo feature (off by default) and must be regenerated whenever the Rust contract changes.

Verify the checked-in TS files match the current Rust source (fails on drift):

```bash
cargo test -p distill-portal-ui-api-contracts --features ts-bindings
```

Regenerate the checked-in TS files after a contract change:

```bash
cargo test -p distill-portal-ui-api-contracts --features ts-bindings -- --ignored regenerate_ts_bindings
```

See `components/ui-api-contracts/README.md` for the tool-choice rationale and the full list of generated files.

## Workspace Verification

```bash
cargo check --workspace
cargo test --workspace
```

 succeeded in 0ms:
# Distill Portal Docs

Start here when you need to orient yourself in the Distill Portal workspace. The backend is Rust under `apps/backend`; the frontend is Bun + Vite + React + TypeScript under `apps/frontend`.

This file is both the docs entry point and the repo map.

## Start With Your Task

| I want to... | Start here |
| --- | --- |
| Modify the inspection UI | [`playbooks/modify-frontend-page.md`](playbooks/modify-frontend-page.md), [`../apps/frontend/README.md`](../apps/frontend/README.md) |
| Change a backend API or JSON payload | [`playbooks/modify-backend-api.md`](playbooks/modify-backend-api.md), [`dependency-rules.md`](dependency-rules.md) |
| Change session storage behavior | [`playbooks/modify-session-store.md`](playbooks/modify-session-store.md) |
| Run or test locally | [`dev-commands.md`](dev-commands.md) |
| Check dependency boundaries | [`dependency-rules.md`](dependency-rules.md) |
| Understand a feature | [`features/inspection-surface.md`](features/inspection-surface.md), [`features/session-store.md`](features/session-store.md) |
| Understand product + architecture intent | [`../PRD.md`](../PRD.md), [`../ARCHITECTURE.md`](../ARCHITECTURE.md) |
| Audit historical planning or delivery | [`../working/README.md`](../working/README.md), [`../progress/README.md`](../progress/README.md) |

## Apps

- `apps/backend` — Rust backend binary: runtime wiring, scan/ingest/store orchestration, machine-consumable HTTP routes. See [`../apps/backend/README.md`](../apps/backend/README.md).
- `apps/frontend` — Bun + Vite + React + TypeScript inspection UI. The unified session list (table, filters, view, drawer body, merge / filter / sort / paginate / persistence helpers) lives under `apps/frontend/src/features/sessions/`; shared React primitives (action bar, scan-errors callout, native-`<dialog>`-backed drawer shell, pagination strip, toast) live under `apps/frontend/src/components/`. CSS is co-located: each `.tsx`/`.ts` imports its sibling `.css` (no CSS Modules, no CSS-in-JS, no Tailwind); `src/styles/` carries only the three global sheets `reset.css`, `tokens.css`, `global.css`. See [`../apps/frontend/README.md`](../apps/frontend/README.md).

## Components

Reusable, app-independent crates. Each owns its own README:

- [`collector-runtime`](../components/collector-runtime/README.md) — source discovery, safe JSONL reads, tool-specific parsers (Claude Code, Codex)
- [`configuration`](../components/configuration/README.md) — backend runtime config loading
- [`ingest-service`](../components/ingest-service/README.md) — content-addressed ingest decisions, replace-on-sync
- [`observability`](../components/observability/README.md) — tracing subscriber bootstrap
- [`raw-session-store`](../components/raw-session-store/README.md) — SQLite metadata + blob-store persistence
- [`ui-api-contracts`](../components/ui-api-contracts/README.md) — shared HTTP payload types; source of truth for the Rust → TS bindings

## Feature Guides

- [`features/inspection-surface.md`](features/inspection-surface.md) — page ownership, API touchpoints, and tests for the inspection workflow
- [`features/session-store.md`](features/session-store.md) — storage ownership, backend touchpoints, and tests for persisted session behavior

## Playbooks

Short recipes for common changes:

- [`playbooks/modify-frontend-page.md`](playbooks/modify-frontend-page.md) — shortest safe path for UI changes
- [`playbooks/modify-backend-api.md`](playbooks/modify-backend-api.md) — shortest safe path for backend API changes
- [`playbooks/modify-session-store.md`](playbooks/modify-session-store.md) — shortest safe path for storage changes

## Verification Map

| Surface | Command |
| --- | --- |
| Workspace compile | `cargo check --workspace` |
| Full Rust test suite | `cargo test --workspace` |
| Backend HTTP API | `cargo test -p distill-portal-backend --test http_api` |
| Collector parsers | `cargo test -p distill-portal-collector-runtime --test parsers` |
| Typed Rust-client HTTP smoke | `cargo test -p distill-portal-e2e --test inspection_surface` |
| TypeScript contract bindings freshness | `cargo test -p distill-portal-ui-api-contracts --features ts-bindings` |
| Frontend unit tests | `bun run test` (from `apps/frontend/`) |
| Browser e2e (Playwright) | `bun run test:e2e` (from `apps/frontend/`) |

## Repo Map

- `apps/backend` — backend binary and its tests
- `apps/frontend` — Bun/React frontend
- `components/*` — reusable implementation crates
- `docs/` — this tree (dependency rules, commands, features, playbooks)
- `tests/e2e` — real HTTP-boundary Rust integration tests
- `tests/fixtures` — sample Claude Code and Codex sessions used by parser and backend tests
- `working/` — historical planning specs for completed phases (see [`../working/README.md`](../working/README.md))
- `progress/` — historical delivery logs for completed phases (see [`../progress/README.md`](../progress/README.md))

## Historical Material

- [`../working/`](../working/) — frozen planning specs. Not current-state instructions. See [`working/README.md`](../working/README.md).
- [`../progress/`](../progress/) — frozen delivery logs. Not current-state instructions. See [`progress/README.md`](../progress/README.md).

  → Gather relevant artifact/spec/code line references
  • Verify the five round-1 fixes directly
  • Recompute WCAG contrast for changed pairs
  • Check for approval-blocking new defects or missing evidence
  • Return exact round-2 review structure
codex
I’ve finished the repo rules pass. Now I’m pulling the relevant artifact lines for colors, motion, tabpanel mounting, and focusability so the audit trail can cite exact locations.
exec
/bin/bash -lc "rg -n \"line\\.text|marker|color-warn|color-ink-muted|R03|RawTab|tabIndex|tabindex|tabpanel|animation|transition|activeTab|key=|space-5|space-10|space-12|space-4|space-8|dataset\\.populated|repopulatePanelContent|activate\\(\" working/phase-5/designs/m2b-shell/prototype.html" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
33: * page-turn fade are the three M2b-active animations and they all zero
37: * The two-layer animation pattern documented in design.md §5:
38: *   <SessionView key={selectedRowKey}>      ← 200 ms page-turn fade
45: *     <div role="tabpanel" hidden={!isActive}
46: *          style={{ animation: isActive
49: *          tabIndex={isActive && (id === "skim" || id === "transcript") ? 0 : undefined}>
50: *       {/* STABLE content — NOT keyed on activeTab.
51: *           Cross-fade fires when style.animation changes from
61:   * previous artifact draft introduced --space-5/10/12 which would have
67:  --space-4: 1rem;
69:  --space-8: 2rem;
77:  --color-ink-muted:      oklch(45% 0.02 70);
84:  --color-warn:    oklch(58% 0.15 60);
96:  --color-text-muted: var(--color-ink-muted);
128:    --color-ink-muted:      oklch(70% 0.01 70);
135:    --color-warn:    oklch(72% 0.13 60);
143:  --color-ink-muted:      oklch(70% 0.01 70);
150:  --color-warn:    oklch(72% 0.13 60);
155: * animation/transition. The prototype mirrors it via a manual override
160:  animation-duration: 0.01ms !important;
161:  animation-iteration-count: 1 !important;
162:  transition-duration: 0.01ms !important;
167:    animation-duration: 0.01ms !important;
168:    animation-iteration-count: 1 !important;
169:    transition-duration: 0.01ms !important;
195:  padding: var(--space-3) var(--space-4);
200:  color: var(--color-ink-muted);
212:.toolbar h1 .marker {
234:  transition: background-color var(--motion-fast) var(--ease-standard);
269:  padding: var(--space-3) var(--space-4);
277:  color: var(--color-ink-muted);
295:  padding: var(--space-2) var(--space-4);
299:  transition: background-color var(--motion-fast) linear;
319:  color: var(--color-ink-muted);
327:  color: var(--color-ink-muted);
345:   * remounted (in production: <SessionView key={selectedRowKey}>). */
346:  animation: session-page-turn var(--motion-disclosure) var(--ease-out) both;
367:  padding: var(--space-4) var(--space-4);
384:  color: var(--color-ink-muted);
390:  color: var(--color-ink-muted);
419:  color: color-mix(in srgb, var(--color-warn) 70%, var(--color-ink));
420:  background: color-mix(in srgb, var(--color-warn) 15%, var(--color-surface));
421:  border-color: color-mix(in srgb, var(--color-warn) 35%, var(--color-surface));
434:  color: color-mix(in srgb, var(--color-warn) 70%, var(--color-ink));
435:  background: color-mix(in srgb, var(--color-warn) 15%, var(--color-surface));
436:  border-color: color-mix(in srgb, var(--color-warn) 35%, var(--color-surface));
441:  padding: var(--space-2) var(--space-4);
451:  color: var(--color-ink-muted);
478:  padding: 0 var(--space-4);
490:  color: var(--color-ink-muted);
492:  /* Hover/active text-color changes are INSTANT — no transition.
498:  color: color-mix(in srgb, var(--color-ink) 80%, var(--color-ink-muted));
538:  transition:
546:.tabpanels {
552:[role="tabpanel"] {
557:  padding: var(--space-6) var(--space-4);  /* --space-5 not in token scale; --space-4 (16 px) is the closest fit */
562:/* Cross-fade-IN — driven by an inline `animation` style toggled on
563: * the active tabpanel from `none` → `tab-fade-in 120ms var(--ease-out) both`.
565: * forbids remounting the panel content, so we use animation-name
566: * re-trigger instead of React `key={activeTab}` remount. The browser
567: * starts the keyframes whenever the inline animation property string
580:  column-gap: var(--space-4);
588:  color: var(--color-ink-muted);
605:  color: var(--color-ink-muted);
615:  color: var(--color-ink-muted);
656:  color: var(--color-ink-muted);
670:  color: var(--color-ink-muted);
679:  padding-top: var(--space-4);
690:   * animation is not on the spec's motion budget at
711:  margin-bottom: var(--space-4);
716:  color: var(--color-ink-muted);
743:  padding: var(--space-4);
758:.raw-pre .line.text {
759:  color: var(--color-ink-muted);
762:.raw-pre .line.text .marker {
763:  /* Phase 4-equivalent: muted-italic marker on the non-JSON fallback
766:   * The .line.text parent already cascades color: var(--color-ink-muted)
767:   * + font-style: italic; the marker just adjusts size + spacing. */
775:  color: var(--color-ink-muted);
788:  color: var(--color-ink-muted);
817:  color: var(--color-ink-muted);
829:  color: var(--color-ink-muted);
833:  padding: var(--space-8) 0;
844:  color: var(--color-ink-muted);
855:  <h1><span class="marker">M2b</span>SessionView shell + Tabs primitive</h1>
879:      <div class="row" role="listitem" aria-current="true" data-row-key="row-active">
930:      <button id="tab-transcript" role="tab" aria-controls="panel-transcript" aria-selected="false" tabindex="-1">Transcript</button>
931:      <button id="tab-skim"       role="tab" aria-controls="panel-skim"       aria-selected="false" tabindex="-1">Skim</button>
932:      <button id="tab-raw"        role="tab" aria-controls="panel-raw"        aria-selected="false" tabindex="-1">Raw</button>
933:      <button id="tab-metadata"   role="tab" aria-controls="panel-metadata"   aria-selected="true"  tabindex="0">Metadata</button>
939:         on activeTab); the cross-fade-IN is driven by toggling an inline
940:         `style.animation` from `none` → `tab-fade-in 120ms var(--ease-out) both`
941:         on the `<div role="tabpanel">` itself. See activate() below.
943:         tabIndex={0} is added by the activate() function to the active
947:    <div class="tabpanels">
948:      <div id="panel-transcript" role="tabpanel" aria-labelledby="tab-transcript" hidden>
949:        <div class="tabpanel-content"></div>
951:      <div id="panel-skim" role="tabpanel" aria-labelledby="tab-skim" hidden>
952:        <div class="tabpanel-content"></div>
954:      <div id="panel-raw" role="tabpanel" aria-labelledby="tab-raw" hidden>
955:        <div class="tabpanel-content"></div>
957:      <div id="panel-metadata" role="tabpanel" aria-labelledby="tab-metadata">
958:        <div class="tabpanel-content"></div>
970: *     - .../RawTab.tsx
976: *     - Cross-fade re-fire on activation via animation-name re-trigger
979: *     - tabIndex=0 on active Skim + Transcript panels (placeholder
1007:    placeIndicator(activeTabId);
1023:  let activeTabId = 'metadata';                   // M2b default per spec
1024:  const visited = new Set([activeTabId]);          // lazy-on-first-activation
1030:  function activate(id, { focus = true } = {}) {
1032:    activeTabId = id;
1036:    // ARIA + tabindex
1040:      t.setAttribute('tabindex', isActive ? '0' : '-1');
1043:    // Panel hidden + lazy mount + cross-fade re-fire via animation-name toggle.
1046:    // then toggling the panel's inline `animation` style between `none` and
1056:        // a11y: inactive panels never carry tabindex.
1057:        p.removeAttribute('tabindex');
1059:        const content = p.querySelector('.tabpanel-content');
1060:        if (content && !content.dataset.populated) content.replaceChildren();
1061:        // animation must be `none` while inactive so the next activation
1062:        // re-fires the keyframe via the property-string transition.
1063:        p.style.animation = 'none';
1069:      const content = p.querySelector('.tabpanel-content');
1070:      if (content && !content.dataset.populated) {
1072:        content.dataset.populated = 'true';
1075:        // Toggle animation-name to re-fire the cross-fade keyframe.
1076:        // Force a reflow between `none` and the real animation so the
1079:        p.style.animation = 'none';
1082:        p.style.animation = 'tab-fade-in var(--motion-base) var(--ease-out) both';
1083:        // Skim + Transcript placeholder panels need tabIndex=0 (no
1087:          p.setAttribute('tabindex', '0');
1089:          p.removeAttribute('tabindex');
1092:        p.style.animation = 'none';
1093:        p.removeAttribute('tabindex');
1115:    const idx = TAB_IDS.indexOf(activeTabId);
1126:    activate(TAB_IDS[nextIdx]);
1130:    tab.addEventListener('click', () => activate(TAB_IDS[idx]));
1135:   * populated ONCE on first visit (see activate()). The repopulate helper
1137:   * can demo state transitions WITHOUT remounting the panel container — the
1140:  function repopulatePanelContent(id) {
1143:    const content = panel.querySelector('.tabpanel-content');
1147:    content.dataset.populated = 'true';
1269:          rel.style.color = 'var(--color-ink-muted)';
1303:    // Prototype-only state toggle — not part of the production RawTab UI.
1397:            line.textContent = raw;
1400:            line.textContent = raw + ' ';
1402:            m.className = 'marker';
1427:      repopulatePanelContent('raw');
1430:        repopulatePanelContent('raw');
1461:    activate(activeTabId, { focus: false });
1464:    requestAnimationFrame(() => placeIndicator(activeTabId));
1465:    window.addEventListener('resize', () => placeIndicator(activeTabId));

exec
/bin/bash -lc 'rg -n "R03|color-warn|color-ink-muted|surface-raised|OKLCH|oklch|AA|contrast|marker|raw" working/phase-5/designs/m2b-shell/colors.md' in /home/huwei/ai_codings/distill-portal
exec
/bin/bash -lc "sed -n '620,670p' working/phase-5.md && sed -n '160,256p' apps/frontend/src/features/sessions/SessionDetail.tsx && sed -n '143,156p' apps/frontend/src/features/sessions/SessionDetail.css" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
1:# M2b WCAG AA contrast verification
3:This is the contrast table for every visible foreground/background
16:1. Hardcodes the post-fix-up oklch values for every M2a token.
18:   `oklch → oklab → linear sRGB → sRGB → relative luminance`.
19:3. Computes the WCAG 2.1 contrast ratio:
36:## WCAG AA gates
51:| T01 | Active tab label                             | `--color-ink`         | `--color-surface`   | 17.10:1  | 15.52:1  | AAA · normal text   | AA 4.5:1        |
52:| T02 | Inactive tab label                           | `--color-ink-muted`   | `--color-surface`   | 7.04:1   | 7.36:1   | AAA · normal text   | AA 4.5:1        |
53:| T03 | Hover-state tab label *(80/20 mix on inactive)* | `color-mix(in srgb, --color-ink 80%, --color-ink-muted)` | `--color-surface`   | ~13.58:1 | ~12.90:1 | AAA · normal text   | AA 4.5:1        |
59:- Light `--color-accent` is `oklch(55% 0.15 50)` post-M2a fix-up
63:- Dark `--color-accent` is `oklch(65% 0.15 50)`. M2a measured
69:investigate (likely an oklch interpolation drift in a different
76:| H01 | Session title (italic Fraunces, ink)         | `--color-ink`         | `--color-surface`   | 17.10:1  | 15.52:1  | AAA · normal text   | AA 4.5:1        |
77:| H02 | Session title fallback (untitled italic muted) | `--color-ink-muted` | `--color-surface`   | 7.04:1   | 7.36:1   | AAA · normal text   | AA 4.5:1        |
78:| H03 | Tool badge text                              | `--color-ink-muted`   | `--color-surface`   | 7.04:1   | 7.36:1   | AAA · normal text   | AA 4.5:1        |
80:| H05 | Status pill — outdated foreground            | warn 70% mix on ink   | warn 15% mix on surface | 5.43:1 | 7.74:1 | AAA · normal text   | AA 4.5:1        |
81:| H06 | Status pill — up-to-date foreground          | success 70% mix on ink | success 15% mix on surface | 6.69:1 | 6.94:1 | AAA · normal text | AA 4.5:1        |
82:| H07 | Status pill — not-stored foreground          | accent 70% mix on ink | accent 15% mix on surface | 5.95:1 | 6.62:1 | AAA · normal text | AA 4.5:1        |
83:| H08 | Status pill — source-missing foreground      | error 70% mix on ink  | error 15% mix on surface | 6.60:1 | 6.91:1 | AAA · normal text | AA 4.5:1        |
84:| H09 | Conflict badge text (warn-recipe pill)       | warn 70% mix on ink   | warn 15% mix on surface | 5.43:1 | 7.74:1 | AAA · normal text   | AA 4.5:1        |
94:| M01 | `<dt>` field labels (snake_case mono)        | `--color-ink-muted`   | `--color-surface`   | 7.04:1   | 7.36:1   | AAA · normal text   | AA 4.5:1        |
95:| M02 | `<dd>` field values (chrome)                 | `--color-ink`         | `--color-surface`   | 17.10:1  | 15.52:1  | AAA · normal text   | AA 4.5:1        |
96:| M03 | `<dd>` mono field values                     | `--color-ink`         | `--color-surface`   | 17.10:1  | 15.52:1  | AAA · normal text   | AA 4.5:1        |
97:| M04 | "(disagreed during load)" muted note         | `--color-ink-muted`   | `--color-surface`   | 7.04:1   | 7.36:1   | AAA · normal text   | AA 4.5:1        |
98:| M05 | (untitled) muted italic value                | `--color-ink-muted`   | `--color-surface`   | 7.04:1   | 7.36:1   | AAA · normal text   | AA 4.5:1        |
99:| M06 | Source path text (mono)                      | `--color-ink`         | `--color-surface`   | 17.10:1  | 15.52:1  | AAA · normal text   | AA 4.5:1        |
100:| M07 | Copy path button text                        | `--color-ink`         | `--color-surface`   | 17.10:1  | 15.52:1  | AAA · normal text   | AA 4.5:1        |
102:| M09 | Copy hint italic text                        | `--color-ink-muted`   | `--color-surface`   | 7.04:1   | 7.36:1   | AAA · normal text   | AA 4.5:1        |
103:| M10 | Subagent sidecar badge text                  | `--color-ink-muted`   | `--color-surface`   | 7.04:1   | 7.36:1   | AAA · normal text   | AA 4.5:1        |
105:| M12 | "Open raw" anchor text                       | `--color-accent`      | `--color-surface`   | 4.84:1   | 5.78:1   | passes 4.5:1 AA · normal text | AA 4.5:1 |
106:| M13 | "Open raw" anchor hover underline            | `--color-accent-hover` | `--color-surface`  | (light L=50: ~5.92:1; dark L=72: ~6.74:1) | — | passes AA · normal text | AA 4.5:1 |
107:| M14 | Top-divider hairline (above Open raw)        | `--color-border`      | `--color-surface`   | 1.49:1   | 1.35:1   | not WCAG-gated      | n/a             |
120:| R01 | Mono `<pre>` raw NDJSON lines                | `--color-ink`         | `--color-surface-raised` | 16.13:1 | 14.84:1 | AAA · normal text | AA 4.5:1 |
121:| R02 | Non-JSON fallback line (italic muted)        | `--color-ink-muted`   | `--color-surface-raised` | 6.64:1 | 7.03:1 | AAA · normal text | AA 4.5:1 |
122:| R03 | Non-JSON line marker (muted italic)          | `--color-ink-muted`   | `--color-surface-raised` | 6.64:1   | 7.03:1   | AAA · normal text | AA 4.5:1 |
123:| R04 | Caption (italic muted)                       | `--color-ink-muted`   | `--color-surface`   | 7.04:1   | 7.36:1   | AAA · normal text   | AA 4.5:1        |
124:| R05 | Loading/idle prose                           | `--color-ink-muted`   | `--color-surface`   | 7.04:1   | 7.36:1   | AAA · normal text   | AA 4.5:1        |
125:| R06 | "Not yet imported" prose (muted italic)      | `--color-ink-muted`   | `--color-surface`   | 7.04:1   | 7.36:1   | AAA · normal text   | AA 4.5:1        |
126:| R07 | Error prose                                  | error 70% mix on ink  | `--color-surface`   | (Phase 4 baseline; ~4.62:1 light / ~5.40:1 dark) | — | passes AA · normal text | AA 4.5:1 |
127:| R08 | Retry button text                            | `--color-ink`         | `--color-surface`   | 17.10:1  | 15.52:1  | AAA · normal text   | AA 4.5:1        |
131:R03 is the non-JSON line marker. It sits on
132:`--color-surface-raised` (the `<pre>` block's background) and reuses
133:`--color-ink-muted` — byte-equivalent to Phase 4's
135:which uses `--color-text-muted` (= `--color-ink-muted` post-M2a
136:alias). The marker reads as muted-italic — distinct from the
139:used `--color-warn` here (3.97:1 light → fails AA normal text);
146:| P01 | Placeholder Fraunces label                   | `--color-ink-muted`   | `--color-surface`   | 7.04:1   | 7.36:1   | AAA · normal text   | AA 4.5:1        |
147:| P02 | Placeholder milestone subline (chrome)       | `--color-ink-muted`   | `--color-surface`   | 7.04:1   | 7.36:1   | AAA · normal text   | AA 4.5:1        |
159:The dashed treatment provides redundant cueing at low contrast
167:| Passing AA normal text (≥ 4.5:1)          | 28    | T01–T03, H01–H09, M01–M07, M09, M10, M12, M13, R01, R02, R03, R04–R08, P01, P02 |
180:specifically because L=60 failed AA-as-text. The M2b indicator
183:indicator a comfortable 4.84:1 / 5.78:1 (AA-text quality applied to a
191:  effective contrast below 3:1?"**
198:  full-saturation `--color-accent` line; the contrast ratio against
219:| `--color-surface`        | `oklch(98% 0.01 70)`   | `oklch(15% 0.01 70)`   |
220:| `--color-surface-raised` | `oklch(96% 0.01 70)`   | `oklch(18% 0.01 70)`   |
221:| `--color-ink`            | `oklch(20% 0.02 70)`   | `oklch(92% 0.01 70)`   |
222:| `--color-ink-muted`      | `oklch(45% 0.02 70)`   | `oklch(70% 0.01 70)`   |
223:| `--color-border`         | `oklch(85% 0.01 70)`   | `oklch(28% 0.01 70)`   |
224:| `--color-border-strong`  | `oklch(65% 0.02 70)`   | `oklch(48% 0.02 70)`   |
225:| `--color-accent`         | `oklch(55% 0.15 50)`   | `oklch(65% 0.15 50)`   |
226:| `--color-accent-hover`   | `oklch(50% 0.16 50)`   | `oklch(72% 0.13 50)`   |
227:| `--color-success`        | `oklch(48% 0.13 155)`  | `oklch(64% 0.13 155)`  |
228:| `--color-warn`           | `oklch(58% 0.15 60)`   | `oklch(72% 0.13 60)`   |
229:| `--color-error`          | `oklch(52% 0.18 25)`   | `oklch(68% 0.16 25)`   |

 succeeded in 0ms:
- **Tool badge** in `--font-mono` `--text-xs` to the right of (or below, on narrow) the title. Subtle chip with hairline border, no background.
- **Status pill** (same recipe as the list-row pill).
- **Conflict badge** if `row.statusConflict` ("Source ↔ stored disagreed"). Status warning color recipe.
- **Subagent sidecar badge** if `row.hasSubagentSidecars` ("Has Claude Code subagent sidecars on disk — not ingested in v1"). Muted info recipe.
- **"Last seen source path"** hint inline near sourcePath if `row.sourcePathIsStale`. `--font-mono` `--text-xs` `--color-ink-muted`.
- **Copy-to-clipboard button** for `row.sourcePath`. Quiet icon-less text button "Copy path".
- **"Open raw" anchor** (target=`_blank`, identical href to Phase 4 — `/api/v1/sessions/<storedSessionUid>/raw`). Quiet text link. **Rendered only when `row.storedSessionUid !== null`**; source-only rows replace it with the same "Not yet imported — click Import to fetch raw" copy used in the Raw tab.
- **On narrow viewports**: a "← Back to list" button row above everything else. Quiet text-link styling, not a chunky button — preserves the editorial mood. **"Back to list" only sets `narrowMode = "list"`** (preserves selection + URL + tab state — see §Mobile / narrow viewport). `Esc` is a *different* gesture: it fully clears selection (and resets to list), per §Routing/URL State.

The header carries no surface chrome (no border, no background fill different from `--color-surface`) — it sits above the tab strip's hairline boundary which provides the visual anchor. Consistent with hairline-over-shadow principle.

### Tab strip

Four tabs: **Transcript** / **Skim** / **Raw** / **Metadata** (left-to-right). Default active tab on first selection at Phase 5 close: **Transcript** — revised from the planner's initial "Skim" recommendation.

**Per-milestone default-tab progression** (per Resolved Decision #11):

- **M2 close** → default = **Metadata** (only fully-functional parsed-content-free tab; Skim + Transcript still placeholder)
- **M3 close** → default = **Metadata** (no UI consumer of parsers yet)
- **M4 close** → default shifts to **Transcript** (Transcript becomes functional)
- **M5 close** → default stays **Transcript** (Skim becomes functional but Transcript is the better landing surface until LLM summaries land in a future phase)

The default-tab choice is a single constant `DEFAULT_TAB_ON_SELECTION` exported from `SessionView.tsx`; the M4 shift is a one-line edit + one test update.

Rationale for not landing on Skim: Skim's `user_turn` blocks render the disabled-summary placeholder until LLM summaries land in a future phase; landing a user on a screen full of "Summary disabled — generation deferred" placeholders feels broken at first glance. Transcript shows real content immediately. Skim becomes the value-add when summaries generate.

Tab state is component-local React state, NOT URL-synced (deferred decision #2). The active-tab value lives on `SessionView` and resets to the `DEFAULT_TAB_ON_SELECTION` constant on every `selectedRowKey` change. Per Resolved Decision #11, that constant is `"metadata"` until M4 ships Transcript, then `"transcript"` from M4 onward.

Tab switching does NOT re-fetch raw bytes — the `useParsedSession` cache hit serves the Skim and Transcript tabs; Raw uses its own independent streaming consumer (`rawPreview.ts`); Metadata bypasses parser fetch entirely (it only reads the SessionRow already in `App.tsx` state).

**Panel mounting strategy:** lazy-on-first-activation + keep-mounted-after.

- `SessionView` tracks `visitedTabs: Set<TabId>` in component-local state, initialized to `new Set([defaultTab])` on every `selectedRowKey` change.
- A tab panel is rendered (React-mounted) only if it is the active tab OR has been visited before (i.e., `visitedTabs.has(tabId)`).
- When the user activates a tab for the first time, it is added to `visitedTabs` and remains mounted for the rest of the selection. Inactive-but-visited panels carry the `hidden` attribute (`display: none` semantics + accessibility-tree exclusion).
- This avoids the contradiction of mounting heavy parsed-content tabs (Skim, Transcript) before they're visited — their `useParsedSession` `useEffect` would otherwise fire and trigger the 5 MB full-document fetch even when the user lands on Metadata.
- On `selectedRowKey` change, `visitedTabs` resets and all panels unmount cleanly. The cache (in `useParsedSession`) persists across selection changes, so re-selecting the same session re-mounts a panel that finds a cache hit.

This preserves transcript scroll position + skim disclosure expansion state across tab switches WITHIN a single selection, while ensuring Skim/Transcript do not preemptively fetch when the user only intends to see Metadata or Raw.

The Tabs primitive at `apps/frontend/src/components/Tabs.tsx` implements ARIA `tablist` / `tab` / `tabpanel` with Left/Right keyboard nav, Home/End to first/last, automatic activation (selection follows focus per [WAI-ARIA Authoring Practices](https://www.w3.org/WAI/ARIA/apg/patterns/tabs/)), and `aria-controls` / `aria-labelledby` linkage.

**Visual treatment (Archive-room):** the tab strip is a flush row of text labels separated by 12 px gaps. The active tab carries a 1 px `--color-accent` ink-stroke indicator beneath its label, animated with `transform: translateX` over `--motion-base` (`120ms ease-standard`) so the indicator slides between tabs rather than teleporting. Implementation: a single absolutely-positioned `<span class="indicator">` inside the `<div role="tablist">`, positioned via `transform` driven by an active-tab data attribute (CSS `:has()` selector OR a `data-active-tab` attribute on the tablist). Inactive tab labels use `--color-ink-muted`; active uses `--color-ink` and `font-weight: 600`. No background fills; no rounded pills. Reduced-motion zeroes the slide.

### State handling for parsed-content tabs (Skim + Transcript)

Both Skim and Transcript invoke `useParsedSession(row)` and switch on its state union. Each non-success state has a dedicated panel; the "success" / "truncated" states render the real content per the per-tab specs below.

| State | Panel content |
|-------|---------------|
| `idle` | Not reachable — Skim/Transcript only mount after a row is selected. (If reached: render the empty-pane copy from §Inspection Surface Layout.) |
      <dl className="drawer-meta">
        <dt>session_key</dt>
        <dd className="mono">{row.sourceSessionKey ?? "—"}</dd>

        <dt>session_uid</dt>
        <dd className="mono">{row.storedSessionUid ?? "—"}</dd>

        <dt>row_key</dt>
        <dd className="mono">{row.rowKey}</dd>

        <dt>tool</dt>
        <dd className="mono">{row.tool}</dd>

        <dt>source_session_id</dt>
        <dd className="mono">{row.sourceSessionId}</dd>

        <dt>presence</dt>
        <dd className="mono">{row.presence}</dd>

        <dt>status</dt>
        <dd>
          <span className={`badge ${row.status.replace(/_/g, "-")}`}>
            {row.status.replace(/_/g, " ")}
          </span>
          {row.statusConflict ? (
            <>
              {" "}
              <span className="muted">(disagreed during load)</span>
            </>
          ) : null}
        </dd>

        <dt>status_conflict</dt>
        <dd className="mono">{row.statusConflict ? "true" : "false"}</dd>

        <dt>title</dt>
        <dd>{row.title ?? <span className="muted">(untitled)</span>}</dd>

        <dt>project_path</dt>
        <dd className="mono">{row.projectPath ?? "—"}</dd>

        <dt>{sourcePathLabel.replace(":", "")}</dt>
        <dd>
          <span className="mono drawer-source-path" ref={sourcePathRef}>
            {sourcePath}
          </span>
          {" "}
          <button
            type="button"
            className="drawer-copy-btn"
            onClick={() => {
              void handleCopyPath();
            }}
          >
            Copy path
          </button>
          {copyHint === "copied" ? (
            <>
              {" "}
              <span className="muted drawer-copy-hint">Copied</span>
            </>
          ) : null}
          {copyHint === "fallback" ? (
            <>
              {" "}
              <span className="muted drawer-copy-hint">
                Selected — press Ctrl/Cmd + C to copy
              </span>
            </>
          ) : null}
        </dd>

        <dt>source_path_is_stale</dt>
        <dd className="mono">
          {row.sourcePathIsStale ? "true" : "false"}
        </dd>

        <dt>source_fingerprint</dt>
        <dd className="mono">{row.sourceFingerprint}</dd>

        <dt>has_subagent_sidecars</dt>
        <dd className="mono">
          {row.hasSubagentSidecars ? "true" : "false"}
        </dd>

        <dt>stored_raw_ref</dt>
        <dd className="mono">{row.storedRawRef ?? "—"}</dd>

        <dt>created_at (source clock)</dt>
        <dd>{renderTimestamp(now, row.createdAt)}</dd>

        <dt>source_updated_at (source clock)</dt>
        <dd>{renderTimestamp(now, row.sourceUpdatedAt)}</dd>

        <dt>ingested_at (backend clock)</dt>
        <dd>{renderTimestamp(now, row.ingestedAt)}</dd>
      </dl>
/* Plain-text fallback rows render with the muted text token so the
 * user can visually distinguish lines that failed JSON.parse from
 * normal NDJSON rows. The fallback marker `<span>` reuses the muted
 * tone for the same reason. AA contrast is preserved (text-muted on
 * surface measured at ≥ 4.7:1 in both light and dark — see chunk
 * evidence pack). */
.raw-preview-line.text {
  color: var(--color-text-muted);
}

.raw-preview-fallback-marker {
  font-style: italic;
  color: var(--color-text-muted);
}

exec
/bin/bash -lc 'rg -n "transition|animation|color|border-color|background-color|three new motion|INSTANT|tab-fade|Motion|budget|surface" working/phase-5/designs/m2b-shell/motion.md' in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
4:re-tabulates the spec §Motion budget rows that M2b activates, names
5:each surface, and pins the implementation idiom. Reduced-motion
6:fallback behavior is documented per surface.
8:## M2b's three new motion surfaces
10:The chunk activates three motion surfaces from the spec's §Motion
11:budget at `working/phase-5.md:84-95`. Two of them are signature
12:details (#5 and #2 from §Design Language). **No other M2b surface
13:animates.** The frozen motion budget permits animation only on
14:`transform`, `opacity`, and `background-color` AND only on the
15:specific surfaces listed in the spec table; M2b activates exactly
20:| 1   | **Tab indicator slide** *(signature #5)* | `transform: translateX` + `scaleX` | `--motion-base` (120 ms) | `--ease-standard` | active-tab change                  | CSS `transition: transform`    |
21:| 2   | **Tab panel cross-fade-IN**              | `opacity: 0 → 1`    | `--motion-base` (120 ms) | `--ease-out`             | active-tab change (CSS `animation-name` toggle on `<div role="tabpanel">` from `none` → `tab-fade-in`) | CSS `@keyframes tab-fade-in` + animation-name re-trigger (NO React key remount) |
24:## M2b's INSTANT (non-animated) surfaces
26:These M2b surfaces undergo state changes with NO animation /
27:transition. The change applies on the next paint without
32:| 4   | Tab text color (active ↔ inactive shift) | `--color-ink-muted` → `--color-ink` (bold) | active-tab change |
33:| 5   | Tab text color (hover, inactive only)    | shifts via `color-mix`    | pointer enter            |
36:| 8   | "Open raw" anchor hover underline        | border-bottom color       | pointer enter            |
39:Why instant rather than animated: `color`, `border-color`, and
40:hover `background-color` on these surfaces are **NOT** in the
41:spec's frozen motion budget at `working/phase-5.md:84-95`. The
42:budget is load-bearing per spec line 1100; adding animations on
43:unlisted surfaces requires a coordinator waiver, which M2b does not
45:animated surfaces (#1, #2, #3) carry the chunk's animation budget.
54:on activation. The transition only animates `transform` (compositor-
64:  background: var(--color-accent);
67:  transition: transform var(--motion-base) var(--ease-standard);
84:CSS animation-name re-trigger on the panel's own
93:@keyframes tab-fade-in {
99:The panel toggles its inline `animation` style based on `isActive`:
107:     style={{ animation: isActive
108:                          ? "tab-fade-in 120ms var(--ease-out) both"
114:The browser fires the keyframe each time the inline `animation`
115:property string changes from `"none"` to a real animation. Active →
117:`animation-fill-mode: both` ensures opacity stays at 1 after the
118:animation completes. Outgoing panel hides instantly via the `hidden`
136:  animation: session-page-turn var(--motion-disclosure) var(--ease-out) both;
145:The two surfaces (cross-fade + page-turn) fire on **separate DOM
159:    animation-duration: 0.01ms !important;
160:    animation-iteration-count: 1 !important;
161:    transition-duration: 0.01ms !important;
167:Effect on M2b's surfaces:
169:- **Tab indicator slide**: zeroed transition → indicator teleports to
171:- **Tab cross-fade-IN**: zeroed animation duration → panel snaps to
173:- **Page-turn fade**: zeroed animation duration → new SessionView
175:- **Instant hover + active surfaces (#4–#9)**: unaffected (they
184:- §Motion table entries that M2b activates: row 1 (Tab strip
188:- §Performance budget (lines 114–128): only `transform` + `opacity` +
189:  `background-color` allowed AND only on the surfaces listed in the
191:  (#2, #3) and nothing else. M2b does NOT animate `color`,
192:  `border-color`, or hover `background-color` on any surface.
193:- §Reduced-motion (lines 97–112): all three M2b animations zero out
204:| `--motion-pulse`       | 600 ms (M1a deep-link pulse — NOT M2b's surface, listed for reference) |

exec
/bin/bash -lc "rg -n \"line\\.text|marker|color-warn|color-ink-muted|R03|RawTab|tabIndex|tabindex|tabpanel|animation|transition|activeTab|key=|space-5|space-10|space-12|space-4|space-8|keep|mounted|focusable|54|29a|5\\.1|6\\.1|3\\.4|4\\.5|7\\.2|7\\.4\" working/phase-5/designs/m2b-shell/design.md" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
1:# M2b — Tabs primitive + SessionView shell rewire + SessionMetadata + RawTab + vestigial drawer-trigger collapse
4:Spec frozen at `working/phase-5.md` @ `05467ad` on `main`.
42:   is **STABLE across tab switches** (NOT remounted): the outer
43:   `<div role="tabpanel" hidden={!isActive}>` toggles the `hidden`
45:   is driven by **CSS `animation-name` re-trigger** (set
46:   `animation: tab-fade-in 120ms var(--ease-out) both` when
47:   `isActive=true`; set `animation: none` when `isActive=false`).
55:   **Do NOT use a React `key={activeTab}` on the panel content
57:   abort RawTab's in-flight `consumeRawPreview` consumer and refire
60:   keep-mounted contract (spec lines 650–658). The cross-fade is
61:   driven by CSS animation re-trigger (above), NOT by React key
63:   key={selectedRowKey}>` layer DOES still remount (because a
67:4. **Page-turn fade** — outer `<SessionView key={selectedRowKey}>`
71:   remount fires the animation. This is signature detail #2 from
84:6. **Raw tab body** — `RawTab.tsx` extracted from `SessionDetail.tsx`'s
132:  over 120 ms (no transform). The two animations are distinct in
161:          aria-selected="false" tabindex="-1">Transcript</button>
163:          aria-selected="false" tabindex="-1">Skim</button>
165:          aria-selected="false" tabindex="-1">Raw</button>
167:          aria-selected="true"  tabindex="0">Metadata</button>
177:- **`tabindex` discipline**: exactly one tab carries `tabindex="0"`
178:  at any time (the active one); the other three carry `tabindex="-1"`.
179:  This is the WAI-ARIA APG roving tabindex pattern. Tab from the
189:  `transform` declaration (one property, one transition); the
190:  transition only animates `transform` (compositor-cheap; no layout
201:<article class="session-pane" key={selectedRowKey} aria-label="Session view">
212:  <Tabs activeTab={activeTab} onActivate={setActiveTab}
216:  <div class="tabpanels">
218:              isActive={activeTab === "transcript"}
220:      {/* placeholder — STABLE React subtree, not remounted */}
227:- **`key={selectedRowKey}`** on the outer `<article>` — every
230:- **`activeTab` state lives on `SessionView`** (component-local,
239:  role="tabpanel" hidden={!isActive}>` only when `isVisited`; renders
241:  `animation-name` toggle on the SAME `<div role="tabpanel">`
242:  element: when `isActive=true`, an inline `style={{ animation:
244:  `isActive=false`, `style={{ animation: "none" }}` is applied. The
245:  browser restarts the animation each time the `animation` property
247:  React tree is **NOT remounted** on tab change — keep-mounted state
248:  (RawTab's in-flight consumer, M3+ Skim/Transcript parser results)
249:  survives intact. **Do NOT add `key={activeTab}` to the panel
264:  conflict badge) plus the Tabs primitive plus the four tabpanels
322:### 3.4 RawTab (`apps/frontend/src/features/sessions/RawTab.tsx`)
332:  `row.storedSessionUid === null`. RawTab never fires the fetch when
342:- Non-JSON fallback lines render with the `(non-JSON line)` marker
343:  preserved verbatim from Phase 4 `RawPreviewLineRow`. The marker is
344:  styled **muted-italic** (`color: var(--color-ink-muted)`,
347:  uses `--color-text-muted`, now aliased to `--color-ink-muted` per
348:  M2a Option B). The marker is NOT a warn-color text — codex
351:  dark 7.03:1 — see colors.md row R03).
365:  `--color-ink-muted`. The `<span>` below is `--font-chrome
366:  --text-sm --color-ink-muted`. Centered vertically + horizontally
385:- **Active**: `aria-selected="true"`, `tabindex="0"`,
387:- **Inactive**: `aria-selected="false"`, `tabindex="-1"`,
388:  `--color-ink-muted`, regular weight.
390:  var(--color-ink) 80%, var(--color-ink-muted))`. NO background tint.
396:  transitioning the color (the tab is about to become active anyway).
426:### 4.5 Tab panel cross-fade direction
431:  immediately. NO fade-out animation.
432:- Incoming panel: CSS `animation-name` toggles from `none` →
433:  `tab-fade-in` on the panel's `<div role="tabpanel">` element. The
436:  STABLE — NOT remounted.
440:fights both the `hidden` attribute (display:none can't transition)
442:keeps the panel mounting story simple.
444:**Why animation-name re-trigger and NOT a `key={activeTab}` remount:**
445:the spec mandates that visited panels stay React-mounted (Resolved
446:Decision #12 / spec lines 650–658) so RawTab's `consumeRawPreview`
447:consumer keeps its in-flight stream alive across tab switches and so
448:M3+ `useParsedSession` results aren't recomputed. A `key={activeTab}`
450:tab change, aborting in-flight work and breaking the keep-mounted
451:contract. The CSS animation-name toggle achieves the same fade-IN
465:  `key={selectedRowKey}` is unchanged → no remount → no animation.
471:  key → null transitions).
481:    animation-duration: 0.01ms !important;
482:    animation-iteration-count: 1 !important;
483:    transition-duration: 0.01ms !important;
489:Effect on M2b's animations:
511:| **ready**            | full tab shell (header + Tabs + tabpanels). Default tab = Metadata. |
534:| `idle`         | `<p class="raw-loading">Loading raw preview…</p>` (briefly held; the existing `useEffect` immediately transitions to `loading`) |
554:copy in `--font-chrome --text-sm`. Both `--color-ink-muted`. A 1 px
569:   2. Session header — the title is in an `<h2>`, not focusable.
571:      `<span>`s, also not focusable.
573:      (`tabindex="0"`). Arrow keys cycle through tabs; Tab moves
575:   4. Active panel content: focusable elements within (Copy path
579:   placeholder + Transcript placeholder have no focusable
584:### 5.1 Animatable surfaces in M2b
587:animation ONLY on `transform`, `opacity`, and `background-color`,
595:| Tab panel cross-fade-IN  | `opacity` (0 → 1)                                     | `--motion-base` (120 ms)       | `--ease-out`                 | active-tab change (CSS `animation-name` toggle on `<div role="tabpanel">` from `none` → `tab-fade-in`) |
596:| Page-turn fade           | `opacity` (0 → 1) + `transform: translateX` (4px → 0) | `--motion-disclosure` (200 ms) | `--ease-out`                 | `selectedRowKey` change (re-mount of `<SessionView key={…}>`)                                                          |
603:hover underline are all **INSTANT** — no `transition` declaration,
604:no `animation`. Hover/active state changes apply on the next paint
607:animations not on the table requires a coordinator waiver. None of
613:rule from M1a still zeroes the three listed animations above.
615:### 5.2 The two-layer animation pattern (load-bearing for the chunk)
618:- Spec line 654 mandates `hidden` for inactive-but-visited panels
622:  panels stay React-mounted across tab switches (so RawTab's
623:  in-flight `consumeRawPreview` consumer keeps streaming, and so
627:`display: none` cannot transition to `display: block` smoothly. The
629:`animation-name` toggle on the SAME `<div role="tabpanel">` element
631:**stable** — never remounted on tab change.
633:**Inner cross-fade — non-remount, CSS animation-name re-trigger:**
636:<div role="tabpanel"
640:     style={{ animation: isActive
643:  {/* STABLE React subtree — NOT keyed on activeTab.
644:      RawTab's in-flight stream survives tab flips. */}
649:The browser starts the keyframes whenever the `animation` property
652:inactive → active does (the string transitions from `"none"`). The
665:<article class="session-pane" key={selectedRowKey}>
668:      activeTab — that's the intent of the page-turn gesture. */}
672:The `.session-pane` element's CSS animation runs once per mount via
673:`animation: session-page-turn var(--motion-disclosure) var(--ease-out) both`.
674:React's `key={selectedRowKey}` change fires a fresh mount → the
679:different document). The inner `key={activeTab}` remount on tab
681:preserve panel state). Cross-fade uses CSS animation-name re-trigger;
682:page-turn uses React key remount. The two animations fire on
693:  1 + translateX(0)" without any visible animation.
694:- The indicator's `transition: transform` is also zeroed. On
700:### 6.1 ARIA roles and properties
707:  tabindex={0|-1}>` — each tab. The `<button>` element handles the
710:- `<div role="tabpanel" aria-labelledby={tabId} hidden={!isActive}>`
716:- **`tabindex={0}` on active tabpanels with no focusable first
718:  tab moves focus into the panel; if the panel has no focusable
719:  children, focus needs the panel itself to be focusable, otherwise
721:  - **Skim panel** (placeholder) → `tabindex="0"` while active.
722:  - **Transcript panel** (placeholder) → `tabindex="0"` while active.
723:  - **Metadata panel** → no `tabindex` needed (the Copy path button
724:    + "Open raw" anchor are focusable).
725:  - **Raw panel** → no `tabindex` needed (the Retry button is
726:    focusable in error/non_2xx; the success-state `<pre>` does not
727:    need a focusable container because the user can scroll via the
729:  Inactive panels (`hidden=true`) MUST NOT carry `tabindex="0"`
730:  (they're outside the layout + a11y tree). The tabindex attribute
786:| **Inactive tab label** (`--color-ink-muted`)     | 7.04:1 | 7.36:1 | AAA · normal text |
789:| **Tool-badge text** (`--color-ink-muted`)        | 7.04:1 | 7.36:1 | AAA · normal text |
792:| **Subagent badge text** (`--color-ink-muted`)    | 7.04:1 | 7.36:1 | AAA · normal text |
857:### 7.2 Cross-fade is fade-IN-only
862:- Spec line 654 mandates the `hidden` HTML attribute on inactive-
863:  but-visited panels (which is `display: none`, not transition-able).
868:`hidden` toggle; incoming panel's `<div role="tabpanel">` toggles
869:its inline `style.animation` from `"none"` to `"tab-fade-in 120ms
871:React tree (see §7.4 for the mechanism rationale). Spec line 87
874:animation sense; it mandates the entrance animation. M2b's
880:entrance animation; this resolution preserves spec intent because
913:### 7.4 CSS animation-name re-trigger — chosen mechanism for tab cross-fade
917:require visited panels to stay React-mounted so RawTab's in-flight
918:`consumeRawPreview` consumer keeps streaming, and so M3+
923:- **(A) CSS animation-name re-trigger via inline style** — chosen.
924:  The panel's `<div role="tabpanel">` carries an inline
925:  `style.animation` that toggles between `"none"` (when inactive)
928:  transitions from `none` to a real animation. The panel's React
929:  subtree is **stable** — never remounted. Reduced-motion is
930:  handled by the global rule (`animation-duration: 0.01ms !important`)
939:- **(C) ~~React `key={activeTab}` remount~~** — REJECTED. Remounting
940:  the panel content tree on every tab change aborts RawTab's
943:  defeats Resolved Decision #12's keep-mounted contract. The
954:inline style on every `activeTab` change. This is automatic — the
956:`activeTab` state, so its `isActive` prop changes drive a re-render,
957:which writes the new `style.animation` string. Active → active
959:inactive → active transitions do. Verified by tracing the React
965:"also present in the header — both surfaces keep the affordance for
996:### 8.1 `working/phase-5.md` (frozen at 05467ad)
1027:  + SessionMetadata.test.tsx + RawTab.test.tsx obligations.
1050:  `SessionMetadata.tsx` and `RawTab.tsx`.
1067:3. Roving tabindex: exactly one tab carries `tabindex="0"` at any
1068:   moment (the active one); the other three carry `tabindex="-1"`.
1076:   with `var(--ease-standard)`, transitioning ONLY `transform`. No
1087:    where `x = activeTabRect.left - tablistRect.left` and `width =
1088:    activeTabRect.width`. The 1 px base is load-bearing: without it,
1091:    `transition: transform var(--motion-base) var(--ease-standard)`
1097:    `useLayoutEffect` re-runs on `activeTab` change AND on a
1105:13. Inactive tab text: `var(--color-ink-muted)`, regular weight, same
1108:    `color-mix(in srgb, var(--color-ink) 80%, var(--color-ink-muted))`.
1113:    `working/phase-5.md:935`), `padding: 0 var(--space-4)` (16 px
1116:    `--space-5` does NOT exist in
1118:    is 1/2/3/4/6/8 only). Use `--space-4` for 16 px horizontal
1132:    `animation-fill-mode: both`. The `to` state explicitly sets
1137:20. `activeTab` is component-local React state, initialized to
1141:22. `setActiveTab(id)` → updates `activeTab` AND adds `id` to
1143:23. Both `activeTab` and `visitedTabs` reset to defaults on
1148:    `var(--color-ink-muted)` italic when row.title is null with
1164:27. Each `<div role="tabpanel" id="panel-{id}" aria-labelledby="tab-{id}"
1167:    object-keyed (`key={tabId}`), NEVER index-keyed (`key={index}`)**
1170:    has mounted, its content tree is STABLE — it MUST NOT carry an
1171:    inner `key={activeTab}` (or any other key that flips on tab
1173:    aborts RawTab's in-flight `consumeRawPreview` consumer and (in
1175:    violates Resolved Decision #12's keep-mounted contract. Required
1182:       raw:        <RawTab row={row} />,
1187:         <TabPanel key={id} id={id} isActive={id === activeTab}>
1193:    `key={id}` on the OUTER `<TabPanel>` (the tab id string), NEVER
1194:    `key={index}` from `.map((_, i) => …)` and NEVER `key={activeTab}`
1197:    activated for the first time. `key={activeTab}` on the inner
1198:    content would defeat the keep-mounted contract per the warning
1200:28. The cross-fade-IN runs on the panel's own `<div role="tabpanel">`
1201:    element via a CSS `animation-name` toggle: when `isActive=true`,
1202:    the panel carries `style={{ animation: "tab-fade-in 120ms
1204:    `style={{ animation: "none" }}`. The browser fires the keyframe
1205:    each time the property string transitions from `"none"` to a
1206:    real animation. The keyframe is defined ONCE at module scope:
1210:    subtree is **stable** — NOT remounted on tab change. Suppressed
1212:    **Do NOT use a React `key={activeTab}` (or any per-activation
1213:    key) on the panel content** — that would defeat the keep-mounted
1216:    the `hidden` attribute (display: none); no fade-out animation.
1217:29a. Skim and Transcript tabpanels carry `tabIndex={0}` while active
1218:    (their placeholder content has no focusable children, so without
1220:    entirely). Metadata and Raw tabpanels do NOT carry `tabIndex`
1223:    natively per WAI-ARIA APG). The `tabIndex` toggles in lockstep
1224:    with `hidden`: inactive panels MUST NOT carry `tabIndex={0}`.
1239:    `var(--color-ink-muted)` text, `--font-chrome --text-xs`.
1251:### RawTab (`apps/frontend/src/features/sessions/RawTab.tsx`)
1253:37. RawTab fires the fetch ONLY when `row.storedSessionUid !== null`.
1260:39. RawTab uses `consumeRawPreview` (from `rawPreview.ts`) unchanged.
1271:42. Non-JSON fallback lines render with the `(non-JSON line)` marker
1284:    (`--font-display --text-lg --color-ink-muted`), milestone copy
1285:    below (`--font-chrome --text-sm --color-ink-muted`). Text-align
1286:    center, `padding: var(--space-8) 0` (32 px vertical).
1287:    **Note**: `--space-12` does NOT exist in
1289:    Use `--space-8` for the placeholder padding; if more breathing
1316:    unmounted. M6 deletes both.
1320:54. `bun test src` passes; the new `Tabs.test.tsx` covers
1325:    `tabIndex={0}`; Metadata via the Copy path button; Raw via
1329:    fade animation presence (test for `animation-name` CSS via
1332:    #12: e.g. mount RawTab as the active tab, switch to Metadata,
1333:    switch back to Raw, assert the previous RawTab instance is
1339:57. `RawTab.test.tsx` covers Phase 4 raw-preview behavior verbatim
1383:  needs a React effect for ARIA + tabindex management, so the
1394:The cross-fade fade-IN-only resolution (§7.2) is a direct read of
1395:spec lines 87 + 654 — there's no contradiction, just a precise

exec
/bin/bash -lc 'rg -n "''^'"\\s*--space-|--color-(warn|ink-muted|surface-raised|surface|ink|danger|success|accent)|oklch|color-scheme\" apps/frontend/src/styles/tokens.css working/phase-5/designs/m2b-shell/prototype.html working/phase-5/designs/m2a-tokens -g '*.css' -g '*.html' -g '*.md'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
working/phase-5/designs/m2b-shell/prototype.html:64:  --space-1: 0.25rem;
working/phase-5/designs/m2b-shell/prototype.html:65:  --space-2: 0.5rem;
working/phase-5/designs/m2b-shell/prototype.html:66:  --space-3: 0.75rem;
working/phase-5/designs/m2b-shell/prototype.html:67:  --space-4: 1rem;
working/phase-5/designs/m2b-shell/prototype.html:68:  --space-6: 1.5rem;
working/phase-5/designs/m2b-shell/prototype.html:69:  --space-8: 2rem;
working/phase-5/designs/m2b-shell/prototype.html:73:  /* === COLORS — oklch source-of-truth (light, post-M2a fix-up) === */
working/phase-5/designs/m2b-shell/prototype.html:74:  --color-surface:        oklch(98% 0.01 70);
working/phase-5/designs/m2b-shell/prototype.html:75:  --color-surface-raised: oklch(96% 0.01 70);
working/phase-5/designs/m2b-shell/prototype.html:76:  --color-ink:            oklch(20% 0.02 70);
working/phase-5/designs/m2b-shell/prototype.html:77:  --color-ink-muted:      oklch(45% 0.02 70);
working/phase-5/designs/m2b-shell/prototype.html:78:  --color-border:         oklch(85% 0.01 70);
working/phase-5/designs/m2b-shell/prototype.html:79:  --color-border-strong:  oklch(65% 0.02 70);
working/phase-5/designs/m2b-shell/prototype.html:80:  --color-accent:         oklch(55% 0.15 50);
working/phase-5/designs/m2b-shell/prototype.html:81:  --color-accent-hover:   oklch(50% 0.16 50);
working/phase-5/designs/m2b-shell/prototype.html:83:  --color-success: oklch(48% 0.13 155);
working/phase-5/designs/m2b-shell/prototype.html:84:  --color-warn:    oklch(58% 0.15 60);
working/phase-5/designs/m2b-shell/prototype.html:85:  --color-error:   oklch(52% 0.18 25);
working/phase-5/designs/m2b-shell/prototype.html:94:  --color-bg:         var(--color-surface);
working/phase-5/designs/m2b-shell/prototype.html:95:  --color-text:       var(--color-ink);
working/phase-5/designs/m2b-shell/prototype.html:96:  --color-text-muted: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:123:@media (prefers-color-scheme: dark) {
working/phase-5/designs/m2b-shell/prototype.html:125:    --color-surface:        oklch(15% 0.01 70);
working/phase-5/designs/m2b-shell/prototype.html:126:    --color-surface-raised: oklch(18% 0.01 70);
working/phase-5/designs/m2b-shell/prototype.html:127:    --color-ink:            oklch(92% 0.01 70);
working/phase-5/designs/m2b-shell/prototype.html:128:    --color-ink-muted:      oklch(70% 0.01 70);
working/phase-5/designs/m2b-shell/prototype.html:129:    --color-border:         oklch(28% 0.01 70);
working/phase-5/designs/m2b-shell/prototype.html:130:    --color-border-strong:  oklch(48% 0.02 70);
working/phase-5/designs/m2b-shell/prototype.html:131:    --color-accent:         oklch(65% 0.15 50);
working/phase-5/designs/m2b-shell/prototype.html:132:    --color-accent-hover:   oklch(72% 0.13 50);
working/phase-5/designs/m2b-shell/prototype.html:134:    --color-success: oklch(64% 0.13 155);
working/phase-5/designs/m2b-shell/prototype.html:135:    --color-warn:    oklch(72% 0.13 60);
working/phase-5/designs/m2b-shell/prototype.html:136:    --color-error:   oklch(68% 0.16 25);
working/phase-5/designs/m2b-shell/prototype.html:140:  --color-surface:        oklch(15% 0.01 70);
working/phase-5/designs/m2b-shell/prototype.html:141:  --color-surface-raised: oklch(18% 0.01 70);
working/phase-5/designs/m2b-shell/prototype.html:142:  --color-ink:            oklch(92% 0.01 70);
working/phase-5/designs/m2b-shell/prototype.html:143:  --color-ink-muted:      oklch(70% 0.01 70);
working/phase-5/designs/m2b-shell/prototype.html:144:  --color-border:         oklch(28% 0.01 70);
working/phase-5/designs/m2b-shell/prototype.html:145:  --color-border-strong:  oklch(48% 0.02 70);
working/phase-5/designs/m2b-shell/prototype.html:146:  --color-accent:         oklch(65% 0.15 50);
working/phase-5/designs/m2b-shell/prototype.html:147:  --color-accent-hover:   oklch(72% 0.13 50);
working/phase-5/designs/m2b-shell/prototype.html:149:  --color-success: oklch(64% 0.13 155);
working/phase-5/designs/m2b-shell/prototype.html:150:  --color-warn:    oklch(72% 0.13 60);
working/phase-5/designs/m2b-shell/prototype.html:151:  --color-error:   oklch(68% 0.16 25);
working/phase-5/designs/m2b-shell/prototype.html:178:  background: var(--color-surface);
working/phase-5/designs/m2b-shell/prototype.html:179:  color: var(--color-ink);
working/phase-5/designs/m2b-shell/prototype.html:196:  background: var(--color-surface-raised);
working/phase-5/designs/m2b-shell/prototype.html:200:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:209:  color: var(--color-ink);
working/phase-5/designs/m2b-shell/prototype.html:213:  color: var(--color-accent);
working/phase-5/designs/m2b-shell/prototype.html:229:  background: var(--color-surface);
working/phase-5/designs/m2b-shell/prototype.html:230:  color: var(--color-ink);
working/phase-5/designs/m2b-shell/prototype.html:237:  background: color-mix(in srgb, var(--color-accent) 8%, var(--color-surface));
working/phase-5/designs/m2b-shell/prototype.html:238:  border-color: color-mix(in srgb, var(--color-accent) 35%, var(--color-border));
working/phase-5/designs/m2b-shell/prototype.html:241:  background: color-mix(in srgb, var(--color-ink) 4%, var(--color-surface));
working/phase-5/designs/m2b-shell/prototype.html:244:  outline: 2px solid var(--color-accent);
working/phase-5/designs/m2b-shell/prototype.html:253:  background: var(--color-surface);
working/phase-5/designs/m2b-shell/prototype.html:265:  background: var(--color-surface);
working/phase-5/designs/m2b-shell/prototype.html:271:  background: var(--color-surface-raised);
working/phase-5/designs/m2b-shell/prototype.html:277:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:283:  color: var(--color-ink);
working/phase-5/designs/m2b-shell/prototype.html:302:  background: color-mix(in srgb, var(--color-ink) 4%, transparent);
working/phase-5/designs/m2b-shell/prototype.html:305:  background: color-mix(in srgb, var(--color-accent) 8%, transparent);
working/phase-5/designs/m2b-shell/prototype.html:306:  box-shadow: inset 2px 0 0 var(--color-accent);
working/phase-5/designs/m2b-shell/prototype.html:311:  color: var(--color-ink);
working/phase-5/designs/m2b-shell/prototype.html:319:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:327:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:340:  background: var(--color-surface);
working/phase-5/designs/m2b-shell/prototype.html:348:@media (prefers-color-scheme: dark) {
working/phase-5/designs/m2b-shell/prototype.html:378:  color: var(--color-ink);
working/phase-5/designs/m2b-shell/prototype.html:384:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:390:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:406:  background: var(--color-surface);
working/phase-5/designs/m2b-shell/prototype.html:407:  color: var(--color-ink);
working/phase-5/designs/m2b-shell/prototype.html:414:  color: color-mix(in srgb, var(--color-success) 70%, var(--color-ink));
working/phase-5/designs/m2b-shell/prototype.html:415:  background: color-mix(in srgb, var(--color-success) 15%, var(--color-surface));
working/phase-5/designs/m2b-shell/prototype.html:416:  border-color: color-mix(in srgb, var(--color-success) 35%, var(--color-surface));
working/phase-5/designs/m2b-shell/prototype.html:419:  color: color-mix(in srgb, var(--color-warn) 70%, var(--color-ink));
working/phase-5/designs/m2b-shell/prototype.html:420:  background: color-mix(in srgb, var(--color-warn) 15%, var(--color-surface));
working/phase-5/designs/m2b-shell/prototype.html:421:  border-color: color-mix(in srgb, var(--color-warn) 35%, var(--color-surface));
working/phase-5/designs/m2b-shell/prototype.html:424:  color: color-mix(in srgb, var(--color-accent) 70%, var(--color-ink));
working/phase-5/designs/m2b-shell/prototype.html:425:  background: color-mix(in srgb, var(--color-accent) 15%, var(--color-surface));
working/phase-5/designs/m2b-shell/prototype.html:426:  border-color: color-mix(in srgb, var(--color-accent) 35%, var(--color-surface));
working/phase-5/designs/m2b-shell/prototype.html:429:  color: color-mix(in srgb, var(--color-error) 70%, var(--color-ink));
working/phase-5/designs/m2b-shell/prototype.html:430:  background: color-mix(in srgb, var(--color-error) 15%, var(--color-surface));
working/phase-5/designs/m2b-shell/prototype.html:431:  border-color: color-mix(in srgb, var(--color-error) 35%, var(--color-surface));
working/phase-5/designs/m2b-shell/prototype.html:434:  color: color-mix(in srgb, var(--color-warn) 70%, var(--color-ink));
working/phase-5/designs/m2b-shell/prototype.html:435:  background: color-mix(in srgb, var(--color-warn) 15%, var(--color-surface));
working/phase-5/designs/m2b-shell/prototype.html:436:  border-color: color-mix(in srgb, var(--color-warn) 35%, var(--color-surface));
working/phase-5/designs/m2b-shell/prototype.html:451:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:460:.back-to-list button:hover { color: var(--color-ink); }
working/phase-5/designs/m2b-shell/prototype.html:462:  outline: 2px solid var(--color-accent);
working/phase-5/designs/m2b-shell/prototype.html:490:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:498:  color: color-mix(in srgb, var(--color-ink) 80%, var(--color-ink-muted));
working/phase-5/designs/m2b-shell/prototype.html:501:  color: var(--color-ink);
working/phase-5/designs/m2b-shell/prototype.html:505:  outline: 2px solid var(--color-accent);
working/phase-5/designs/m2b-shell/prototype.html:510:  color: var(--color-ink);
working/phase-5/designs/m2b-shell/prototype.html:534:  background: var(--color-accent);
working/phase-5/designs/m2b-shell/prototype.html:588:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:595:  color: var(--color-ink);
working/phase-5/designs/m2b-shell/prototype.html:605:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:615:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:630:  color: var(--color-ink);
working/phase-5/designs/m2b-shell/prototype.html:636:  background: var(--color-surface);
working/phase-5/designs/m2b-shell/prototype.html:637:  color: var(--color-ink);
working/phase-5/designs/m2b-shell/prototype.html:648:  background: color-mix(in srgb, var(--color-ink) 4%, var(--color-surface));
working/phase-5/designs/m2b-shell/prototype.html:651:  outline: 2px solid var(--color-accent);
working/phase-5/designs/m2b-shell/prototype.html:656:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:670:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:676:/* "Open raw" anchor — quiet text link in --color-accent (4.84:1 / 5.78:1) */
working/phase-5/designs/m2b-shell/prototype.html:683:  color: var(--color-accent);
working/phase-5/designs/m2b-shell/prototype.html:694:  color: var(--color-accent-hover);
working/phase-5/designs/m2b-shell/prototype.html:695:  border-bottom-color: var(--color-accent-hover);
working/phase-5/designs/m2b-shell/prototype.html:698:  outline: 2px solid var(--color-accent);
working/phase-5/designs/m2b-shell/prototype.html:716:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:726:  background: var(--color-surface);
working/phase-5/designs/m2b-shell/prototype.html:729:  color: var(--color-ink);
working/phase-5/designs/m2b-shell/prototype.html:736:  outline: 2px solid var(--color-accent);
working/phase-5/designs/m2b-shell/prototype.html:746:  background: var(--color-surface-raised);
working/phase-5/designs/m2b-shell/prototype.html:750:  color: var(--color-ink);
working/phase-5/designs/m2b-shell/prototype.html:759:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:766:   * The .line.text parent already cascades color: var(--color-ink-muted)
working/phase-5/designs/m2b-shell/prototype.html:775:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:782:  color: color-mix(in srgb, var(--color-error) 70%, var(--color-ink));
working/phase-5/designs/m2b-shell/prototype.html:788:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:795:  background: var(--color-surface);
working/phase-5/designs/m2b-shell/prototype.html:796:  color: var(--color-ink);
working/phase-5/designs/m2b-shell/prototype.html:807:  background: color-mix(in srgb, var(--color-ink) 4%, var(--color-surface));
working/phase-5/designs/m2b-shell/prototype.html:810:  outline: 2px solid var(--color-accent);
working/phase-5/designs/m2b-shell/prototype.html:817:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:829:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:844:  color: var(--color-ink-muted);
working/phase-5/designs/m2b-shell/prototype.html:885:        <span class="row-title">Codex review · M2a oklch ramp</span>
working/phase-5/designs/m2b-shell/prototype.html:1269:          rel.style.color = 'var(--color-ink-muted)';
apps/frontend/src/styles/tokens.css:11: *   - oklch() is the source-of-truth color space (warm-paper light /
apps/frontend/src/styles/tokens.css:13: *     native oklch support. The existing 24 hex literals below form a
apps/frontend/src/styles/tokens.css:14: *     `@supports not (color: oklch(0% 0 0))` fallback layer for older
apps/frontend/src/styles/tokens.css:19: *     mapping to the new canonical names (`--color-ink`,
apps/frontend/src/styles/tokens.css:20: *     `--color-surface`, `--color-ink-muted`, `--font-chrome`). This
apps/frontend/src/styles/tokens.css:45: *   This file declares both the canonical oklch tokens AND the aliases AND
apps/frontend/src/styles/tokens.css:54: *   inside the `@media (prefers-color-scheme: dark)` block + the
apps/frontend/src/styles/tokens.css:63:  /* ===== Color tokens (oklch source-of-truth) ===== */
apps/frontend/src/styles/tokens.css:66:  --color-surface: oklch(98% 0.01 70);
apps/frontend/src/styles/tokens.css:67:  --color-surface-raised: oklch(96% 0.01 70);
apps/frontend/src/styles/tokens.css:68:  --color-ink: oklch(20% 0.02 70);
apps/frontend/src/styles/tokens.css:69:  --color-ink-muted: oklch(45% 0.02 70);
apps/frontend/src/styles/tokens.css:70:  --color-border: oklch(85% 0.01 70);
apps/frontend/src/styles/tokens.css:71:  --color-border-strong: oklch(65% 0.02 70);
apps/frontend/src/styles/tokens.css:72:  --color-accent: oklch(55% 0.15 50);
apps/frontend/src/styles/tokens.css:73:  --color-accent-hover: oklch(50% 0.16 50);
apps/frontend/src/styles/tokens.css:74:  --color-success: oklch(48% 0.13 155);
apps/frontend/src/styles/tokens.css:75:  --color-warn: oklch(58% 0.15 60);
apps/frontend/src/styles/tokens.css:76:  --color-error: oklch(52% 0.18 25);
apps/frontend/src/styles/tokens.css:81:   * does NOT need an oklch counterpart (alpha-on-black is colorspace-
apps/frontend/src/styles/tokens.css:92:  --color-text: var(--color-ink);
apps/frontend/src/styles/tokens.css:93:  --color-text-muted: var(--color-ink-muted);
apps/frontend/src/styles/tokens.css:94:  --color-bg: var(--color-surface);
apps/frontend/src/styles/tokens.css:97:  --space-1: 0.25rem;
apps/frontend/src/styles/tokens.css:98:  --space-2: 0.5rem;
apps/frontend/src/styles/tokens.css:99:  --space-3: 0.75rem;
apps/frontend/src/styles/tokens.css:100:  --space-4: 1rem;
apps/frontend/src/styles/tokens.css:101:  --space-6: 1.5rem;
apps/frontend/src/styles/tokens.css:102:  --space-8: 2rem;
apps/frontend/src/styles/tokens.css:190:/* ===== Dark mode (oklch source-of-truth) =====
apps/frontend/src/styles/tokens.css:196:@media (prefers-color-scheme: dark) {
apps/frontend/src/styles/tokens.css:198:    --color-surface: oklch(15% 0.01 70);
apps/frontend/src/styles/tokens.css:199:    --color-surface-raised: oklch(18% 0.01 70);
apps/frontend/src/styles/tokens.css:200:    --color-ink: oklch(92% 0.01 70);
apps/frontend/src/styles/tokens.css:201:    --color-ink-muted: oklch(70% 0.01 70);
apps/frontend/src/styles/tokens.css:202:    --color-border: oklch(28% 0.01 70);
apps/frontend/src/styles/tokens.css:203:    --color-border-strong: oklch(48% 0.02 70);
apps/frontend/src/styles/tokens.css:204:    --color-accent: oklch(65% 0.15 50);
apps/frontend/src/styles/tokens.css:205:    --color-accent-hover: oklch(72% 0.13 50);
apps/frontend/src/styles/tokens.css:206:    --color-success: oklch(64% 0.13 155);
apps/frontend/src/styles/tokens.css:207:    --color-warn: oklch(72% 0.13 60);
apps/frontend/src/styles/tokens.css:208:    --color-error: oklch(68% 0.16 25);
apps/frontend/src/styles/tokens.css:214: * For browsers without native `oklch()` support (Chromium <111,
apps/frontend/src/styles/tokens.css:226: * the canonical name picks up either the oklch value (modern browsers)
apps/frontend/src/styles/tokens.css:228:@supports not (color: oklch(0% 0 0)) {
apps/frontend/src/styles/tokens.css:231:    --color-surface: #f8f9fb;
apps/frontend/src/styles/tokens.css:232:    --color-surface-raised: #ffffff;
apps/frontend/src/styles/tokens.css:235:    --color-ink: #14161a;
apps/frontend/src/styles/tokens.css:236:    --color-ink-muted: #5a606b;
apps/frontend/src/styles/tokens.css:237:    --color-accent: #2864d4;
apps/frontend/src/styles/tokens.css:238:    --color-accent-hover: #1d4fa8;
apps/frontend/src/styles/tokens.css:239:    --color-success: #1f7a4a;
apps/frontend/src/styles/tokens.css:240:    --color-warn: #b86b07;
apps/frontend/src/styles/tokens.css:244:  @media (prefers-color-scheme: dark) {
apps/frontend/src/styles/tokens.css:247:      --color-surface: #151821;
apps/frontend/src/styles/tokens.css:248:      --color-surface-raised: #1b1f2a;
apps/frontend/src/styles/tokens.css:251:      --color-ink: #e8eaef;
apps/frontend/src/styles/tokens.css:252:      --color-ink-muted: #9098a6;
apps/frontend/src/styles/tokens.css:253:      --color-accent: #6da5ff;
apps/frontend/src/styles/tokens.css:254:      --color-accent-hover: #8bbbff;
apps/frontend/src/styles/tokens.css:255:      --color-success: #5fb68a;
apps/frontend/src/styles/tokens.css:256:      --color-warn: #e0a75a;
apps/frontend/src/styles/tokens.css:311:   * BUT every Chromium/Safari/Firefox build that ships oklch() also
working/phase-5/designs/m2a-tokens/colors.md:11:| `--color-surface`        | revised  | `oklch(98% 0.01 70)`        | `oklch(15% 0.01 70)`          |
working/phase-5/designs/m2a-tokens/colors.md:12:| `--color-surface-raised` | revised  | `oklch(96% 0.01 70)`        | `oklch(18% 0.01 70)`          |
working/phase-5/designs/m2a-tokens/colors.md:13:| `--color-ink`            | NEW      | `oklch(20% 0.02 70)`        | `oklch(92% 0.01 70)`          |
working/phase-5/designs/m2a-tokens/colors.md:14:| `--color-ink-muted`      | NEW      | `oklch(45% 0.02 70)`        | `oklch(70% 0.01 70)`          |
working/phase-5/designs/m2a-tokens/colors.md:15:| `--color-border`         | revised  | `oklch(85% 0.01 70)`        | `oklch(28% 0.01 70)`          |
working/phase-5/designs/m2a-tokens/colors.md:16:| `--color-border-strong`  | revised  | `oklch(70% 0.02 70)`        | `oklch(40% 0.02 70)`          |
working/phase-5/designs/m2a-tokens/colors.md:17:| `--color-accent`         | revised  | `oklch(60% 0.15 50)`        | `oklch(65% 0.15 50)`          |
working/phase-5/designs/m2a-tokens/colors.md:18:| `--color-accent-hover`   | revised  | `oklch(50% 0.16 50)`        | `oklch(72% 0.13 50)`          |
working/phase-5/designs/m2a-tokens/colors.md:19:| `--color-success`        | revised  | `oklch(48% 0.13 155)`       | `oklch(64% 0.13 155)`         |
working/phase-5/designs/m2a-tokens/colors.md:20:| `--color-warn`           | revised  | `oklch(58% 0.15 60)`        | `oklch(72% 0.13 60)`          |
working/phase-5/designs/m2a-tokens/colors.md:21:| `--color-error`          | revised  | `oklch(52% 0.18 25)`        | `oklch(68% 0.16 25)`          |
working/phase-5/designs/m2a-tokens/colors.md:27:| `--color-text`        | `var(--color-ink)`       |
working/phase-5/designs/m2a-tokens/colors.md:28:| `--color-text-muted`  | `var(--color-ink-muted)` |
working/phase-5/designs/m2a-tokens/colors.md:29:| `--color-bg`          | `var(--color-surface)`   |
working/phase-5/designs/m2a-tokens/colors.md:34:These are unchanged from M1a/M1b — they consume `var(--color-accent)`
working/phase-5/designs/m2a-tokens/colors.md:35:or `var(--color-ink)` and retint automatically when the underlying
working/phase-5/designs/m2a-tokens/colors.md:36:token shifts to oklch. The developer does NOT re-author them.
working/phase-5/designs/m2a-tokens/colors.md:40:| `color-mix(in srgb, var(--color-ink) 4%, transparent)`                    | row hover tint (M1a; M1b previously read `--color-text` → aliased) |
working/phase-5/designs/m2a-tokens/colors.md:41:| `color-mix(in srgb, var(--color-accent) 8%, transparent)`                 | selected row tint (resting)                      |
working/phase-5/designs/m2a-tokens/colors.md:42:| `color-mix(in srgb, var(--color-accent) 12%, transparent)`                | selected + hover tint                            |
working/phase-5/designs/m2a-tokens/colors.md:43:| `color-mix(in srgb, var(--color-accent) 22%, transparent)`                | deep-link pulse keyframe peak                    |
working/phase-5/designs/m2a-tokens/colors.md:44:| `color-mix(in srgb, var(--color-success) 15%, var(--color-surface))`      | `.badge.up-to-date` background                   |
working/phase-5/designs/m2a-tokens/colors.md:45:| `color-mix(in srgb, var(--color-success) 70%, var(--color-ink))`          | `.badge.up-to-date` foreground                   |
working/phase-5/designs/m2a-tokens/colors.md:46:| `color-mix(in srgb, var(--color-success) 35%, var(--color-surface))`      | `.badge.up-to-date` border                       |
working/phase-5/designs/m2a-tokens/colors.md:49:The recipes use `in srgb` color space mixing. The new oklch source
working/phase-5/designs/m2a-tokens/colors.md:51:slightly different from a hypothetical `in oklch` mix, but every
working/phase-5/designs/m2a-tokens/colors.md:54:not change `in srgb` to `in oklch`** in M2a — it would silently
working/phase-5/designs/m2a-tokens/colors.md:60:`@supports not (color: oklch(0% 0 0))` fallback layer. The fallback
working/phase-5/designs/m2a-tokens/colors.md:72:All M2a color decisions have dark-mode oklch redefinitions in
working/phase-5/designs/m2a-tokens/colors.md:82:hold under the new oklch ramp (every pill clears AA at 4.5:1 on
working/phase-5/designs/m2a-tokens/colors.md:84:`--color-surface` in light mode (4.7:1) — it sits closest to the
working/phase-5/designs/m2a-tokens/colors.md:86:after oklch CIE2000 conversion, the mitigation is to bump the warn
working/phase-5/designs/m2a-tokens/colors.md:93:`--color-warn` (`#b86b07` light / `#e0a75a` dark in fallback;
working/phase-5/designs/m2a-tokens/colors.md:94:`oklch(58% 0.15 60)` / `oklch(72% 0.13 60)` in oklch). The §7
working/phase-5/designs/m2a-tokens/colors.md:96:`--color-surface` (4.9:1 light / 5.4:1 dark) and the selected-row
working/phase-5/designs/m2a-tokens/colors.md:97:tint (4.6:1 light / 5.1:1 dark) under the new oklch ramp.
working/phase-5/designs/m2a-tokens/design.md:29:aliasing maps the Phase 4 baseline names through to the new oklch
working/phase-5/designs/m2a-tokens/design.md:56:  one-token swap (`--color-accent`) cascading through five `color-mix`
working/phase-5/designs/m2a-tokens/design.md:60:  `oklch(98% 0.01 70)` / `oklch(20% 0.02 70)` — both are warmer by
working/phase-5/designs/m2a-tokens/design.md:65:  black) to `oklch(15% 0.01 70)` (deep warm ink).
working/phase-5/designs/m2a-tokens/design.md:84:  `--color-accent` for the indicator stroke, `--color-ink` /
working/phase-5/designs/m2a-tokens/design.md:85:  `--color-ink-muted` for active vs inactive labels. Every value
working/phase-5/designs/m2a-tokens/design.md:88:  assistant default surface) inherit the new oklch accent
working/phase-5/designs/m2a-tokens/design.md:105:- **One accent, used surgically** — `--color-accent` is sienna in
working/phase-5/designs/m2a-tokens/design.md:112:The full enumeration of every M2a token, the light + dark oklch value,
working/phase-5/designs/m2a-tokens/design.md:126:| `--color-surface`         | `oklch(98% 0.01 70)`       | `oklch(15% 0.01 70)`         | `#f8f9fb` / `#151821`        | revised value; existing token name (Phase 4)                 |
working/phase-5/designs/m2a-tokens/design.md:127:| `--color-surface-raised`  | `oklch(96% 0.01 70)`       | `oklch(18% 0.01 70)`         | `#ffffff` / `#1b1f2a`        | revised value                                                |
working/phase-5/designs/m2a-tokens/design.md:128:| `--color-ink`             | `oklch(20% 0.02 70)`       | `oklch(92% 0.01 70)`         | `#14161a` / `#e8eaef`        | **NEW name** (canonical)                                     |
working/phase-5/designs/m2a-tokens/design.md:129:| `--color-ink-muted`       | `oklch(45% 0.02 70)`       | `oklch(70% 0.01 70)`         | `#5a606b` / `#9098a6`        | **NEW name** (canonical)                                     |
working/phase-5/designs/m2a-tokens/design.md:130:| `--color-border`          | `oklch(85% 0.01 70)`       | `oklch(28% 0.01 70)`         | `#e3e5ea` / `#262b36`        | revised value                                                |
working/phase-5/designs/m2a-tokens/design.md:131:| `--color-border-strong`   | `oklch(65% 0.02 70)`       | `oklch(48% 0.02 70)`         | `#8a909c` / `#5e6571`        | revised value (post fix-up; lifted from L70/L40 for SC 1.4.11) |
working/phase-5/designs/m2a-tokens/design.md:132:| `--color-accent`          | `oklch(55% 0.15 50)`       | `oklch(65% 0.15 50)`         | `#2864d4` / `#6da5ff`        | sienna 70°-hue intent; light L55 post fix-up (was L60; failed AA as text); **fallback is BLUE** (see §6 tradeoff) |
working/phase-5/designs/m2a-tokens/design.md:133:| `--color-accent-hover`    | `oklch(50% 0.16 50)`       | `oklch(72% 0.13 50)`         | `#1d4fa8` / `#8bbbff`        | revised; carryover token name                                |
working/phase-5/designs/m2a-tokens/design.md:134:| `--color-success`         | `oklch(48% 0.13 155)`      | `oklch(64% 0.13 155)`        | `#1f7a4a` / `#5fb68a`        | inherited; oklch transposed for parity                       |
working/phase-5/designs/m2a-tokens/design.md:135:| `--color-warn`            | `oklch(58% 0.15 60)`       | `oklch(72% 0.13 60)`         | `#b86b07` / `#e0a75a`        | inherited; oklch transposed                                  |
working/phase-5/designs/m2a-tokens/design.md:136:| `--color-error`           | `oklch(52% 0.18 25)`       | `oklch(68% 0.16 25)`         | `#b13838` / `#e57d7d`        | inherited; oklch transposed                                  |
working/phase-5/designs/m2a-tokens/design.md:138:| `--color-text`            | `var(--color-ink)`         | —                            | —                            | back-compat → 11 feature CSS files keep working               |
working/phase-5/designs/m2a-tokens/design.md:139:| `--color-text-muted`      | `var(--color-ink-muted)`   | —                            | —                            | back-compat                                                  |
working/phase-5/designs/m2a-tokens/design.md:140:| `--color-bg`              | `var(--color-surface)`     | —                            | —                            | back-compat                                                  |
working/phase-5/designs/m2a-tokens/design.md:317:  - **Add** the canonical color tokens (`--color-ink`,
working/phase-5/designs/m2a-tokens/design.md:318:    `--color-ink-muted`, etc.) with oklch values for light + dark.
working/phase-5/designs/m2a-tokens/design.md:320:    (`--color-text: var(--color-ink); …`) right after the canonical
working/phase-5/designs/m2a-tokens/design.md:323:    new oklch values.
working/phase-5/designs/m2a-tokens/design.md:330:  - **Add** the `@supports not (color: oklch(0% 0 0))` block with
working/phase-5/designs/m2a-tokens/design.md:343:    `@media (prefers-color-scheme: dark) { .session-pane { background-image: none; } }`.
working/phase-5/designs/m2a-tokens/design.md:358:    its `0%` peak (`color-mix(... var(--color-accent) 22% ...)`) and
working/phase-5/designs/m2a-tokens/design.md:359:    `100%` resting (`color-mix(... var(--color-accent) 8% ...)`)
working/phase-5/designs/m2a-tokens/design.md:360:    already consume `var(--color-accent)`, so the new oklch sienna
working/phase-5/designs/m2a-tokens/design.md:373:oklch → linear sRGB → relative luminance pipeline) is authoritative
working/phase-5/designs/m2a-tokens/design.md:378:| `--color-ink` on `--color-surface`                      | 17.10:1| 15.52:1| AAA · normal  |
working/phase-5/designs/m2a-tokens/design.md:379:| `--color-ink-muted` on `--color-surface`                | 7.04:1 | 7.36:1 | AAA · normal  |
working/phase-5/designs/m2a-tokens/design.md:380:| `--color-ink` on `--color-surface-raised`               | 16.13:1| 14.84:1| AAA · normal  |
working/phase-5/designs/m2a-tokens/design.md:381:| `--color-ink` on row hover (4% ink)                     | 15.79:1| 14.56:1| AAA · normal  |
working/phase-5/designs/m2a-tokens/design.md:382:| `--color-ink` on row selected (8% accent)               | 15.36:1| 14.41:1| AAA · normal  |
working/phase-5/designs/m2a-tokens/design.md:383:| `--color-ink` on deep-link pulse peak (22% accent)      | 12.62:1| 11.83:1| AAA · normal  |
working/phase-5/designs/m2a-tokens/design.md:384:| `--color-warn` (informational; no longer text-consumed post round 2) on `--color-surface` | 4.21:1 | 7.68:1 | not consumed as text post round 2 |
working/phase-5/designs/m2a-tokens/design.md:385:| `--color-warn` (informational; no longer text-consumed post round 2) on row selected tint | 3.79:1 | 7.13:1 | not consumed as text post round 2 |
working/phase-5/designs/m2a-tokens/design.md:386:| `.title-cell-refresh` italic (`--color-text-muted`) on `--color-surface` | 7.04:1 | 7.36:1 | AAA · normal |
working/phase-5/designs/m2a-tokens/design.md:388:| `--color-accent` (post fix-up L=55) on `--color-surface`| 4.84:1 | 5.78:1 | AA · normal   |
working/phase-5/designs/m2a-tokens/design.md:389:| `--color-accent` on `--color-surface-raised`            | 4.57:1 | 5.52:1 | AA · normal   |
working/phase-5/designs/m2a-tokens/design.md:397:| `--color-border-strong` (post fix-up) on `--color-surface` | 3.06:1 | 3.00:1 | SC 1.4.11 (cusp dark) |
working/phase-5/designs/m2a-tokens/design.md:398:| `--color-border` on `--color-surface` (decorative)      | 1.49:1 | 1.35:1 | not WCAG-gated |
working/phase-5/designs/m2a-tokens/design.md:404:   `--color-accent` to L=55. Post-fix-up: 3.22:1 — passes.
working/phase-5/designs/m2a-tokens/design.md:405:2. **`--color-accent` as normal text** (`.action-bar-clear`,
working/phase-5/designs/m2a-tokens/design.md:409:3. **`--color-border-strong`** vs `--color-surface`: pre-fix-up light
working/phase-5/designs/m2a-tokens/design.md:418:  real M2a regression (the oklch retint dropped the pair from
working/phase-5/designs/m2a-tokens/design.md:421:  `.title-cell-refresh` color from `var(--color-warn)` to
working/phase-5/designs/m2a-tokens/design.md:426:  Post-fix-up: 7.04:1 light / 7.36:1 dark vs `--color-surface`;
working/phase-5/designs/m2a-tokens/design.md:428:  comfortable margin. The `--color-warn` token itself is unchanged
working/phase-5/designs/m2a-tokens/design.md:429:  (still `oklch(58% 0.15 60)` / `oklch(72% 0.13 60)`); pill + toast-
working/phase-5/designs/m2a-tokens/design.md:449:- On `oklch(98% 0.01 70)` light surface, the wash is just-detectable
working/phase-5/designs/m2a-tokens/design.md:451:- On `oklch(20% 0.02 70)` ink (body text), the wash does NOT intrude
working/phase-5/designs/m2a-tokens/design.md:454:  alpha never exceeds ~0.03 against the `--color-surface` baseline.
working/phase-5/designs/m2a-tokens/design.md:455:- On the Toast surface (which sits on `--color-surface-raised`,
working/phase-5/designs/m2a-tokens/design.md:456:  not `--color-surface`), the wash does NOT apply — only
working/phase-5/designs/m2a-tokens/design.md:461:In dark mode `--color-surface` is `oklch(15% 0.01 70)` — already a
working/phase-5/designs/m2a-tokens/design.md:485:@media (prefers-color-scheme: dark) {
working/phase-5/designs/m2a-tokens/design.md:511:`--color-ink` / `--color-surface` names) would have been **cleaner**
working/phase-5/designs/m2a-tokens/design.md:515:(`--color-text: var(--color-ink); --color-text-muted: var(--color-ink-muted); --color-bg: var(--color-surface); --font-sans: var(--font-chrome);`)
working/phase-5/designs/m2a-tokens/design.md:562:Rendering on a browser without `oklch()` support (Chromium <111,
working/phase-5/designs/m2a-tokens/design.md:565:`--color-accent` (`#2864d4`) is blue, not sienna; the deep-link pulse
working/phase-5/designs/m2a-tokens/design.md:580:**Decision: Option A.** Modern Chromium ships oklch (the e2e
working/phase-5/designs/m2a-tokens/design.md:655:- §Design Tokens → §Color tokens, lines 870-885 — oklch ramp.
working/phase-5/designs/m2a-tokens/design.md:664:- §Acceptance Criteria, line 1098 — warm-paper / oklch / Fraunces.
working/phase-5/designs/m2a-tokens/design.md:670:- §Resolved Decisions #16 — oklch source-of-truth + hex fallback.
working/phase-5/designs/m2a-tokens/design.md:685:  (`.title-cell-refresh` was warmed to `--color-warn` in M1b; M2a
working/phase-5/designs/m2a-tokens/design.md:687:  warn-as-text pair regressed under the oklch retint; sticky footer
working/phase-5/designs/m2a-tokens/design.md:699:1. `tokens.css` declares `--color-ink`, `--color-ink-muted`,
working/phase-5/designs/m2a-tokens/design.md:700:   `--color-surface`, `--color-surface-raised`, `--color-border`,
working/phase-5/designs/m2a-tokens/design.md:701:   `--color-border-strong`, `--color-accent`, `--color-accent-hover`
working/phase-5/designs/m2a-tokens/design.md:702:   with light-mode oklch values matching §3.
working/phase-5/designs/m2a-tokens/design.md:703:2. `@media (prefers-color-scheme: dark)` block in `tokens.css`
working/phase-5/designs/m2a-tokens/design.md:704:   re-declares the same tokens with dark-mode oklch values matching §3.
working/phase-5/designs/m2a-tokens/design.md:705:3. The four alias declarations (`--color-text: var(--color-ink);`
working/phase-5/designs/m2a-tokens/design.md:708:4. Status colors (`--color-success`, `--color-warn`, `--color-error`)
working/phase-5/designs/m2a-tokens/design.md:709:   are revised to oklch values per the §3 table; light + dark.
working/phase-5/designs/m2a-tokens/design.md:737:15. `@supports not (color: oklch(0% 0 0))` block in `tokens.css`
working/phase-5/designs/m2a-tokens/design.md:748:    is **unchanged** — it already consumes `var(--color-accent)`,
working/phase-5/designs/m2a-tokens/design.md:749:    so the new oklch sienna retints the keyframe automatically.
working/phase-5/designs/m2a-tokens/design.md:754:    `@media (prefers-color-scheme: dark) { .session-pane { background-image: none; } }`.
working/phase-5/designs/m2a-tokens/design.md:769:    `.toast.error` / `.toast.info` against `--color-surface-raised`
working/phase-5/designs/m2a-tokens/design.md:814:  measurement comes in below 3:1 (oklch interpolation drift), the
working/phase-5/designs/m2a-tokens/motion.md:42:peak (`color-mix(... var(--color-accent) 22% ...)`) and `100%` resting
working/phase-5/designs/m2a-tokens/motion.md:43:(`color-mix(... var(--color-accent) 8% ...)`) consume
working/phase-5/designs/m2a-tokens/motion.md:44:`var(--color-accent)`, so the new oklch sienna retints the keyframe
working/phase-5/designs/m2a-tokens/wcag.md:5:the new oklch ramp. Ratios are now **codex-measured** (not designer-
working/phase-5/designs/m2a-tokens/wcag.md:7:script that converts each oklch value via the canonical
working/phase-5/designs/m2a-tokens/wcag.md:8:oklch → oklab → linear sRGB → sRGB → relative luminance pipeline
working/phase-5/designs/m2a-tokens/wcag.md:21:The values reflect the post-fix-up oklch ramp:
working/phase-5/designs/m2a-tokens/wcag.md:22:- `--color-accent` light = `oklch(55% 0.15 50)` (was `60%` pre-fix-up;
working/phase-5/designs/m2a-tokens/wcag.md:24:- `--color-border-strong` light = `oklch(65% 0.02 70)` (was `70%`;
working/phase-5/designs/m2a-tokens/wcag.md:26:- `--color-border-strong` dark = `oklch(48% 0.02 70)` (was `40%`;
working/phase-5/designs/m2a-tokens/wcag.md:28:- `--color-accent` dark stays at `oklch(65% 0.15 50)` (codex did not
working/phase-5/designs/m2a-tokens/wcag.md:36:| 01 | `--color-ink`                                       | `--color-surface`                                | 17.10:1 | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:37:| 02 | `--color-ink-muted`                                 | `--color-surface`                                | 7.04:1  | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:38:| 03 | `--color-ink`                                       | `--color-surface-raised`                         | 16.13:1 | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:39:| 04 | `--color-ink-muted`                                 | `--color-surface-raised`                         | 6.64:1  | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:40:| 05 | `--color-ink`                                       | row hover (4% ink mix)                           | 15.79:1 | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:41:| 06 | `--color-ink`                                       | row selected (8% accent mix)                     | 15.36:1 | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:42:| 07 | `--color-ink`                                       | row deep-link pulse peak (22% accent mix)        | 12.62:1 | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:43:| 08 | `--color-warn` (no longer used as text — was `.title-cell-refresh`; reverted to `--color-text-muted` in fix-up round 2) | `--color-surface`                                | 4.21:1 (informational) | not consumed as text post round 2 |
working/phase-5/designs/m2a-tokens/wcag.md:44:| 09 | `--color-warn` (informational; not consumed as text post round 2)              | row selected tint                                | 3.79:1 (informational) | not consumed as text post round 2 |
working/phase-5/designs/m2a-tokens/wcag.md:45:| 08b | `.title-cell-refresh` italic (`--color-text-muted`)                  | `--color-surface`                                | 7.04:1  | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:49:| 10 | `--color-accent` (text use, post fix-up L=55)       | `--color-surface`                                | 4.84:1  | AA · normal         |
working/phase-5/designs/m2a-tokens/wcag.md:50:| 10b | `--color-accent` (text use, post fix-up L=55)      | `--color-surface-raised`                         | 4.57:1  | AA · normal         |
working/phase-5/designs/m2a-tokens/wcag.md:55:| 15 | `.toast.success` border (65% mix)                   | `--color-surface-raised`                         | 3.34:1  | SC 1.4.11           |
working/phase-5/designs/m2a-tokens/wcag.md:56:| 16 | `.toast.error` border (65% mix)                     | `--color-surface-raised`                         | 3.53:1  | SC 1.4.11           |
working/phase-5/designs/m2a-tokens/wcag.md:57:| 17 | `.toast.info` border (70% mix, post fix-up)         | `--color-surface-raised`                         | 3.22:1  | SC 1.4.11           |
working/phase-5/designs/m2a-tokens/wcag.md:58:| 18 | `--color-border-strong` (post fix-up L=65)          | `--color-surface`                                | 3.06:1  | SC 1.4.11           |
working/phase-5/designs/m2a-tokens/wcag.md:59:| 19 | `--color-border` (decorative)                       | `--color-surface`                                | 1.49:1  | not-gated           |
working/phase-5/designs/m2a-tokens/wcag.md:60:| 20 | empty-pane preface (display italic on `--color-ink`)| session-pane (surface + 3% noise)                | ~17.0:1 | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:61:| 21 | `--color-ink-muted` (Open detail resting)           | `--color-surface`                                | 7.04:1  | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:67:| 01 | `--color-ink`                                       | `--color-surface`                                | 15.52:1 | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:68:| 02 | `--color-ink-muted`                                 | `--color-surface`                                | 7.36:1  | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:69:| 03 | `--color-ink`                                       | `--color-surface-raised`                         | 14.84:1 | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:70:| 04 | `--color-ink-muted`                                 | `--color-surface-raised`                         | 7.03:1  | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:71:| 05 | `--color-ink`                                       | row hover (4% ink mix)                           | 14.56:1 | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:72:| 06 | `--color-ink`                                       | row selected (8% accent mix)                     | 14.41:1 | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:73:| 07 | `--color-ink`                                       | row deep-link pulse peak (22% accent mix)        | 11.83:1 | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:74:| 08 | `--color-warn`                                      | `--color-surface`                                | 7.68:1  | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:75:| 09 | `--color-warn`                                      | row selected tint                                | 7.13:1  | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:76:| 10 | `--color-accent`                                    | `--color-surface`                                | 5.78:1  | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:77:| 10b | `--color-accent`                                   | `--color-surface-raised`                         | 5.52:1  | AAA · normal        |
working/phase-5/designs/m2a-tokens/wcag.md:82:| 15 | `.toast.success` border (65% mix)                   | `--color-surface-raised`                         | 3.55:1  | SC 1.4.11           |
working/phase-5/designs/m2a-tokens/wcag.md:83:| 16 | `.toast.error` border (65% mix)                     | `--color-surface-raised`                         | 3.60:1  | SC 1.4.11           |
working/phase-5/designs/m2a-tokens/wcag.md:84:| 17 | `.toast.info` border (70% mix)                      | `--color-surface-raised`                         | 3.62:1  | SC 1.4.11           |
working/phase-5/designs/m2a-tokens/wcag.md:85:| 18 | `--color-border-strong` (post fix-up L=48)          | `--color-surface`                                | 3.00:1  | SC 1.4.11 (cusp)    |
working/phase-5/designs/m2a-tokens/wcag.md:86:| 19 | `--color-border`                                    | `--color-surface`                                | 1.35:1  | not-gated           |
working/phase-5/designs/m2a-tokens/wcag.md:93:- **`.title-cell-refresh` consumed `--color-warn` directly as
working/phase-5/designs/m2a-tokens/wcag.md:94:  normal-size text**. The M2a oklch retint regressed the warn-as-text
working/phase-5/designs/m2a-tokens/wcag.md:95:  pair vs `--color-surface` from ~5.3:1 (Phase 4 hex `#b86b07` on
working/phase-5/designs/m2a-tokens/wcag.md:96:  `#ffffff`) to **4.21:1** (`oklch(58% 0.15 60)` on
working/phase-5/designs/m2a-tokens/wcag.md:97:  `oklch(98% 0.005 250)`) — fails AA 4.5:1. Worse against the row-
working/phase-5/designs/m2a-tokens/wcag.md:101:    `var(--color-warn)` to `var(--color-text-muted)` in
working/phase-5/designs/m2a-tokens/wcag.md:108:    "the swap is a single token". The `--color-warn` token itself
working/phase-5/designs/m2a-tokens/wcag.md:109:    stays at `oklch(58% 0.15 60)` (light) / `oklch(72% 0.13 60)`
working/phase-5/designs/m2a-tokens/wcag.md:115:Three codex BLOCKING findings were resolved by adjusting two oklch
working/phase-5/designs/m2a-tokens/wcag.md:118:- **Finding 1**: `.toast.info` border (70% mix(`--color-accent`,
working/phase-5/designs/m2a-tokens/wcag.md:119:  `--color-border`)) vs `--color-surface-raised`.
working/phase-5/designs/m2a-tokens/wcag.md:122:- **Finding 2**: `--color-accent` as normal-size text (`.action-bar-clear`,
working/phase-5/designs/m2a-tokens/wcag.md:126:- **Finding 3**: `--color-border-strong` vs `--color-surface`.
working/phase-5/designs/m2a-tokens/wcag.md:140:  `.title-cell-refresh` from `var(--color-warn)` to
working/phase-5/designs/m2a-tokens/wcag.md:147:  `--color-warn` token itself is unchanged (`oklch(58% 0.15 60)`
working/phase-5/designs/m2a-tokens/wcag.md:148:  light / `oklch(72% 0.13 60)` dark); the warn pill + warn toast
working/phase-5/designs/m2a-tokens/wcag.md:158:  oklch L=48 was codex's recommended target; my measurement reads
working/phase-5/designs/m2a-tokens/wcag.md:162:  `oklch(50% 0.02 70)` (lifts to ~3.18:1). **Round 2 disposition**:
working/phase-5/designs/m2a-tokens/wcag.md:172:(12.62:1 light / 11.83:1 dark for `--color-ink` against the pulse
working/phase-5/designs/m2a-tokens/wcag.md:180:`--color-border` against `--color-surface` lands at 1.49:1 (light) /
working/phase-5/designs/m2a-tokens/wcag.md:198:1. Hardcodes the post-fix-up oklch values for every M2a token.
working/phase-5/designs/m2a-tokens/wcag.md:199:2. Converts each oklch value to sRGB via the canonical CSS Color L4
working/phase-5/designs/m2a-tokens/wcag.md:207:oklch tokens.
working/phase-5/designs/m2a-tokens/wcag.md:209:## Hex fallback contrast (`@supports not (color: oklch(0% 0 0))`)
working/phase-5/designs/m2a-tokens/wcag.md:212:with the oklch fix to keep parity:
working/phase-5/designs/m2a-tokens/wcag.md:218:clears WCAG SC 1.4.11 in addition to the oklch primary layer. The
working/phase-5/designs/m2a-tokens/prototype.html:13: * production: a "tokens" :root block + @media (prefers-color-scheme: dark)
working/phase-5/designs/m2a-tokens/prototype.html:14: * + an `@supports not (color: oklch(0% 0 0))` hex fallback layer. The
working/phase-5/designs/m2a-tokens/prototype.html:17: * spec-canonical names (--color-ink, --color-surface, --motion-pulse).
working/phase-5/designs/m2a-tokens/prototype.html:23:/* ----- 1. Token layer (oklch source-of-truth) ------------------------- */
working/phase-5/designs/m2a-tokens/prototype.html:27:  --space-1: 0.25rem;
working/phase-5/designs/m2a-tokens/prototype.html:28:  --space-2: 0.5rem;
working/phase-5/designs/m2a-tokens/prototype.html:29:  --space-3: 0.75rem;
working/phase-5/designs/m2a-tokens/prototype.html:30:  --space-4: 1rem;
working/phase-5/designs/m2a-tokens/prototype.html:31:  --space-5: 1.25rem;
working/phase-5/designs/m2a-tokens/prototype.html:32:  --space-6: 1.5rem;
working/phase-5/designs/m2a-tokens/prototype.html:33:  --space-8: 2rem;
working/phase-5/designs/m2a-tokens/prototype.html:34:  --space-10: 2.5rem;
working/phase-5/designs/m2a-tokens/prototype.html:35:  --space-12: 3rem;
working/phase-5/designs/m2a-tokens/prototype.html:41:  /* === COLORS — oklch source-of-truth (light) === */
working/phase-5/designs/m2a-tokens/prototype.html:42:  --color-surface:        oklch(98% 0.01 70);
working/phase-5/designs/m2a-tokens/prototype.html:43:  --color-surface-raised: oklch(96% 0.01 70);
working/phase-5/designs/m2a-tokens/prototype.html:44:  --color-ink:            oklch(20% 0.02 70);
working/phase-5/designs/m2a-tokens/prototype.html:45:  --color-ink-muted:      oklch(45% 0.02 70);
working/phase-5/designs/m2a-tokens/prototype.html:46:  --color-border:         oklch(85% 0.01 70);
working/phase-5/designs/m2a-tokens/prototype.html:47:  --color-border-strong:  oklch(70% 0.02 70);
working/phase-5/designs/m2a-tokens/prototype.html:48:  --color-accent:         oklch(60% 0.15 50);
working/phase-5/designs/m2a-tokens/prototype.html:49:  --color-accent-hover:   oklch(50% 0.16 50);
working/phase-5/designs/m2a-tokens/prototype.html:51:  /* Status colors (Phase 4 baseline; left in oklch space for parity) */
working/phase-5/designs/m2a-tokens/prototype.html:52:  --color-success: oklch(48% 0.13 155);
working/phase-5/designs/m2a-tokens/prototype.html:53:  --color-warn:    oklch(58% 0.15 60);
working/phase-5/designs/m2a-tokens/prototype.html:54:  --color-error:   oklch(52% 0.18 25);
working/phase-5/designs/m2a-tokens/prototype.html:64:  --color-bg:         var(--color-surface);
working/phase-5/designs/m2a-tokens/prototype.html:65:  --color-text:       var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:66:  --color-text-muted: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:96:/* In production this is `@media (prefers-color-scheme: dark)`; the
working/phase-5/designs/m2a-tokens/prototype.html:100:@media (prefers-color-scheme: dark) {
working/phase-5/designs/m2a-tokens/prototype.html:102:    --color-surface:        oklch(15% 0.01 70);
working/phase-5/designs/m2a-tokens/prototype.html:103:    --color-surface-raised: oklch(18% 0.01 70);
working/phase-5/designs/m2a-tokens/prototype.html:104:    --color-ink:            oklch(92% 0.01 70);
working/phase-5/designs/m2a-tokens/prototype.html:105:    --color-ink-muted:      oklch(70% 0.01 70);
working/phase-5/designs/m2a-tokens/prototype.html:106:    --color-border:         oklch(28% 0.01 70);
working/phase-5/designs/m2a-tokens/prototype.html:107:    --color-border-strong:  oklch(40% 0.02 70);
working/phase-5/designs/m2a-tokens/prototype.html:108:    --color-accent:         oklch(65% 0.15 50);
working/phase-5/designs/m2a-tokens/prototype.html:109:    --color-accent-hover:   oklch(72% 0.13 50);
working/phase-5/designs/m2a-tokens/prototype.html:111:    --color-success: oklch(64% 0.13 155);
working/phase-5/designs/m2a-tokens/prototype.html:112:    --color-warn:    oklch(72% 0.13 60);
working/phase-5/designs/m2a-tokens/prototype.html:113:    --color-error:   oklch(68% 0.16 25);
working/phase-5/designs/m2a-tokens/prototype.html:117:  --color-surface:        oklch(15% 0.01 70);
working/phase-5/designs/m2a-tokens/prototype.html:118:  --color-surface-raised: oklch(18% 0.01 70);
working/phase-5/designs/m2a-tokens/prototype.html:119:  --color-ink:            oklch(92% 0.01 70);
working/phase-5/designs/m2a-tokens/prototype.html:120:  --color-ink-muted:      oklch(70% 0.01 70);
working/phase-5/designs/m2a-tokens/prototype.html:121:  --color-border:         oklch(28% 0.01 70);
working/phase-5/designs/m2a-tokens/prototype.html:122:  --color-border-strong:  oklch(40% 0.02 70);
working/phase-5/designs/m2a-tokens/prototype.html:123:  --color-accent:         oklch(65% 0.15 50);
working/phase-5/designs/m2a-tokens/prototype.html:124:  --color-accent-hover:   oklch(72% 0.13 50);
working/phase-5/designs/m2a-tokens/prototype.html:126:  --color-success: oklch(64% 0.13 155);
working/phase-5/designs/m2a-tokens/prototype.html:127:  --color-warn:    oklch(72% 0.13 60);
working/phase-5/designs/m2a-tokens/prototype.html:128:  --color-error:   oklch(68% 0.16 25);
working/phase-5/designs/m2a-tokens/prototype.html:132: * Browsers without `oklch()` get the Phase 4 baseline palette. The hex
working/phase-5/designs/m2a-tokens/prototype.html:136:@supports not (color: oklch(0% 0 0)) {
working/phase-5/designs/m2a-tokens/prototype.html:139:    --color-surface:        #f8f9fb;
working/phase-5/designs/m2a-tokens/prototype.html:140:    --color-surface-raised: #ffffff;
working/phase-5/designs/m2a-tokens/prototype.html:145:    --color-accent:         #2864d4;
working/phase-5/designs/m2a-tokens/prototype.html:146:    --color-accent-hover:   #1d4fa8;
working/phase-5/designs/m2a-tokens/prototype.html:147:    --color-success:        #1f7a4a;
working/phase-5/designs/m2a-tokens/prototype.html:148:    --color-warn:           #b86b07;
working/phase-5/designs/m2a-tokens/prototype.html:151:  @media (prefers-color-scheme: dark) {
working/phase-5/designs/m2a-tokens/prototype.html:154:      --color-surface:        #151821;
working/phase-5/designs/m2a-tokens/prototype.html:155:      --color-surface-raised: #1b1f2a;
working/phase-5/designs/m2a-tokens/prototype.html:160:      --color-accent:         #6da5ff;
working/phase-5/designs/m2a-tokens/prototype.html:161:      --color-accent-hover:   #8bbbff;
working/phase-5/designs/m2a-tokens/prototype.html:162:      --color-success:        #5fb68a;
working/phase-5/designs/m2a-tokens/prototype.html:163:      --color-warn:           #e0a75a;
working/phase-5/designs/m2a-tokens/prototype.html:215:  background: var(--color-surface);
working/phase-5/designs/m2a-tokens/prototype.html:216:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:248:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:254:  color: var(--color-accent);
working/phase-5/designs/m2a-tokens/prototype.html:262:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:271:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:275: * `prefers-color-scheme` source-of-truth. */
working/phase-5/designs/m2a-tokens/prototype.html:287:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:292:.theme-toggle:hover { border-color: var(--color-accent); }
working/phase-5/designs/m2a-tokens/prototype.html:294:  outline: 2px solid var(--color-accent);
working/phase-5/designs/m2a-tokens/prototype.html:308:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:316:  color: var(--color-accent);
working/phase-5/designs/m2a-tokens/prototype.html:323:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:342:  background: var(--color-surface-raised);
working/phase-5/designs/m2a-tokens/prototype.html:347:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:359:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:366:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:369:.swatch-surface        .chip { background: var(--color-surface); }
working/phase-5/designs/m2a-tokens/prototype.html:370:.swatch-surface-raised .chip { background: var(--color-surface-raised); }
working/phase-5/designs/m2a-tokens/prototype.html:371:.swatch-ink            .chip { background: var(--color-ink); border-color: var(--color-ink); }
working/phase-5/designs/m2a-tokens/prototype.html:372:.swatch-ink-muted      .chip { background: var(--color-ink-muted); border-color: var(--color-ink-muted); }
working/phase-5/designs/m2a-tokens/prototype.html:375:.swatch-accent         .chip { background: var(--color-accent); border-color: var(--color-accent); }
working/phase-5/designs/m2a-tokens/prototype.html:376:.swatch-success        .chip { background: var(--color-success); border-color: var(--color-success); }
working/phase-5/designs/m2a-tokens/prototype.html:377:.swatch-warn           .chip { background: var(--color-warn); border-color: var(--color-warn); }
working/phase-5/designs/m2a-tokens/prototype.html:386:  background: var(--color-surface-raised);
working/phase-5/designs/m2a-tokens/prototype.html:399:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:401:.typo-panel .specimen { color: var(--color-ink); }
working/phase-5/designs/m2a-tokens/prototype.html:417:  background: var(--color-surface-raised);
working/phase-5/designs/m2a-tokens/prototype.html:428:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:434:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:440:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:452:  background: var(--color-surface);
working/phase-5/designs/m2a-tokens/prototype.html:466:  background: var(--color-surface);
working/phase-5/designs/m2a-tokens/prototype.html:480:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:482:  background: var(--color-surface-raised);
working/phase-5/designs/m2a-tokens/prototype.html:491:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:497:  background: color-mix(in srgb, var(--color-ink) 4%, var(--color-surface-raised));
working/phase-5/designs/m2a-tokens/prototype.html:500:  outline: 2px solid var(--color-accent);
working/phase-5/designs/m2a-tokens/prototype.html:510:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:522:  background: var(--color-surface-raised);
working/phase-5/designs/m2a-tokens/prototype.html:532:  background: var(--color-surface);
working/phase-5/designs/m2a-tokens/prototype.html:533:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:537:  color: color-mix(in srgb, var(--color-accent) 70%, var(--color-ink));
working/phase-5/designs/m2a-tokens/prototype.html:538:  background: color-mix(in srgb, var(--color-accent) 15%, var(--color-surface));
working/phase-5/designs/m2a-tokens/prototype.html:539:  border-color: color-mix(in srgb, var(--color-accent) 35%, var(--color-surface));
working/phase-5/designs/m2a-tokens/prototype.html:562:  background-color: color-mix(in srgb, var(--color-ink) 4%, transparent);
working/phase-5/designs/m2a-tokens/prototype.html:565:  background-color: color-mix(in srgb, var(--color-accent) 8%, transparent);
working/phase-5/designs/m2a-tokens/prototype.html:566:  box-shadow: inset 2px 0 0 var(--color-accent);
working/phase-5/designs/m2a-tokens/prototype.html:569:  background-color: color-mix(in srgb, var(--color-accent) 12%, transparent);
working/phase-5/designs/m2a-tokens/prototype.html:575:  0%   { background-color: color-mix(in srgb, var(--color-accent) 22%, transparent); }
working/phase-5/designs/m2a-tokens/prototype.html:576:  100% { background-color: color-mix(in srgb, var(--color-accent)  8%, transparent); }
working/phase-5/designs/m2a-tokens/prototype.html:584:  background: var(--color-surface);
working/phase-5/designs/m2a-tokens/prototype.html:589:  background: var(--color-accent);
working/phase-5/designs/m2a-tokens/prototype.html:590:  border-color: var(--color-accent);
working/phase-5/designs/m2a-tokens/prototype.html:591:  background-image: linear-gradient(45deg, transparent 35%, var(--color-surface) 36%, var(--color-surface) 50%, transparent 51%);
working/phase-5/designs/m2a-tokens/prototype.html:601:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:614:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:623:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:637:  color: var(--color-warn);
working/phase-5/designs/m2a-tokens/prototype.html:641:/* Status pill — Phase 4 recipe inherited, retinted by oklch tokens */
working/phase-5/designs/m2a-tokens/prototype.html:655:  background: color-mix(in srgb, var(--color-success) 15%, var(--color-surface));
working/phase-5/designs/m2a-tokens/prototype.html:656:  color:      color-mix(in srgb, var(--color-success) 70%, var(--color-ink));
working/phase-5/designs/m2a-tokens/prototype.html:657:  border-color: color-mix(in srgb, var(--color-success) 35%, var(--color-surface));
working/phase-5/designs/m2a-tokens/prototype.html:660:  background: color-mix(in srgb, var(--color-accent) 15%, var(--color-surface));
working/phase-5/designs/m2a-tokens/prototype.html:661:  color:      color-mix(in srgb, var(--color-accent) 70%, var(--color-ink));
working/phase-5/designs/m2a-tokens/prototype.html:662:  border-color: color-mix(in srgb, var(--color-accent) 35%, var(--color-surface));
working/phase-5/designs/m2a-tokens/prototype.html:665:  background: color-mix(in srgb, var(--color-warn) 15%, var(--color-surface));
working/phase-5/designs/m2a-tokens/prototype.html:666:  color:      color-mix(in srgb, var(--color-warn) 70%, var(--color-ink));
working/phase-5/designs/m2a-tokens/prototype.html:667:  border-color: color-mix(in srgb, var(--color-warn) 35%, var(--color-surface));
working/phase-5/designs/m2a-tokens/prototype.html:670:  background: color-mix(in srgb, var(--color-error) 15%, var(--color-surface));
working/phase-5/designs/m2a-tokens/prototype.html:671:  color:      color-mix(in srgb, var(--color-error) 70%, var(--color-ink));
working/phase-5/designs/m2a-tokens/prototype.html:672:  border-color: color-mix(in srgb, var(--color-error) 35%, var(--color-surface));
working/phase-5/designs/m2a-tokens/prototype.html:680:  background: var(--color-surface);
working/phase-5/designs/m2a-tokens/prototype.html:689:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:694:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:699:  background: var(--color-surface);
working/phase-5/designs/m2a-tokens/prototype.html:707:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:710:.action-bar button:hover { border-color: var(--color-accent); }
working/phase-5/designs/m2a-tokens/prototype.html:712:  outline: 2px solid var(--color-accent);
working/phase-5/designs/m2a-tokens/prototype.html:724:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:735:  background: var(--color-surface);
working/phase-5/designs/m2a-tokens/prototype.html:738:   * automatically; the @media (prefers-color-scheme: dark) rule below
working/phase-5/designs/m2a-tokens/prototype.html:744:@media (prefers-color-scheme: dark) {
working/phase-5/designs/m2a-tokens/prototype.html:764:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:773:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:780:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:793:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:800:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:804:  outline: 2px solid var(--color-accent);
working/phase-5/designs/m2a-tokens/prototype.html:811:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:818:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:823:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:835:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:839:  outline: 2px solid var(--color-accent);
working/phase-5/designs/m2a-tokens/prototype.html:852:  background: var(--color-surface-raised);
working/phase-5/designs/m2a-tokens/prototype.html:853:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:859:.toast.success { border-color: color-mix(in srgb, var(--color-success) 65%, var(--color-border)); }
working/phase-5/designs/m2a-tokens/prototype.html:861:.toast.info    { border-color: color-mix(in srgb, var(--color-accent)  65%, var(--color-border)); }
working/phase-5/designs/m2a-tokens/prototype.html:865:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:870:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:886:  background: var(--color-surface-raised);
working/phase-5/designs/m2a-tokens/prototype.html:893:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:900:  background: var(--color-surface);
working/phase-5/designs/m2a-tokens/prototype.html:906:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:910:.motion-card .demo:hover { background: color-mix(in srgb, var(--color-accent) 8%, transparent); }
working/phase-5/designs/m2a-tokens/prototype.html:914:.motion-card .demo.pulse-trigger { background: color-mix(in srgb, var(--color-accent) 8%, transparent); }
working/phase-5/designs/m2a-tokens/prototype.html:921:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:946:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:947:  background: var(--color-surface-raised);
working/phase-5/designs/m2a-tokens/prototype.html:968:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:977:  color: color-mix(in srgb, var(--color-success) 80%, var(--color-ink));
working/phase-5/designs/m2a-tokens/prototype.html:980:  color: color-mix(in srgb, var(--color-warn) 80%, var(--color-ink));
working/phase-5/designs/m2a-tokens/prototype.html:985:.pair-ink-on-surface       { background: var(--color-surface); color: var(--color-ink); }
working/phase-5/designs/m2a-tokens/prototype.html:986:.pair-muted-on-surface     { background: var(--color-surface); color: var(--color-ink-muted); }
working/phase-5/designs/m2a-tokens/prototype.html:987:.pair-ink-on-raised        { background: var(--color-surface-raised); color: var(--color-ink); }
working/phase-5/designs/m2a-tokens/prototype.html:988:.pair-ink-on-hover         { background: color-mix(in srgb, var(--color-ink) 4%, var(--color-surface)); color: var(--color-ink); }
working/phase-5/designs/m2a-tokens/prototype.html:989:.pair-ink-on-selected      { background: color-mix(in srgb, var(--color-accent) 8%, var(--color-surface)); color: var(--color-ink); }
working/phase-5/designs/m2a-tokens/prototype.html:990:.pair-ink-on-pulse-peak    { background: color-mix(in srgb, var(--color-accent) 22%, var(--color-surface)); color: var(--color-ink); }
working/phase-5/designs/m2a-tokens/prototype.html:991:.pair-warn-on-surface      { background: var(--color-surface); color: var(--color-warn); font-style: italic; }
working/phase-5/designs/m2a-tokens/prototype.html:992:.pair-warn-on-selected     { background: color-mix(in srgb, var(--color-accent) 8%, var(--color-surface)); color: var(--color-warn); font-style: italic; }
working/phase-5/designs/m2a-tokens/prototype.html:993:.pair-accent-on-surface    { background: var(--color-surface); color: var(--color-accent); font-weight: 600; }
working/phase-5/designs/m2a-tokens/prototype.html:994:.pair-success-pill         { background: color-mix(in srgb, var(--color-success) 15%, var(--color-surface)); color: color-mix(in srgb, var(--color-success) 70%, var(--color-ink)); }
working/phase-5/designs/m2a-tokens/prototype.html:995:.pair-error-pill           { background: color-mix(in srgb, var(--color-error) 15%, var(--color-surface));   color: color-mix(in srgb, var(--color-error) 70%, var(--color-ink)); }
working/phase-5/designs/m2a-tokens/prototype.html:996:.pair-toast-border-success { background: var(--color-surface-raised); border: 2px solid color-mix(in srgb, var(--color-success) 65%, var(--color-border)); }
working/phase-5/designs/m2a-tokens/prototype.html:997:.pair-toast-border-error   { background: var(--color-surface-raised); border: 2px solid color-mix(in srgb, var(--color-error) 65%, var(--color-border)); }
working/phase-5/designs/m2a-tokens/prototype.html:998:.pair-toast-border-info    { background: var(--color-surface-raised); border: 2px solid color-mix(in srgb, var(--color-accent) 65%, var(--color-border)); }
working/phase-5/designs/m2a-tokens/prototype.html:1000:  background: var(--color-surface);
working/phase-5/designs/m2a-tokens/prototype.html:1003:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:1013:  color: var(--color-ink-muted);
working/phase-5/designs/m2a-tokens/prototype.html:1023:  color: var(--color-ink);
working/phase-5/designs/m2a-tokens/prototype.html:1053:      tokens carried forward. <code>oklch()</code> is the source of truth; the
working/phase-5/designs/m2a-tokens/prototype.html:1054:      <code>@supports not (color: oklch(0% 0 0))</code> block below restores the
working/phase-5/designs/m2a-tokens/prototype.html:1061:        <dt>--color-surface</dt>           <dd>oklch(98% 0.01 70)</dd>
working/phase-5/designs/m2a-tokens/prototype.html:1062:        <dt>dark</dt>                       <dd>oklch(15% 0.01 70)</dd>
working/phase-5/designs/m2a-tokens/prototype.html:1069:        <dt>--color-surface-raised</dt>     <dd>oklch(96% 0.01 70)</dd>
working/phase-5/designs/m2a-tokens/prototype.html:1070:        <dt>dark</dt>                       <dd>oklch(18% 0.01 70)</dd>
working/phase-5/designs/m2a-tokens/prototype.html:1077:        <dt>--color-ink</dt>                <dd>oklch(20% 0.02 70)</dd>
working/phase-5/designs/m2a-tokens/prototype.html:1078:        <dt>dark</dt>                       <dd>oklch(92% 0.01 70)</dd>
working/phase-5/designs/m2a-tokens/prototype.html:1085:        <dt>--color-ink-muted</dt>          <dd>oklch(45% 0.02 70)</dd>
working/phase-5/designs/m2a-tokens/prototype.html:1086:        <dt>dark</dt>                       <dd>oklch(70% 0.01 70)</dd>
working/phase-5/designs/m2a-tokens/prototype.html:1093:        <dt>--color-border</dt>             <dd>oklch(85% 0.01 70)</dd>
working/phase-5/designs/m2a-tokens/prototype.html:1094:        <dt>dark</dt>                       <dd>oklch(28% 0.01 70)</dd>
working/phase-5/designs/m2a-tokens/prototype.html:1101:        <dt>--color-border-strong</dt>      <dd>oklch(70% 0.02 70)</dd>
working/phase-5/designs/m2a-tokens/prototype.html:1102:        <dt>dark</dt>                       <dd>oklch(40% 0.02 70)</dd>
working/phase-5/designs/m2a-tokens/prototype.html:1109:        <dt>--color-accent</dt>             <dd>oklch(60% 0.15 50)</dd>
working/phase-5/designs/m2a-tokens/prototype.html:1110:        <dt>dark</dt>                       <dd>oklch(65% 0.15 50)</dd>
working/phase-5/designs/m2a-tokens/prototype.html:1117:        <dt>--color-success</dt>            <dd>oklch(48% 0.13 155)</dd>
working/phase-5/designs/m2a-tokens/prototype.html:1124:        <dt>--color-warn</dt>               <dd>oklch(58% 0.15 60)</dd>
working/phase-5/designs/m2a-tokens/prototype.html:1131:        <dt>--color-error</dt>              <dd>oklch(52% 0.18 25)</dd>
working/phase-5/designs/m2a-tokens/prototype.html:1161:      <div class="specimen typo-mono" style="color: var(--color-ink-muted)">
working/phase-5/designs/m2a-tokens/prototype.html:1172:      <div class="specimen typo-mono" style="color: var(--color-ink-muted)">
working/phase-5/designs/m2a-tokens/prototype.html:1183:      <div class="specimen typo-mono" style="color: var(--color-ink-muted)">
working/phase-5/designs/m2a-tokens/prototype.html:1377:      <code>--color-surface-raised</code>. Codex caught a ~2.7:1 border at
working/phase-5/designs/m2a-tokens/prototype.html:1379:      threshold under the new oklch ramp.
working/phase-5/designs/m2a-tokens/prototype.html:1441:      are designer-measured against the oklch CIE2000 conversions (light mode).
working/phase-5/designs/m2a-tokens/prototype.html:1458:          <td><code>--color-ink</code> on <code>--color-surface</code></td>
working/phase-5/designs/m2a-tokens/prototype.html:1465:          <td><code>--color-ink-muted</code> on <code>--color-surface</code></td>
working/phase-5/designs/m2a-tokens/prototype.html:1472:          <td><code>--color-ink</code> on <code>--color-surface-raised</code></td>
working/phase-5/designs/m2a-tokens/prototype.html:1479:          <td><code>--color-ink</code> on row hover (4% ink mix)</td>
working/phase-5/designs/m2a-tokens/prototype.html:1486:          <td><code>--color-ink</code> on row selected (8% accent mix)</td>
working/phase-5/designs/m2a-tokens/prototype.html:1493:          <td><code>--color-ink</code> on deep-link pulse peak (22% accent)</td>
working/phase-5/designs/m2a-tokens/prototype.html:1500:          <td><code>--color-warn</code> on <code>--color-surface</code></td>
working/phase-5/designs/m2a-tokens/prototype.html:1507:          <td><code>--color-warn</code> on row selected tint</td>
working/phase-5/designs/m2a-tokens/prototype.html:1514:          <td><code>--color-accent</code> 100% on <code>--color-surface</code></td>
working/phase-5/designs/m2a-tokens/prototype.html:1535:          <td>Toast.success border on <code>--color-surface-raised</code></td>
working/phase-5/designs/m2a-tokens/prototype.html:1542:          <td>Toast.error border on <code>--color-surface-raised</code></td>
working/phase-5/designs/m2a-tokens/prototype.html:1549:          <td>Toast.info border on <code>--color-surface-raised</code></td>
working/phase-5/designs/m2a-tokens/prototype.html:1556:          <td>Hairline (<code>--color-border</code>) on <code>--color-surface</code></td>
working/phase-5/designs/m2a-tokens/prototype.html:1564:          <td><span class="swatch-pair"><span style="background: var(--color-surface); border-top: 2px solid var(--color-border-strong); display: block; padding: 6px 14px; color: var(--color-ink-muted);">sticky-edge anchor</span></span></td>
working/phase-5/designs/m2a-tokens/prototype.html:1590:  // @media(prefers-color-scheme) AND the data-theme override.

codex
The first pass shows the claimed raw-marker, token-scale, and keep-mounted documentation changes are present. I’m now checking the actual CSS/JS contexts, because a motion verdict depends on whether any remaining transitions are on M2b-controlled surfaces or only inherited prototype chrome.
exec
/bin/bash -lc "nl -ba working/phase-5/designs/m2b-shell/prototype.html | sed -n '220,315p'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
   220	}
   221	.toolbar .spacer { flex: 1; }
   222	.toolbar .ctrl {
   223	  display: inline-flex;
   224	  align-items: center;
   225	  gap: var(--space-1);
   226	  padding: var(--space-1) var(--space-3);
   227	  border: 1px solid var(--color-border);
   228	  border-radius: var(--radius-sm);
   229	  background: var(--color-surface);
   230	  color: var(--color-ink);
   231	  font-family: var(--font-chrome);
   232	  font-size: var(--text-xs);
   233	  cursor: pointer;
   234	  transition: background-color var(--motion-fast) var(--ease-standard);
   235	}
   236	.toolbar .ctrl[aria-pressed="true"] {
   237	  background: color-mix(in srgb, var(--color-accent) 8%, var(--color-surface));
   238	  border-color: color-mix(in srgb, var(--color-accent) 35%, var(--color-border));
   239	}
   240	.toolbar .ctrl:hover {
   241	  background: color-mix(in srgb, var(--color-ink) 4%, var(--color-surface));
   242	}
   243	.toolbar .ctrl:focus-visible {
   244	  outline: 2px solid var(--color-accent);
   245	  outline-offset: 2px;
   246	}
   247	
   248	/* ----- Inspection shell (M1a recipe; included here for context) ------- */
   249	.inspection {
   250	  display: grid;
   251	  grid-template-columns: minmax(300px, 380px) 1fr;
   252	  min-height: calc(100vh - 49px);
   253	  background: var(--color-surface);
   254	}
   255	@media (max-width: 899.98px) {
   256	  .inspection { grid-template-columns: 1fr; }
   257	}
   258	
   259	/* ----- List pane (stub; M1a/M1b territory, not M2b — a single selected
   260	 *       row + a few siblings are enough to anchor the prototype) -------- */
   261	.list-pane {
   262	  border-right: 1px solid var(--color-border);
   263	  display: flex;
   264	  flex-direction: column;
   265	  background: var(--color-surface);
   266	  min-inline-size: 0;
   267	}
   268	.list-pane header {
   269	  padding: var(--space-3) var(--space-4);
   270	  border-bottom: 1px solid var(--color-border);
   271	  background: var(--color-surface-raised);
   272	  display: flex;
   273	  align-items: center;
   274	  gap: var(--space-2);
   275	  font-family: var(--font-mono);
   276	  font-size: var(--text-xs);
   277	  color: var(--color-ink-muted);
   278	  text-transform: uppercase;
   279	  letter-spacing: 0.06em;
   280	}
   281	.list-pane header strong {
   282	  font-weight: 600;
   283	  color: var(--color-ink);
   284	  text-transform: none;
   285	  letter-spacing: 0;
   286	  font-family: var(--font-chrome);
   287	  font-size: var(--text-sm);
   288	}
   289	.list-rows { flex: 1; }
   290	.row {
   291	  display: grid;
   292	  grid-template-columns: 1fr 5rem;
   293	  align-items: baseline;
   294	  gap: var(--space-2);
   295	  padding: var(--space-2) var(--space-4);
   296	  border-bottom: 1px solid var(--color-border);
   297	  cursor: pointer;
   298	  min-height: 36px;
   299	  transition: background-color var(--motion-fast) linear;
   300	}
   301	.row:hover {
   302	  background: color-mix(in srgb, var(--color-ink) 4%, transparent);
   303	}
   304	.row[aria-current="true"] {
   305	  background: color-mix(in srgb, var(--color-accent) 8%, transparent);
   306	  box-shadow: inset 2px 0 0 var(--color-accent);
   307	}
   308	.row .row-title {
   309	  font-family: var(--font-chrome);
   310	  font-size: var(--text-sm);
   311	  color: var(--color-ink);
   312	  white-space: nowrap;
   313	  overflow: hidden;
   314	  text-overflow: ellipsis;
   315	}

exec
/bin/bash -lc "nl -ba working/phase-5/designs/m2b-shell/prototype.html | sed -n '1028,1095p'" in /home/huwei/ai_codings/distill-portal
exec
/bin/bash -lc "nl -ba working/phase-5/designs/m2b-shell/prototype.html | sed -n '620,705p'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
  1028	  }
  1029	
  1030	  function activate(id, { focus = true } = {}) {
  1031	    if (!TAB_IDS.includes(id)) return;
  1032	    activeTabId = id;
  1033	    visited.add(id);
  1034	    tablist.setAttribute('data-active-tab', id);
  1035	
  1036	    // ARIA + tabindex
  1037	    tabs.forEach(t => {
  1038	      const isActive = t.id === `tab-${id}`;
  1039	      t.setAttribute('aria-selected', String(isActive));
  1040	      t.setAttribute('tabindex', isActive ? '0' : '-1');
  1041	    });
  1042	
  1043	    // Panel hidden + lazy mount + cross-fade re-fire via animation-name toggle.
  1044	    // Production: the panel's React subtree is STABLE; this prototype mirrors
  1045	    // that by populating the panel content ONCE on first visit (lazy mount),
  1046	    // then toggling the panel's inline `animation` style between `none` and
  1047	    // `tab-fade-in 120ms var(--ease-out) both` so the keyframes re-fire
  1048	    // without disturbing the DOM children. This honors the keep-mounted
  1049	    // contract (Resolved Decision #12 / spec lines 650–658).
  1050	    panels.forEach(p => {
  1051	      const pid = p.id.replace('panel-', '');
  1052	      const shouldRender = visited.has(pid);
  1053	      const isActive = pid === id;
  1054	      if (!shouldRender) {
  1055	        p.hidden = true;
  1056	        // a11y: inactive panels never carry tabindex.
  1057	        p.removeAttribute('tabindex');
  1058	        // unvisited panels stay empty (lazy mount) — content not yet in DOM.
  1059	        const content = p.querySelector('.tabpanel-content');
  1060	        if (content && !content.dataset.populated) content.replaceChildren();
  1061	        // animation must be `none` while inactive so the next activation
  1062	        // re-fires the keyframe via the property-string transition.
  1063	        p.style.animation = 'none';
  1064	        return;
  1065	      }
  1066	      p.hidden = !isActive;
  1067	      // populate ONCE on first visit; subsequent activations leave the
  1068	      // existing content in place (keep-mounted).
  1069	      const content = p.querySelector('.tabpanel-content');
  1070	      if (content && !content.dataset.populated) {
  1071	        content.appendChild(buildPanelBody(pid));
  1072	        content.dataset.populated = 'true';
  1073	      }
  1074	      if (isActive) {
  1075	        // Toggle animation-name to re-fire the cross-fade keyframe.
  1076	        // Force a reflow between `none` and the real animation so the
  1077	        // browser registers the property-string change even when the
  1078	        // previous activation was on the same element type.
  1079	        p.style.animation = 'none';
  1080	        // eslint-disable-next-line no-unused-expressions
  1081	        p.offsetHeight; // reflow
  1082	        p.style.animation = 'tab-fade-in var(--motion-base) var(--ease-out) both';
  1083	        // Skim + Transcript placeholder panels need tabIndex=0 (no
  1084	        // focusable child). Metadata + Raw don't (Copy path / Retry /
  1085	        // Open raw + scrollable <pre> handle focus).
  1086	        if (pid === 'skim' || pid === 'transcript') {
  1087	          p.setAttribute('tabindex', '0');
  1088	        } else {
  1089	          p.removeAttribute('tabindex');
  1090	        }
  1091	      } else {
  1092	        p.style.animation = 'none';
  1093	        p.removeAttribute('tabindex');
  1094	      }
  1095	    });

 succeeded in 0ms:
   620	/* Source-path row + Copy path button + "Last seen" label swap */
   621	.source-path-row {
   622	  display: flex;
   623	  flex-wrap: wrap;
   624	  align-items: baseline;
   625	  gap: var(--space-2);
   626	}
   627	.source-path-row .path {
   628	  font-family: var(--font-mono);
   629	  font-size: var(--text-sm);
   630	  color: var(--color-ink);
   631	  word-break: break-all;
   632	}
   633	.copy-path-btn {
   634	  appearance: none;
   635	  border: 1px solid var(--color-border);
   636	  background: var(--color-surface);
   637	  color: var(--color-ink);
   638	  border-radius: var(--radius-sm);
   639	  padding: 2px var(--space-2);
   640	  font-family: var(--font-chrome);
   641	  font-size: var(--text-xs);
   642	  cursor: pointer;
   643	  /* Hover background change is INSTANT — Copy path button's
   644	   * background-color is not on the spec's motion budget at
   645	   * working/phase-5.md:84-95. */
   646	}
   647	.copy-path-btn:hover {
   648	  background: color-mix(in srgb, var(--color-ink) 4%, var(--color-surface));
   649	}
   650	.copy-path-btn:focus-visible {
   651	  outline: 2px solid var(--color-accent);
   652	  outline-offset: 2px;
   653	}
   654	.copy-hint {
   655	  font-size: var(--text-xs);
   656	  color: var(--color-ink-muted);
   657	  font-style: italic;
   658	}
   659	
   660	/* Subagent sidecar badge — dashed hairline so it reads as informational,
   661	 * not an action chip. Lives inline next to the has_subagent_sidecars row. */
   662	.subagent-badge {
   663	  display: inline-flex;
   664	  align-items: center;
   665	  gap: var(--space-1);
   666	  padding: var(--space-1) var(--space-2);
   667	  border: 1px dashed var(--color-border-strong);
   668	  border-radius: var(--radius-sm);
   669	  background: transparent;
   670	  color: var(--color-ink-muted);
   671	  font-family: var(--font-chrome);
   672	  font-size: var(--text-xs);
   673	  line-height: 1.4;
   674	}
   675	
   676	/* "Open raw" anchor — quiet text link in --color-accent (4.84:1 / 5.78:1) */
   677	.open-raw {
   678	  margin-top: var(--space-6);
   679	  padding-top: var(--space-4);
   680	  border-top: 1px solid var(--color-border);
   681	}
   682	.open-raw a {
   683	  color: var(--color-accent);
   684	  font-family: var(--font-chrome);
   685	  font-size: var(--text-sm);
   686	  text-decoration: none;
   687	  border-bottom: 1px solid transparent;
   688	  padding-bottom: 1px;
   689	  /* Hover underline (border-color) change is INSTANT — `border-color`
   690	   * animation is not on the spec's motion budget at
   691	   * working/phase-5.md:84-95. */
   692	}
   693	.open-raw a:hover {
   694	  color: var(--color-accent-hover);
   695	  border-bottom-color: var(--color-accent-hover);
   696	}
   697	.open-raw a:focus-visible {
   698	  outline: 2px solid var(--color-accent);
   699	  outline-offset: 2px;
   700	  border-radius: 2px;
   701	}
   702	
   703	/* ============================================================================
   704	 *  Raw tab body — caption-driven state machine; mono pre-block for success.
   705	 * =========================================================================== */

exec
/bin/bash -lc "nl -ba working/phase-5/designs/m2b-shell/prototype.html | sed -n '470,545p'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
   470	 *  Tabs primitive — ARIA tablist + 1 px sliding ink-stroke indicator.
   471	 * =========================================================================== */
   472	.tabs {
   473	  position: relative;
   474	  display: flex;
   475	  align-items: stretch;
   476	  gap: var(--space-3);             /* 12 px gap between tabs (spec line 59) */
   477	  height: 2.5rem;                  /* spec line 59 — strip height */
   478	  padding: 0 var(--space-4);
   479	  border-bottom: 1px solid var(--color-border);
   480	}
   481	.tabs [role="tab"] {
   482	  appearance: none;
   483	  border: 0;
   484	  background: transparent;
   485	  cursor: pointer;
   486	  padding: var(--space-2) 0;       /* no horizontal padding — gap handles spacing */
   487	  font-family: var(--font-chrome);
   488	  font-size: var(--text-sm);
   489	  letter-spacing: 0.01em;
   490	  color: var(--color-ink-muted);
   491	  line-height: var(--leading-tight);
   492	  /* Hover/active text-color changes are INSTANT — no transition.
   493	   * The frozen motion budget (working/phase-5.md:84-95) lists only
   494	   * `transform`, `opacity`, and `background-color` on enumerated
   495	   * surfaces. Tab text color is not on that list. */
   496	}
   497	.tabs [role="tab"]:hover {
   498	  color: color-mix(in srgb, var(--color-ink) 80%, var(--color-ink-muted));
   499	}
   500	.tabs [role="tab"][aria-selected="true"] {
   501	  color: var(--color-ink);
   502	  font-weight: 600;
   503	}
   504	.tabs [role="tab"]:focus-visible {
   505	  outline: 2px solid var(--color-accent);
   506	  outline-offset: 2px;
   507	  border-radius: 2px;
   508	}
   509	.tabs [role="tab"]:active {           /* mouse-down feedback */
   510	  color: var(--color-ink);
   511	}
   512	.tabs [role="tab"][aria-disabled="true"] {
   513	  opacity: 0.4;
   514	  cursor: not-allowed;
   515	  pointer-events: none;
   516	}
   517	
   518	/* The single ink-stroke indicator — production version uses
   519	 * transform: translateX driven by data-active-tab. Here the offsets are
   520	 * computed from the active tab's getBoundingClientRect on activation.
   521	 *
   522	 * IMPORTANT: the indicator's intrinsic CSS box is 1×1 px. The JS sets
   523	 * `transform: translateX(${x}px) scaleX(${width}px)` to grow the 1 px
   524	 * base box to the active tab's measured width. Without the explicit
   525	 * `width: 1px` here, an empty absolutely-positioned <span> resolves to
   526	 * `width: auto` → 0, and `scaleX(N) × 0 = 0` would render an invisible
   527	 * indicator. */
   528	.tabs .indicator {
   529	  position: absolute;
   530	  bottom: -1px;                       /* sits ON the bottom hairline */
   531	  left: 0;
   532	  width: 1px;                          /* base extent — required for scaleX growth */
   533	  height: 1px;
   534	  background: var(--color-accent);
   535	  transform: translateX(0) scaleX(0);
   536	  transform-origin: left;
   537	  /* indicator slide — spec line 86: `transform: translateX` 120 ms ease-standard */
   538	  transition:
   539	    transform var(--motion-base) var(--ease-standard);
   540	  pointer-events: none;
   541	}
   542	
   543	/* ============================================================================
   544	 *  Tab panels — lazy mount + hidden-on-inactive-but-visited + cross-fade-IN.
   545	 * =========================================================================== */

exec
/bin/bash -lc "nl -ba working/phase-5/designs/m2b-shell/prototype.html | sed -n '740,815p'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
   740	
   741	.raw-pre {
   742	  margin: 0;
   743	  padding: var(--space-4);
   744	  border: 1px solid var(--color-border);
   745	  border-radius: var(--radius-sm);
   746	  background: var(--color-surface-raised);
   747	  font-family: var(--font-mono);
   748	  font-size: var(--text-xs);
   749	  line-height: var(--leading-comfortable);
   750	  color: var(--color-ink);
   751	  max-height: 16rem;                  /* M4 raw-preview block, M2a literal */
   752	  overflow: auto;
   753	  white-space: pre;
   754	}
   755	.raw-pre .line {
   756	  display: block;
   757	}
   758	.raw-pre .line.text {
   759	  color: var(--color-ink-muted);
   760	  font-style: italic;
   761	}
   762	.raw-pre .line.text .marker {
   763	  /* Phase 4-equivalent: muted-italic marker on the non-JSON fallback
   764	   * line. Codex measured warn-on-surface-raised at 3.97:1 light → fails
   765	   * AA. Muted-on-surface-raised passes (light 6.64:1 / dark 7.03:1).
   766	   * The .line.text parent already cascades color: var(--color-ink-muted)
   767	   * + font-style: italic; the marker just adjusts size + spacing. */
   768	  font-size: var(--text-xs);
   769	  margin-left: var(--space-2);
   770	}
   771	.raw-caption {
   772	  margin: var(--space-2) 0 0;
   773	  font-family: var(--font-chrome);
   774	  font-size: var(--text-xs);
   775	  color: var(--color-ink-muted);
   776	  font-style: italic;
   777	}
   778	.raw-error {
   779	  margin: 0 0 var(--space-3);
   780	  font-family: var(--font-chrome);
   781	  font-size: var(--text-sm);
   782	  color: color-mix(in srgb, var(--color-error) 70%, var(--color-ink));
   783	}
   784	.raw-loading {
   785	  margin: 0;
   786	  font-family: var(--font-chrome);
   787	  font-size: var(--text-sm);
   788	  color: var(--color-ink-muted);
   789	  font-style: italic;
   790	  letter-spacing: 0.01em;
   791	}
   792	.retry-btn {
   793	  appearance: none;
   794	  border: 1px solid var(--color-border-strong);
   795	  background: var(--color-surface);
   796	  color: var(--color-ink);
   797	  border-radius: var(--radius-sm);
   798	  padding: var(--space-1) var(--space-3);
   799	  font-family: var(--font-chrome);
   800	  font-size: var(--text-xs);
   801	  cursor: pointer;
   802	  /* Hover background change is INSTANT — Retry button's
   803	   * background-color is not on the spec's motion budget at
   804	   * working/phase-5.md:84-95. */
   805	}
   806	.retry-btn:hover {
   807	  background: color-mix(in srgb, var(--color-ink) 4%, var(--color-surface));
   808	}
   809	.retry-btn:focus-visible {
   810	  outline: 2px solid var(--color-accent);
   811	  outline-offset: 2px;
   812	}
   813	.not-imported {
   814	  margin: 0;
   815	  font-family: var(--font-chrome);

exec
/bin/bash -lc "nl -ba working/phase-5/designs/m2b-shell/design.md | sed -n '322,352p' && nl -ba working/phase-5/designs/m2b-shell/design.md | sed -n '426,452p' && nl -ba working/phase-5/designs/m2b-shell/design.md | sed -n '584,608p' && nl -ba working/phase-5/designs/m2b-shell/design.md | sed -n '700,730p'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
   322	### 3.4 RawTab (`apps/frontend/src/features/sessions/RawTab.tsx`)
   323	
   324	Byte-equivalent to Phase 4 `RawPreviewBlock`:
   325	
   326	- One `useEffect` per `sessionUid` change: creates an `AbortController`,
   327	  calls `streamSessionRaw` + `consumeRawPreview`, sets state, returns a
   328	  cleanup that aborts the controller.
   329	- State machine: `loading` (initial) → `success` | `error` |
   330	  `non_2xx` | `idle` (idle never reached in current code path).
   331	- Plus the **"Not yet imported"** branch when
   332	  `row.storedSessionUid === null`. RawTab never fires the fetch when
   333	  the row is source-only; it short-circuits to the muted prose.
   334	- Caption strings preserved EXACTLY from Phase 4
   335	  (`describeCaption(lineCount, reachedLineCap, reachedByteCap)`):
   336	  - byte cap fired → `"Stopped at byte cap — full payload not downloaded."`
   337	  - line cap fired → ``Showing first ${N} lines of the raw payload.``
   338	  - neither cap → ``Showing first ${N} lines (full payload below the caps).``
   339	  - both caps → byte-cap caption wins.
   340	- Retry button bumps a local `attempt` counter, re-runs the effect
   341	  (refires the fetch).
   342	- Non-JSON fallback lines render with the `(non-JSON line)` marker
   343	  preserved verbatim from Phase 4 `RawPreviewLineRow`. The marker is
   344	  styled **muted-italic** (`color: var(--color-ink-muted)`,
   345	  `font-style: italic`) — byte-equivalent to Phase 4 (the Phase 4
   346	  source `apps/frontend/src/features/sessions/SessionDetail.css:143-156`
   347	  uses `--color-text-muted`, now aliased to `--color-ink-muted` per
   348	  M2a Option B). The marker is NOT a warn-color text — codex
   349	  measured warn-on-surface-raised at 3.97:1 light, which fails AA
   350	  for normal text. Muted-on-surface-raised passes (light 6.64:1 /
   351	  dark 7.03:1 — see colors.md row R03).
   352	
   426	### 4.5 Tab panel cross-fade direction
   427	
   428	Cross-fade is **fade-IN-only** (per spec line 87 + planner refinement):
   429	
   430	- Outgoing panel: hidden via `hidden` HTML attribute → `display: none`
   431	  immediately. NO fade-out animation.
   432	- Incoming panel: CSS `animation-name` toggles from `none` →
   433	  `tab-fade-in` on the panel's `<div role="tabpanel">` element. The
   434	  browser starts the keyframes (opacity 0 → 1 over 120 ms
   435	  `--ease-out`) on the property change. The panel's React subtree is
   436	  STABLE — NOT remounted.
   437	
   438	The asymmetry is intentional: a true cross-fade (out + in
   439	overlapping) would require two panels rendered simultaneously, which
   440	fights both the `hidden` attribute (display:none can't transition)
   441	AND the lazy-mount discipline. Fade-in-only matches spec intent and
   442	keeps the panel mounting story simple.
   443	
   444	**Why animation-name re-trigger and NOT a `key={activeTab}` remount:**
   445	the spec mandates that visited panels stay React-mounted (Resolved
   446	Decision #12 / spec lines 650–658) so RawTab's `consumeRawPreview`
   447	consumer keeps its in-flight stream alive across tab switches and so
   448	M3+ `useParsedSession` results aren't recomputed. A `key={activeTab}`
   449	on the panel content would unmount and remount the subtree on every
   450	tab change, aborting in-flight work and breaking the keep-mounted
   451	contract. The CSS animation-name toggle achieves the same fade-IN
   452	visual without touching the React tree.
   584	### 5.1 Animatable surfaces in M2b
   585	
   586	The frozen motion budget at `working/phase-5.md:84-95` permits
   587	animation ONLY on `transform`, `opacity`, and `background-color`,
   588	and ONLY on the surfaces explicitly listed in that table. M2b
   589	activates exactly the three rows the spec authorizes for this chunk
   590	(rows 1, 2, and 7 of the spec table):
   591	
   592	| Surface                  | Property                                              | Duration                       | Easing                       | Trigger                                                                                                                |
   593	|--------------------------|-------------------------------------------------------|--------------------------------|------------------------------|------------------------------------------------------------------------------------------------------------------------|
   594	| Tab indicator slide      | `transform: translateX` + `scaleX`                    | `--motion-base` (120 ms)       | `--ease-standard`            | active-tab change                                                                                                      |
   595	| Tab panel cross-fade-IN  | `opacity` (0 → 1)                                     | `--motion-base` (120 ms)       | `--ease-out`                 | active-tab change (CSS `animation-name` toggle on `<div role="tabpanel">` from `none` → `tab-fade-in`) |
   596	| Page-turn fade           | `opacity` (0 → 1) + `transform: translateX` (4px → 0) | `--motion-disclosure` (200 ms) | `--ease-out`                 | `selectedRowKey` change (re-mount of `<SessionView key={…}>`)                                                          |
   597	
   598	All values come from existing M2a tokens; no new motion tokens added.
   599	
   600	**No other M2b surface animates.** The active ↔ inactive tab text
   601	color change, the inactive-tab hover color shift, the Copy path
   602	button hover, the Retry button hover, and the "Open raw" anchor
   603	hover underline are all **INSTANT** — no `transition` declaration,
   604	no `animation`. Hover/active state changes apply on the next paint
   605	without interpolation. This is the spec's frozen motion budget
   606	(line 1100: "WCAG AA" + load-bearing motion table); adding
   607	animations not on the table requires a coordinator waiver. None of
   608	M2b's hover/active surfaces appears in the spec's table → all
   700	### 6.1 ARIA roles and properties
   701	
   702	Per WAI-ARIA APG tabs pattern (`https://www.w3.org/WAI/ARIA/apg/patterns/tabs/`):
   703	
   704	- `<div role="tablist" aria-label="Session content tabs">` — the tab
   705	  strip container.
   706	- `<button role="tab" aria-selected={true|false} aria-controls={panelId}
   707	  tabindex={0|-1}>` — each tab. The `<button>` element handles the
   708	  Enter/Space activation natively; the explicit `aria-selected`
   709	  attribute carries the active state.
   710	- `<div role="tabpanel" aria-labelledby={tabId} hidden={!isActive}>`
   711	  — each panel. The `hidden` attribute hides the panel from the
   712	  accessibility tree AND from the layout (display: none). When
   713	  `isActive`, the panel is rendered visible.
   714	- `aria-controls` ↔ `aria-labelledby` linkage forms the tab ↔ panel
   715	  relationship.
   716	- **`tabindex={0}` on active tabpanels with no focusable first
   717	  child** (WAI-ARIA APG Tabs pattern). The Tab key from the active
   718	  tab moves focus into the panel; if the panel has no focusable
   719	  children, focus needs the panel itself to be focusable, otherwise
   720	  Tab skips past the panel entirely. At M2b:
   721	  - **Skim panel** (placeholder) → `tabindex="0"` while active.
   722	  - **Transcript panel** (placeholder) → `tabindex="0"` while active.
   723	  - **Metadata panel** → no `tabindex` needed (the Copy path button
   724	    + "Open raw" anchor are focusable).
   725	  - **Raw panel** → no `tabindex` needed (the Retry button is
   726	    focusable in error/non_2xx; the success-state `<pre>` does not
   727	    need a focusable container because the user can scroll via the
   728	    surrounding scroll region).
   729	  Inactive panels (`hidden=true`) MUST NOT carry `tabindex="0"`
   730	  (they're outside the layout + a11y tree). The tabindex attribute

exec
/bin/bash -lc "nl -ba working/phase-5/designs/m2b-shell/design.md | sed -n '730,735p'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
   730	  (they're outside the layout + a11y tree). The tabindex attribute
   731	  is added/removed in lockstep with `hidden` toggling.
   732	
   733	### 6.2 Keyboard interaction matrix
   734	
   735	| Key                | When focus is on…                      | Effect                          |

exec
/bin/bash -lc "nl -ba working/phase-5/designs/m2b-shell/design.md | sed -n '1160,1225p'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
  1160	    state per Resolved Decision #17).
  1161	
  1162	### Tab panel mounting + cross-fade
  1163	
  1164	27. Each `<div role="tabpanel" id="panel-{id}" aria-labelledby="tab-{id}"
  1165	    hidden={!isActive}>` is rendered ONLY when `visitedTabs.has(id)`.
  1166	    Unvisited panels are NOT in the DOM. **Panel rendering MUST be
  1167	    object-keyed (`key={tabId}`), NEVER index-keyed (`key={index}`)**
  1168	    so that skipping an unvisited mid-sequence panel never causes
  1169	    React to re-key the remaining panels by position. **Once a panel
  1170	    has mounted, its content tree is STABLE — it MUST NOT carry an
  1171	    inner `key={activeTab}` (or any other key that flips on tab
  1172	    change).** Remounting the panel content on every tab change
  1173	    aborts RawTab's in-flight `consumeRawPreview` consumer and (in
  1174	    M3+) re-triggers `useParsedSession` for Skim/Transcript — that
  1175	    violates Resolved Decision #12's keep-mounted contract. Required
  1176	    pattern (or strictly equivalent):
  1177	
  1178	    ```jsx
  1179	    {(Object.entries({
  1180	       transcript: <TranscriptPanel />,
  1181	       skim:       <SkimPanel />,
  1182	       raw:        <RawTab row={row} />,
  1183	       metadata:   <SessionMetadata row={row} />,
  1184	     }) as Array<[TabId, ReactNode]>)
  1185	       .filter(([id]) => visitedTabs.has(id))
  1186	       .map(([id, content]) => (
  1187	         <TabPanel key={id} id={id} isActive={id === activeTab}>
  1188	           {content}
  1189	         </TabPanel>
  1190	       ))}
  1191	    ```
  1192	
  1193	    `key={id}` on the OUTER `<TabPanel>` (the tab id string), NEVER
  1194	    `key={index}` from `.map((_, i) => …)` and NEVER `key={activeTab}`
  1195	    on the inner content. Index-keying would cause the wrong panel to
  1196	    remount when a previously-unvisited mid-sequence tab is
  1197	    activated for the first time. `key={activeTab}` on the inner
  1198	    content would defeat the keep-mounted contract per the warning
  1199	    above.
  1200	28. The cross-fade-IN runs on the panel's own `<div role="tabpanel">`
  1201	    element via a CSS `animation-name` toggle: when `isActive=true`,
  1202	    the panel carries `style={{ animation: "tab-fade-in 120ms
  1203	    var(--ease-out) both" }}`; when `isActive=false`, it carries
  1204	    `style={{ animation: "none" }}`. The browser fires the keyframe
  1205	    each time the property string transitions from `"none"` to a
  1206	    real animation. The keyframe is defined ONCE at module scope:
  1207	    `@keyframes tab-fade-in { from { opacity: 0 } to { opacity: 1 } }`.
  1208	    Active → active re-renders DO NOT re-fire (the property string
  1209	    is unchanged); only inactive → active does. The panel's React
  1210	    subtree is **stable** — NOT remounted on tab change. Suppressed
  1211	    under `prefers-reduced-motion: reduce` via the M1a global rule.
  1212	    **Do NOT use a React `key={activeTab}` (or any per-activation
  1213	    key) on the panel content** — that would defeat the keep-mounted
  1214	    contract; see item 27.
  1215	29. Cross-fade is fade-IN-only — outgoing panel hides instantly via
  1216	    the `hidden` attribute (display: none); no fade-out animation.
  1217	29a. Skim and Transcript tabpanels carry `tabIndex={0}` while active
  1218	    (their placeholder content has no focusable children, so without
  1219	    this Tab from the active tab would skip past the panel
  1220	    entirely). Metadata and Raw tabpanels do NOT carry `tabIndex`
  1221	    (their first meaningful content — Copy path button on Metadata,
  1222	    Retry button or scrollable `<pre>` on Raw — handles focus
  1223	    natively per WAI-ARIA APG). The `tabIndex` toggles in lockstep
  1224	    with `hidden`: inactive panels MUST NOT carry `tabIndex={0}`.
  1225	

exec
/bin/bash -lc "nl -ba working/phase-5/designs/m2b-shell/motion.md | sed -n '8,45p' && nl -ba working/phase-5/designs/m2b-shell/motion.md | sed -n '184,193p'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
     8	## M2b's three new motion surfaces
     9	
    10	The chunk activates three motion surfaces from the spec's §Motion
    11	budget at `working/phase-5.md:84-95`. Two of them are signature
    12	details (#5 and #2 from §Design Language). **No other M2b surface
    13	animates.** The frozen motion budget permits animation only on
    14	`transform`, `opacity`, and `background-color` AND only on the
    15	specific surfaces listed in the spec table; M2b activates exactly
    16	the three rows below and nothing more.
    17	
    18	| #   | Surface                                  | Property            | Duration             | Easing                    | Trigger                            | Idiom                          |
    19	|-----|------------------------------------------|---------------------|----------------------|---------------------------|------------------------------------|--------------------------------|
    20	| 1   | **Tab indicator slide** *(signature #5)* | `transform: translateX` + `scaleX` | `--motion-base` (120 ms) | `--ease-standard` | active-tab change                  | CSS `transition: transform`    |
    21	| 2   | **Tab panel cross-fade-IN**              | `opacity: 0 → 1`    | `--motion-base` (120 ms) | `--ease-out`             | active-tab change (CSS `animation-name` toggle on `<div role="tabpanel">` from `none` → `tab-fade-in`) | CSS `@keyframes tab-fade-in` + animation-name re-trigger (NO React key remount) |
    22	| 3   | **Page-turn fade** *(signature #2)*      | `opacity: 0 → 1` + `transform: translateX(4px → 0)` | `--motion-disclosure` (200 ms) | `--ease-out`        | `selectedRowKey` change (mount of `<SessionView key={...}>`) | CSS `@keyframes session-page-turn` + React key remount on the OUTER session-pane element |
    23	
    24	## M2b's INSTANT (non-animated) surfaces
    25	
    26	These M2b surfaces undergo state changes with NO animation /
    27	transition. The change applies on the next paint without
    28	interpolation:
    29	
    30	| #   | Surface                                  | State change              | Trigger                  |
    31	|-----|------------------------------------------|---------------------------|--------------------------|
    32	| 4   | Tab text color (active ↔ inactive shift) | `--color-ink-muted` → `--color-ink` (bold) | active-tab change |
    33	| 5   | Tab text color (hover, inactive only)    | shifts via `color-mix`    | pointer enter            |
    34	| 6   | Copy path button hover                   | background tint           | pointer enter            |
    35	| 7   | Retry button hover                       | background tint           | pointer enter            |
    36	| 8   | "Open raw" anchor hover underline        | border-bottom color       | pointer enter            |
    37	| 9   | Tab focus-visible outline                | outline appears           | focus arrives            |
    38	
    39	Why instant rather than animated: `color`, `border-color`, and
    40	hover `background-color` on these surfaces are **NOT** in the
    41	spec's frozen motion budget at `working/phase-5.md:84-95`. The
    42	budget is load-bearing per spec line 1100; adding animations on
    43	unlisted surfaces requires a coordinator waiver, which M2b does not
    44	seek. Instant state changes still feel responsive at 60 Hz; the
    45	animated surfaces (#1, #2, #3) carry the chunk's animation budget.
   184	- §Motion table entries that M2b activates: row 1 (Tab strip
   185	  indicator), row 2 (Tab panel cross-fade), row 7 (Session-pane
   186	  content fade). All three are present in this artifact and
   187	  nothing else animates.
   188	- §Performance budget (lines 114–128): only `transform` + `opacity` +
   189	  `background-color` allowed AND only on the surfaces listed in the
   190	  spec table. M2b activates `transform` (#1, #3) and `opacity`
   191	  (#2, #3) and nothing else. M2b does NOT animate `color`,
   192	  `border-color`, or hover `background-color` on any surface.
   193	- §Reduced-motion (lines 97–112): all three M2b animations zero out

exec
/bin/bash -lc "nl -ba working/phase-5/designs/m2b-shell/design.md | sed -n '1318,1335p'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
  1318	### Acceptance verification
  1319	
  1320	54. `bun test src` passes; the new `Tabs.test.tsx` covers
  1321	    keyboard nav (ArrowLeft/Right/Home/End), ARIA roles,
  1322	    `aria-selected` toggling, panel switching, focus-after-activation,
  1323	    AND that **Tab from the active tab moves focus into the
  1324	    panel** for ALL four panels: Skim + Transcript via the panel's
  1325	    `tabIndex={0}`; Metadata via the Copy path button; Raw via
  1326	    Retry / scrollable container.
  1327	55. `SessionView.test.tsx` covers default-tab = "metadata",
  1328	    `visitedTabs` lazy mount, `hidden` attribute toggling, page-turn
  1329	    fade animation presence (test for `animation-name` CSS via
  1330	    `getComputedStyle`), AND that the panel content does NOT
  1331	    remount on tab change (regression guard for Resolved Decision
  1332	    #12: e.g. mount RawTab as the active tab, switch to Metadata,
  1333	    switch back to Raw, assert the previous RawTab instance is
  1334	    still in the DOM via a stable React ref, NOT a fresh one).
  1335	56. `SessionMetadata.test.tsx` covers all 18 fields render with the

exec
/bin/bash -lc "nl -ba working/phase-5.md | sed -n '80,112p' && nl -ba working/phase-5.md | sed -n '646,658p'" in /home/huwei/ai_codings/distill-portal
exec
/bin/bash -lc "nl -ba working/phase-5/designs/m2b-shell/colors.md | sed -n '116,140p' && nl -ba working/phase-5/designs/m2b-shell/colors.md | sed -n '216,230p'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
    80	## Motion & Micro-interactions
    81	
    82	A single motion budget governs all of Phase 5. Reduced-motion users never see any of it; the table below is the comprehensive enumeration so reviewers can audit at chunk close.
    83	
    84	| Surface | Property animated | Duration | Easing | Trigger |
    85	|---------|--------------------|----------|--------|---------|
    86	| Tab strip indicator | `transform: translateX` | 120 ms | `cubic-bezier(0.4, 0, 0.2, 1)` | active-tab change |
    87	| Tab panel cross-fade | `opacity` | 120 ms | `ease-out` | active-tab change |
    88	| Disclosure (`<details>`) expand/collapse | `block-size` (via `interpolate-size: allow-keywords`) | 200 ms | `ease-in-out` | user toggle |
    89	| Row hover tint | `background-color` | 80 ms | `linear` | pointer enter |
    90	| Selected row tint | `background-color` | 120 ms | `ease-out` | selection change |
    91	| **Deep-link pulse** | `background-color` (one-shot) | 600 ms | `ease-out` | URL-driven selection on mount only |
    92	| Session-pane content fade | `opacity` + `translateX(4px → 0)` | 200 ms | `ease-out` | `selectedRowKey` change |
    93	| Skim-block stagger on first paint | `opacity` + `translateY(4px → 0)` per block | 40 ms × N (max 8 blocks) | `ease-out` | first paint per session |
    94	| Truncation banner appearance | `opacity` | 120 ms | `ease-out` | `parsed.truncated` becomes true |
    95	| Toast queue (existing Phase 4) | unchanged | unchanged | unchanged | n/a |
    96	
    97	### Reduced-motion
    98	
    99	A single rule in `global.css`:
   100	
   101	```css
   102	@media (prefers-reduced-motion: reduce) {
   103	  *, *::before, *::after {
   104	    animation-duration: 0.01ms !important;
   105	    animation-iteration-count: 1 !important;
   106	    transition-duration: 0.01ms !important;
   107	    scroll-behavior: auto !important;
   108	  }
   109	}
   110	```
   111	
   112	This zero-out is testable in M6's WCAG checklist: with reduced-motion on, every transition completes within one paint frame. The signature noise-overlay also disappears under this query (the wash texture is decorative).
   646	Tab state is component-local React state, NOT URL-synced (deferred decision #2). The active-tab value lives on `SessionView` and resets to the `DEFAULT_TAB_ON_SELECTION` constant on every `selectedRowKey` change. Per Resolved Decision #11, that constant is `"metadata"` until M4 ships Transcript, then `"transcript"` from M4 onward.
   647	
   648	Tab switching does NOT re-fetch raw bytes — the `useParsedSession` cache hit serves the Skim and Transcript tabs; Raw uses its own independent streaming consumer (`rawPreview.ts`); Metadata bypasses parser fetch entirely (it only reads the SessionRow already in `App.tsx` state).
   649	
   650	**Panel mounting strategy:** lazy-on-first-activation + keep-mounted-after.
   651	
   652	- `SessionView` tracks `visitedTabs: Set<TabId>` in component-local state, initialized to `new Set([defaultTab])` on every `selectedRowKey` change.
   653	- A tab panel is rendered (React-mounted) only if it is the active tab OR has been visited before (i.e., `visitedTabs.has(tabId)`).
   654	- When the user activates a tab for the first time, it is added to `visitedTabs` and remains mounted for the rest of the selection. Inactive-but-visited panels carry the `hidden` attribute (`display: none` semantics + accessibility-tree exclusion).
   655	- This avoids the contradiction of mounting heavy parsed-content tabs (Skim, Transcript) before they're visited — their `useParsedSession` `useEffect` would otherwise fire and trigger the 5 MB full-document fetch even when the user lands on Metadata.
   656	- On `selectedRowKey` change, `visitedTabs` resets and all panels unmount cleanly. The cache (in `useParsedSession`) persists across selection changes, so re-selecting the same session re-mounts a panel that finds a cache hit.
   657	
   658	This preserves transcript scroll position + skim disclosure expansion state across tab switches WITHIN a single selection, while ensuring Skim/Transcript do not preemptively fetch when the user only intends to see Metadata or Raw.

 succeeded in 0ms:
   116	### Raw tab body
   117	
   118	| #   | Surface                                      | Foreground            | Background          | Light    | Dark     | Verdict             | Bar             |
   119	|-----|----------------------------------------------|-----------------------|---------------------|----------|----------|---------------------|-----------------|
   120	| R01 | Mono `<pre>` raw NDJSON lines                | `--color-ink`         | `--color-surface-raised` | 16.13:1 | 14.84:1 | AAA · normal text | AA 4.5:1 |
   121	| R02 | Non-JSON fallback line (italic muted)        | `--color-ink-muted`   | `--color-surface-raised` | 6.64:1 | 7.03:1 | AAA · normal text | AA 4.5:1 |
   122	| R03 | Non-JSON line marker (muted italic)          | `--color-ink-muted`   | `--color-surface-raised` | 6.64:1   | 7.03:1   | AAA · normal text | AA 4.5:1 |
   123	| R04 | Caption (italic muted)                       | `--color-ink-muted`   | `--color-surface`   | 7.04:1   | 7.36:1   | AAA · normal text   | AA 4.5:1        |
   124	| R05 | Loading/idle prose                           | `--color-ink-muted`   | `--color-surface`   | 7.04:1   | 7.36:1   | AAA · normal text   | AA 4.5:1        |
   125	| R06 | "Not yet imported" prose (muted italic)      | `--color-ink-muted`   | `--color-surface`   | 7.04:1   | 7.36:1   | AAA · normal text   | AA 4.5:1        |
   126	| R07 | Error prose                                  | error 70% mix on ink  | `--color-surface`   | (Phase 4 baseline; ~4.62:1 light / ~5.40:1 dark) | — | passes AA · normal text | AA 4.5:1 |
   127	| R08 | Retry button text                            | `--color-ink`         | `--color-surface`   | 17.10:1  | 15.52:1  | AAA · normal text   | AA 4.5:1        |
   128	| R09 | Retry button border                          | `--color-border-strong` | `--color-surface` | 3.06:1   | 3.00:1   | passes 3:1 (dark cusp — same as M11) | SC 1.4.11 3:1 |
   129	| R10 | Pre-block border                             | `--color-border`      | `--color-surface`   | 1.49:1   | 1.35:1   | not WCAG-gated (decorative) | n/a |
   130	
   131	R03 is the non-JSON line marker. It sits on
   132	`--color-surface-raised` (the `<pre>` block's background) and reuses
   133	`--color-ink-muted` — byte-equivalent to Phase 4's
   134	`apps/frontend/src/features/sessions/SessionDetail.css:143-156`,
   135	which uses `--color-text-muted` (= `--color-ink-muted` post-M2a
   136	alias). The marker reads as muted-italic — distinct from the
   137	JSON-line ink, but unmistakably text rather than chrome. **Codex
   138	round 3 caught a regression** in the previous artifact draft that
   139	used `--color-warn` here (3.97:1 light → fails AA normal text);
   140	this row records the corrected pair.
   216	
   217	| Token                    | Light                  | Dark                   |
   218	|--------------------------|------------------------|------------------------|
   219	| `--color-surface`        | `oklch(98% 0.01 70)`   | `oklch(15% 0.01 70)`   |
   220	| `--color-surface-raised` | `oklch(96% 0.01 70)`   | `oklch(18% 0.01 70)`   |
   221	| `--color-ink`            | `oklch(20% 0.02 70)`   | `oklch(92% 0.01 70)`   |
   222	| `--color-ink-muted`      | `oklch(45% 0.02 70)`   | `oklch(70% 0.01 70)`   |
   223	| `--color-border`         | `oklch(85% 0.01 70)`   | `oklch(28% 0.01 70)`   |
   224	| `--color-border-strong`  | `oklch(65% 0.02 70)`   | `oklch(48% 0.02 70)`   |
   225	| `--color-accent`         | `oklch(55% 0.15 50)`   | `oklch(65% 0.15 50)`   |
   226	| `--color-accent-hover`   | `oklch(50% 0.16 50)`   | `oklch(72% 0.13 50)`   |
   227	| `--color-success`        | `oklch(48% 0.13 155)`  | `oklch(64% 0.13 155)`  |
   228	| `--color-warn`           | `oklch(58% 0.15 60)`   | `oklch(72% 0.13 60)`   |
   229	| `--color-error`          | `oklch(52% 0.18 25)`   | `oklch(68% 0.16 25)`   |
   230	

exec
/bin/bash -lc "rg -n \"transition\\s*:|transition-property\\s*:|animation\\s*:|@keyframes\" working/phase-5/designs/m2b-shell/prototype.html" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
46: *          style={{ animation: isActive
234:  transition: background-color var(--motion-fast) var(--ease-standard);
299:  transition: background-color var(--motion-fast) linear;
346:  animation: session-page-turn var(--motion-disclosure) var(--ease-out) both;
356:@keyframes session-page-turn {
538:  transition:
569:@keyframes tab-fade-in {

exec
/bin/bash -lc 'rg -n "--space-(5|10|12)" working/phase-5/designs/m2b-shell apps/frontend/src/styles/tokens.css' in /home/huwei/ai_codings/distill-portal
 exited 2 in 0ms:
rg: unrecognized flag --space-(5|10|12)

exec
/bin/bash -lc 'rg -n -- "--space-(5|10|12)" working/phase-5/designs/m2b-shell apps/frontend/src/styles/tokens.css' in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
working/phase-5/designs/m2b-shell/design.md:1116:    `--space-5` does NOT exist in
working/phase-5/designs/m2b-shell/design.md:1287:    **Note**: `--space-12` does NOT exist in
working/phase-5/designs/m2b-shell/prototype.html:61:   * previous artifact draft introduced --space-5/10/12 which would have
working/phase-5/designs/m2b-shell/prototype.html:557:  padding: var(--space-6) var(--space-4);  /* --space-5 not in token scale; --space-4 (16 px) is the closest fit */

exec
/bin/bash -lc "rg -n -- \"var\\(--space-(5|10|12)\\)|--space-(5|10|12)\\s*:\" working/phase-5/designs/m2b-shell apps/frontend/src/styles/tokens.css" in /home/huwei/ai_codings/distill-portal
 exited 1 in 0ms:
exec
/bin/bash -lc "nl -ba apps/frontend/src/styles/tokens.css | sed -n '96,103p' && nl -ba working/phase-5/designs/m2b-shell/prototype.html | sed -n '56,70p' && nl -ba working/phase-5/designs/m2b-shell/prototype.html | sed -n '552,558p' && nl -ba working/phase-5/designs/m2b-shell/prototype.html | sed -n '829,834p'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
    96	  /* ===== Spacing scale (unchanged from Phase 4) ===== */
    97	  --space-1: 0.25rem;
    98	  --space-2: 0.5rem;
    99	  --space-3: 0.75rem;
   100	  --space-4: 1rem;
   101	  --space-6: 1.5rem;
   102	  --space-8: 2rem;
   103	
    56	 * ===================================================================== */
    57	
    58	:root {
    59	  /* Spacing scale — byte-equivalent to apps/frontend/src/styles/tokens.css:96-102.
    60	   * The production scale is 1, 2, 3, 4, 6, 8 ONLY — no 5, 10, or 12. The
    61	   * previous artifact draft introduced --space-5/10/12 which would have
    62	   * resolved to 0 in production CSS (var() fallback to nothing). Codex
    63	   * round 3 caught this; M2b uses only the six tokens below. */
    64	  --space-1: 0.25rem;
    65	  --space-2: 0.5rem;
    66	  --space-3: 0.75rem;
    67	  --space-4: 1rem;
    68	  --space-6: 1.5rem;
    69	  --space-8: 2rem;
    70	
   552	[role="tabpanel"] {
   553	  /* outer shell — provides ARIA + a11y. Inactive-but-visited panels carry
   554	   * the `hidden` HTML attribute (display:none + accessibility tree exclusion).
   555	   * The browser handles hidden semantics — no extra CSS needed beyond
   556	   * letting the default user-agent stylesheet apply. */
   557	  padding: var(--space-6) var(--space-4);  /* --space-5 not in token scale; --space-4 (16 px) is the closest fit */
   558	  max-inline-size: var(--measure);          /* 70ch reading measure */
   829	  color: var(--color-ink-muted);
   830	  font-style: italic;
   831	  letter-spacing: 0.01em;
   832	  text-align: center;
   833	  padding: var(--space-8) 0;
   834	  border: 1px dashed var(--color-border);

codex
I’m recomputing contrast from the OKLCH values rather than trusting the artifact table. The relevant new pair is muted text on the raised raw-preview surface; I’ll also keep the old warn pair in the audit for comparison.
exec
/bin/bash -lc "python3 - <<'PY'
import math

def oklch_to_lin_srgb(Lp,C,hdeg):
    L=Lp/100.0
    h=math.radians(hdeg)
    a=C*math.cos(h)
    b=C*math.sin(h)
    l_=L + 0.3963377774*a + 0.2158037573*b
    m_=L - 0.1055613458*a - 0.0638541728*b
    s_=L - 0.0894841775*a - 1.2914855480*b
    l=l_**3
    m=m_**3
    s=s_**3
    r=+4.0767416621*l -3.3077115913*m +0.2309699292*s
    g=-1.2684380046*l +2.6097574011*m -0.3413193965*s
    bb=-0.0041960863*l -0.7034186147*m +1.7076147010*s
    return r,g,bb

def rel_lum(rgb):
    r,g,b=rgb
    return 0.2126*r + 0.7152*g + 0.0722*b

def contrast(fg,bg):
    L1=rel_lum(oklch_to_lin_srgb(*fg))
    L2=rel_lum(oklch_to_lin_srgb(*bg))
    hi=max(L1,L2); lo=min(L1,L2)
    return (hi+0.05)/(lo+0.05), L1, L2

pairs = {
  'new light ink-muted on surface-raised': ((45,0.02,70),(96,0.01,70)),
  'new dark ink-muted on surface-raised': ((70,0.01,70),(18,0.01,70)),
  'old light warn on surface-raised': ((58,0.15,60),(96,0.01,70)),
  'old dark warn on surface-raised': ((72,0.13,60),(18,0.01,70)),
  'muted on surface light': ((45,0.02,70),(98,0.01,70)),
  'muted on surface dark': ((70,0.01,70),(15,0.01,70)),
  'accent hover on surface light': ((50,0.16,50),(98,0.01,70)),
  'accent hover on surface dark': ((72,0.13,50),(15,0.01,70)),
}
for name,(fg,bg) in pairs.items():
    ratio,Lfg,Lbg=contrast(fg,bg)
    rgbfg=oklch_to_lin_srgb(*fg); rgbbg=oklch_to_lin_srgb(*bg)
    print(f'{name}: ratio={ratio:.4f}:1 Lfg={Lfg:.6f} Lbg={Lbg:.6f} fg_lin={tuple(round(x,6) for x in rgbfg)} bg_lin={tuple(round(x,6) for x in rgbbg)}')
PY" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
new light ink-muted on surface-raised: ratio=6.6377:1 Lfg=0.090687 Lbg=0.883835 fg_lin=(0.108681, 0.087677, 0.067512) bg_lin=(0.923944, 0.877491, 0.82858)
new dark ink-muted on surface-raised: ratio=7.0348:1 Lfg=0.342515 Lbg=0.005796 fg_lin=(0.363891, 0.339111, 0.313291) bg_lin=(0.007245, 0.005549, 0.00397)
old light warn on surface-raised: ratio=3.9907:1 Lfg=0.184003 Lbg=0.883835 fg_lin=(0.480154, 0.115885, -0.013269) bg_lin=(0.923944, 0.877491, 0.82858)
old dark warn on surface-raised: ratio=7.3407:1 Lfg=0.359582 Lbg=0.005796 fg_lin=(0.740367, 0.276145, 0.064825) bg_lin=(0.007245, 0.005549, 0.00397)
muted on surface light: ratio=7.0387:1 Lfg=0.090687 Lbg=0.940254 fg_lin=(0.108681, 0.087677, 0.067512) bg_lin=(0.982046, 0.933646, 0.882656)
muted on surface dark: ratio=7.3575:1 Lfg=0.342515 Lbg=0.003349 fg_lin=(0.363891, 0.339111, 0.313291) bg_lin=(0.004362, 0.003174, 0.002101)
accent hover on surface light: ratio=6.0392:1 Lfg=0.113971 Lbg=0.940254 fg_lin=(0.383625, 0.046977, -0.016427) bg_lin=(0.982046, 0.933646, 0.882656)
accent hover on surface dark: ratio=7.6157:1 Lfg=0.356293 Lbg=0.003349 fg_lin=(0.782634, 0.256268, 0.091717) bg_lin=(0.004362, 0.003174, 0.002101)

exec
/bin/bash -lc "python3 - <<'PY'
# quick old warn ratio with simple linear channel clamp for comparison
import math

def oklch_to_lin_srgb(Lp,C,hdeg):
    L=Lp/100.0; h=math.radians(hdeg); a=C*math.cos(h); b=C*math.sin(h)
    l_=L + 0.3963377774*a + 0.2158037573*b
    m_=L - 0.1055613458*a - 0.0638541728*b
    s_=L - 0.0894841775*a - 1.2914855480*b
    l,m,s=l_**3,m_**3,s_**3
    return (4.0767416621*l-3.3077115913*m+0.2309699292*s,
            -1.2684380046*l+2.6097574011*m-0.3413193965*s,
            -0.0041960863*l-0.7034186147*m+1.7076147010*s)

def lum(rgb, clamp=False):
    if clamp: rgb=tuple(min(1,max(0,x)) for x in rgb)
    r,g,b=rgb
    return 0.2126*r+0.7152*g+0.0722*b

def ratio(fg,bg,clamp=False):
    a=lum(oklch_to_lin_srgb(*fg),clamp); c=lum(oklch_to_lin_srgb(*bg),clamp)
    return (max(a,c)+.05)/(min(a,c)+.05), a,c
for clamp in (False, True):
    print('clamp', clamp, ratio((58,0.15,60),(96,0.01,70),clamp))
PY" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
clamp False (3.990690349949004, 0.18400345514354993, 0.8838353302960892)
clamp True (3.9744183398527992, 0.18496150894137522, 0.8838353302960892)

exec
/bin/bash -lc "rg -n \"key=\\{activeTab\\}|key\\s*=\\s*activeTab|activeTab.*key|key\\s*=\\s*\\{index\\}|key=\\{index\\}|remount\" working/phase-5/designs/m2b-shell" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
working/phase-5/designs/m2b-shell/prototype.html:17: *      cross-fade-IN-only via React key remount; here driven by a vanilla
working/phase-5/designs/m2b-shell/prototype.html:19: *   4. Page-turn fade (selectedRowKey change → outer SessionView remounts
working/phase-5/designs/m2b-shell/prototype.html:39: *                                             (React remount on selection
working/phase-5/designs/m2b-shell/prototype.html:345:   * remounted (in production: <SessionView key={selectedRowKey}>). */
working/phase-5/designs/m2b-shell/prototype.html:565: * forbids remounting the panel content, so we use animation-name
working/phase-5/designs/m2b-shell/prototype.html:566: * re-trigger instead of React `key={activeTab}` remount. The browser
working/phase-5/designs/m2b-shell/prototype.html:902:  <!-- SESSION PANE — outer keyed by selectedRowKey; remounts on switch. -->
working/phase-5/designs/m2b-shell/prototype.html:977: *       on the panel's own element (NOT a remount of panel content —
working/phase-5/designs/m2b-shell/prototype.html:986: *       NOT a remount of the panel container.
working/phase-5/designs/m2b-shell/prototype.html:1137:   * can demo state transitions WITHOUT remounting the panel container — the
working/phase-5/designs/m2b-shell/design.md:42:   is **STABLE across tab switches** (NOT remounted): the outer
working/phase-5/designs/m2b-shell/design.md:55:   **Do NOT use a React `key={activeTab}` on the panel content
working/phase-5/designs/m2b-shell/design.md:62:   remount. The page-turn fade at the OUTER `<SessionView
working/phase-5/designs/m2b-shell/design.md:63:   key={selectedRowKey}>` layer DOES still remount (because a
working/phase-5/designs/m2b-shell/design.md:65:   tab-panel cross-fade is non-remounting.
working/phase-5/designs/m2b-shell/design.md:71:   remount fires the animation. This is signature detail #2 from
working/phase-5/designs/m2b-shell/design.md:220:      {/* placeholder — STABLE React subtree, not remounted */}
working/phase-5/designs/m2b-shell/design.md:228:  selection change remounts the whole subtree, which fires the
working/phase-5/designs/m2b-shell/design.md:233:  remount.
working/phase-5/designs/m2b-shell/design.md:236:  remount (= every selection change). Setting `setActiveTab(id)`
working/phase-5/designs/m2b-shell/design.md:247:  React tree is **NOT remounted** on tab change — keep-mounted state
working/phase-5/designs/m2b-shell/design.md:249:  survives intact. **Do NOT add `key={activeTab}` to the panel
working/phase-5/designs/m2b-shell/design.md:436:  STABLE — NOT remounted.
working/phase-5/designs/m2b-shell/design.md:444:**Why animation-name re-trigger and NOT a `key={activeTab}` remount:**
working/phase-5/designs/m2b-shell/design.md:448:M3+ `useParsedSession` results aren't recomputed. A `key={activeTab}`
working/phase-5/designs/m2b-shell/design.md:449:on the panel content would unmount and remount the subtree on every
working/phase-5/designs/m2b-shell/design.md:465:  `key={selectedRowKey}` is unchanged → no remount → no animation.
working/phase-5/designs/m2b-shell/design.md:631:**stable** — never remounted on tab change.
working/phase-5/designs/m2b-shell/design.md:633:**Inner cross-fade — non-remount, CSS animation-name re-trigger:**
working/phase-5/designs/m2b-shell/design.md:662:**Outer page-turn fade — React `key` remount IS correct here:**
working/phase-5/designs/m2b-shell/design.md:666:  {/* React remounts this on every selectedRowKey change.
working/phase-5/designs/m2b-shell/design.md:677:**Do NOT conflate the two layers.** The outer remount on
working/phase-5/designs/m2b-shell/design.md:679:different document). The inner `key={activeTab}` remount on tab
working/phase-5/designs/m2b-shell/design.md:682:page-turn uses React key remount. The two animations fire on
working/phase-5/designs/m2b-shell/design.md:757:- On `selectedRowKey` change: the `<SessionView>` remounts; focus is
working/phase-5/designs/m2b-shell/design.md:870:var(--ease-out) both"`, firing the keyframe without remounting the
working/phase-5/designs/m2b-shell/design.md:915:Cross-fade-IN must re-fire on every tab change WITHOUT remounting
working/phase-5/designs/m2b-shell/design.md:929:  subtree is **stable** — never remounted. Reduced-motion is
working/phase-5/designs/m2b-shell/design.md:939:- **(C) ~~React `key={activeTab}` remount~~** — REJECTED. Remounting
working/phase-5/designs/m2b-shell/design.md:1126:18. `<SessionView>` is keyed on `selectedRowKey` so React remounts on
working/phase-5/designs/m2b-shell/design.md:1144:    `selectedRowKey` change (via the `key` remount; no manual reset
working/phase-5/designs/m2b-shell/design.md:1167:    object-keyed (`key={tabId}`), NEVER index-keyed (`key={index}`)**
working/phase-5/designs/m2b-shell/design.md:1171:    inner `key={activeTab}` (or any other key that flips on tab
working/phase-5/designs/m2b-shell/design.md:1194:    `key={index}` from `.map((_, i) => …)` and NEVER `key={activeTab}`
working/phase-5/designs/m2b-shell/design.md:1196:    remount when a previously-unvisited mid-sequence tab is
working/phase-5/designs/m2b-shell/design.md:1197:    activated for the first time. `key={activeTab}` on the inner
working/phase-5/designs/m2b-shell/design.md:1210:    subtree is **stable** — NOT remounted on tab change. Suppressed
working/phase-5/designs/m2b-shell/design.md:1212:    **Do NOT use a React `key={activeTab}` (or any per-activation
working/phase-5/designs/m2b-shell/design.md:1331:    remount on tab change (regression guard for Resolved Decision
working/phase-5/designs/m2b-shell/motion.md:21:| 2   | **Tab panel cross-fade-IN**              | `opacity: 0 → 1`    | `--motion-base` (120 ms) | `--ease-out`             | active-tab change (CSS `animation-name` toggle on `<div role="tabpanel">` from `none` → `tab-fade-in`) | CSS `@keyframes tab-fade-in` + animation-name re-trigger (NO React key remount) |
working/phase-5/designs/m2b-shell/motion.md:22:| 3   | **Page-turn fade** *(signature #2)*      | `opacity: 0 → 1` + `transform: translateX(4px → 0)` | `--motion-disclosure` (200 ms) | `--ease-out`        | `selectedRowKey` change (mount of `<SessionView key={...}>`) | CSS `@keyframes session-page-turn` + React key remount on the OUTER session-pane element |
working/phase-5/designs/m2b-shell/motion.md:110:  {/* STABLE content — never remounted on tab change */}
working/phase-5/designs/m2b-shell/motion.md:121:**Do NOT use a React `key={activeTab}` on the panel content** —
working/phase-5/designs/m2b-shell/motion.md:122:that would unmount + remount the subtree on every tab change,
working/phase-5/designs/m2b-shell/motion.md:125:`key={activeTab}`; the corrected mechanism above does NOT touch the

exec
/bin/bash -lc "nl -ba working/phase-5/designs/m2b-shell/prototype.html | sed -n '1,52p' && nl -ba working/phase-5/designs/m2b-shell/motion.md | sed -n '84,126p'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
     1	<!doctype html>
     2	<html lang="en">
     3	<head>
     4	<meta charset="utf-8" />
     5	<meta name="viewport" content="width=device-width, initial-scale=1" />
     6	<title>distill-portal · Phase 5 · M2b — Tabs primitive + SessionView shell</title>
     7	
     8	<style>
     9	/* =========================================================================
    10	 * M2b reference prototype — Archive-room Design Language (FROZEN at M2a).
    11	 *
    12	 * Visible surfaces demonstrated:
    13	 *   1. Tab strip (tablist + 4 tabs + sliding 1px ink-stroke indicator).
    14	 *   2. Session-pane minimal header (title + tool badge + status pill +
    15	 *      conflict badge).
    16	 *   3. Tab panels (lazy-on-first-activation, hidden-on-inactive-but-visited,
    17	 *      cross-fade-IN-only via React key remount; here driven by a vanilla
    18	 *      key-bumping wrapper).
    19	 *   4. Page-turn fade (selectedRowKey change → outer SessionView remounts
    20	 *      with opacity + translateX entrance).
    21	 *   5. Metadata tab body — 18-field <dl> with subagent sidecar badge,
    22	 *      "Last seen source path" label swap, statusConflict badge, Copy path
    23	 *      button, "Open raw" anchor.
    24	 *   6. Raw tab body — caption-driven success state plus a state toggle
    25	 *      that surfaces idle / loading / error / non_2xx / not_imported
    26	 *      branches.
    27	 *   7. Skim placeholder — "Coming in Milestone 5" muted + editorial.
    28	 *   8. Transcript placeholder — "Coming in Milestone 4" same treatment.
    29	 *
    30	 * NO new tokens introduced. Every color, motion, and font reference goes
    31	 * through the M2a-canonical name. Only `transform` / `opacity` /
    32	 * `background-color` animate. Indicator slide, tab cross-fade, and
    33	 * page-turn fade are the three M2b-active animations and they all zero
    34	 * out under [data-motion="reduce"] (which mirrors the production
    35	 * @media (prefers-reduced-motion: reduce) rule).
    36	 *
    37	 * The two-layer animation pattern documented in design.md §5:
    38	 *   <SessionView key={selectedRowKey}>      ← 200 ms page-turn fade
    39	 *                                             (React remount on selection
    40	 *                                              change is correct — selection
    41	 *                                              IS a different document)
    42	 *     <header />                              (mounts with parent)
    43	 *     <Tabs />                                (mounts with parent;
    44	 *                                              indicator snaps to default)
    45	 *     <div role="tabpanel" hidden={!isActive}
    46	 *          style={{ animation: isActive
    47	 *                     ? "tab-fade-in 120ms var(--ease-out) both"
    48	 *                     : "none" }}
    49	 *          tabIndex={isActive && (id === "skim" || id === "transcript") ? 0 : undefined}>
    50	 *       {/* STABLE content — NOT keyed on activeTab.
    51	 *           Cross-fade fires when style.animation changes from
    52	 *           "none" to a real value. Keep-mounted contract per
    84	CSS animation-name re-trigger on the panel's own
    85	`<div role="tabpanel">` element. The panel's React subtree is
    86	**STABLE** across tab switches (Resolved Decision #12 / spec lines
    87	650–658 require visited panels to stay React-mounted so RawTab's
    88	`consumeRawPreview` consumer keeps streaming and so M3+
    89	`useParsedSession` results are not recomputed on every flip). The
    90	keyframe is defined once at module scope:
    91	
    92	```css
    93	@keyframes tab-fade-in {
    94	  from { opacity: 0; }
    95	  to   { opacity: 1; }
    96	}
    97	```
    98	
    99	The panel toggles its inline `animation` style based on `isActive`:
   100	
   101	```jsx
   102	<div role="tabpanel"
   103	     hidden={!isActive}
   104	     id={`panel-${tabId}`}
   105	     aria-labelledby={`tab-${tabId}`}
   106	     tabIndex={isActive && (tabId === "skim" || tabId === "transcript") ? 0 : undefined}
   107	     style={{ animation: isActive
   108	                          ? "tab-fade-in 120ms var(--ease-out) both"
   109	                          : "none" }}>
   110	  {/* STABLE content — never remounted on tab change */}
   111	</div>
   112	```
   113	
   114	The browser fires the keyframe each time the inline `animation`
   115	property string changes from `"none"` to a real animation. Active →
   116	active re-renders DON'T re-fire (the property string is unchanged).
   117	`animation-fill-mode: both` ensures opacity stays at 1 after the
   118	animation completes. Outgoing panel hides instantly via the `hidden`
   119	attribute (display: none) — no fade-out.
   120	
   121	**Do NOT use a React `key={activeTab}` on the panel content** —
   122	that would unmount + remount the subtree on every tab change,
   123	aborting in-flight work and breaking the keep-mounted contract.
   124	Codex round 3 caught the previous artifact draft regressing to
   125	`key={activeTab}`; the corrected mechanism above does NOT touch the
   126	React tree on tab change.

exec
/bin/bash -lc 'rg -n "key-bumping|React key remount|via React key remount|key remount" working/phase-5/designs/m2b-shell/prototype.html working/phase-5/designs/m2b-shell/design.md working/phase-5/designs/m2b-shell/motion.md' in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
working/phase-5/designs/m2b-shell/motion.md:21:| 2   | **Tab panel cross-fade-IN**              | `opacity: 0 → 1`    | `--motion-base` (120 ms) | `--ease-out`             | active-tab change (CSS `animation-name` toggle on `<div role="tabpanel">` from `none` → `tab-fade-in`) | CSS `@keyframes tab-fade-in` + animation-name re-trigger (NO React key remount) |
working/phase-5/designs/m2b-shell/motion.md:22:| 3   | **Page-turn fade** *(signature #2)*      | `opacity: 0 → 1` + `transform: translateX(4px → 0)` | `--motion-disclosure` (200 ms) | `--ease-out`        | `selectedRowKey` change (mount of `<SessionView key={...}>`) | CSS `@keyframes session-page-turn` + React key remount on the OUTER session-pane element |
working/phase-5/designs/m2b-shell/prototype.html:17: *      cross-fade-IN-only via React key remount; here driven by a vanilla
working/phase-5/designs/m2b-shell/prototype.html:18: *      key-bumping wrapper).
working/phase-5/designs/m2b-shell/design.md:682:page-turn uses React key remount. The two animations fire on

exec
/bin/bash -lc "nl -ba working/phase-5/designs/m2b-shell/prototype.html | sed -n '1288,1435p'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
  1288	    a.target = '_blank';
  1289	    a.rel = 'noopener noreferrer';
  1290	    a.textContent = 'Open raw ↗';
  1291	    openRaw.appendChild(a);
  1292	    wrap.appendChild(openRaw);
  1293	
  1294	    return wrap;
  1295	  }
  1296	
  1297	  /* ---------- Raw panel + state toggle --------------------------------- */
  1298	  let rawState = 'success';
  1299	
  1300	  function buildRaw() {
  1301	    const wrap = document.createDocumentFragment();
  1302	
  1303	    // Prototype-only state toggle — not part of the production RawTab UI.
  1304	    const toggle = document.createElement('div');
  1305	    toggle.className = 'raw-state-toggle';
  1306	    const label = document.createElement('span');
  1307	    label.textContent = 'state:';
  1308	    toggle.appendChild(label);
  1309	    for (const s of ['idle', 'loading', 'success', 'error', 'non_2xx', 'not_imported']) {
  1310	      const lbl = document.createElement('label');
  1311	      const inp = document.createElement('input');
  1312	      inp.type = 'radio';
  1313	      inp.name = 'rawstate';
  1314	      inp.value = s;
  1315	      if (s === rawState) inp.checked = true;
  1316	      const txt = document.createElement('span');
  1317	      txt.textContent = s;
  1318	      lbl.appendChild(inp);
  1319	      lbl.appendChild(txt);
  1320	      inp.addEventListener('change', () => {
  1321	        rawState = s;
  1322	        // Re-render only the body, not the toggle, so the radio's focus is
  1323	        // preserved and the cross-fade fires on the body alone.
  1324	        renderRawBody(body);
  1325	      });
  1326	      toggle.appendChild(lbl);
  1327	    }
  1328	    wrap.appendChild(toggle);
  1329	
  1330	    const body = document.createElement('div');
  1331	    renderRawBody(body);
  1332	    wrap.appendChild(body);
  1333	
  1334	    return wrap;
  1335	  }
  1336	
  1337	  function renderRawBody(host) {
  1338	    host.replaceChildren();
  1339	    switch (rawState) {
  1340	      case 'idle':
  1341	      case 'loading': {
  1342	        const p = document.createElement('p');
  1343	        p.className = 'raw-loading';
  1344	        p.textContent = 'Loading raw preview…';
  1345	        host.appendChild(p);
  1346	        return;
  1347	      }
  1348	      case 'error': {
  1349	        const p = document.createElement('p');
  1350	        p.className = 'raw-error';
  1351	        p.textContent = 'Failed to load raw preview: network unreachable.';
  1352	        host.appendChild(p);
  1353	        host.appendChild(retryButton());
  1354	        return;
  1355	      }
  1356	      case 'non_2xx': {
  1357	        const p = document.createElement('p');
  1358	        p.className = 'raw-error';
  1359	        p.textContent = 'HTTP 503: backend unavailable — service is restarting.';
  1360	        host.appendChild(p);
  1361	        host.appendChild(retryButton());
  1362	        return;
  1363	      }
  1364	      case 'not_imported': {
  1365	        const p = document.createElement('p');
  1366	        p.className = 'not-imported';
  1367	        p.textContent =
  1368	          'This session has not been imported yet — only the source-side '
  1369	          + 'metadata is available. Click Import in the action bar to fetch '
  1370	          + 'the raw payload.';
  1371	        host.appendChild(p);
  1372	        return;
  1373	      }
  1374	      case 'success':
  1375	      default: {
  1376	        const pre = document.createElement('pre');
  1377	        pre.className = 'raw-pre';
  1378	        pre.setAttribute('aria-label', 'Raw NDJSON preview');
  1379	        const lines = [
  1380	          '{"type":"user","message":{"role":"user","content":"Refactor the SessionView..."},"timestamp":"2026-04-28T17:42:00Z"}',
  1381	          '{"type":"assistant","message":{"role":"assistant","content":[{"type":"text","text":"I\'ll start by extracting SessionMetadata from SessionDetail."}]},"timestamp":"2026-04-28T17:42:05Z"}',
  1382	          '{"type":"assistant","message":{"role":"assistant","content":[{"type":"tool_use","id":"toolu_01","name":"Read","input":{"file_path":"/repo/apps/frontend/src/features/sessions/SessionDetail.tsx"}}]},"timestamp":"2026-04-28T17:42:09Z"}',
  1383	          '{"type":"user","message":{"role":"user","content":[{"type":"tool_result","tool_use_id":"toolu_01","content":"// 478 lines of SessionDetail.tsx ..."}]},"timestamp":"2026-04-28T17:42:09Z"}',
  1384	          '{"type":"assistant","message":{"role":"assistant","content":[{"type":"text","text":"Got it — copying the <dl> block verbatim."}]},"timestamp":"2026-04-28T17:42:11Z"}',
  1385	          '{"type":"assistant","message":{"role":"assistant","content":[{"type":"tool_use","id":"toolu_02","name":"Write","input":{"file_path":"/repo/apps/frontend/src/features/sessions/SessionMetadata.tsx"}}]},"timestamp":"2026-04-28T17:42:13Z"}',
  1386	          '{"type":"user","message":{"role":"user","content":[{"type":"tool_result","tool_use_id":"toolu_02","content":"OK"}]},"timestamp":"2026-04-28T17:42:13Z"}',
  1387	          '{"type":"summary","summary":"M2b extraction in flight","leafUuid":"0a3"}',
  1388	          '{"type":"user","message":{"role":"user","content":"Now wire the Tabs primitive."},"timestamp":"2026-04-28T17:42:18Z"}',
  1389	          '{"type":"assistant","message":{"role":"assistant","content":[{"type":"text","text":"Tabs.tsx is going to render aria-tab buttons + indicator span."}]},"timestamp":"2026-04-28T17:42:21Z"}',
  1390	          'not-a-json-line — bad sentinel for the parser',
  1391	          '{"type":"assistant","message":{"role":"assistant","content":[{"type":"text","text":"Indicator slides via transform: translateX driven by data-active-tab."}]},"timestamp":"2026-04-28T17:42:25Z"}',
  1392	        ];
  1393	        for (const raw of lines) {
  1394	          const line = document.createElement('div');
  1395	          line.className = 'line';
  1396	          if (raw.startsWith('{') || raw.startsWith('[')) {
  1397	            line.textContent = raw;
  1398	          } else {
  1399	            line.classList.add('text');
  1400	            line.textContent = raw + ' ';
  1401	            const m = document.createElement('span');
  1402	            m.className = 'marker';
  1403	            m.textContent = '(non-JSON line)';
  1404	            line.appendChild(m);
  1405	          }
  1406	          pre.appendChild(line);
  1407	        }
  1408	        const cap = document.createElement('p');
  1409	        cap.className = 'raw-caption';
  1410	        cap.textContent = 'Showing first 12 lines (full payload below the caps).';
  1411	        host.appendChild(pre);
  1412	        host.appendChild(cap);
  1413	        return;
  1414	      }
  1415	    }
  1416	  }
  1417	
  1418	  function retryButton() {
  1419	    const btn = document.createElement('button');
  1420	    btn.type = 'button';
  1421	    btn.className = 'retry-btn';
  1422	    btn.textContent = 'Retry';
  1423	    btn.addEventListener('click', () => {
  1424	      rawState = 'loading';
  1425	      // re-render Raw panel CONTENT in place (panel container stays mounted;
  1426	      // keep-mounted contract preserved).
  1427	      repopulatePanelContent('raw');
  1428	      setTimeout(() => {
  1429	        rawState = 'success';
  1430	        repopulatePanelContent('raw');
  1431	      }, 600);
  1432	    });
  1433	    return btn;
  1434	  }
  1435	

exec
/bin/bash -lc "nl -ba working/phase-5/designs/m2b-shell/design.md | sed -n '190,252p' && nl -ba working/phase-5/designs/m2b-shell/design.md | sed -n '632,645p'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
   190	  transition only animates `transform` (compositor-cheap; no layout
   191	  reflow). **Do not omit `width: 1px`** — an empty absolutely-
   192	  positioned span resolves to `width: auto` (= 0); `scaleX(N) × 0 = 0`
   193	  would render an invisible indicator.
   194	- **Disabled state pinned for future**: `aria-disabled="true"` on a
   195	  tab makes it `opacity: 0.4; cursor: not-allowed; pointer-events:
   196	  none;`. M2b ships no disabled tabs but the recipe is on file.
   197	
   198	### 3.2 SessionView shell (`apps/frontend/src/features/sessions/SessionView.tsx`)
   199	
   200	```text
   201	<article class="session-pane" key={selectedRowKey} aria-label="Session view">
   202	  {/* Narrow-mode "Back to list" quiet text-button row — only renders below 900 px. */}
   203	  <div class="back-to-list">…</div>
   204	
   205	  <header class="session-header">
   206	    <h2 class="session-title">{row.title || "(untitled)"}</h2>
   207	    <span class="tool-badge">{row.tool}</span>
   208	    <span class="badge {variant}">{row.status}</span>
   209	    {row.statusConflict && <span class="badge conflict">Conflict</span>}
   210	  </header>
   211	
   212	  <Tabs activeTab={activeTab} onActivate={setActiveTab}
   213	        defaultTab={DEFAULT_TAB_ON_SELECTION}
   214	        labels={[…Transcript / Skim / Raw / Metadata]} />
   215	
   216	  <div class="tabpanels">
   217	    <TabPanel id="panel-transcript" labelledBy="tab-transcript"
   218	              isActive={activeTab === "transcript"}
   219	              isVisited={visitedTabs.has("transcript")}>
   220	      {/* placeholder — STABLE React subtree, not remounted */}
   221	    </TabPanel>
   222	    {/* …Skim, Raw, Metadata identical structure */}
   223	  </div>
   224	</article>
   225	```
   226	
   227	- **`key={selectedRowKey}`** on the outer `<article>` — every
   228	  selection change remounts the whole subtree, which fires the
   229	  page-turn fade @keyframes.
   230	- **`activeTab` state lives on `SessionView`** (component-local,
   231	  NOT URL-synced — Resolved Decision #2). Reset to
   232	  `DEFAULT_TAB_ON_SELECTION` on `selectedRowKey` change via the same
   233	  remount.
   234	- **`visitedTabs: Set<TabId>`** state lives on `SessionView`,
   235	  initialized to `new Set([DEFAULT_TAB_ON_SELECTION])` on every
   236	  remount (= every selection change). Setting `setActiveTab(id)`
   237	  also adds `id` to `visitedTabs`.
   238	- **TabPanel** is a thin wrapper: renders the outer `<div
   239	  role="tabpanel" hidden={!isActive}>` only when `isVisited`; renders
   240	  `null` when `!isVisited`. The cross-fade-IN is driven by a CSS
   241	  `animation-name` toggle on the SAME `<div role="tabpanel">`
   242	  element: when `isActive=true`, an inline `style={{ animation:
   243	  "tab-fade-in 120ms var(--ease-out) both" }}` is applied; when
   244	  `isActive=false`, `style={{ animation: "none" }}` is applied. The
   245	  browser restarts the animation each time the `animation` property
   246	  string changes from `"none"` to `"tab-fade-in …"`. The panel's
   247	  React tree is **NOT remounted** on tab change — keep-mounted state
   248	  (RawTab's in-flight consumer, M3+ Skim/Transcript parser results)
   249	  survives intact. **Do NOT add `key={activeTab}` to the panel
   250	  content** — that would defeat Resolved Decision #12.
   251	
   252	- **M1a → M2b minimal header delta** (verified by reading
   632	
   633	**Inner cross-fade — non-remount, CSS animation-name re-trigger:**
   634	
   635	```jsx
   636	<div role="tabpanel"
   637	     hidden={!isActive}
   638	     id={`panel-${tabId}`}
   639	     aria-labelledby={`tab-${tabId}`}
   640	     style={{ animation: isActive
   641	                          ? "tab-fade-in 120ms var(--ease-out) both"
   642	                          : "none" }}>
   643	  {/* STABLE React subtree — NOT keyed on activeTab.
   644	      RawTab's in-flight stream survives tab flips. */}
   645	  {content}

exec
/bin/bash -lc "nl -ba working/phase-5/designs/m2b-shell/prototype.html | sed -n '552,582p' && nl -ba working/phase-5/designs/m2b-shell/prototype.html | sed -n '930,960p'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
   552	[role="tabpanel"] {
   553	  /* outer shell — provides ARIA + a11y. Inactive-but-visited panels carry
   554	   * the `hidden` HTML attribute (display:none + accessibility tree exclusion).
   555	   * The browser handles hidden semantics — no extra CSS needed beyond
   556	   * letting the default user-agent stylesheet apply. */
   557	  padding: var(--space-6) var(--space-4);  /* --space-5 not in token scale; --space-4 (16 px) is the closest fit */
   558	  max-inline-size: var(--measure);          /* 70ch reading measure */
   559	  margin-inline: 0;
   560	}
   561	
   562	/* Cross-fade-IN — driven by an inline `animation` style toggled on
   563	 * the active tabpanel from `none` → `tab-fade-in 120ms var(--ease-out) both`.
   564	 * The keep-mounted contract (Resolved Decision #12 / spec lines 650–658)
   565	 * forbids remounting the panel content, so we use animation-name
   566	 * re-trigger instead of React `key={activeTab}` remount. The browser
   567	 * starts the keyframes whenever the inline animation property string
   568	 * changes from `none` to a real value. */
   569	@keyframes tab-fade-in {
   570	  from { opacity: 0; }
   571	  to   { opacity: 1; }
   572	}
   573	
   574	/* ============================================================================
   575	 *  Metadata tab body — 18-field <dl> + subagent badge + "Open raw" anchor.
   576	 * =========================================================================== */
   577	.metadata-meta {
   578	  display: grid;
   579	  grid-template-columns: minmax(11rem, max-content) 1fr;
   580	  column-gap: var(--space-4);
   581	  row-gap: var(--space-2);
   582	  margin: 0;
   930	      <button id="tab-transcript" role="tab" aria-controls="panel-transcript" aria-selected="false" tabindex="-1">Transcript</button>
   931	      <button id="tab-skim"       role="tab" aria-controls="panel-skim"       aria-selected="false" tabindex="-1">Skim</button>
   932	      <button id="tab-raw"        role="tab" aria-controls="panel-raw"        aria-selected="false" tabindex="-1">Raw</button>
   933	      <button id="tab-metadata"   role="tab" aria-controls="panel-metadata"   aria-selected="true"  tabindex="0">Metadata</button>
   934	      <span class="indicator" aria-hidden="true"></span>
   935	    </div>
   936	
   937	    <!-- TAB PANELS — visited tabs stay mounted with `hidden`, unvisited
   938	         remain unmounted. The panel's React subtree is STABLE (NOT keyed
   939	         on activeTab); the cross-fade-IN is driven by toggling an inline
   940	         `style.animation` from `none` → `tab-fade-in 120ms var(--ease-out) both`
   941	         on the `<div role="tabpanel">` itself. See activate() below.
   942	
   943	         tabIndex={0} is added by the activate() function to the active
   944	         placeholder panel (Skim, Transcript) only — Metadata + Raw have
   945	         focusable first content (Copy path button / Retry / Open raw)
   946	         and don't need it. WAI-ARIA APG Tabs pattern. -->
   947	    <div class="tabpanels">
   948	      <div id="panel-transcript" role="tabpanel" aria-labelledby="tab-transcript" hidden>
   949	        <div class="tabpanel-content"></div>
   950	      </div>
   951	      <div id="panel-skim" role="tabpanel" aria-labelledby="tab-skim" hidden>
   952	        <div class="tabpanel-content"></div>
   953	      </div>
   954	      <div id="panel-raw" role="tabpanel" aria-labelledby="tab-raw" hidden>
   955	        <div class="tabpanel-content"></div>
   956	      </div>
   957	      <div id="panel-metadata" role="tabpanel" aria-labelledby="tab-metadata">
   958	        <div class="tabpanel-content"></div>
   959	      </div>
   960	    </div>

exec
/bin/bash -lc "nl -ba apps/frontend/src/features/sessions/SessionDetail.tsx | sed -n '1,170p'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
     1	// Drawer body for the unified inspection list.
     2	//
     3	// Renders every field on the merged `SessionRow` so a user can inspect
     4	// a single session without leaving the page. Per
     5	// `working/phase-4.md` §Session Detail Drawer the body contains:
     6	//
     7	//   - Title header with the tool badge, status pill, and (when
     8	//     `statusConflict`) a small "Conflict" badge alongside.
     9	//   - Metadata `<dl>` listing every SessionRow field with a labeled
    10	//     `<dt>` + `<dd>`. Timestamp fields render the absolute ISO value
    11	//     PLUS a relative form via `relativeTimeFrom(now, value)`. The
    12	//     `createdAt` and `sourceUpdatedAt` fields are annotated
    13	//     "(source clock)" because the backend records them from the
    14	//     source machine and they may be skewed; `ingestedAt` is annotated
    15	//     "(backend clock)" because it is monotonic.
    16	//   - Source path with a "Last seen source path:" label swap when
    17	//     `sourcePathIsStale` is true (the file is no longer
    18	//     discoverable). A copy-to-clipboard button sits next to it; the
    19	//     fallback when `navigator.clipboard` is unavailable selects the
    20	//     path text so the user can copy it manually with Ctrl+C.
    21	//   - "View raw" anchor (only rendered for stored sessions, i.e.
    22	//     `storedSessionUid !== null`) opens the existing
    23	//     `/api/v1/sessions/:uid/raw` endpoint in a new tab.
    24	//   - Raw preview block (stored sessions only). The block streams the
    25	//     `/api/v1/sessions/:uid/raw` endpoint via `streamSessionRaw` +
    26	//     `consumeRawPreview` and renders the structured `RawPreviewState`
    27	//     (loading / success / error / non_2xx). M4 Chunk E2 added the
    28	//     live block — the placeholder from E1 is gone.
    29	//
    30	// SessionDetail is presentational EXCEPT for the raw-preview block,
    31	// which owns its own fetch lifecycle (one AbortController per
    32	// SessionDetail mount, restarted whenever the row changes — e.g. the
    33	// user closes the drawer and opens a different row). The parent
    34	// (`SessionsView`) owns the open-state, the row lookup, and the
    35	// "now" timestamp pinning. All field reads are done at render time
    36	// so the drawer always shows whatever the merged row carries when
    37	// the parent picks a key.
    38	//
    39	// As of M6 (Chunk G) the status pill is rendered inline (the dedicated
    40	// `StatusBadge` component was retired). The transform —
    41	// `variant = status.replace(/_/g, "-")` for the CSS class and
    42	// `label = status.replace(/_/g, " ")` for the visible text — is
    43	// preserved byte-for-byte at both call sites (drawer header + status
    44	// `<dd>`) so the DOM shape stays `<span class="badge {variant}">
    45	// {label}</span>` and matches `SessionsTable.tsx` exactly.
    46	//
    47	// CSS lives in the sibling `SessionDetail.css` (drawer-body chrome +
    48	// raw-preview block). The drawer SHELL (`.drawer`, `.drawer::backdrop`,
    49	// `.drawer-header`, `.drawer-title`, `.drawer-body`, `.drawer-close`)
    50	// lives in `components/Drawer.css`. The `.badge` shell + variant
    51	// recipes are in `SessionsTable.css`.
    52	import { useCallback, useEffect, useRef, useState } from "react";
    53	import { ApiError, streamSessionRaw } from "../../lib/api";
    54	import { relativeTimeFrom } from "./relativeTime";
    55	import {
    56	  consumeRawPreview,
    57	  type RawPreviewLine,
    58	  type RawPreviewState,
    59	} from "./rawPreview";
    60	import type { SessionRow } from "./types";
    61	import "./SessionDetail.css";
    62	
    63	export type SessionDetailProps = {
    64	  /** The merged row to render. */
    65	  row: SessionRow;
    66	  /** Pinned-`now` ISO string used for relative-time labelling. */
    67	  now: string;
    68	};
    69	
    70	const COPY_HINT_TIMEOUT_MS = 2000;
    71	
    72	export function SessionDetail({ row, now }: SessionDetailProps) {
    73	  const sourcePath = row.sourcePath;
    74	  const sourcePathRef = useRef<HTMLSpanElement | null>(null);
    75	  const [copyHint, setCopyHint] = useState<
    76	    "idle" | "copied" | "fallback"
    77	  >("idle");
    78	
    79	  // Reset the "Copied" / "Select to copy" hint after a short delay so
    80	  // it does not stay forever.
    81	  useEffect(() => {
    82	    if (copyHint === "idle") return;
    83	    const id = setTimeout(() => setCopyHint("idle"), COPY_HINT_TIMEOUT_MS);
    84	    return () => clearTimeout(id);
    85	  }, [copyHint]);
    86	
    87	  // Reset the hint when the row changes (the user opened a different
    88	  // drawer entry).
    89	  useEffect(() => {
    90	    setCopyHint("idle");
    91	  }, [row.rowKey]);
    92	
    93	  const handleCopyPath = async () => {
    94	    // Try the modern Clipboard API first. Browsers without it (older
    95	    // Chromium variants, locked-down test runners, jsdom-style shims)
    96	    // throw on call or simply do not expose the API; fall through to
    97	    // the manual-select fallback.
    98	    const clipboard = (
    99	      globalThis as unknown as {
   100	        navigator?: { clipboard?: { writeText?: (s: string) => Promise<void> } };
   101	      }
   102	    ).navigator?.clipboard;
   103	    if (clipboard?.writeText) {
   104	      try {
   105	        await clipboard.writeText(sourcePath);
   106	        setCopyHint("copied");
   107	        return;
   108	      } catch {
   109	        // Fall through to the manual-select fallback.
   110	      }
   111	    }
   112	    // Fallback: programmatically select the path text so the user can
   113	    // press Ctrl/Cmd + C themselves. We never throw — clipboard
   114	    // failure should not break the drawer.
   115	    const span = sourcePathRef.current;
   116	    if (span !== null) {
   117	      const range = document.createRange();
   118	      range.selectNodeContents(span);
   119	      const selection = (
   120	        globalThis as unknown as { getSelection?: () => Selection | null }
   121	      ).getSelection?.();
   122	      if (selection) {
   123	        selection.removeAllRanges();
   124	        selection.addRange(range);
   125	      }
   126	    }
   127	    setCopyHint("fallback");
   128	  };
   129	
   130	  const sourcePathLabel = row.sourcePathIsStale
   131	    ? "Last seen source path:"
   132	    : "Source path:";
   133	
   134	  // Only render the "View raw" anchor and the raw preview placeholder
   135	  // when the session is actually stored. A `source_only` row has no
   136	  // backend resource to view.
   137	  const rawHref =
   138	    row.storedSessionUid !== null
   139	      ? `/api/v1/sessions/${row.storedSessionUid}/raw`
   140	      : null;
   141	
   142	  return (
   143	    <div className="drawer-body">
   144	      <header className="drawer-header">
   145	        <h2 className="drawer-title">{row.title ?? "(untitled)"}</h2>
   146	        <span className="badge mono drawer-tool-badge">{row.tool}</span>
   147	        <span className={`badge ${row.status.replace(/_/g, "-")}`}>
   148	          {row.status.replace(/_/g, " ")}
   149	        </span>
   150	        {row.statusConflict ? (
   151	          <span
   152	            className="badge drawer-conflict-badge"
   153	            title="Source and stored status disagreed during load — refresh to re-fetch."
   154	          >
   155	            Conflict
   156	          </span>
   157	        ) : null}
   158	      </header>
   159	
   160	      <dl className="drawer-meta">
   161	        <dt>session_key</dt>
   162	        <dd className="mono">{row.sourceSessionKey ?? "—"}</dd>
   163	
   164	        <dt>session_uid</dt>
   165	        <dd className="mono">{row.storedSessionUid ?? "—"}</dd>
   166	
   167	        <dt>row_key</dt>
   168	        <dd className="mono">{row.rowKey}</dd>
   169	
   170	        <dt>tool</dt>

exec
/bin/bash -lc 'rg -n "RawPreview|raw-preview|raw-pre|tabIndex|pre|Retry|raw" apps/frontend/src/features/sessions/SessionDetail.tsx apps/frontend/src/features/sessions/SessionDetail.css' in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
apps/frontend/src/features/sessions/SessionDetail.css:2: * Feature-local CSS for the SessionDetail drawer body.
apps/frontend/src/features/sessions/SessionDetail.css:6: * Selectors owned here (M4 drawer-body + raw-preview vocabulary):
apps/frontend/src/features/sessions/SessionDetail.css:7: *   Drawer-body chrome (M4 Chunk E1):
apps/frontend/src/features/sessions/SessionDetail.css:8: *     - `.drawer-meta`, `.drawer-meta dt`, `.drawer-meta dd`
apps/frontend/src/features/sessions/SessionDetail.css:9: *     - `.drawer-source-path`
apps/frontend/src/features/sessions/SessionDetail.css:10: *     - `.drawer-copy-btn`, `.drawer-copy-btn:hover`, `.drawer-copy-hint`
apps/frontend/src/features/sessions/SessionDetail.css:11: *     - `.drawer-raw-link-row`, `.drawer-raw-preview`,
apps/frontend/src/features/sessions/SessionDetail.css:12: *       `.drawer-raw-preview h3`
apps/frontend/src/features/sessions/SessionDetail.css:13: *     - `.drawer-tool-badge` — header tool pill; reuses the `.badge`
apps/frontend/src/features/sessions/SessionDetail.css:17: *     - `.drawer-conflict-badge` — header "Conflict" pill; reuses the
apps/frontend/src/features/sessions/SessionDetail.css:21: *   Streaming raw-preview block (M4 Chunk E2):
apps/frontend/src/features/sessions/SessionDetail.css:22: *     - `.raw-preview` — `<pre>` wrapper; max-height clamp + internal
apps/frontend/src/features/sessions/SessionDetail.css:23: *       scroll so a 20-line stream still fits within the drawer body.
apps/frontend/src/features/sessions/SessionDetail.css:24: *     - `.raw-preview-line`, `.raw-preview-line.text`
apps/frontend/src/features/sessions/SessionDetail.css:25: *     - `.raw-preview-fallback-marker`
apps/frontend/src/features/sessions/SessionDetail.css:26: *     - `.raw-preview-caption`, `.raw-preview-error`,
apps/frontend/src/features/sessions/SessionDetail.css:27: *       `.raw-preview-loading`
apps/frontend/src/features/sessions/SessionDetail.css:30: *   - `max-height: 16rem` on `.raw-preview` — caps preview block so
apps/frontend/src/features/sessions/SessionDetail.css:31: *     the drawer body still fits in the viewport even when the
apps/frontend/src/features/sessions/SessionDetail.css:34: * The drawer SHELL (`.drawer`, `.drawer::backdrop`, `.drawer-header`,
apps/frontend/src/features/sessions/SessionDetail.css:35: * `.drawer-title`, `.drawer-body`, `.drawer-close`) lives in
apps/frontend/src/features/sessions/SessionDetail.css:36: * `Drawer.css` next to the consuming component; the drawer-body content
apps/frontend/src/features/sessions/SessionDetail.css:40:.drawer-tool-badge {
apps/frontend/src/features/sessions/SessionDetail.css:50:.drawer-conflict-badge {
apps/frontend/src/features/sessions/SessionDetail.css:51:  /* Conflict pill in the drawer header. Reuses the M1 source-missing
apps/frontend/src/features/sessions/SessionDetail.css:59:.drawer-meta {
apps/frontend/src/features/sessions/SessionDetail.css:66:.drawer-meta dt {
apps/frontend/src/features/sessions/SessionDetail.css:72:.drawer-meta dd {
apps/frontend/src/features/sessions/SessionDetail.css:78:.drawer-source-path {
apps/frontend/src/features/sessions/SessionDetail.css:80:   * grid cell so they don't blow out the drawer width. */
apps/frontend/src/features/sessions/SessionDetail.css:84:.drawer-copy-btn {
apps/frontend/src/features/sessions/SessionDetail.css:95:.drawer-copy-btn:hover {
apps/frontend/src/features/sessions/SessionDetail.css:99:.drawer-copy-hint {
apps/frontend/src/features/sessions/SessionDetail.css:103:.drawer-raw-link-row {
apps/frontend/src/features/sessions/SessionDetail.css:107:.drawer-raw-preview {
apps/frontend/src/features/sessions/SessionDetail.css:111:.drawer-raw-preview h3 {
apps/frontend/src/features/sessions/SessionDetail.css:117:/* ---------- Streaming raw preview block (M4 Chunk E2) ---------- */
apps/frontend/src/features/sessions/SessionDetail.css:119:/* `<pre>` wrapper around the rendered NDJSON lines. Capped at 16rem
apps/frontend/src/features/sessions/SessionDetail.css:120: * tall so the drawer body still fits in the viewport even when the
apps/frontend/src/features/sessions/SessionDetail.css:122: * horizontally so the user never scrolls sideways inside the preview
apps/frontend/src/features/sessions/SessionDetail.css:124:.raw-preview {
apps/frontend/src/features/sessions/SessionDetail.css:136:.raw-preview-line {
apps/frontend/src/features/sessions/SessionDetail.css:137:  white-space: pre-wrap;
apps/frontend/src/features/sessions/SessionDetail.css:146: * tone for the same reason. AA contrast is preserved (text-muted on
apps/frontend/src/features/sessions/SessionDetail.css:149:.raw-preview-line.text {
apps/frontend/src/features/sessions/SessionDetail.css:153:.raw-preview-fallback-marker {
apps/frontend/src/features/sessions/SessionDetail.css:158:.raw-preview-caption {
apps/frontend/src/features/sessions/SessionDetail.css:165:.raw-preview-error {
apps/frontend/src/features/sessions/SessionDetail.css:170:.raw-preview-loading {
apps/frontend/src/features/sessions/SessionDetail.tsx:1:// Drawer body for the unified inspection list.
apps/frontend/src/features/sessions/SessionDetail.tsx:5:// `working/phase-4.md` §Session Detail Drawer the body contains:
apps/frontend/src/features/sessions/SessionDetail.tsx:21://   - "View raw" anchor (only rendered for stored sessions, i.e.
apps/frontend/src/features/sessions/SessionDetail.tsx:23://     `/api/v1/sessions/:uid/raw` endpoint in a new tab.
apps/frontend/src/features/sessions/SessionDetail.tsx:24://   - Raw preview block (stored sessions only). The block streams the
apps/frontend/src/features/sessions/SessionDetail.tsx:25://     `/api/v1/sessions/:uid/raw` endpoint via `streamSessionRaw` +
apps/frontend/src/features/sessions/SessionDetail.tsx:26://     `consumeRawPreview` and renders the structured `RawPreviewState`
apps/frontend/src/features/sessions/SessionDetail.tsx:30:// SessionDetail is presentational EXCEPT for the raw-preview block,
apps/frontend/src/features/sessions/SessionDetail.tsx:33:// user closes the drawer and opens a different row). The parent
apps/frontend/src/features/sessions/SessionDetail.tsx:36:// so the drawer always shows whatever the merged row carries when
apps/frontend/src/features/sessions/SessionDetail.tsx:43:// preserved byte-for-byte at both call sites (drawer header + status
apps/frontend/src/features/sessions/SessionDetail.tsx:47:// CSS lives in the sibling `SessionDetail.css` (drawer-body chrome +
apps/frontend/src/features/sessions/SessionDetail.tsx:48:// raw-preview block). The drawer SHELL (`.drawer`, `.drawer::backdrop`,
apps/frontend/src/features/sessions/SessionDetail.tsx:49:// `.drawer-header`, `.drawer-title`, `.drawer-body`, `.drawer-close`)
apps/frontend/src/features/sessions/SessionDetail.tsx:50:// lives in `components/Drawer.css`. The `.badge` shell + variant
apps/frontend/src/features/sessions/SessionDetail.tsx:56:  consumeRawPreview,
apps/frontend/src/features/sessions/SessionDetail.tsx:57:  type RawPreviewLine,
apps/frontend/src/features/sessions/SessionDetail.tsx:58:  type RawPreviewState,
apps/frontend/src/features/sessions/SessionDetail.tsx:59:} from "./rawPreview";
apps/frontend/src/features/sessions/SessionDetail.tsx:88:  // drawer entry).
apps/frontend/src/features/sessions/SessionDetail.tsx:113:    // press Ctrl/Cmd + C themselves. We never throw — clipboard
apps/frontend/src/features/sessions/SessionDetail.tsx:114:    // failure should not break the drawer.
apps/frontend/src/features/sessions/SessionDetail.tsx:134:  // Only render the "View raw" anchor and the raw preview placeholder
apps/frontend/src/features/sessions/SessionDetail.tsx:137:  const rawHref =
apps/frontend/src/features/sessions/SessionDetail.tsx:139:      ? `/api/v1/sessions/${row.storedSessionUid}/raw`
apps/frontend/src/features/sessions/SessionDetail.tsx:143:    <div className="drawer-body">
apps/frontend/src/features/sessions/SessionDetail.tsx:144:      <header className="drawer-header">
apps/frontend/src/features/sessions/SessionDetail.tsx:145:        <h2 className="drawer-title">{row.title ?? "(untitled)"}</h2>
apps/frontend/src/features/sessions/SessionDetail.tsx:146:        <span className="badge mono drawer-tool-badge">{row.tool}</span>
apps/frontend/src/features/sessions/SessionDetail.tsx:152:            className="badge drawer-conflict-badge"
apps/frontend/src/features/sessions/SessionDetail.tsx:160:      <dl className="drawer-meta">
apps/frontend/src/features/sessions/SessionDetail.tsx:176:        <dt>presence</dt>
apps/frontend/src/features/sessions/SessionDetail.tsx:177:        <dd className="mono">{row.presence}</dd>
apps/frontend/src/features/sessions/SessionDetail.tsx:203:          <span className="mono drawer-source-path" ref={sourcePathRef}>
apps/frontend/src/features/sessions/SessionDetail.tsx:209:            className="drawer-copy-btn"
apps/frontend/src/features/sessions/SessionDetail.tsx:219:              <span className="muted drawer-copy-hint">Copied</span>
apps/frontend/src/features/sessions/SessionDetail.tsx:225:              <span className="muted drawer-copy-hint">
apps/frontend/src/features/sessions/SessionDetail.tsx:226:                Selected — press Ctrl/Cmd + C to copy
apps/frontend/src/features/sessions/SessionDetail.tsx:245:        <dt>stored_raw_ref</dt>
apps/frontend/src/features/sessions/SessionDetail.tsx:258:      {rawHref !== null ? (
apps/frontend/src/features/sessions/SessionDetail.tsx:259:        <p className="drawer-raw-link-row">
apps/frontend/src/features/sessions/SessionDetail.tsx:261:            className="raw-link"
apps/frontend/src/features/sessions/SessionDetail.tsx:262:            href={rawHref}
apps/frontend/src/features/sessions/SessionDetail.tsx:266:            View raw
apps/frontend/src/features/sessions/SessionDetail.tsx:272:        <section className="drawer-raw-preview">
apps/frontend/src/features/sessions/SessionDetail.tsx:273:          <h3>Raw preview</h3>
apps/frontend/src/features/sessions/SessionDetail.tsx:274:          <RawPreviewBlock sessionUid={row.storedSessionUid} />
apps/frontend/src/features/sessions/SessionDetail.tsx:282: * Streaming raw-preview block.
apps/frontend/src/features/sessions/SessionDetail.tsx:286: * `streamSessionRaw` + `consumeRawPreview`, and stores the resulting
apps/frontend/src/features/sessions/SessionDetail.tsx:287: * `RawPreviewState`. On unmount (drawer close OR a different row
apps/frontend/src/features/sessions/SessionDetail.tsx:292: * The Retry button wires through to the same effect by bumping a
apps/frontend/src/features/sessions/SessionDetail.tsx:296:type RawPreviewBlockProps = { sessionUid: string };
apps/frontend/src/features/sessions/SessionDetail.tsx:298:function RawPreviewBlock({ sessionUid }: RawPreviewBlockProps) {
apps/frontend/src/features/sessions/SessionDetail.tsx:299:  const [state, setState] = useState<RawPreviewState>({ kind: "loading" });
apps/frontend/src/features/sessions/SessionDetail.tsx:301:  // to refire the fetch on Retry without duplicating the logic.
apps/frontend/src/features/sessions/SessionDetail.tsx:312:        const result = await consumeRawPreview(response, controller.signal);
apps/frontend/src/features/sessions/SessionDetail.tsx:341:  const handleRetry = useCallback(() => {
apps/frontend/src/features/sessions/SessionDetail.tsx:347:      <p className="raw-preview-loading">Loading raw preview…</p>
apps/frontend/src/features/sessions/SessionDetail.tsx:354:        <p className="raw-preview-error">
apps/frontend/src/features/sessions/SessionDetail.tsx:355:          Failed to load raw preview: {state.message}
apps/frontend/src/features/sessions/SessionDetail.tsx:358:          <button type="button" onClick={handleRetry}>
apps/frontend/src/features/sessions/SessionDetail.tsx:359:            Retry
apps/frontend/src/features/sessions/SessionDetail.tsx:369:        <p className="raw-preview-error">
apps/frontend/src/features/sessions/SessionDetail.tsx:373:          <button type="button" onClick={handleRetry}>
apps/frontend/src/features/sessions/SessionDetail.tsx:374:            Retry
apps/frontend/src/features/sessions/SessionDetail.tsx:391:      <pre className="raw-preview" aria-label="Raw NDJSON preview">
apps/frontend/src/features/sessions/SessionDetail.tsx:393:          <RawPreviewLineRow key={idx} line={line} />
apps/frontend/src/features/sessions/SessionDetail.tsx:395:      </pre>
apps/frontend/src/features/sessions/SessionDetail.tsx:396:      <p className="raw-preview-caption">{caption}</p>
apps/frontend/src/features/sessions/SessionDetail.tsx:402: * One rendered preview line. JSON lines render their raw NDJSON; text
apps/frontend/src/features/sessions/SessionDetail.tsx:408:function RawPreviewLineRow({ line }: { line: RawPreviewLine }) {
apps/frontend/src/features/sessions/SessionDetail.tsx:410:    return <div className="raw-preview-line">{line.raw}</div>;
apps/frontend/src/features/sessions/SessionDetail.tsx:413:    <div className="raw-preview-line text">
apps/frontend/src/features/sessions/SessionDetail.tsx:414:      {line.raw}
apps/frontend/src/features/sessions/SessionDetail.tsx:416:      <span className="raw-preview-fallback-marker">(non-JSON line)</span>
apps/frontend/src/features/sessions/SessionDetail.tsx:426: *     §Session Detail Drawer)
apps/frontend/src/features/sessions/SessionDetail.tsx:427: *   - line cap fired → "Showing first 20 lines of the raw payload."
apps/frontend/src/features/sessions/SessionDetail.tsx:445:    return `Showing first ${lineCount} lines of the raw payload.`;
apps/frontend/src/features/sessions/SessionDetail.tsx:452: * megabyte of HTML on a 500 does not blow out the drawer.

exec
/bin/bash -lc "nl -ba apps/frontend/src/features/sessions/SessionDetail.tsx | sed -n '256,360p'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
   256	      </dl>
   257	
   258	      {rawHref !== null ? (
   259	        <p className="drawer-raw-link-row">
   260	          <a
   261	            className="raw-link"
   262	            href={rawHref}
   263	            target="_blank"
   264	            rel="noopener noreferrer"
   265	          >
   266	            View raw
   267	          </a>
   268	        </p>
   269	      ) : null}
   270	
   271	      {row.storedSessionUid !== null ? (
   272	        <section className="drawer-raw-preview">
   273	          <h3>Raw preview</h3>
   274	          <RawPreviewBlock sessionUid={row.storedSessionUid} />
   275	        </section>
   276	      ) : null}
   277	    </div>
   278	  );
   279	}
   280	
   281	/**
   282	 * Streaming raw-preview block.
   283	 *
   284	 * Owns one fetch lifecycle per `sessionUid`. On mount (and whenever
   285	 * `sessionUid` changes), creates an `AbortController`, calls
   286	 * `streamSessionRaw` + `consumeRawPreview`, and stores the resulting
   287	 * `RawPreviewState`. On unmount (drawer close OR a different row
   288	 * opened), aborts the controller — the consumer's abort handling
   289	 * cancels the in-flight reader and re-throws AbortError, which we
   290	 * silently ignore.
   291	 *
   292	 * The Retry button wires through to the same effect by bumping a
   293	 * local "attempt" counter, which is included in the effect's dep
   294	 * array so the effect re-runs on click.
   295	 */
   296	type RawPreviewBlockProps = { sessionUid: string };
   297	
   298	function RawPreviewBlock({ sessionUid }: RawPreviewBlockProps) {
   299	  const [state, setState] = useState<RawPreviewState>({ kind: "loading" });
   300	  // Bumping `attempt` re-runs the effect, which is the cleanest way
   301	  // to refire the fetch on Retry without duplicating the logic.
   302	  const [attempt, setAttempt] = useState(0);
   303	
   304	  useEffect(() => {
   305	    let cancelled = false;
   306	    const controller = new AbortController();
   307	    setState({ kind: "loading" });
   308	
   309	    void (async () => {
   310	      try {
   311	        const response = await streamSessionRaw(sessionUid, controller.signal);
   312	        const result = await consumeRawPreview(response, controller.signal);
   313	        if (!cancelled) {
   314	          setState(result);
   315	        }
   316	      } catch (err) {
   317	        // AbortError is a normal close-time outcome; do not mutate
   318	        // state because the component is about to unmount or a
   319	        // newer effect has already taken over.
   320	        if (isAbortError(err)) return;
   321	        if (cancelled) return;
   322	        if (err instanceof ApiError) {
   323	          setState({
   324	            kind: "non_2xx",
   325	            status: err.status,
   326	            bodySnippet: truncateForDisplay(err.body),
   327	          });
   328	          return;
   329	        }
   330	        const message = err instanceof Error ? err.message : String(err);
   331	        setState({ kind: "error", message });
   332	      }
   333	    })();
   334	
   335	    return () => {
   336	      cancelled = true;
   337	      controller.abort();
   338	    };
   339	  }, [sessionUid, attempt]);
   340	
   341	  const handleRetry = useCallback(() => {
   342	    setAttempt((a) => a + 1);
   343	  }, []);
   344	
   345	  if (state.kind === "idle" || state.kind === "loading") {
   346	    return (
   347	      <p className="raw-preview-loading">Loading raw preview…</p>
   348	    );
   349	  }
   350	
   351	  if (state.kind === "error") {
   352	    return (
   353	      <div>
   354	        <p className="raw-preview-error">
   355	          Failed to load raw preview: {state.message}
   356	        </p>
   357	        <p>
   358	          <button type="button" onClick={handleRetry}>
   359	            Retry
   360	          </button>

exec
/bin/bash -lc "nl -ba apps/frontend/src/features/sessions/SessionDetail.tsx | sed -n '351,400p' && nl -ba apps/frontend/src/features/sessions/SessionDetail.css | sed -n '119,135p'" in /home/huwei/ai_codings/distill-portal
 succeeded in 0ms:
   351	  if (state.kind === "error") {
   352	    return (
   353	      <div>
   354	        <p className="raw-preview-error">
   355	          Failed to load raw preview: {state.message}
   356	        </p>
   357	        <p>
   358	          <button type="button" onClick={handleRetry}>
   359	            Retry
   360	          </button>
   361	        </p>
   362	      </div>
   363	    );
   364	  }
   365	
   366	  if (state.kind === "non_2xx") {
   367	    return (
   368	      <div>
   369	        <p className="raw-preview-error">
   370	          HTTP {state.status}: {state.bodySnippet}
   371	        </p>
   372	        <p>
   373	          <button type="button" onClick={handleRetry}>
   374	            Retry
   375	          </button>
   376	        </p>
   377	      </div>
   378	    );
   379	  }
   380	
   381	  // success
   382	  const { lines, reachedLineCap, reachedByteCap } = state;
   383	  const caption = describeCaption(
   384	    lines.length,
   385	    reachedLineCap,
   386	    reachedByteCap,
   387	  );
   388	
   389	  return (
   390	    <div>
   391	      <pre className="raw-preview" aria-label="Raw NDJSON preview">
   392	        {lines.map((line, idx) => (
   393	          <RawPreviewLineRow key={idx} line={line} />
   394	        ))}
   395	      </pre>
   396	      <p className="raw-preview-caption">{caption}</p>
   397	    </div>
   398	  );
   399	}
   400	
   119	/* `<pre>` wrapper around the rendered NDJSON lines. Capped at 16rem
   120	 * tall so the drawer body still fits in the viewport even when the
   121	 * payload streams 20 long lines; long lines wrap rather than overflow
   122	 * horizontally so the user never scrolls sideways inside the preview
   123	 * block. */
   124	.raw-preview {
   125	  margin: 0;
   126	  background: var(--color-surface);
   127	  border: 1px solid var(--color-border);
   128	  border-radius: var(--radius-md);
   129	  padding: var(--space-2) var(--space-3);
   130	  max-height: 16rem;
   131	  overflow: auto;
   132	  font-family: var(--font-mono);
   133	  font-size: var(--text-xs);
   134	}
   135	

  ✓ Gather relevant artifact/spec/code line references
  ✓ Verify the five round-1 fixes directly
  ✓ Recompute WCAG contrast for changed pairs
  ✓ Check for approval-blocking new defects or missing evidence
  → Return exact round-2 review structure
codex
- **Verdict**: needs changes

- **Round-1 Verification Trace**:
  1. verified fixed at `working/phase-5/designs/m2b-shell/prototype.html:758` / `:762` and `colors.md:120`
  2. verified fixed at `working/phase-5/designs/m2b-shell/prototype.html:492`, `:643`, `:689`, `:802`
  3. verified fixed at `working/phase-5/designs/m2b-shell/prototype.html:64`, `:557`, `:833`
  4. partially fixed (still needs `prototype.html:17-18` rewritten; it still says tab-panel cross-fade is via “React key remount” / “key-bumping wrapper,” contradicting the fixed keep-mounted mechanism)
  5. verified fixed at `working/phase-5/designs/m2b-shell/prototype.html:1083` and `design.md:1217`

- **Findings**:
  - BLOCKING — `working/phase-5/designs/m2b-shell/design.md:725`, spec section `design.md §6.1` / acceptance item 54: Raw is exempted from `tabIndex`, but Raw success renders a non-focusable `<pre>` in the artifact (`prototype.html:1376`) and no Retry button exists in success state. The promised “Raw via Retry / scrollable container” focus path is not actually specified unless the scrollable container gets `tabIndex={0}` or the Raw panel does when it has no focusable child.

- **WCAG Numerical Audit**:
  - R03 new light pair: `--color-ink-muted oklch(45% 0.02 70)` on `--color-surface-raised oklch(96% 0.01 70)`: luminance `0.090687` on `0.883835`, contrast `6.6377:1` — PASS AA normal text.
  - R03 new dark pair: `--color-ink-muted oklch(70% 0.01 70)` on `--color-surface-raised oklch(18% 0.01 70)`: luminance `0.342515` on `0.005796`, contrast `7.0348:1` — PASS AA normal text.
  - Old warn comparison: light warn-on-raised is ~`3.97–3.99:1` depending on sRGB clipping — FAIL; dark is `7.3407:1` — PASS.
  - No additional round-2 color pair introduced by the revision.

- **Missing Evidence**: none

- **Required Changes**:
  1. Replace `prototype.html:17-18` with the same animation-name re-trigger / stable subtree wording used elsewhere.
  2. Resolve Raw focusability: either make the Raw success scroll container focusable with `tabIndex={0}`, or add `tabIndex={0}` to the active Raw panel whenever Raw has no focusable child. Update `design.md §6.1`, checklist item 29a, test obligation 54, and prototype comments/code accordingly.

- **Notes**: none
tokens used
99,655
- **Verdict**: needs changes

- **Round-1 Verification Trace**:
  1. verified fixed at `working/phase-5/designs/m2b-shell/prototype.html:758` / `:762` and `colors.md:120`
  2. verified fixed at `working/phase-5/designs/m2b-shell/prototype.html:492`, `:643`, `:689`, `:802`
  3. verified fixed at `working/phase-5/designs/m2b-shell/prototype.html:64`, `:557`, `:833`
  4. partially fixed (still needs `prototype.html:17-18` rewritten; it still says tab-panel cross-fade is via “React key remount” / “key-bumping wrapper,” contradicting the fixed keep-mounted mechanism)
  5. verified fixed at `working/phase-5/designs/m2b-shell/prototype.html:1083` and `design.md:1217`

- **Findings**:
  - BLOCKING — `working/phase-5/designs/m2b-shell/design.md:725`, spec section `design.md §6.1` / acceptance item 54: Raw is exempted from `tabIndex`, but Raw success renders a non-focusable `<pre>` in the artifact (`prototype.html:1376`) and no Retry button exists in success state. The promised “Raw via Retry / scrollable container” focus path is not actually specified unless the scrollable container gets `tabIndex={0}` or the Raw panel does when it has no focusable child.

- **WCAG Numerical Audit**:
  - R03 new light pair: `--color-ink-muted oklch(45% 0.02 70)` on `--color-surface-raised oklch(96% 0.01 70)`: luminance `0.090687` on `0.883835`, contrast `6.6377:1` — PASS AA normal text.
  - R03 new dark pair: `--color-ink-muted oklch(70% 0.01 70)` on `--color-surface-raised oklch(18% 0.01 70)`: luminance `0.342515` on `0.005796`, contrast `7.0348:1` — PASS AA normal text.
  - Old warn comparison: light warn-on-raised is ~`3.97–3.99:1` depending on sRGB clipping — FAIL; dark is `7.3407:1` — PASS.
  - No additional round-2 color pair introduced by the revision.

- **Missing Evidence**: none

- **Required Changes**:
  1. Replace `prototype.html:17-18` with the same animation-name re-trigger / stable subtree wording used elsewhere.
  2. Resolve Raw focusability: either make the Raw success scroll container focusable with `tabIndex={0}`, or add `tabIndex={0}` to the active Raw panel whenever Raw has no focusable child. Update `design.md §6.1`, checklist item 29a, test obligation 54, and prototype comments/code accordingly.

- **Notes**: none
