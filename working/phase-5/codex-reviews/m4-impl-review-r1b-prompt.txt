External non-Claude reviewer for distill-portal Phase 5 M4 implementation (round 1, rerun).

## Round 1a result

Your previous run returned `needs more evidence` and asked permission to inspect. **You have full sandbox permission. Execute every check below directly. Return concrete file:line findings + command outputs.**

## What M4 ships

Logic-only chunk superseded; M4 is the FIRST visible UI surface in Phase 5. TranscriptView renders parsed messages on the Transcript tab. Mounts inside the M2b Tabs primitive.

## M4 implementation under review

NEW files:
- `/home/huwei/ai_codings/distill-portal/apps/frontend/src/features/sessions/TranscriptView.tsx` (566 lines)
- `/home/huwei/ai_codings/distill-portal/apps/frontend/src/features/sessions/TranscriptView.css` (471 lines, 27 tokens, ZERO new tokens, ZERO hex)
- `/home/huwei/ai_codings/distill-portal/apps/frontend/src/features/sessions/TranscriptView.test.tsx` (931 lines, 44 tests)
- `/home/huwei/ai_codings/distill-portal/apps/frontend/tests/fixtures/transcript-5k.builder.ts` (209 lines)
- `/home/huwei/ai_codings/distill-portal/apps/frontend/e2e/transcript-perf.spec.ts` (154 lines)

MODIFIED files:
- `/home/huwei/ai_codings/distill-portal/apps/frontend/src/features/sessions/SessionView.tsx` (DEFAULT_TAB shift, <TranscriptView /> mount, "Open raw" anchor)
- `/home/huwei/ai_codings/distill-portal/apps/frontend/src/features/sessions/SessionView.css` (.session-open-raw)
- `/home/huwei/ai_codings/distill-portal/apps/frontend/src/features/sessions/SessionView.test.tsx`
- `/home/huwei/ai_codings/distill-portal/apps/frontend/src/features/sessions/useParsedSession.ts` (in-flight coalescing AbortError → bumps retryNonce; M3b fix surfaced by M4)
- `/home/huwei/ai_codings/distill-portal/apps/frontend/src/App.test.tsx`
- `/home/huwei/ai_codings/distill-portal/apps/frontend/e2e/inspection.spec.ts`

## Spec source-of-truth

- `/home/huwei/ai_codings/distill-portal/working/phase-5.md` lines 699-717 (Transcript tab spec)
- lines 84-95 (motion budget)
- lines 1027-1040 (M4 DoD)
- lines 62-78 (signature details)
- lines 233-348 (Data Model)
- Resolved Decisions #11, #12, #16, #18

Spec frozen at commit `05467ad` on `main`.

## YOU MUST EXECUTE these commands in your sandbox

```bash
cd /home/huwei/ai_codings/distill-portal

# Test gates
cd apps/frontend && bun run test 2>&1 | tail -10
cd apps/frontend && bunx tsc --noEmit 2>&1 | tail -5
cd apps/frontend && bun run build 2>&1 | tail -10
cd ../..

# Token discipline (M2b r1 #3 precedent)
rg -nE 'var\(--[a-z-]+\)' apps/frontend/src/features/sessions/TranscriptView.css | sort -u
rg -nE 'var\(--[a-z-]+\)' apps/frontend/src/features/sessions/SessionView.css | sort -u
rg -n '#[0-9a-fA-F]{3,8}' apps/frontend/src/ | wc -l   # MUST equal 24
rg -c '^\s*--' apps/frontend/src/styles/tokens.css     # MUST equal 83

# Cross-check that every TranscriptView token exists in tokens.css
for tok in $(rg -nE 'var\(--[a-z-]+\)' apps/frontend/src/features/sessions/TranscriptView.css | grep -oE '\-\-[a-z-]+' | sort -u); do
  grep -q "${tok}:" apps/frontend/src/styles/tokens.css || echo "MISSING: ${tok}"
done

# Spec-literal compliance
grep -nE 'Truncated at 5 MB' apps/frontend/src/features/sessions/TranscriptView.tsx
grep -nE 'parse warnings' apps/frontend/src/features/sessions/TranscriptView.tsx
grep -nE '(SESSION RESUMED|CONVERSATION COMPACTED)' apps/frontend/src/features/sessions/TranscriptView.tsx
grep -nE 'Unrecognized line' apps/frontend/src/features/sessions/TranscriptView.tsx
grep -nE 'Open raw' apps/frontend/src/features/sessions/SessionView.tsx

# Motion budget — verify NO disallowed properties animated
rg -n 'transition' apps/frontend/src/features/sessions/TranscriptView.css
rg -n 'animation' apps/frontend/src/features/sessions/TranscriptView.css
rg -n 'transition.*color|transition.*border|transition.*width|transition.*height|transition.*top|transition.*padding' apps/frontend/src/features/sessions/

# Keep-mounted contract
rg -n 'key={' apps/frontend/src/features/sessions/TranscriptView.tsx
rg -n 'key={' apps/frontend/src/features/sessions/SessionView.tsx

# Boundary <li role="separator">
grep -nE 'role=.separator.' apps/frontend/src/features/sessions/TranscriptView.tsx

# tabIndex={0} on active tabpanel (M2b Option-A)
grep -nE 'tabIndex' apps/frontend/src/features/sessions/SessionView.tsx

# useParsedSession StrictMode fix
grep -nE 'AbortError|retryNonce' apps/frontend/src/features/sessions/useParsedSession.ts

# Protected paths
git diff --stat HEAD -- apps/backend/ components/ tests/e2e/ Cargo.toml Cargo.lock
git diff HEAD -- apps/frontend/package.json apps/frontend/bun.lock 2>/dev/null
```

## Block if you find any of these

### Spec literal violations (codex M3a r1 + r4 precedent)
- Truncation banner copy NOT verbatim per spec line 715: "Truncated at 5 MB — full payload not parsed. Use the **Open raw** anchor in the session header to inspect the full payload." (with `**Open raw**` rendering as `<strong>Open raw</strong>`).
- Parse-warnings copy NOT "{N} parse warnings — click to view." for ALL N (including N=1 reading "1 parse warnings"; spec literal beats grammar).
- Boundary copy NOT "SESSION RESUMED" or "CONVERSATION COMPACTED" verbatim uppercase.
- Unknown line: payload ≤ 80 chars MUST NOT have trailing ellipsis (slice-before-ellipsis rule per design.md §3.7).

### Spacing rhythm
- 16/24/32 px three-magnitude rule — verify CSS adjacent-sibling selectors apply correctly across `:has()` browser support.
- max-inline-size: var(--measure) (= 70ch).

### Token discipline (codex M2b r1 #3 precedent)
- Any TranscriptView.css `var(--*)` not declared in tokens.css → BLOCKING.
- Hex isolation 24, token count 83 — drift → BLOCKING.

### Motion budget (codex M2b r1 #2 precedent)
- ANY transition on color, border-color, width, height, top, padding, transform-on-message-panels, background-color-on-message-panels → BLOCKING.

### Keep-mounted contract
- ANY `key=` on TranscriptView root or children that would remount on tab switch → BLOCKING.

### a11y
- Missing tabIndex={0} on active Transcript tabpanel.
- Boundary not `<li ... role="separator" aria-orientation="horizontal">` flat shape.
- Truncation banner without ARIA role.
- Retry button without accessible name.

### useParsedSession AbortError fix
- Verify the fix (in-flight coalescing AbortError now bumps retryNonce).
- All M3b useParsedSession tests should still pass (run `bun test src/features/sessions/useParsedSession.test.ts`).
- No infinite retry loop risk: examine the dep array + retryNonce semantics.

### Performance acceptance
Spec line 1032 says "> 16 ms per frame for scroll" triggers escape-hatch slot 2. Developer reports median 16.7 ms (rAF 60 Hz floor), p95 16.8 ms, dropped frames 0. Developer re-interpreted operationally as "0 dropped frames (>25 ms)". Verify the rAF cadence floor argument: at 60 Hz, the inter-frame delta is 16.67 ms minimum, so a strict `< 16` is unattainable on a healthy Chromium. Should the spec literal be amended? Or is the operational interpretation acceptable?

## Files to inspect (you have full read access)

All paths under `/home/huwei/ai_codings/distill-portal/`. Read TranscriptView.{tsx,css,test.tsx}, the modified files, the design artifact, the plan, and the spec.

EXECUTE every command. Provide concrete file:line findings + command outputs. DO NOT decline to inspect.

## Output exactly

- Verdict: approved | approved with nits | needs changes | needs more evidence
- Findings: bullets with file:line, or none
- Missing Evidence: required missing evidence, or none
- Required Changes: precise file:line + before/after, or none
- Notes: optional improvements, or none

Begin review now.
